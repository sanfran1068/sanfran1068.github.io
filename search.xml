<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Markdown</title>
    <url>/2014/12/25/2014-12-25-markdown-programming-language.html</url>
    <content><![CDATA[<p>本篇文章是关于本站博客文章的Markdown格式的文档。<br><span id="more"></span></p>
<ul>
<li><a href="#block-elements">段落元素</a><ul>
<li><a href="#paragraphs-and-line-breaks">p和br</a></li>
<li><a href="#headers">标题</a></li>
<li><a href="#blockquotes">段落引用</a></li>
<li><a href="#lists">列表</a></li>
<li><a href="#code-blocks">代码</a></li>
<li><a href="#horizontal-rules">Horizontal Rules</a></li>
<li><a href="#table">表格</a></li>
</ul>
</li>
<li><a href="#span-elements">段内元素</a><ul>
<li><a href="#links">链接</a></li>
<li><a href="#emphasis">强调</a></li>
<li><a href="#code">代码</a></li>
<li><a href="#images">图片</a></li>
</ul>
</li>
<li><a href="#miscellaneous">混杂模式</a><ul>
<li><a href="#automatic-links">自动链接</a></li>
<li><a href="#backslash-escapes">Backslash Escapes</a></li>
</ul>
</li>
<li><a href="#inline-html">内联HTML</a></li>
</ul>
<!-- more -->
<h2 id="Block-Elements"><a href="#Block-Elements" class="headerlink" title="Block Elements"></a>Block Elements</h2><h3 id="Paragraphs-and-Line-Breaks"><a href="#Paragraphs-and-Line-Breaks" class="headerlink" title="Paragraphs and Line Breaks"></a>Paragraphs and Line Breaks</h3><h4 id="段落"><a href="#段落" class="headerlink" title="段落"></a>段落</h4><p>HTML标签: <code>&lt;p&gt;</code></p>
<p>在一段后进行回车空出一行可以达到该效果。</p>
<p>代码:</p>
<pre><code>This will be
inline.

This is second paragraph.
</code></pre><p>预览:</p>
<hr>
<p>This will be<br>inline.</p>
<p>This is second paragraph.</p>
<hr>
<h3 id="Headers"><a href="#Headers" class="headerlink" title="Headers"></a>Headers</h3><p>Markdown支持两种方式的标题：Setext/atx.</p>
<h4 id="Setext"><a href="#Setext" class="headerlink" title="Setext"></a>Setext</h4><p>HTML标签: <code>&lt;h1&gt;</code>, <code>&lt;h2&gt;</code></p>
<p>使用等号‘=’在标题下划线代表<code>&lt;h1&gt;</code>， 使用连字符‘-’在标题下划线表示 <code>&lt;h2&gt;</code>，其中符号的数量可以不做限制.</p>
<p>代码:</p>
<pre><code>This is an H1
=============
This is an H2
-------------
</code></pre><p>预览:</p>
<hr>
<h1 id="This-is-an-H1"><a href="#This-is-an-H1" class="headerlink" title="This is an H1"></a>This is an H1</h1><h2 id="This-is-an-H2"><a href="#This-is-an-H2" class="headerlink" title="This is an H2"></a>This is an H2</h2><hr>
<h4 id="atx"><a href="#atx" class="headerlink" title="atx"></a>atx</h4><p>HTML标签: <code>&lt;h1&gt;</code>, <code>&lt;h2&gt;</code>, <code>&lt;h3&gt;</code>, <code>&lt;h4&gt;</code>, <code>&lt;h5&gt;</code>, <code>&lt;h6&gt;</code></p>
<p>使用1到6个‘#’号来表示标题标签 <code>&lt;h1&gt;</code> - <code>&lt;h6&gt;</code>.</p>
<p>代码:</p>
<pre><code># This is an H1
## This is an H2
###### This is an H6
</code></pre><p>预览:</p>
<hr>
<h1 id="This-is-an-H1-1"><a href="#This-is-an-H1-1" class="headerlink" title="This is an H1"></a>This is an H1</h1><h2 id="This-is-an-H2-1"><a href="#This-is-an-H2-1" class="headerlink" title="This is an H2"></a>This is an H2</h2><h6 id="This-is-an-H6"><a href="#This-is-an-H6" class="headerlink" title="This is an H6"></a>This is an H6</h6><hr>
<p>atx格式的标题可以进行闭合如<code>### This is an H3 ######</code>，可见两端的‘#’号数量不需要相等。</p>
<h3 id="Blockquotes"><a href="#Blockquotes" class="headerlink" title="Blockquotes"></a>Blockquotes</h3><p>HTML标签: <code>&lt;blockquote&gt;</code></p>
<p>需要再每一个段落之前加‘&gt;’符号来形成段落引用。</p>
<p>Code:</p>
<pre><code>&gt; This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,
&gt; &gt; This is nested blockquote.
&gt; Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.
&gt;
&gt; id sem consectetuer libero luctus adipiscing.
</code></pre><p>Preview:</p>
<hr>
<blockquote>
<p>This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,</p>
<blockquote>
<p> This is nested blockquote.</p>
</blockquote>
</blockquote>
<blockquote>
<p>Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.</p>
<p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse</p>
</blockquote>
<hr>
<h3 id="Lists"><a href="#Lists" class="headerlink" title="Lists"></a>Lists</h3><p>Markdown支持有序和无序列表。</p>
<h4 id="无序列表"><a href="#无序列表" class="headerlink" title="无序列表"></a>无序列表</h4><p>HTML标签: <code>&lt;ul&gt;</code></p>
<p>无序列表可以使用‘*’、‘+’、‘-’三种符号开头并在符号后添加一个空格。</p>
<p>代码:</p>
<pre><code>*   Red
+   Green
—   Blue
</code></pre><p>预览:</p>
<hr>
<ul>
<li>Red</li>
</ul>
<ul>
<li>Green</li>
</ul>
<ul>
<li>Blue</li>
</ul>
<h4 id="Ordered"><a href="#Ordered" class="headerlink" title="Ordered"></a>Ordered</h4><p>HTML标签: <code>&lt;ol&gt;</code></p>
<p>有序列表通过数字和点符号‘.’以及空格开头来表示，前面的数字是几无所谓，会自动解析成从小到大的顺序。</p>
<p>代码:</p>
<pre><code>1.  Bird
22.  McHale
3.  Parish
44\. Cat
</code></pre><p>预览:</p>
<hr>
<ol>
<li>Bird</li>
<li>McHale</li>
<li>Parish<br>44.  Cat</li>
</ol>
<hr>
<h4 id="缩进"><a href="#缩进" class="headerlink" title="缩进"></a>缩进</h4><p>段落级别的缩进使用tab添加在段落之前即可。</p>
<h3 id="Code-Blocks"><a href="#Code-Blocks" class="headerlink" title="Code Blocks"></a>Code Blocks</h3><p>HTML标签: <code>&lt;pre&gt;</code></p>
<p>代码的每一行都缩进至少‘4个空格’和‘1个tab’，代码结束是以第一个不缩进的段落为止。</p>
<p>代码:</p>
<pre><code>This is a normal paragraph:

    This is a code block.
</code></pre><p>代码:</p>
<hr>
<p>This is a normal paragraph:</p>
<pre><code>This is a code block.
</code></pre><hr>
<h4 id="包裹代码"><a href="#包裹代码" class="headerlink" title="包裹代码"></a>包裹代码</h4><p>可以使用`*3后跟语法名称来进行代码的包裹，从而避免每一行的缩进并可以进行语法高亮。</p>
<p>代码:</p>
<pre><code>Here&apos;s an example:

<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">test</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;notice the blank line before this function?&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</code></pre><p>预览:</p>
<hr>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function test() &#123;</span><br><span class="line">  console.log(&quot;notice the blank line before this function?&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr>
<h3 id="Horizontal-Rules"><a href="#Horizontal-Rules" class="headerlink" title="Horizontal Rules"></a>Horizontal Rules</h3><p>HTML Tag: <code>&lt;hr /&gt;</code><br>分界线可以使用’*‘、’-‘、’_’</p>
<p>代码:</p>
<pre><code>* * *
***
*****
- - -
---------------------------------------
___
</code></pre><p>预览:</p>
<hr>
<hr>
<hr>
<hr>
<hr>
<hr>
<hr>
<hr>
<h3 id="Table"><a href="#Table" class="headerlink" title="Table"></a>Table</h3><p>HTML标签: <code>&lt;table&gt;</code></p>
<p>表格使用‘|’、‘:’、‘-’三种符号进行构造，其中‘-’符号必须有至少3个才能构造表格。<br>代码:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">| Left | Center | Right |</span><br><span class="line">|:-----|:------:|------:|</span><br><span class="line">|aaa   |bbb     |ccc    |</span><br><span class="line">|ddd   |eee     |fff    |</span><br><span class="line"></span><br><span class="line">A |B</span><br><span class="line">--|--</span><br><span class="line">12|45</span><br></pre></td></tr></table></figure>
<p>预览:</p>
<hr>
<table>
<thead>
<tr>
<th style="text-align:left">Left</th>
<th style="text-align:center">Center</th>
<th style="text-align:right">Right</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">aaa</td>
<td style="text-align:center">bbb</td>
<td style="text-align:right">ccc</td>
</tr>
<tr>
<td style="text-align:left">ddd</td>
<td style="text-align:center">eee</td>
<td style="text-align:right">fff</td>
</tr>
</tbody>
</table>
<table>
<thead>
<tr>
<th>A</th>
<th>B</th>
</tr>
</thead>
<tbody>
<tr>
<td>12</td>
<td>45</td>
</tr>
</tbody>
</table>
<hr>
<h2 id="Span-Elements"><a href="#Span-Elements" class="headerlink" title="Span Elements"></a>Span Elements</h2><h3 id="Links"><a href="#Links" class="headerlink" title="Links"></a>Links</h3><p>HTML标签: <code>&lt;a&gt;</code></p>
<p>Markdown支持两种类型的链接：内联在段落中和引用模式.</p>
<h4 id="内联"><a href="#内联" class="headerlink" title="内联"></a>内联</h4><p>内联在段落中<code>[Link Text](URL &quot;Title&quot;)</code>，如果是在本站的网址，可以使用相对地址进行访问。</p>
<p>代码:</p>
<pre><code>This is [an example](http://example.com/ &quot;Title&quot;) inline link.

See my [About](/about/) page for details.
</code></pre><p>预览:</p>
<hr>
<p>This is <a href="http://example.com/" title="Title">an example</a> inline link.</p>
<p>See my <a href="/about/">About</a> page for details.</p>
<hr>
<h4 id="参考引用"><a href="#参考引用" class="headerlink" title="参考引用"></a>参考引用</h4><p>参考链接的形式如此：<code>[id]: URL &quot;Title&quot;</code></p>
<p>代码:</p>
<pre><code>[id]: http://example.com/  &quot;Optional Title Here&quot;
This is [an example][id] reference-style link.
</code></pre><p>预览:</p>
<hr>
<p>This is <a href="http://example.com/" title="Optional Title Here">an example</a> reference-style link.</p>
<hr>
<h3 id="Emphasis"><a href="#Emphasis" class="headerlink" title="Emphasis"></a>Emphasis</h3><p>HTML标签: <code>&lt;em&gt;</code>, <code>&lt;strong&gt;</code></p>
<p>Markdown使用‘*’、‘_’符号进行斜体的标注，‘**’、‘__’符号进行粗体的标注，如果直接使用‘*’符号可以加‘\’进行转义。</p>
<p>代码:</p>
<pre><code>*single asterisks*
_single underscores_
**double asterisks**
__double underscores__
</code></pre><p>预览:</p>
<hr>
<p><em>single asterisks</em></p>
<p><em>single underscores</em></p>
<p><strong>double asterisks</strong></p>
<p><strong>double underscores</strong></p>
<h3 id="Code"><a href="#Code" class="headerlink" title="Code"></a>Code</h3><p>HTML标签: <code>&lt;code&gt;</code></p>
<p>使用‘`’来对内联的代码进行包裹，<strong>backtick quotes (`)</strong>.</p>
<p>Code:</p>
<pre><code>Use the `printf()` function.
</code></pre><p>Preview:</p>
<hr>
<p>Use the <code>printf()</code> function.</p>
<hr>
<h3 id="Images"><a href="#Images" class="headerlink" title="Images"></a>Images</h3><p>HTML标签: <code>&lt;img /&gt;</code></p>
<p>Markdown支持内联和参考引用两种方式进行图片的添加。</p>
<h4 id="内联-1"><a href="#内联-1" class="headerlink" title="内联"></a>内联</h4><p>内联图片代码如此： <code>![Alt text](URL &quot;Title&quot;)</code>，其中title是可选的一项。</p>
<p>代码:</p>
<pre><code>![Alt text](/path/to/img.jpg)

![Alt text](/path/to/img.jpg &quot;Optional title&quot;)
</code></pre><h4 id="Reference"><a href="#Reference" class="headerlink" title="Reference"></a>Reference</h4><p>参考引用方式的代码如此： <code>![Alt text][id]</code></p>
<p>代码:</p>
<pre><code>[img id]: url/to/image  &quot;Optional title attribute&quot;
![Alt text][img id]
</code></pre><h2 id="Miscellaneous"><a href="#Miscellaneous" class="headerlink" title="Miscellaneous"></a>Miscellaneous</h2><h3 id="Automatic-Links"><a href="#Automatic-Links" class="headerlink" title="Automatic Links"></a>Automatic Links</h3><p>使用‘&lt; &gt;’符号将链接包裹形成自动链接。</p>
<p>代码:</p>
<pre><code>&lt;http://example.com/&gt;

&lt;address@example.com&gt;
</code></pre><p>预览:</p>
<hr>
<p><a href="http://example.com/">http://example.com/</a></p>
<p><a href="mailto:&#x61;&#x64;&#100;&#114;&#101;&#115;&#115;&#x40;&#x65;&#120;&#x61;&#109;&#x70;&#x6c;&#101;&#46;&#x63;&#x6f;&#x6d;">&#x61;&#x64;&#100;&#114;&#101;&#115;&#115;&#x40;&#x65;&#120;&#x61;&#109;&#x70;&#x6c;&#101;&#46;&#x63;&#x6f;&#x6d;</a></p>
<hr>
<h3 id="Backslash-Escapes"><a href="#Backslash-Escapes" class="headerlink" title="Backslash Escapes"></a>Backslash Escapes</h3><p>使用反斜线来进行某些符号的转义</p>
<p>代码:</p>
<pre><code>\*literal asterisks\*
</code></pre><p>预览:</p>
<hr>
<p>*literal asterisks*</p>
<hr>
<p>支持以下符号:</p>
<p>Code:</p>
<pre><code>\   backslash
`   backtick
*   asterisk
_   underscore
&#123;&#125;  curly braces
[]  square brackets
()  parentheses
#   hash mark
+   plus sign
-   minus sign (hyphen)
.   dot
!   exclamation mark
</code></pre><h2 id="Inline-HTML"><a href="#Inline-HTML" class="headerlink" title="Inline HTML"></a>Inline HTML</h2><p>如果需要一些HTML中的标签而Markdown中没有的，可以直接使用HTML标签进行包裹使用。</p>
<p>代码:</p>
<pre><code>This is a regular paragraph.

&lt;table&gt;
    &lt;tr&gt;
        &lt;td&gt;Foo&lt;/td&gt;
    &lt;/tr&gt;
&lt;/table&gt;

This is another regular paragraph.
</code></pre><p>预览:</p>
<hr>
<p>This is a regular paragraph.</p>
<table><br>    <tr><br>        <td>Foo</td><br>    </tr><br></table>

<p>This is another regular paragraph.</p>
<hr>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Markdown</tag>
      </tags>
  </entry>
  <entry>
    <title>Jena学习笔记</title>
    <url>/2015/09/21/2015-11-20-jena-learning-note.html</url>
    <content><![CDATA[<h4 id="Jena初步了解"><a href="#Jena初步了解" class="headerlink" title="Jena初步了解"></a>Jena初步了解</h4><p>JenaAPI的框架如下图：<br><img src="https://img.alicdn.com/imgextra/i3/O1CN01r61JlO21WqWYrTMd4_!!6000000006993-2-tps-610-590.png" alt=""></p>
<span id="more"></span>
<p>如上图所示，利用Jena进行语义网处理应用的开发有通过HTTP和Servlet响应的B/S模式，还有通过JenaAPI进行Java编程来处理本体数据。</p>
<ul>
<li>RDF API：Jena提供了对RDF文件的创建、读/写以及一些基本操作的接口（方法）；</li>
<li>SPARQL API：Jena提供一套对本体的三元组的query机制，类似于SQL语言对于数据库的操作，SPARQL对于本体的查询也可以通过简单的查询语句达到目的，这也为B/S模式的本体展示和交互提供了基础；</li>
<li>Ontology API：</li>
<li>Fuseki</li>
</ul>
<h4 id="RDF-API"><a href="#RDF-API" class="headerlink" title="RDF API"></a>RDF API</h4><h5 id="名词解释"><a href="#名词解释" class="headerlink" title="名词解释"></a>名词解释</h5><ul>
<li>Blank Node：一阶逻辑中表示一个无URI的resource</li>
<li>Literal：可作为一个property的值（value）</li>
<li>Object：the resource or literal pointed to by the arc（箭头指向的一端）</li>
<li>Subject：the resource from which the arc leaves（箭头离开的一端）</li>
<li><p>Triple：</p>
<ul>
<li>Predicate：三元组中的属性部分（关系）（the property that labels the arc）</li>
<li>Property：一个resource的属性，指三元组中的关系</li>
<li>Resouce：实体，通常由URI命名的资源</li>
</ul>
<p>Statement：指一个fact，即一个三元组（Each arc in an RDF Model）</p>
</li>
</ul>
<h5 id="对RDF的操作（JavaAPI）"><a href="#对RDF的操作（JavaAPI）" class="headerlink" title="对RDF的操作（JavaAPI）"></a>对RDF的操作（JavaAPI）</h5><ul>
<li>Create graph    </li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// some definitions</span></span><br><span class="line"><span class="keyword">static</span> <span class="type">String</span> <span class="variable">personURI</span> <span class="operator">=</span> <span class="string">&quot;http://somewhere/JohnSmith&quot;</span>;</span><br><span class="line"><span class="keyword">static</span> <span class="type">String</span> <span class="variable">fullName</span> <span class="operator">=</span> <span class="string">&quot;John Smith&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// create an empty Model</span></span><br><span class="line"><span class="type">Model</span> <span class="variable">model</span> <span class="operator">=</span> ModelFactory.createDefaultModel();</span><br><span class="line"></span><br><span class="line"><span class="comment">// create the resource</span></span><br><span class="line"><span class="type">Resource</span> <span class="variable">johnSmith</span> <span class="operator">=</span> model.createResource(personURI);</span><br><span class="line"></span><br><span class="line"><span class="comment">// add the property</span></span><br><span class="line"> johnSmith.addProperty(VCARD.FN, fullName);</span><br></pre></td></tr></table></figure>
<ul>
<li>Create resource and add the property</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//Resouce example</span></span><br><span class="line"><span class="type">Resource</span> <span class="variable">johnSmith</span> <span class="operator">=</span> model.createResource(personURI).addProperty(VCARD.FN, fullName);</span><br></pre></td></tr></table></figure>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// some definitions</span></span><br><span class="line"><span class="type">String</span> <span class="variable">personURI</span>    <span class="operator">=</span> <span class="string">&quot;http://somewhere/JohnSmith&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">givenName</span>    <span class="operator">=</span> <span class="string">&quot;John&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">familyName</span>   <span class="operator">=</span> <span class="string">&quot;Smith&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">fullName</span>     <span class="operator">=</span> givenName + <span class="string">&quot; &quot;</span> + familyName;</span><br><span class="line"></span><br><span class="line"><span class="comment">// create an empty Model</span></span><br><span class="line"><span class="type">Model</span> <span class="variable">model</span> <span class="operator">=</span> ModelFactory.createDefaultModel();</span><br><span class="line"></span><br><span class="line"><span class="comment">// create the resource</span></span><br><span class="line"><span class="comment">// and add the properties cascading style</span></span><br><span class="line"><span class="type">Resource</span> <span class="variable">johnSmith</span></span><br><span class="line">  <span class="operator">=</span> model.createResource(personURI)</span><br><span class="line">         .addProperty(VCARD.FN, fullName)</span><br><span class="line">         .addProperty(VCARD.N,</span><br><span class="line">                      model.createResource()</span><br><span class="line">                           .addProperty(VCARD.Given, givenName)</span><br><span class="line">                           .addProperty(VCARD.Family, familyName));</span><br></pre></td></tr></table></figure>
<ul>
<li>Manipulate the stmt</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// list the statements in the Model</span></span><br><span class="line"><span class="type">StmtIterator</span> <span class="variable">iter</span> <span class="operator">=</span> model.listStatements();</span><br><span class="line"></span><br><span class="line"><span class="comment">// print out the predicate, subject and object of each statement</span></span><br><span class="line"><span class="keyword">while</span> (iter.hasNext()) &#123;</span><br><span class="line">    <span class="type">Statement</span> <span class="variable">stmt</span>      <span class="operator">=</span> iter.nextStatement();  <span class="comment">// get next statement</span></span><br><span class="line">    <span class="type">Resource</span>  <span class="variable">subject</span>   <span class="operator">=</span> stmt.getSubject();     <span class="comment">// get the subject</span></span><br><span class="line">    <span class="type">Property</span>  <span class="variable">predicate</span> <span class="operator">=</span> stmt.getPredicate();   <span class="comment">// get the predicate</span></span><br><span class="line">    <span class="type">RDFNode</span>   <span class="variable">object</span>    <span class="operator">=</span> stmt.getObject();      <span class="comment">// get the object</span></span><br><span class="line"></span><br><span class="line">    System.out.print(subject.toString());</span><br><span class="line">    System.out.print(<span class="string">&quot; &quot;</span> + predicate.toString() + <span class="string">&quot; &quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (object <span class="keyword">instanceof</span> Resource) &#123;</span><br><span class="line">       System.out.print(object.toString());</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// object is a literal</span></span><br><span class="line">        System.out.print(<span class="string">&quot; \&quot;&quot;</span> + object.toString() + <span class="string">&quot;\&quot;&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">&quot; .&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由于object可以是Resource或者Literal，所以object的类型为RDFNode。</p>
<ul>
<li>Writing RDF</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// now write the model in XML form to a file</span></span><br><span class="line">model.write(System.out);</span><br><span class="line">model.write(System.out, <span class="string">&quot;RDF/XML-ABBREV&quot;</span>); <span class="comment">// write the model in XML form to a file</span></span><br><span class="line">model.write(System.out, <span class="string">&quot;N-TRIPLES&quot;</span>); <span class="comment">// write the model in N-TRIPLES form to a file</span></span><br></pre></td></tr></table></figure>
<ul>
<li>Reading RDF</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// create an empty model</span></span><br><span class="line"> <span class="type">Model</span> <span class="variable">model</span> <span class="operator">=</span> ModelFactory.createDefaultModel();</span><br><span class="line"></span><br><span class="line"> <span class="comment">// use the FileManager to find the input file</span></span><br><span class="line"> <span class="type">InputStream</span> <span class="variable">in</span> <span class="operator">=</span> FileManager.get().open( inputFileName );</span><br><span class="line"><span class="keyword">if</span> (in == <span class="literal">null</span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(</span><br><span class="line">                                 <span class="string">&quot;File: &quot;</span> + inputFileName + <span class="string">&quot; not found&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// read the RDF/XML file</span></span><br><span class="line">model.read(in, <span class="literal">null</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// write it to standard out</span></span><br><span class="line">model.write(System.out);</span><br></pre></td></tr></table></figure>
<h4 id="SPARQL-API"><a href="#SPARQL-API" class="headerlink" title="SPARQL API"></a>SPARQL API</h4><p>需要保存在.rq文件中进行使用。</p>
<ul>
<li>Basic query：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT ?xWHERE&#123; ?x&lt;http://www.w3.org/2001/vcard-rdf/3.0#FN&gt;  &quot;JohnSmith&quot; &#125;</span><br></pre></td></tr></table></figure>
<p>其中&lt;&gt;中为URI，“”中为literal，？x为查询变量</p>
<ul>
<li>Basic pattern</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">WHERE</span><br><span class="line">  &#123; ?y  &lt;http://www.w3.org/2001/vcard-rdf/3.0#Family&gt;  &quot;Smith&quot; .</span><br><span class="line">    ?y  &lt;http://www.w3.org/2001/vcard-rdf/3.0#Given&gt;  ?givenName .</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>basic patterns：是一组三元组的模式；<br>prefix and blanknodes：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PREFIX vcard:      &lt;http://www.w3.org/2001/vcard-rdf/3.0#&gt;</span><br><span class="line"></span><br><span class="line">SELECT ?y ?givenName</span><br><span class="line">WHERE</span><br><span class="line"> &#123; ?y vcard:Family &quot;Smith&quot; .</span><br><span class="line">   ?y vcard:Given  ?givenName .</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>Filters</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">PREFIX vcard: &lt;http://www.w3.org/2001/vcard-rdf/3.0#&gt;</span><br><span class="line"></span><br><span class="line">SELECT ?g</span><br><span class="line">WHERE</span><br><span class="line">&#123; ?y vcard:Given ?g .</span><br><span class="line">  FILTER regex(?g, &quot;r&quot;, &quot;i&quot;) &#125;</span><br></pre></td></tr></table></figure>
<p>上面的加粗为FILTER，其中最后一个“”中的i表示case-insensitive</p>
<ul>
<li>Optionals（表示如果有这个属性那么就显示在结果中，如果没有就不显示）</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PREFIX info:    &lt;http://somewhere/peopleInfo#&gt;</span><br><span class="line">PREFIX vcard:   &lt;http://www.w3.org/2001/vcard-rdf/3.0#&gt;</span><br><span class="line"></span><br><span class="line">SELECT ?name ?age</span><br><span class="line">WHERE</span><br><span class="line">&#123;</span><br><span class="line">    ?person vcard:FN  ?name .</span><br><span class="line">    OPTIONAL &#123; ?person info:age ?age &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>optionals with filters：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PREFIX info:        &lt;http://somewhere/peopleInfo#&gt;</span><br><span class="line">PREFIX vcard:      &lt;http://www.w3.org/2001/vcard-rdf/3.0#&gt;</span><br><span class="line"></span><br><span class="line">SELECT ?name ?age</span><br><span class="line">WHERE</span><br><span class="line">&#123;</span><br><span class="line">    ?person vcard:FN  ?name .</span><br><span class="line">    OPTIONAL &#123; ?person info:age ?age . FILTER ( ?age &gt; 24 ) &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>Unions：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PREFIX foaf: &lt;http://xmlns.com/foaf/0.1/&gt;</span><br><span class="line">PREFIX vCard: &lt;http://www.w3.org/2001/vcard-rdf/3.0#&gt;</span><br><span class="line"></span><br><span class="line">SELECT ?name</span><br><span class="line">WHERE</span><br><span class="line">&#123;</span><br><span class="line">   &#123; [] foaf:name ?name &#125; UNION &#123; [] vCard:FN ?name &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>相当于FILTER ( ?p = foaf:name || ?p = vCard:FN)<br>如果是分开资源那么只要分别用不同变量标注即可：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT ?name1 ?name2</span><br><span class="line">WHERE</span><br><span class="line">&#123;</span><br><span class="line">   &#123; [] foaf:name ?name1 &#125; UNION &#123; [] vCard:FN ?name2 &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>SPARQL result forms：</p>
<ul>
<li>SELECT – Return a table of results.</li>
<li>CONSTRUCT – Return an RDF graph, based on a template in the query.</li>
<li>DESCRIBE – Return an RDF graph, based on what the query processor is configured to return.</li>
<li>ASK – Ask a boolean query.</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>JenaAPI</tag>
        <tag>API</tag>
        <tag>Ontology</tag>
        <tag>基础知识</tag>
      </tags>
  </entry>
  <entry>
    <title>Node-js基础学习笔记</title>
    <url>/2016/12/13/2016-12-13-nodejs-basic-learning-note.html</url>
    <content><![CDATA[<h4 id="Node-js基础学习笔记"><a href="#Node-js基础学习笔记" class="headerlink" title="Node.js基础学习笔记"></a>Node.js基础学习笔记</h4><p>在前端的学习过程当中，不可避免地要学习到一定的后台服务器的技术。之前一直都沉浸在servlet中无法自拔，所以Java一度成为自己还比较熟悉的后台语言，但是相对于本身比较熟悉的JavaScript来说，当然是尽量用JS来处理后台最好不过了，所以Node.js顺其自然成为了学习路上必要的一道关卡。</p>
<p>略过安装、配置和创建第一个应用，想必很多Node.js相关网站都可以找到相应的教程，本文仅记录在学习Node.js基础知识中一些关键点技术。</p>
<span id="more"></span>
<h5 id="Node-js应用"><a href="#Node-js应用" class="headerlink" title="Node.js应用"></a>Node.js应用</h5><p>Node.js 应用是由下面几部分组成的：</p>
<ul>
<li>引入 required 模块：我们可以使用 require 指令来载入 Node.js 模块。</li>
<li>创建服务器：服务器可以监听客户端的请求，类似于 Apache 、Nginx 等 HTTP 服务器。</li>
<li><p>接收请求与响应请求 服务器很容易创建，客户端可以使用浏览器或终端发送 HTTP 请求，服务器接收请求后返回响应数据。</p>
</li>
<li><p>创建一个服务器：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> http = <span class="built_in">require</span>(<span class="string">&#x27;http&#x27;</span>);</span><br><span class="line">http.<span class="title function_">createServer</span>(<span class="keyword">function</span> (<span class="params">request, response</span>) &#123;</span><br><span class="line">    <span class="comment">// 发送 HTTP 头部 </span></span><br><span class="line">    <span class="comment">// HTTP 状态值: 200 : OK</span></span><br><span class="line">    <span class="comment">// 内容类型: text/plain</span></span><br><span class="line">    response.<span class="title function_">writeHead</span>(<span class="number">200</span>, &#123;<span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;text/plain&#x27;</span>&#125;);</span><br><span class="line">    <span class="comment">// 发送响应数据 &quot;Hello World&quot;</span></span><br><span class="line">    response.<span class="title function_">end</span>(<span class="string">&#x27;Hello World\n&#x27;</span>);</span><br><span class="line">&#125;).<span class="title function_">listen</span>(<span class="number">8888</span>);</span><br><span class="line"><span class="comment">// 终端打印如下信息</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Server running at http://127.0.0.1:8888/&#x27;</span>);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h5 id="回调函数"><a href="#回调函数" class="headerlink" title="回调函数"></a>回调函数</h5><p>Node.js是单进程单线程应用程序，但是通过事件和回调支持并发和异步。</p>
<p>Node的所有API都支持回调函数，并作为一个独立线程运行，完美支持异步处理大量的请求。回调函数作为<strong>最后一个参数</strong>传入异步函数，而回调函数的第一个参数为<strong>错误对象</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">&quot;fs&quot;</span>);</span><br><span class="line">fs.<span class="title function_">readFile</span>(<span class="string">&#x27;input.txt&#x27;</span>, <span class="keyword">function</span> (<span class="params">err, data</span>) &#123;</span><br><span class="line">   <span class="keyword">if</span> (err)&#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(err.<span class="property">stack</span>);</span><br><span class="line">      <span class="keyword">return</span>;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(data.<span class="title function_">toString</span>());</span><br><span class="line">&#125;);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;程序执行完毕&quot;</span>);</span><br></pre></td></tr></table></figure>
<h5 id="事件循环"><a href="#事件循环" class="headerlink" title="事件循环"></a>事件循环</h5><p>Node.js使用事件驱动模型，所有的事件机制都是用设计模式中观察者模式实现。代码中引入events模块，通过实例化EventEmitter类来绑定和监听事件：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> events = <span class="built_in">require</span>(<span class="string">&#x27;events&#x27;</span>);                 <span class="comment">// 引入 events 模块</span></span><br><span class="line"><span class="keyword">var</span> eventEmitter = <span class="keyword">new</span> events.<span class="title class_">EventEmitter</span>();   <span class="comment">// 创建 eventEmitter 对象</span></span><br><span class="line">event.<span class="title function_">on</span>(<span class="string">&#x27;some_event&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;             <span class="comment">// 绑定事件，function中可添加多参数</span></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;some_event 事件触发&#x27;</span>); </span><br><span class="line">&#125;); </span><br><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span>(<span class="params"></span>) &#123;                         <span class="comment">// 触发事件</span></span><br><span class="line">    event.<span class="title function_">emit</span>(<span class="string">&#x27;some_event&#x27;</span>); </span><br><span class="line">&#125;, <span class="number">1000</span>);</span><br></pre></td></tr></table></figure>
<p>events模块下只有一个对象：events.EventEmitter，其核心就是<strong>事件触发与事件监听</strong>两者的封装。EventEmitter的每个事件都由一个事件名（一个字符串）和若干参数构成。一般来说，不会直接使用EventEmitter，而是在对象中继承它，包括fs、net、http在内都是EventEmitter的子类（为了符合事件语义，并且符合JS的原型对象机制）。</p>
<ul>
<li><p>EventEmitter对象的属性</p>
<ul>
<li>.addListener(event, listener)：为指定事件添加一个监听器到监听器数组的尾部</li>
<li>.on(event, listener)：为指定事件注册一个监听器，接受一个字符串 event 和一个回调函数</li>
<li>.once(event, listener):为指定事件注册一个单次监听器，即 监听器最多只会触发一次，触发后立刻解除该监听器</li>
<li>.removeListener(event, listener)：移除指定事件的某个监听器，监听器必须是该事件已经注册过的监听器。<br>它接受两个参数，第一个是事件名称，第二个是回调函数名称</li>
<li>.removeAllListeners([event])：移除所有事件的所有监听器， 如果指定事件，则移除指定事件的所有监听器</li>
<li>setMaxListeners(n)：默认情况下， EventEmitters 如果你添加的监听器超过 10 个就会输出警告信息；setMaxListeners 函数用于提高监听器的默认限制的数量</li>
<li>listeners(event)：返回指定事件的监听器数组。</li>
<li>emit(event, [arg1], [arg2], […])：按参数的顺序执行每个监听器，如果事件有注册监听返回 true，否则返回 false。</li>
</ul>
</li>
</ul>
<h5 id="缓冲区"><a href="#缓冲区" class="headerlink" title="缓冲区"></a>缓冲区</h5><p>由于JS中不支持二进制数据类型，所以在处理像TCP流或文件流时，Node.js提供了Buffer类，用来创建一个专门存放二进制数据的缓存区（一个Buffer类类似于一个整数数组）。创建Buffer实例有多种方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+ 以字节数量创建：var buf = new Buffer(10);</span><br><span class="line">+ 以数组创建：var buf = new Buffer([10, 20, 30, 40, 50]);</span><br><span class="line">+ 以字符串创建：var buf = new Buffer(&quot;www.runoob.com&quot;, &quot;utf-8&quot;);</span><br></pre></td></tr></table></figure>
<ul>
<li>写入缓冲区：buf.write(string[, offset[, length]][, encoding]);参数分别为必须的写入缓冲区的字符串，可选的开始写入索引值（默认为0）、写入字节数（默认buffer.length）、使用的编码（默认为utf8）。该写入的方法会<strong>返回实际写入的大小</strong></li>
<li>读缓冲区：buf.toString([encoding[, start[, end]]]);</li>
<li>将Buffer实例转换为JSON对象：buf.toJSON()</li>
<li>缓冲区合并：Buffer.concat(bufArray[, totalLength]);其中bufArray是需要进行合并的Buffer实例数组</li>
<li>缓冲区比较：buf.compare(otherBuffer);返回负数表示buf在前，0表示相同，正数表示buf在后</li>
<li>拷贝缓冲区：buf.copy(targetBuffer[, targetStart[, sourceStart[, sourceEnd]]]);其中buf是被复制的，targetBuffer是复制的目标缓冲区</li>
<li>缓冲区裁剪：buf.slice([start[, end]]);返回一个新的缓冲区，与旧缓冲区指向同一块内存，索引不同</li>
<li>缓冲区长度：buf.length;返回缓冲区所占内存长度</li>
</ul>
<h5 id="Stream"><a href="#Stream" class="headerlink" title="Stream"></a>Stream</h5><p>是一个抽象接口，流类型有四种：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">+ Readable：可读操作</span><br><span class="line">+ Writable：可写操作</span><br><span class="line">+ Duplex：可读可写操作</span><br><span class="line">+ Transform：操作被写入数据，然后读出结果</span><br></pre></td></tr></table></figure>
<p>所有的Stream对象都是EventEmitter的实例，所以可以绑定的常用事件有data（有数据可读时触发）、end（没有更多数据可读时触发）、error（接收和写入过程发生错误时触发）、finish（所有数据写入到底层系统时触发）。</p>
<p>Node中又有很多对象实现了这个接口，例如对http服务器发起请求的request对象就是一个Stream，stdout（标准输出）也是一个Stream。</p>
<ul>
<li><p>读取流</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">&quot;fs&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> data = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> readerStream = fs.<span class="title function_">createReadStream</span>(<span class="string">&#x27;input.txt&#x27;</span>);<span class="comment">//创建可读流</span></span><br><span class="line">readerStream.<span class="title function_">setEncoding</span>(<span class="string">&#x27;UTF8&#x27;</span>);</span><br><span class="line"><span class="comment">// 处理流事件 --&gt; data, end, and error</span></span><br><span class="line">readerStream.<span class="title function_">on</span>(<span class="string">&#x27;data&#x27;</span>, <span class="keyword">function</span>(<span class="params">chunk</span>) &#123;    <span class="comment">//此处的data并不是读取结果字符串</span></span><br><span class="line">   data += chunk;</span><br><span class="line">&#125;);</span><br><span class="line">readerStream.<span class="title function_">on</span>(<span class="string">&#x27;end&#x27;</span>,<span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(data);</span><br><span class="line">&#125;);</span><br><span class="line">readerStream.<span class="title function_">on</span>(<span class="string">&#x27;error&#x27;</span>, <span class="keyword">function</span>(<span class="params">err</span>)&#123;</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(err.<span class="property">stack</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
<li><p>写入流</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">&quot;fs&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> data = <span class="string">&#x27;菜鸟教程官网地址：www.runoob.com&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> writerStream = fs.<span class="title function_">createWriteStream</span>(<span class="string">&#x27;output.txt&#x27;</span>);<span class="comment">//创建写入流</span></span><br><span class="line">writerStream.<span class="title function_">write</span>(data,<span class="string">&#x27;UTF8&#x27;</span>);</span><br><span class="line">writerStream.<span class="title function_">end</span>();                                   <span class="comment">// 标记文件末尾</span></span><br><span class="line">writerStream.<span class="title function_">on</span>(<span class="string">&#x27;finish&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;写入完成。&quot;</span>);</span><br><span class="line">&#125;);</span><br><span class="line">writerStream.<span class="title function_">on</span>(<span class="string">&#x27;error&#x27;</span>, <span class="keyword">function</span>(<span class="params">err</span>)&#123;</span><br><span class="line">   <span class="variable language_">console</span>.<span class="title function_">log</span>(err.<span class="property">stack</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>Nodejs</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS中的伪类与伪元素</title>
    <url>/2017/03/10/2017-03-10-pseudoCSS-part1-pseudoElement-pseudoClass-note.html</url>
    <content><![CDATA[<h4 id="简介-1"><a href="#简介-1" class="headerlink" title="简介[^1]"></a>简介[^1]</h4><p>样式表中的选择器是控制网页样式输出的非常重要的工具之一，也是css通过DOM树来控制其样式的最好的方式之一。而除了标签选择器、类选择器、ID选择器、属性选择器以及元素关系选择器（父子、兄弟等选择器），CSS3为我们提供了一种更为复杂同时也是更为方便设计者的选择方式-<strong>伪类</strong>（Pseudo-Classes）和<strong>伪元素</strong>（Pseudo-Elements）。</p>
<p>伪类和伪元素通常由<strong>冒号</strong>（colon）引导，而CSS3重新规定伪元素之前需要由两个冒号引导，但是为了使旧版本网站也能够统一使用和显示（IE8及以下浏览器无法理解双冒号的语法），浏览器则仍支持单冒号引导的伪元素方式（<em>除了少数特定的伪元素之外</em>）。</p>
<span id="more"></span>
<h4 id="伪类和伪元素的区别-2"><a href="#伪类和伪元素的区别-2" class="headerlink" title="伪类和伪元素的区别[^2]"></a>伪类和伪元素的区别[^2]</h4><p>CSS中的伪类根据元素的编码或动态状态等来选择HTML文档中特定部分，即选择HTML文档中的某元素（通过用户的干预或者随着时间的变化而产生动态改变状态），CSS2又将该特性扩展到了DOM树的某些“虚拟组件”和可推断部分（例如first-child），可以出现在选择器链条中的任何位置；</p>
<p>CSS中的伪元素是用来强调某些元素的子部分，允许选择并改变那些除DOM中指定的元素之外的部分的样式（对DOM树中未包含的逻辑元素进行选择和改变样式）(可以用来展示比如头字母大写等常见的排版效果)；伪元素有一些使用的限制条件：</p>
<ul>
<li>不允许出现在<body>中的in-line样式（只能出现在外部样式表或位于<head><style></style></head>的样式中）；</li>
<li>在选择器的链条中只能出现在最后一位（即要位于任何类和ID选择之后）；</li>
<li>每个选择器中只能出现一个伪元素（若要在单个元素结构中城县多个伪元素的选择，必须使用多个选择器/样式声明）；</li>
</ul>
<h3 id="伪类"><a href="#伪类" class="headerlink" title="伪类"></a>伪类</h3><p>伪类就像是class一样，以冒号引导（e.g. :hover等），可以以<strong>标签:伪类</strong>和<strong>类:伪类</strong>的形式存在于CSS样式表中。</p>
<h4 id="动态伪类"><a href="#动态伪类" class="headerlink" title="动态伪类"></a>动态伪类</h4><h5 id="链接样式的动态伪类"><a href="#链接样式的动态伪类" class="headerlink" title="链接样式的动态伪类"></a>链接样式的动态伪类</h5><p>针对链接，用户可以无任何操作、将光标移至链接上、点击、点击后放开光标，将光标从链接上移走等操作。</p>
<ul>
<li><p>a:link<br>选择用户还未进行过任何操作的链接；</p>
</li>
<li><p>a:visited<br>选择用户已经点击过的链接；</p>
</li>
<li><p>a:hover<br>选择用户将鼠标移至上方的链接；同时，:hover伪类也可以用在除链接以外的元素上（e.g. <p>或是<div>）；</p>
</li>
<li><p>a:active<br>选择用户使用鼠标正在点击（按下）的链接；</p>
</li>
</ul>
<p>样例代码：<br><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.example</span> <span class="selector-tag">a</span><span class="selector-pseudo">:link</span> &#123;<span class="attribute">color</span>:gray;&#125; <span class="comment">/*链接没被访问时为灰色*/</span></span><br><span class="line"><span class="selector-class">.example</span> <span class="selector-tag">a</span><span class="selector-pseudo">:visited</span> &#123;<span class="attribute">color</span>:maroon;&#125; <span class="comment">/*链接被访问过后前景色为红色*/</span></span><br><span class="line"><span class="selector-class">.example</span> <span class="selector-tag">a</span><span class="selector-pseudo">:hover</span> &#123;<span class="attribute">color</span>:blue;&#125; <span class="comment">/*鼠标悬浮在链接上时前景色为蓝色*/</span></span><br><span class="line"><span class="selector-class">.example</span> <span class="selector-tag">a</span><span class="selector-pseudo">:active</span> &#123;<span class="attribute">color</span>:darkblue;&#125; <span class="comment">/*鼠标按下链接前景色为深蓝色*/</span></span><br></pre></td></tr></table></figure></p>
<p>这里需要特别注意的是，这四个动态伪类在样式表中定义的顺序是不可以任意排序的，而是要遵循唯一的一个顺序，即<strong>L</strong>O<strong>V</strong>E/<strong>HA</strong>TE原则，即以a:link/a:visited/a:hover/a:active的顺序来定义。这是因为在CSS样式表中针对同一个元素的样式定义要<em>从一般到特殊</em>。一个链接在用户未进行操作的时候可能同时拥有:link、:visited和:hover三种样式属性，而无论如何:hover都会覆盖前面两种样式，所以:hover要在前面两种样式之后定义，而:link又是最一般的样式，所以要放在第一位来进行定义；最后:active则能覆盖所有之前三种属样式，所以放在最后一位进行定义。一句话来总结这一特性，<em>越容易被覆盖的样式越先定义，覆盖越多的样式要越晚定义</em>。</p>
<h5 id="表单样式的动态伪类"><a href="#表单样式的动态伪类" class="headerlink" title="表单样式的动态伪类"></a>表单样式的动态伪类</h5><p>常对于输入类的表单进行选择并展示或修改其样式的动态伪类；</p>
<ul>
<li><p>:focus<br>与链接中的:hover的作用类似，是当用户将光标的注意力集中到某元素而改变其样式，多用于用户在网页上的表单进行输入的元素进行选择（e.g. \<input>或\<textarea>）。</p>
</li>
<li><p>UI元素状态伪类(IE8及以下不支持该伪类)：</p>
<ul>
<li><p>:enabled<br>选择可以进行输入和选择等操作的表单元素进行样式的声明；</p>
</li>
<li><p>:disabled<br>选择不能进行输入和选择等操作的表单元素进行样式的声明；</p>
</li>
<li><p>:checked<br>选择已经选择的表单元素（type为radio或checkbox元素）进行样式声明；</p>
</li>
<li><p>:unchecked<br>选择还未进行选择的表单元素进行样式声明；</p>
</li>
</ul>
</li>
</ul>
<h4 id="子选择器（-nth选择器）"><a href="#子选择器（-nth选择器）" class="headerlink" title="子选择器（:nth选择器）"></a>子选择器（:nth选择器）</h4><p>CSS原本提供了后代选择器（descendant selector），但是针对于某些特定的场景，例如列表和表格制定行列的样式改变，CSS又提供了伪类选择器来为这些子类选择器提供服务。</p>
<ul>
<li>:first-child<br>选择指定元素中第一个子元素；</li>
<li>:last-child<br>选择指定元素中最后一个子元素；</li>
<li>:nth-child(parameter)<br>选择某个元素的一个或多个特定的子元素，参数可以是自然数，也可以是像2n，3n+1，-n+3这样的公式进行有规律的子元素选择；</li>
<li>:nth-last-child()<br>与:nth-child()作用相同，但是是从选定元素的最后一个子元素开始算起；</li>
<li>:nth-of-type()<br>与:nth-child()作用相同，但是可以选择指定类别的子元素；</li>
<li>:nth-last-of-type()<br>与:nth-nth-of-type()作用相同，但是是从选定元素的最后一个子元素开始算起；</li>
<li>:first-of-type<br>与:first-child作用相同，只是可以选择子元素的类型；</li>
<li>:last-of-type<br>与:last-child作用相同，只是可以选择子元素的类型；</li>
<li>:only-child<br>当指定元素只有一个子元素时，选择该子元素；</li>
<li>:only-of-type<br>当指定元素只有一个指定类型的子元素时，选择该子元素；</li>
<li>:empty<br>当制定元素中没有任何内容时；</li>
</ul>
<h4 id="特殊伪类选择器"><a href="#特殊伪类选择器" class="headerlink" title="特殊伪类选择器"></a>特殊伪类选择器</h4><ul>
<li><p>:target<br>该伪类类似于HTML中的锚，指向文档中某个具体的元素，常用于点击出现某些指定元素样式的改变（点击按钮，某元素由display:none变为display:block;这样的出现效果）；</p>
</li>
<li><p>:not<br>也被称作negation pseudo-class，也就是否定取反。当使用类似于</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span><span class="selector-pseudo">:not</span>(<span class="selector-class">.exampleStyle</span>)&#123;<span class="attribute">color</span>:green&#125;</span><br></pre></td></tr></table></figure>
<p>时，意为选择所有不包含。exampleStyle类的p元素进行选择改变字体颜色为green。</p>
</li>
</ul>
<h3 id="伪元素"><a href="#伪元素" class="headerlink" title="伪元素"></a>伪元素</h3><h4 id="before和-after"><a href="#before和-after" class="headerlink" title="::before和::after"></a>::before和::after</h4><ul>
<li>::before<br>可以在某给定元素上添加提前展示的内容：<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.tip</span><span class="selector-pseudo">::before</span> &#123;<span class="attribute">content</span>: <span class="string">&quot;HOT TIP!&quot;</span> &#125;</span><br></pre></td></tr></table></figure></li>
<li>::after<br>可以在某给定元素上添加提前展示的内容：<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.tip</span><span class="selector-pseudo">::after</span> &#123;<span class="attribute">content</span>: <span class="string">&quot;HOT TIP!&quot;</span> &#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="文字排版常见的-first-line和-first-letter"><a href="#文字排版常见的-first-line和-first-letter" class="headerlink" title="文字排版常见的::first-line和::first-letter"></a>文字排版常见的::first-line和::first-letter</h4><ul>
<li><p>::first-line<br>选择制定元素的第一行（例如改变每个段落的第一行文本样式）；</p>
</li>
<li><p>::first-letter<br>选择制定文本块的第一个字母进行样式设置，多用于进行段落排版；</p>
</li>
</ul>
<h4 id="光标选中文本样式-selection"><a href="#光标选中文本样式-selection" class="headerlink" title="光标选中文本样式::selection"></a>光标选中文本样式::selection</h4><p>::selection用来改变浏览网页选中文本的默认效果；</p>
<p>[^1]: 《CSS, The Missing Manual, 4th Edition》<br>[^2]: <a href="[http://www.d.umn.edu/~lcarlson/csswork/selectors/pseudo_dif.html">Difference between a pseudo-class and a pseudo-element</a></p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>读书笔记</tag>
        <tag>CSS3</tag>
        <tag>Selector</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript正则表达式学习笔记[上]</title>
    <url>/2017/04/12/2017-03-20-JavaScript-RegExpression-learning-note.html</url>
    <content><![CDATA[<h4 id="正则表达式-Regular-Expression"><a href="#正则表达式-Regular-Expression" class="headerlink" title="正则表达式 Regular Expression"></a>正则表达式 Regular Expression</h4><p>正则表达式是由一个字符序列形成的搜索模式,可用于所有文本搜索和文本替换的操作。[^1]它的重要性就不言而喻了。本文结合《JavaScript高级程序设计》一书并整理其他资料，对正则表达式的基础进行一个学习笔记的记录。更加详细的正则表达式学习心得将在之后的笔记中进行记录。</p>
<span id="more"></span>
<h4 id="正则表达式构成"><a href="#正则表达式构成" class="headerlink" title="正则表达式构成"></a>正则表达式构成</h4><p>正则表达式由一些<strong>普通字符</strong>和一些<strong>元字符</strong>（metacharacters）组成。普通字符包括大小写的字母和数字，而元字符（用于匹配任何单个字符）则具有特殊的含义[^2]：</p>
<h5 id="元字符"><a href="#元字符" class="headerlink" title="元字符"></a>元字符</h5><table>
<thead>
<tr>
<th>元字符</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>.</td>
<td>匹配除“\r\n”之外任何单个字符；要匹配“\r\n”在内的任何字符，使用“[\s\S]”</td>
</tr>
<tr>
<td>\0</td>
<td>查找NULL字符</td>
</tr>
<tr>
<td>\b</td>
<td>匹配单词边界；也就是指单词和空格间的位置<a href="正则表达式的“匹配”有两种概念，一种是匹配字符，一种是匹配位置，这里的\b就是匹配位置的">^3</a>；例，“er\b”可匹配“never”的”er”</td>
</tr>
<tr>
<td>\B</td>
<td>匹配非单词边界；“er\B”能匹配“verb”中的“er”，但不能匹配“never”中的“er”</td>
</tr>
<tr>
<td>\cX</td>
<td>匹配由X指明的控制字符；<em>X的值必须为A-Z或a-z之一*</em>；否则，视为一个原义的“c”字符</td>
</tr>
<tr>
<td>\d</td>
<td>匹配一个数字字符；等价于[0-9]</td>
</tr>
<tr>
<td>\D</td>
<td>匹配一个非数字字符；等价于[^0-9]</td>
</tr>
<tr>
<td>\f</td>
<td>匹配一个换页符；等价于\x0c和<strong>\cL</strong></td>
</tr>
<tr>
<td>\n</td>
<td>匹配一个换行符；等价于\x0a和<strong>\cJ</strong></td>
</tr>
<tr>
<td>\r</td>
<td>匹配一个回车符；等价于\x0d和<strong>\cM</strong></td>
</tr>
<tr>
<td>\s</td>
<td>匹配任何不可见字符；包括空格、制表符、换页符等；等价于[\f\n\r\t\v]</td>
</tr>
<tr>
<td>\S</td>
<td>匹配任何可见字符；等价于[^\f\n\r\t\v]</td>
</tr>
<tr>
<td>\t</td>
<td>匹配一个制表符；等价于\x09和\cI</td>
</tr>
<tr>
<td>\v</td>
<td>匹配一个垂直制表符；等价于\x0b和\cK</td>
</tr>
<tr>
<td>\w</td>
<td>匹配<strong>包括</strong>下划线的任何单词字符；该单词字符使用<strong>Unicode字符集</strong></td>
</tr>
<tr>
<td>\W</td>
<td>匹配任何非单词字符；等价于“[^A-Za-z0-9_]”</td>
</tr>
<tr>
<td>\xdd</td>
<td>匹配十六进制dd转义值；<strong>十六进制转义值必须为确定的两个数字长</strong></td>
</tr>
<tr>
<td>\n</td>
<td>标识一个八进制转义值或一个向后引用。如果\n之前至少n个获取的子表达式，则n为向后引用</td>
</tr>
<tr>
<td>\uN</td>
<td>匹配N，其中N是一个用四个十六进制数字表示的Unicode字符</td>
</tr>
</tbody>
</table>
<h5 id="转义字符"><a href="#转义字符" class="headerlink" title="转义字符"></a>转义字符</h5><table>
<thead>
<tr>
<th>转义字符</th>
<th>描述（若字符本身要出现，需在特殊字符前面加\）</th>
</tr>
</thead>
<tbody>
<tr>
<td>\</td>
<td>将下一个字符标记符、或一个向后引用、或一个八进制转义符</td>
</tr>
<tr>
<td>^</td>
<td>匹配输入字符串开始位置；若设置了正则表达式的mFlag，^也匹配“\n”或“\r”之后的位置</td>
</tr>
<tr>
<td>$</td>
<td>匹配输入字符串结束位置；若设置了正则表达式的mFlag，$也匹配“\n”或“\r”之前的位置</td>
</tr>
<tr>
<td>*</td>
<td>匹配前置子表达式任意次；例，zo*能匹配“z”，“zo”以及“zoo”，等价于”o{0,}”</td>
</tr>
<tr>
<td>+</td>
<td>匹配前置子表达式大于等于1次；例，“zo+”能匹配“zo”以及“zoo”,等价于{1,}</td>
</tr>
<tr>
<td>?</td>
<td>匹配前置子表达式零次或一次。例，“do(es)?”可以匹配“do”或“does”,等价于{0,1}</td>
</tr>
<tr>
<td>.</td>
<td>匹配除“\r\n”之外任何单个字符；要匹配“\r\n”在内的任何字符，请使用像“[\s\S]”模式</td>
</tr>
<tr>
<td></td>
<td></td>
<td>将两个匹配条件进行逻辑“或”（Or）运算。<strong>注意：这个元字符不是所有的软件都支持的</strong></td>
</tr>
</tbody>
</table>
<h5 id="量词类正则表达式"><a href="#量词类正则表达式" class="headerlink" title="量词类正则表达式"></a>量词类正则表达式</h5><table>
<thead>
<tr>
<th>量词表达式</th>
<th>描述（若字符本身要出现，需在特殊字符前面加\）</th>
</tr>
</thead>
<tbody>
<tr>
<td>n+</td>
<td>n为某pattern，匹配任何至少包含一个n的字符串</td>
</tr>
<tr>
<td>n*</td>
<td>n为某pattern，匹配零个或者多个n的字符串</td>
</tr>
<tr>
<td>n?</td>
<td>n为某pattern，匹配零个或者1个n的字符串</td>
</tr>
<tr>
<td>n{x}</td>
<td>n为某pattern，匹配包含x个n的序列字符串</td>
</tr>
<tr>
<td>n{x,y}</td>
<td>n为某pattern，匹配至少x个，最多y个n的字符串</td>
</tr>
<tr>
<td>n{x,}</td>
<td>n为某pattern，匹配至少x个的字符串</td>
</tr>
</tbody>
</table>
<h5 id="获取-非获取以及正反向预查的正则表达式"><a href="#获取-非获取以及正反向预查的正则表达式" class="headerlink" title="获取/非获取以及正反向预查的正则表达式"></a>获取/非获取以及正反向预查的正则表达式</h5><table>
<thead>
<tr>
<th>获取/非获取RegExp</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>(pattern)</td>
<td>匹配pattern并获取这一匹配；所获取的匹配可以从产生的Matches集合得到，请使用“(”或“)”匹配圆括号字符</td>
</tr>
<tr>
<td>(?:pattern)</td>
<td>非获取匹配；例，“industr(?:y</td>
<td>ies)”</td>
</tr>
<tr>
<td>(?=pattern)</td>
<td>非获取匹配，正向肯定预查，在任何匹配pattern的字符串开始处匹配查找字符串，该匹配不需要获取供以后使用。例如，“Windows(?=95</td>
<td>98</td>
<td>NT</td>
<td>2000)”能匹配“Windows2000”中的“Windows”，但不能匹配“Windows3.1”中的“Windows”。预查不消耗字符，也就是说，在一个匹配发生后，在最后一次匹配之后立即开始下一次匹配的搜索，而不是从包含预查的字符之后开始。</td>
</tr>
<tr>
<td>(?!pattern)</td>
<td>非获取匹配，正向否定预查，在任何不匹配pattern的字符串开始处匹配查找字符串，该匹配不需要获取供以后使用。例如“Windows(?!95</td>
<td>98</td>
<td>NT</td>
<td>2000)”能匹配“Windows3.1”中的“Windows”，但不能匹配“Windows2000”中的“Windows”。</td>
</tr>
<tr>
<td>(?&lt;=pattern)</td>
<td>非获取匹配，反向肯定预查，与正向肯定预查类似，只是方向相反。例如，“(?&lt;=95</td>
<td>98</td>
<td>NT</td>
<td>2000)Windows”能匹配“2000Windows”中的“Windows”，但不能匹配“3.1Windows”中的“Windows”。</td>
</tr>
<tr>
<td>(?&lt;!pattern)</td>
<td>非获取匹配，反向否定预查，与正向否定预查类似，只是方向相反。</td>
</tr>
<tr>
<td>x</td>
<td>y</td>
<td>匹配x或y。例如，“z</td>
<td>food”能匹配“z”或“food”(此处请谨慎)。“[z</td>
<td>f]ood”则匹配“zood”或“food”或”</td>
<td>ood”。</td>
</tr>
</tbody>
</table>
<h5 id="范围类正则表达式"><a href="#范围类正则表达式" class="headerlink" title="[]范围类正则表达式"></a>[]范围类正则表达式</h5><table>
<thead>
<tr>
<th>[]正则表达式</th>
<th>描述（只要[]中任意一个字符匹配就可以）</th>
</tr>
</thead>
<tbody>
<tr>
<td>[0-9]</td>
<td>字符范围；匹配指定范围内的任意一个数字</td>
</tr>
<tr>
<td>[xyz]</td>
<td>字符集合；匹配所包含的任意一个字符。例，“[abc]”可以匹配“plain”中的“a”</td>
</tr>
<tr>
<td>[^xyz]</td>
<td>负值字符集合；匹配未包含的任意一个字符。例，“[^abc]”可以匹配“plain”中的“plin”</td>
</tr>
<tr>
<td>[a-z]</td>
<td>字符范围；匹配指定范围内的任意一个字符</td>
</tr>
<tr>
<td>[^a-z]</td>
<td>负值字符范围；匹配任何不在指定范围内的任意字符</td>
</tr>
</tbody>
</table>
<h5 id="其他类型"><a href="#其他类型" class="headerlink" title="其他类型"></a>其他类型</h5><table>
<thead>
<tr>
<th>其他表达式</th>
<th>描述</th>
</tr>
</thead>
<tbody>
<tr>
<td>\p{P}</td>
<td>小写p是property的意思，表示Unicode属性，用于Unicode正表达式的前缀。中括号内的“P”表示Unicode字符集七个字符属性之一：标点字符<a href="其他六个属性：L：字母；M：标记符号（一般不会单独出现）；Z：分隔符（比如空格、换行等）；S：符号（比如数学符号、货币符号等）；N：数字（比如阿拉伯数字、罗马数字等）；C：其他字符。*注：此语法部分语言不支持，例：javascript。">^4</a></td>
</tr>
<tr>
<td>\&lt; &gt;</td>
<td>匹配一个<strong>word</strong>的开始（\&lt;）和结束（&gt;）</td>
</tr>
<tr>
<td>( )</td>
<td>将()内的表达式定义为“组”（group），并且将匹配这个表达式的字符保存到一个临时区域（一个正则表达式中最多可以保存9个），它们可以用\1到\9的符号来引用</td>
</tr>
</tbody>
</table>
<h4 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h4><ul>
<li><p>直接量语法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">/pattern/flags;</span><br></pre></td></tr></table></figure>
<p>其中//包含的内部（pattern）是正则表达式字符串的主体；最后的flags是正则表达式的修饰标志，包含属性有g，i，m三种，分别是与全局匹配，不区分大小写匹配和多行匹配三种修饰标志。</p>
<p>实例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> patt = <span class="regexp">/[bc]at/i</span>; <span class="comment">//匹配第一个“bat”或”cat“，不区分大小写</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>使用构造函数创建RegExp对象语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">RegExp</span>(pattern, attributes);</span><br></pre></td></tr></table></figure>
<p>其中，pattern部分与上面直接量语法中的pattern相同，attributes是上面flags相同的标志；</p>
</li>
</ul>
<h4 id="RegExp实例的属性和方法"><a href="#RegExp实例的属性和方法" class="headerlink" title="RegExp实例的属性和方法"></a>RegExp实例的属性和方法</h4><h5 id="RegExp实例的属性"><a href="#RegExp实例的属性" class="headerlink" title="RegExp实例的属性"></a>RegExp实例的属性</h5><p>RegExp的每一个实例都具有下列属性，这些属性可以获取到该实例pattern的相关信息：</p>
<ul>
<li>.global：bool值，表示是否设置了g标志</li>
<li>.ignoreCase：bool值，表示是否设置了i标志</li>
<li>.multiline：bool值，表示是否设置了m标志</li>
<li>.lastIndex：整数值，表示开始搜索下一个匹配项的自负位置，从0算起</li>
<li>.source：正则表达式的字符串表示，按照<strong><em>字面量形式</em></strong>而非传入构造参数中的字符串模式返回；</li>
</ul>
<h5 id="RegExp实例的方法"><a href="#RegExp实例的方法" class="headerlink" title="RegExp实例的方法"></a>RegExp实例的方法</h5><ul>
<li><p>exec()，该方法是专门为捕获组而设计的：</p>
<ul>
<li>语法：RegExpObject.exec(testString)</li>
<li>参数：接受一个参数，即要应用该模式的字符串；</li>
<li>返回：包含第一个匹配项信息的数组（无匹配项时返回null）；且返回值含有两个额外属性，index表示匹配项在字符串中的位置/input表示应用正则表达式的字符串；</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> testString = <span class="string">&quot;mom and dad and baby&quot;</span>;</span><br><span class="line"><span class="keyword">var</span> pattern = <span class="regexp">/mom( and dad( and baby)?)?/gi</span>;</span><br><span class="line"><span class="keyword">var</span> matches = pattern.<span class="title function_">exec</span>(testString);</span><br><span class="line"><span class="title function_">alert</span>(matches.<span class="property">index</span>);   <span class="comment">//0</span></span><br><span class="line"><span class="title function_">alert</span>(matches.<span class="property">input</span>);   <span class="comment">//&quot;mom and dad and baby&quot;</span></span><br><span class="line"><span class="title function_">alert</span>(matches.[<span class="number">0</span>]);     <span class="comment">//&quot;mom adn dad and baby&quot;</span></span><br><span class="line"><span class="title function_">alert</span>(matches.[<span class="number">1</span>]);     <span class="comment">//&quot; and dad and baby&quot;</span></span><br><span class="line"><span class="title function_">alert</span>(matches.[<span class="number">2</span>]);     <span class="comment">//&quot; and baby&quot;</span></span><br></pre></td></tr></table></figure>
<p>上述示例代码中，可以看到，返回的数组第一项是与整个模式匹配的字符串，其他项是与模式中的捕获组匹配的字符串。关于“组”的相关内容请参考上一章节的表格中的/()/项。</p>
<p><strong>有关全局和非全局模式</strong><br>当pattern为全局模式时，每次调用exec()进行匹配时会返回下一个匹配项，即exec()调用过后，matches.index可能会增加，pattern.lastIndex也是同样会增加；如果pattern为非全剧模式时，每次调用exec()进行匹配时都会返回第一个匹配项。</p>
</li>
<li><p>test()，简单匹配。在不知道要进行匹配的文本内容情况下使用改方法比较好：</p>
<ul>
<li>语法：RegExpObject.test(testString)；</li>
<li>参数：接受一个参数，即要进行匹配的文本字符串；</li>
<li>返回：如果字符串testString中含有与RegExpObject匹配的文本的话，就返回true，否则返回false；</li>
</ul>
</li>
</ul>
<h5 id="RegExp构造函数属性"><a href="#RegExp构造函数属性" class="headerlink" title="RegExp构造函数属性"></a>RegExp构造函数属性</h5><p>RegExp构造函数包含一些属性，这些属性适用于作用与众的所有正则表达式，并且基于所执行的最近一次正则表达式操作而变化。这些属性分别有一个长属性名和一个短属性名（Opera不支持短属性名）：</p>
<table>
<thead>
<tr>
<th>长属性名</th>
<th>短属性名</th>
<th>说明</th>
</tr>
</thead>
<tbody>
<tr>
<td>input</td>
<td>$_</td>
<td>最近一次要匹配的字符串</td>
</tr>
<tr>
<td>lastMatch</td>
<td>$&amp;</td>
<td>最近一次的匹配项</td>
</tr>
<tr>
<td>lastParen</td>
<td>$+</td>
<td>最近一次要匹配的捕获组</td>
</tr>
<tr>
<td>leftContext</td>
<td>$`</td>
<td>input字符串中lastMatch之前的文本</td>
</tr>
<tr>
<td>multiline</td>
<td>$*</td>
<td>布尔值，表示是否所有表达式都是用多行模式</td>
</tr>
<tr>
<td>rightContext</td>
<td>$’</td>
<td>input字符串中lastMatch之后的文本</td>
</tr>
</tbody>
</table>
<p>其中，前三项是Opera没有实现的属性。使用这些属性能够从exec()和test()执行的操作中提取更具体的信息。</p>
<p>[^1]:From <a href="http://www.runoob.com/js/js-regexp.htm">http://www.runoob.com/js/js-regexp.htm</a><br>[^2]:From <a href="http://baike.baidu.com/item/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F?sefr=enterbtn">http://baike.baidu.com/item/%E6%AD%A3%E5%88%99%E8%A1%A8%E8%BE%BE%E5%BC%8F?sefr=enterbtn</a></p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>正则表达式</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript中的原型与继承-原型篇</title>
    <url>/2017/04/12/2017-04-12-JavaScript-Prototype-and-Inheritance-1.html</url>
    <content><![CDATA[<h3 id="原型"><a href="#原型" class="headerlink" title="原型"></a>原型</h3><p>构造函数模式下，对象中的方法作用域只在对象中，对象的不同实例的同名函数都是不相等的。为了解决这个问题，js中创建的每一个函数都有一个prototype属性，这个属性是一个指针，指向一个对象，而这个对象的用途是包含可以由特定类型的所由实例共享的属性和方法。</p>
<span id="more"></span>
<h4 id="原型对象"><a href="#原型对象" class="headerlink" title="原型对象"></a>原型对象</h4><p>JavaScript 中并没有提供类的实现，虽然在ES2015/ES6之中引入了 class 关键字，但是 JavaScript 仍然是基于原型的。而 JavaScript 中创建对象的方法有：new Object() 方法、字面量方法、工厂模式、构造函数方法和<strong>原型模式</strong>。其中，使用原型模式创建对象可以令所有实例共享方法，减少内存消耗，有利于对象继承。</p>
<p>JavaScript 中的<strong>继承</strong>则是体现在一种结构上——对象，所有的对象都是由Object衍生的对象，所有的对象都继承了 Object.prototype 的方法和属性（也有可能被覆盖）。每一个对象都有一个内部链接到另一个对象，这个对象成为它的原型（prototype）。而且，该原型对象也有自己的原型，直到追溯到一个以 null 为原型的对象，因为 null 是没有原型的，所以可以作为这个<strong>原型链（prototype chain</strong>中的最终链接。</p>
<p>只要创建了一个新函数，就会为该函数创建一个prototype属性，这个属性指向函数的原型对象。所有原型对象都自动获得一个constructor属性，这个属性包含一个指向prototype属性所在函数的指针。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN01EtwJ2z1ekpSHFw3CL_!!6000000003910-2-tps-880-359.png" alt=""></p>
<h5 id="原型对象的创建"><a href="#原型对象的创建" class="headerlink" title="原型对象的创建"></a>原型对象的创建</h5><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params">name, age, job</span>) &#123; </span><br><span class="line">     <span class="variable language_">this</span>.<span class="property">name</span> = name; </span><br><span class="line">     <span class="variable language_">this</span>.<span class="property">age</span> = age; </span><br><span class="line">     <span class="variable language_">this</span>.<span class="property">job</span> = job; </span><br><span class="line"> &#125; </span><br><span class="line"> <span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123; </span><br><span class="line">     <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>); </span><br><span class="line"> &#125; </span><br><span class="line"> <span class="keyword">var</span> person1 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Nicholas&quot;</span>, <span class="number">29</span>, <span class="string">&quot;Lawyer&quot;</span>); </span><br><span class="line"> <span class="keyword">var</span> person2 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Katie&quot;</span>, <span class="number">30</span> <span class="string">&quot;Account&quot;</span>); </span><br><span class="line"> <span class="keyword">var</span> person3 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Nicholas&quot;</span>, <span class="number">29</span>, <span class="string">&quot;Lawyer&quot;</span>); </span><br><span class="line"> person1.<span class="title function_">sayName</span>();     <span class="comment">//&quot;Nicholas&quot; </span></span><br><span class="line"> person2.<span class="title function_">sayName</span>();     <span class="comment">//&quot;Katie&quot;</span></span><br><span class="line"> <span class="title function_">alert</span>(person1.<span class="property">sayName</span> == person3.<span class="property">sayName</span>);    <span class="comment">//true </span></span><br></pre></td></tr></table></figure>
<p>更加简单的原型对象创建语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params"></span>)&#123;&#125;</span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123; </span><br><span class="line">   name : <span class="string">&quot;Nicholas&quot;</span>, </span><br><span class="line">   age : <span class="number">29</span>, </span><br><span class="line">   job : <span class="string">&quot;Software Engineer&quot;</span>, </span><br><span class="line">   sayName : <span class="keyword">function</span>(<span class="params"></span>) &#123; </span><br><span class="line">     <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>) </span><br><span class="line">   &#125; </span><br><span class="line">&#125;; </span><br></pre></td></tr></table></figure>
<ul>
<li><p>代码搜索某属性的路径：</p>
<p>  上面代码中出现了Person.prototype,通过该形式可以获得原型对象，并且可以为其添加属性和方法。这段代码也展示了对象属性和方法搜索的路径：每当代码读取某个对象的某个属性时，进行目标为该属性的名字，搜索首先从实例（person1）本身开始，若有该名字的属性则返回该属性，若没有则继续搜索该实例中原型指针指向的原型对象，在原型对象中查找该名字的属性。所以说<strong>实例是共享原型中保存的属性和方法</strong>。</p>
</li>
<li><p>实例的属性，原型的属性？</p>
<p>  一个对象的所有实例尽管可以共享该原型对象的所有属性和方法，但是却无法重写原型对象中的属性和方法，只能通过利用同名属性和方法来覆盖和屏蔽原型对象中的属性和方法；若想把实例中覆盖的属性还原，可以通过delete操作。使用hasOwnProperty()方法可以检测一个属性是存在于实例中还是存在于原型中。</p>
</li>
<li><p>怎样获得原型对象:</p>
<p>  下面的代码展示了Object.getPrototypeOf(obj)方法和<strong>proto</strong>属性的使用：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Object</span>.<span class="title function_">getPrototypeOf</span>(person1) === <span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>;     <span class="comment">//true </span></span><br><span class="line">person1.<span class="property">__proto__</span> === <span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>;    <span class="comment">//true </span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>可以看到，Object.getPrototypeOf(obj)是获取obj对象的原型对象的方法，这个方法将在<strong>利用原型实现继承的情况中发挥非常重要的作用</strong>。obj.<strong>proto</strong> 也是如此，是每一个对象都拥有的属性，但是<strong>proto</strong>并不是一个规范的属性（当使用Object.create()方法创建对象时，<strong>proto</strong> 并不能指向该对象的原型对象），其对应的标准属性应当是[[Prototype]]。</p>
<h5 id="原型对象和构造函数"><a href="#原型对象和构造函数" class="headerlink" title="原型对象和构造函数"></a>原型对象和构造函数</h5><p>对于对象Person来说，它的构造函数是Person()，它的原型对象为Person.prototype；而Person.prototype.constructor又会指回Person。而对象Person的实例person1和person2都包含有一个属性[[Prototype]]（也就是上面提到的<strong>proto</strong>）它们都指向Person.prototype；同时，person1和person2也可以通过isprototypeOf(）方法来确定是否与确定对象之间有这种关系：</p>
<figure class="highlight javascript"><br><br><pre><br><br><div class="line">alert(Person.prototype.isPrototypeOf(person1));    <span class="comment">//true</span></div><br><br></pre><br><br></figure>

<h5 id="原型与in操作符"><a href="#原型与in操作符" class="headerlink" title="原型与in操作符"></a>原型与in操作符</h5><ul>
<li><p>in操作符单独使用的情况</p>
<p>in操作符单独使用会在通过对象能够访问给定属性时返回true，无论该属性存在于实例中还是在原型中：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接上面Person对象代码 </span></span><br><span class="line"><span class="keyword">var</span> person4 = <span class="keyword">new</span> <span class="title class_">Person</span>(); </span><br><span class="line"><span class="title function_">alert</span>(person4.<span class="title function_">hasOwnproperty</span>(<span class="string">&quot;name&quot;</span>));     <span class="comment">//由于person4实例没有覆盖原型中的name所以返回false </span></span><br><span class="line"><span class="title function_">alert</span>(<span class="string">&quot;name&quot;</span> <span class="keyword">in</span> person4);    <span class="comment">//true </span></span><br></pre></td></tr></table></figure>
</li>
<li><p>for-in循环</p>
<p>该种方式是返回所有能够通过对象访问的、可枚举的属性，包括实例和原型中的属性，并且屏蔽了原型中不可枚举（[[Enumerable]]）的属性（仅在IE8及更早版本中有不可枚举的属性）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> o = &#123; </span><br><span class="line">   toString : <span class="keyword">function</span>(<span class="params"></span>) &#123; </span><br><span class="line">     <span class="keyword">return</span> <span class="string">&quot;my Object&quot;</span> </span><br><span class="line">   &#125; </span><br><span class="line"> &#125; </span><br><span class="line"> <span class="keyword">for</span> (<span class="keyword">var</span> prop <span class="keyword">in</span> o) &#123; </span><br><span class="line">   <span class="keyword">if</span> (prop == <span class="string">&quot;toString&quot;</span>) &#123; </span><br><span class="line">     <span class="title function_">alert</span>(<span class="string">&quot;Found toString&quot;</span>);      <span class="comment">//IE中无法显示 </span></span><br><span class="line">   &#125; </span><br><span class="line"> &#125;     </span><br></pre></td></tr></table></figure>
<p>若想获取对象中所有可枚举的实例属性，可以使用Object.keys(),该函数接受一个对象或实例作为参数（当然也可以是原型对象），返回一个包含所有可枚举属性的字符串数组。示例代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接Person对象代码 </span></span><br><span class="line"><span class="keyword">var</span> keys = <span class="title class_">Object</span>.<span class="title function_">keys</span>(person.<span class="property"><span class="keyword">prototype</span></span>); </span><br><span class="line"><span class="keyword">var</span> person5 = <span class="keyword">new</span> <span class="title class_">Person</span>(); </span><br><span class="line">person5.<span class="property">name</span> = <span class="string">&quot;Rob&quot;</span>; </span><br><span class="line">person5.<span class="property">age</span> = <span class="number">31</span>; </span><br><span class="line"><span class="title function_">alert</span>(keys); </span><br><span class="line"><span class="title function_">alert</span>(<span class="title class_">Object</span>.<span class="title function_">keys</span>(person5)); </span><br><span class="line"><span class="keyword">var</span> keys2 = <span class="title class_">Object</span> .<span class="title function_">getOwnPropertyNames</span>(<span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>);     </span><br><span class="line"><span class="title function_">alert</span>(keys2); </span><br></pre></td></tr></table></figure>
<p>上述代码中出现的Object.getOwnPropertyNames(Person.prototype)方法可以得到所有的可枚举/不可枚举的实例属性（列入constructor）。</p>
</li>
</ul>
<h5 id="原型的动态性"><a href="#原型的动态性" class="headerlink" title="原型的动态性"></a>原型的动态性</h5><p>由于从原型中查找值的过程试一次搜索，因此对原型对象所做的任何修改都能从实例上反映出来。即，可以先创建实例，再修改原型对象中的属性或函数，实例依旧可以访问该属性和函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接person对象代码 </span></span><br><span class="line"> <span class="keyword">var</span> friend = <span class="keyword">new</span> <span class="title class_">Person</span>(); </span><br><span class="line"> <span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">sayHi</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123; </span><br><span class="line">   <span class="title function_">alert</span>(<span class="string">&quot;hi!&quot;</span>); </span><br><span class="line"> &#125;; </span><br><span class="line"> friend.<span class="title function_">sayHi</span>();  <span class="comment">//返回”hi” </span></span><br></pre></td></tr></table></figure>
<p>尽管像以上代码中所看到的可以为原型添加属性或方法，但是如果重写整个原型对象，就会切断实例的构造函数与最初原型对象之间的联系。这是因为实例中的指针仅指向原型，而非构造函数。</p>
<h5 id="原生对象的原型"><a href="#原生对象的原型" class="headerlink" title="原生对象的原型"></a>原生对象的原型</h5><p>不仅是自定义的对象，JavaScript中所有原生的引用类型，都是采用这种模式创建的原生对象。通过原生对象的原型可以取得所有默认方法的引用，也可以自己添加新的方法。</p>
<h4 id="原型对象的缺点"><a href="#原型对象的缺点" class="headerlink" title="原型对象的缺点"></a>原型对象的缺点</h4><ul>
<li><p>由于原型中所有属性都是可以被实例共享，而对于原型中含有引用类型值的属性来说就会发生问题：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接person对象代码 </span></span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">friends</span> = [<span class="string">&quot;Shelby&quot;</span>, <span class="string">&quot;Court&quot;</span>]; </span><br><span class="line"><span class="keyword">var</span> person6 = <span class="keyword">new</span> <span class="title class_">Person</span>(); </span><br><span class="line">person6.<span class="property">friends</span>.<span class="title function_">push</span>(<span class="string">&quot;Van&quot;</span>); </span><br><span class="line"><span class="title function_">alert</span>(person6.<span class="property">friends</span>); </span><br><span class="line"><span class="title function_">alert</span>(person1.<span class="property">friends</span>);     <span class="comment">//此时两个Person的实例拥有相同的朋友，很可能发生错误 </span></span><br></pre></td></tr></table></figure>
<p>可以看到，由于原型的属性可以被共享的这一特性，原型对象中包含的引用类型值很可能被修改之后导致实例的属性也发生错误，必须要在实例属性中覆盖才可以。所以为了解决这个问题，提出了以下组合构造函数模式和原型模式的方法来创建对象：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//改写上面的Person对象 </span></span><br><span class="line"><span class="comment">//首先使用构造函数模式创建对象Person并加入容易被修改的属性name，age，job </span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params">name, age, job</span>) &#123; </span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">name</span> = name; </span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">age</span> = age; </span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">job</span> = job; </span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">friends</span> = [<span class="string">&quot;Shelby&quot;</span>, <span class="string">&quot;Court&quot;</span>]; </span><br><span class="line">&#125; </span><br><span class="line"><span class="comment">//再使用原型模式创建构造函数和不容易被修改的属性和函数，这样可以发挥原型的共享机制并减少内存消耗 </span></span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123; </span><br><span class="line">  constructor : <span class="title class_">Person</span>,    <span class="comment">//将构造函数放入原型中 </span></span><br><span class="line">  sayName : <span class="keyword">function</span>(<span class="params"></span>) &#123; </span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>); </span><br><span class="line">  &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>原型</tag>
        <tag>继承</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript中的原型与继承-继承篇</title>
    <url>/2017/04/13/2017-04-13-JavaScript-Prototype-and-Inheritance-2.html</url>
    <content><![CDATA[<h3 id="原型链"><a href="#原型链" class="headerlink" title="原型链"></a>原型链</h3><p>原型链是实现继承的主要方法，基本思想是<strong>利用原型让一个引用类型继承另一个引用类型的属性和方法</strong>。回顾构造函数、原型和实例之间的关系：每个构造函数都有一个原型对象，原型对象都包含一个指向构造函数的指针，而实例都包含一个指向原型的内部指针。</p>
<p>此时我们<strong>令原型对象成为另一个对象的实例，该原型对象将包含一个指向另一个原型对象的指针</strong>，若是这样层层递进，就会构成一条实例和原型的链条，这就是原型链的基本概念。</p>
<span id="more"></span>
<p><img src="https://img.alicdn.com/imgextra/i3/O1CN01cZNppR1oI6m42LpPs_!!6000000005201-1-tps-1536-932.gif" alt=""></p>
<p>由上图可以看出，原型链中，每个对象都有一个内部指针指向原型对象，而原型对象内部也会有指针指向另一个原型对象。图中新创建的Fruit和继承Fruit的Apple和Banana这样的对象的原型对象，首先会追溯到Object，而通过JavaScript的控制台使用Object.getPrototypeOf(obj)的方法得出Object的原型是function，而function的原型又会指向到Object，最后Object的原型最终会是null，到此为止null再也不会有原型对象。</p>
<p>这幅图也能很形象地展示出之前讲到过的原型搜索机制，即，当以读取模式访问一个实例属性时，1）首先搜索实例；2）搜索该实例的原型对象；3）搜索该实例的原型对象的原型对象；以此类推，直到找到null。需要注意的是：</p>
<ul>
<li>所有引用类型都默认继承了Object；</li>
<li>通过instanceof操作符来确定原型和实例之间的关系；</li>
<li>由于子类继承过程中需要重写父类中的某些方法或者添加不存在的方法，<strong>给原型添加方法的代码一定要放在原型继承的语句之后</strong>；</li>
<li>在通过原型链实现继承时，不能使用对象字面量创建原型方法；</li>
<li>由于原型的实例都共享原型中的属性和方法，所以会导致原型中可能包含的引用类型（Function、Array）也被迫同步给每一个实例，所以需要一些解决办法。</li>
</ul>
<h3 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h3><h4 id="继承方式"><a href="#继承方式" class="headerlink" title="继承方式"></a>继承方式</h4><h5 id="经典继承"><a href="#经典继承" class="headerlink" title="经典继承"></a>经典继承</h5><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">SuperType</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">colors</span> = [<span class="string">&quot;red&quot;</span>, <span class="string">&quot;blue&quot;</span>, <span class="string">&quot;green&quot;</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">SubType</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="comment">//继承了SuperType</span></span><br><span class="line">  <span class="title class_">SuperType</span>.<span class="title function_">call</span>(<span class="variable language_">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> instance1 = <span class="keyword">new</span> <span class="title class_">SubType</span>();</span><br><span class="line">instance1.<span class="property">colors</span>.<span class="title function_">push</span>(<span class="string">&quot;blaack&quot;</span>);</span><br><span class="line"><span class="title function_">alert</span>(instance1.<span class="property">colors</span>);    <span class="comment">//&quot;red,blue,green,black&quot;</span></span><br><span class="line"><span class="keyword">var</span> instance2 = <span class="keyword">new</span> <span class="title class_">SubType</span>();</span><br><span class="line"><span class="title function_">alert</span>(instance2.<span class="property">colors</span>);    <span class="comment">//&quot;red,blue,green&quot;</span></span><br></pre></td></tr></table></figure>
<p>该方法，子类中使用call函数调用了父类中的函数，当然也可以向其中传入相应的参数。然而这个方法需要将所有的方法都在构造函数中定义，造成了函数复用无法实现。</p>
<h5 id="组合继承（原型链-构造函数）"><a href="#组合继承（原型链-构造函数）" class="headerlink" title="组合继承（原型链+构造函数）"></a>组合继承（原型链+构造函数）</h5><p>参考上一篇文章中的“原型对象的缺点”以及上一点“经典继承”，结合起来就是组合继承的方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">SuperType</span>(<span class="params">name</span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">name</span> = name;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">colors</span> = [<span class="string">&quot;red&quot;</span>, <span class="string">&quot;blue&quot;</span>, <span class="string">&quot;green&quot;</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">SuperType</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">SubType</span>(<span class="params">name, age</span>) &#123;</span><br><span class="line">  <span class="title class_">SuperType</span>.<span class="title function_">call</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">age</span> = age;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">SubType</span>.<span class="property"><span class="keyword">prototype</span></span> = <span class="keyword">new</span> <span class="title class_">SuperType</span>();</span><br><span class="line"><span class="title class_">SubType</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">constructor</span> = <span class="title class_">SubType</span>;  <span class="comment">//这一步是必须的吗？</span></span><br><span class="line"><span class="title class_">SubType</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">sayAge</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">age</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>然而，组合继承也有一定的缺陷，即，无论何种情况下进行继承，都会调用量词父类的构造函数（一次是创建子类原型，其次是子类构造函数内部）</p>
<h5 id="原型式继承"><a href="#原型式继承" class="headerlink" title="原型式继承"></a>原型式继承</h5><p>原理是使用一个函数将一个对象进行复制，具体代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//将传入的对象o进行复制，副本传给一个新的构造函数</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">object</span>(<span class="params">o</span>) &#123;</span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">F</span>(<span class="params"></span>)&#123;&#125;</span><br><span class="line">  F.<span class="property"><span class="keyword">prototype</span></span> = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="title function_">F</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>而ES5中新增了Object.create()方法来规范原型式继承，该方法接受两个参数，第一个是所要复制的对象，第二个是属性描述符定义的属性和属性值。（不支持IE8及以下浏览器）</p>
<p>同样的，原型式继承也存在新创建的实例都必须默认共享原型对象的属性和方法，对于引用类型的属性是存在缺陷的。</p>
<h5 id="寄生式继承"><a href="#寄生式继承" class="headerlink" title="寄生式继承"></a>寄生式继承</h5><p>寄生式继承的思路与寄生构造函数和工厂模式类似，即，创建一个仅用于封装继承过程的函数，该函数返回集成之后的对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createAnother</span>(<span class="params">originalObj</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> cloneObj = <span class="title function_">object</span>(originalObj);</span><br><span class="line">  clone.<span class="property">sayHi</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Hi&quot;</span>);</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> cloneObj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="寄生组合式继承"><a href="#寄生组合式继承" class="headerlink" title="寄生组合式继承"></a>寄生组合式继承</h5><p>为了解决组合继承中必须调用两次父类对象的构造函数这一问题，提出了寄生组合式继承。寄生组合式继承的思路是，<strong>通过借用构造函数来几成熟型，通过原型链的混成形式来继承方法</strong>。本质上，是使用寄生式继承来继承超类型的原型，再将结果指定给子类型的原型。其基本模式如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">inheritPrototype</span>(<span class="params">subType, superType</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> prototype = <span class="title class_">Object</span>.<span class="title function_">create</span>(superType.<span class="property"><span class="keyword">prototype</span></span>);    <span class="comment">//创建父类副本</span></span><br><span class="line">  prototype.<span class="property">constructor</span> = subType;    <span class="comment">//加强副本对象</span></span><br><span class="line">  subType.<span class="property"><span class="keyword">prototype</span></span> = prototype;    <span class="comment">//完成原型继承父类副本</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该方式之调用一次SuperType的构造函数，与此同时，原型链仍保持不变（仍能够使用instanceof和isPrototypeOf()方法确定继承关系），所以该方法目前来说是引用类型最理想的继承方式。</p>
<h3 id="关于原型与继承的总结"><a href="#关于原型与继承的总结" class="headerlink" title="关于原型与继承的总结"></a>关于原型与继承的总结</h3><p>在JavaScript的编程过程中，需要考虑创建的对象是否需要被继承，如果有这样的需要，我们应当及时地使用组合模式（构造函数创建包含引用类型的属性，原型来创建该对象的函数方法）来创建该对象（构造函数）；在继承的过程中，最好采用寄生组合式继承的方法来进行对象的继承，以达到提高效率且保护原型链的目的。</p>
<p>在考虑原型和继承的过程中，需要牢记实例、对象（构造函数）和原型三者之间的关系。</p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>原型</tag>
        <tag>继承</tag>
      </tags>
  </entry>
  <entry>
    <title>IJavascript-个人在线前端代码解释器安装与使用</title>
    <url>/2017/05/09/2017-04-15-Online-Javascript-Interpreter-IJavascript.html</url>
    <content><![CDATA[<h4 id="Jupyter-开源的文档-代码编辑、解释、分享工具"><a href="#Jupyter-开源的文档-代码编辑、解释、分享工具" class="headerlink" title="Jupyter-开源的文档/代码编辑、解释、分享工具"></a>Jupyter-开源的文档/代码编辑、解释、分享工具</h4><p>ProjectJupyter<a href="https://github.com/jupyter">1</a>是一项开源的项目，只在创造一个平台供各种开发人员、前沿科学工作者以及坐在电脑前进行工作的各位进行协同开发。该项目下拥有Jupyter Notebook、nbdime、qtconsole等开源应用。本文主要讲述使用Jupyter Notebook应用和IJavascript插件来进行前端代码的可视化编辑、解释以及协同分享。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN01KMRQCb1j1YNXyLSAg_!!6000000004488-49-tps-824-340.webp" alt=""></p>
<span id="more"></span>
<h4 id="安装Jupyter-Notebook"><a href="#安装Jupyter-Notebook" class="headerlink" title="安装Jupyter Notebook"></a>安装Jupyter Notebook</h4><ul>
<li><strong>Python</strong> 环境时安装Jupyter Notebook的必要环境（3.3以上或2.7均可）</li>
<li>可以先从官网下载Anaconda安装包，Anaconda中继承了NumPy和Jupyter Notebook等工具包，所以直接可以打开Jupyter Notebook应用</li>
<li><p>macOS系统下可以通过终端的pip工具进行安装：</p>
<ul>
<li><p>首先要升级pip到最新版本：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">pip3 install --upgrade pip</span><br></pre></td></tr></table></figure>
<p>如果pip3命令不存在，需要安装pip3或者（如果电脑中安装的是python2）使用pip命令进行安装。</p>
</li>
<li><p>使用pip3命令进行安装jupyter：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">pip3 install jupyter</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h4 id="安装IJavascript"><a href="#安装IJavascript" class="headerlink" title="安装IJavascript"></a>安装IJavascript</h4><p>Jupyter Notebook自带python语言编写程序的编译，如果需要其他语言版本的notebook（Julia，Ruby，Haskell等），还需要安装相应语言对应的Kernel<a href="https://github.com/jupyter/jupyter/wiki/Jupyter-kernels">2</a>，对于前端工程师来说，当然是需要一个Html、CSS和JavaScript集成的在线解释器，所以IJavascript<a href="https://github.com/n-riesco/ijavascript">3</a>就应运而生。IJavascript是Jupyter Notebook的Kernel（支持该功能的是Jupyter messaging protocol）之一。IJavascript同时也在npm包管理工具中，而npm又需要Node.js环境。所以，需要首先确定系统中有Node.js和npm的环境。</p>
<p>在终端中运行以下代码可以完成IJavascript Kernel的安装：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">ruby -e <span class="string">&quot;$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)&quot;</span></span><br><span class="line">brew install pkg-config node zeromq</span><br><span class="line">sudo easy_install pip</span><br><span class="line">sudo pip install --upgrade pyzmq jupyter</span><br><span class="line">sudo npm install -g ijavascript</span><br></pre></td></tr></table></figure>
<h4 id="IJavascript的使用"><a href="#IJavascript的使用" class="headerlink" title="IJavascript的使用"></a>IJavascript的使用</h4><p>在终端中输入代码</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">jupyter notebook</span><br></pre></td></tr></table></figure>
<p>启动Jupyter Notebook，可以在浏览器打开地址为<a href="http://localhost:8888的web应用，在右上角“new”选项中选择Javascript(Node.js">http://localhost:8888的web应用，在右上角“new”选项中选择Javascript(Node.js),就可以开始使用了！</a>,就可以开始使用了！)</p>
<p>IJavascript不仅可以像控制台一样进行交互式的JavaScript调试，而且可以对HTML和SVG代码进行输出，只需要在输入的时候加上$$html$$或$$svg$$就可以进行代码的所见即所得了。详细的教程可以查看IJavascript官方文档<a href="http://n-riesco.github.io/ijavascript/index.html">4</a>.</p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Jupyter</tag>
        <tag>前端</tag>
        <tag>解释器</tag>
        <tag>说明书</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript高级程序设计笔记整理</title>
    <url>/2017/05/09/2017-05-08-JavaScript-Professional-JavaScript-for-Web-Developer-notebook.html</url>
    <content><![CDATA[<h3 id="第三章-基本概念"><a href="#第三章-基本概念" class="headerlink" title="第三章 基本概念"></a>第三章 基本概念</h3><h4 id="语法："><a href="#语法：" class="headerlink" title="语法："></a>语法：</h4><ul>
<li><strong>区分大小写</strong>：ECMAScript中的一切都区分大小写</li>
<li><strong>标识符</strong>：变量、函数、属性、函数参数等的名字，由字母，下划线，一个美元符号开头</li>
<li><strong>注释</strong>：//单行注释，/<em> </em>/多行注释（尽量少用该种注释方法）</li>
<li><strong>严格模式</strong>：在脚本顶部或者某函数内顶部添加”use strict”,支持IE10+,Firefox4+，Safari5.1+，Opera12+，Chrome；</li>
</ul>
<span id="more"></span>
<h4 id="语句"><a href="#语句" class="headerlink" title="语句"></a>语句</h4><p>由分号结尾，多条语句以花括号包围形成代码块，有以下几种语句类型：</p>
<ul>
<li>_if_ 语句：与Java中的条件判断语句语法相同</li>
<li><em>do-while</em> 语句：相同条件下会比while循环语句少执行一次</li>
<li><em>while</em> 语句：与Java中的条件判断语句语法相同</li>
<li><em>for</em> 语句：与Java中的条件判断语句语法相同</li>
<li><em>for-in</em> 语句：支持任何有迭代属性的对象（Object、Array、Set、Map等）进行枚举循环操作</li>
<li><p><em>label</em> 语句：添加标签一般与循环语句配合使用，使用break或continue语句引用</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> temp=<span class="number">0</span>;  </span><br><span class="line"><span class="attr">start</span>:  </span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>; i&lt;<span class="number">5</span>; i++) &#123;  </span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">var</span> m=<span class="number">0</span>; m&lt;<span class="number">5</span>; m++) &#123;  </span><br><span class="line">      <span class="keyword">if</span>(m==<span class="number">1</span>) &#123;  </span><br><span class="line">          <span class="keyword">break</span> start;  <span class="comment">//输出1，若去掉start将输出5</span></span><br><span class="line">      &#125;  </span><br><span class="line">      temp++;  </span><br><span class="line">  &#125;  </span><br><span class="line">&#125;  </span><br><span class="line"><span class="title function_">alert</span>(temp);</span><br></pre></td></tr></table></figure>
</li>
<li><p><em>break</em> 和 <em>continue</em> 语句：后面可引用label</p>
</li>
<li><p><em>with</em> 语句：将代码作用域设置到一个特定对象中，避免多次编写同一个对象的工作</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">with</span>(<span class="params">someObject</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> qs = search.<span class="title function_">substring</span>(<span class="number">1</span>);   <span class="comment">//如果没有使用with语句，这个表达式应该是var qs = someObject.search.substring(1);</span></span><br><span class="line">  <span class="keyword">var</span> hostName = hostname;</span><br><span class="line">  <span class="keyword">var</span> url = href;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><em>switch</em> 语句:与Java中的条件判断语句语法相同，注意使用<strong>break</strong>语句</p>
</li>
</ul>
<h4 id="数据类型和变量"><a href="#数据类型和变量" class="headerlink" title="数据类型和变量"></a>数据类型和变量</h4><ul>
<li><p><strong>关键字和保留字</strong>：<br>表达式和代码块中出现关键字和保留字一定要留意是否可用</p>
</li>
<li><p><strong>变量</strong>：</p>
<ul>
<li>ECMAScript中变量为松散类型，var定义局部变量，可以不赋值（此时为undefined类型）</li>
<li>无var关键词时定义的是全局变量（但是严格模式下会抛出错误）</li>
</ul>
</li>
<li><p><strong>数据类型</strong>：<br>由typeof操作符可以显示给定变量的数据类型，有Undefined、Boolean、String、Number、Object（Array、Null）、Function六中数据类型；</p>
<ul>
<li>Undefined：未声量变量使用typeof操作符时显示undefined，而且对未声明变量使用alert()方法会报错；</li>
<li>Boolean：true&amp;false，注意各种数据类型对应boolean值得转换规则；</li>
<li>Number：八进制0开头，十六进制0x开头；NaN不等于任何值（包括NaN本身）；非数值转换为数值的三个函数：number(),parseInt(“10”,进制基数)，parseFloat()；数值转换（true:1，false:0，null:0，undefined:NaN）；字符串转换规则（只包含有效数字转化为非0开头数值，空字符串转化为0，字符串包含除有效数字之外的转换为NaN）；</li>
<li>String：字符串不可变，改变时会将原来的变量值销毁再重新赋值；数值、布尔值、对象和字符串值都有toString()方法；</li>
<li>Object：都有Constructor、hasOwnproperty(propertyName)、isPrototypeOf(object)、propertyIsEnumerable(propertyName)、toLocaleString()、toString()、valueOf();</li>
<li>Null：对值为null的变量使用typeof时返回object，因为null表示一个空对象指针；null==undefined总是返回true；</li>
</ul>
</li>
<li><p><strong>操作符</strong>：</p>
<ul>
<li>一元：++、–、+、-、~位、!逻辑、</li>
<li>二元：&amp;位、|位、^、&lt;&lt;、&gt;&gt;、&gt;&gt;&gt;、&amp;&amp;逻辑、||逻辑、+、-、*、/、%、&gt;、&lt;、&gt;=、&lt;=、==、!=、===、!==、</li>
<li>三元：boolean_expression？true_value:false_value;</li>
<li><p>赋值：=、+=、-=、*=、/=、%=、&lt;&lt;=、&gt;&gt;=、&gt;&gt;&gt;=</p>
<p><strong>运算符优先级</strong>：.、[]、()先于delete、new、typeof、+（一元）、—（一元）、！、~先于*、/、%先于+、-先于&gt;=、&lt;=、&gt;、&lt;先于===、!==先于&amp;&amp;先于||先于?:</p>
</li>
</ul>
</li>
<li><p><strong>函数</strong>：使用function关键词声明函数，参数类似数组并可省略，参数（和js中的基本类型）都是传值而非传引用（例如string对象）；名字相同的函数只有后定义的函数可以使用；</p>
</li>
</ul>
<h3 id="第四章-变量、作用域和内存问题"><a href="#第四章-变量、作用域和内存问题" class="headerlink" title="第四章 变量、作用域和内存问题"></a>第四章 变量、作用域和内存问题</h3><h4 id="基本类型和引用类型："><a href="#基本类型和引用类型：" class="headerlink" title="基本类型和引用类型："></a>基本类型和引用类型：</h4><ul>
<li>除Object之外的基本类型都是按值访问，对象则是通过访问内存中对象的引用；</li>
<li>基本类型无法添加和改变属性；</li>
<li>复制基本类型值会产生一个独立副本，而复制引用类型值时两者是指向同一个对象的指针；</li>
<li>检测基本类型值时使用typeof，<strong>检测引用类型值</strong> 时使用instanceof。另外，除了instanceof之外还可以使用return somevar.<strong>proto</strong>.constructor==sometype或者Object.prototype.toString.call(somevar)。</li>
</ul>
<h4 id="执行环境和作用域："><a href="#执行环境和作用域：" class="headerlink" title="执行环境和作用域："></a>执行环境和作用域：</h4><ul>
<li>局部执行环境（某个函数、let绑定代码块等）：某个执行环境中的所有代码执行完后，该环境被销毁，其中的所有变量和函数定义也随之消失；</li>
<li>全局执行环境：Web浏览器中，全局执行环境被认为是window对象；</li>
<li>每个函数也都有自己的执行环境，执行流（ECMAScript）进入函数，该函数环境被推入一个环境栈；</li>
<li>代码在一个环境中执行，会创建一个作用域链，用途是保证对执行环境有权访问的所有变量和函数有序访问；</li>
<li>延长作用域链：try-catch语句中的catch以及with语句</li>
<li>js中 <strong>没有块级作用域</strong>（任何花括号中的代码块），例如在if和for中的变量可以在if和for之外访问：<ol>
<li>声明变量：使用var声明的变量自动被添加到最接近的环境中，没有用var声明会被添加到全局环境；</li>
<li>查询标识符：某环境中出现一个标识符时，要通过搜索确定该标识符代表什么，从作用域链前端开始，向上逐级查询，匹配到的第一个停止搜索，变量就绪；若全局中都没找到，则算未声明；</li>
</ol>
</li>
</ul>
<h4 id="垃圾回收：js具有自动垃圾收集机制"><a href="#垃圾回收：js具有自动垃圾收集机制" class="headerlink" title="垃圾回收：js具有自动垃圾收集机制"></a>垃圾回收：js具有自动垃圾收集机制</h4><ul>
<li><strong>标记清除</strong>：变量进入某环境，标记该变量，离开环境时进行离开标记，释放内存；</li>
<li><strong>引用计数</strong>：声明某变量并将引用类型值赋给该变量，该值引用次数为1，每被赋给一次就加1；若包含对这个值引用的变量取得另外一个值，则减1，该值变成0时就释放内存空间；</li>
<li><strong>管理内存</strong>：为了占用最少内存达到更好性能，要将不再使用的变量赋值为null来释放其引用；</li>
</ul>
<h3 id="第五章-引用类型"><a href="#第五章-引用类型" class="headerlink" title="第五章 引用类型"></a>第五章 引用类型</h3><h4 id="Object类型"><a href="#Object类型" class="headerlink" title="Object类型"></a>Object类型</h4><ul>
<li><p><strong>创建对象时首选字面量创建方法</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  <span class="string">&quot;name&quot;</span> : <span class="string">&quot;Nicholas&quot;</span>,</span><br><span class="line">  <span class="string">&quot;age&quot;</span> : <span class="number">29</span>,</span><br><span class="line">  <span class="number">5</span> : <span class="literal">true</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="Array类型"><a href="#Array类型" class="headerlink" title="Array类型"></a><a href="#Array类型" title="Array类型"></a>Array类型</h4><ul>
<li><p><strong>定义</strong>：<code>var color = new Array(3);</code></p>
</li>
<li><p><strong>访问</strong>：采用数组下标[]的方式进行访问，使用.length得到数组长度；</p>
</li>
<li><p><strong>检测</strong>：value instanceof Array(不推荐),<em>Array.isArray</em>(somearrayvalue)(推荐)；</p>
</li>
<li><p><strong>转换</strong>：toLocaleString()/toString()/valueOf(),均生成 <strong>逗号分隔</strong> 的字符串其中，toLocaleString()方法转换时每一项都是使用toLocaleString()转换的，toString()也是Array.join(someJointSymbol)，默认用逗号连接；</p>
</li>
<li><p><strong>插入/删除</strong>：除了直接用下标添加删除，最好用栈方法LIFO-push(任意项参数)在尾部增加任意数量参数，pop()删除最后一项；同时还有队列方法FIFO,在栈方法基础上增加shift()可删除第一项，unshift()可以在前端添加任意项；</p>
</li>
<li><p><strong>排序</strong>：sort()升序排列（转化为字符串进行比较，数字比较会出错，需要在参数中提供比较函数compare()）；reverse()反转数组顺序；</p>
</li>
<li><p><strong>操作方法</strong></p>
<ul>
<li><p><strong>连接</strong>：somearray.concat(otherarrays)返回连接所有数组的副本；</p>
</li>
<li><p><strong>删除</strong>：somearray.splice(起始位置，删除项数，插入项)-splice(0,2)删除前两项，splice(2,0,”red”,”green”)在当前数组位置2插入两项，splice(2,1,”red”,”green”)删除第二项并插入；</p>
</li>
<li><p><strong>位置方法</strong>：获取数组中元素的位置，indexOf()和lastIndexOf(),返回数组下表对应元素，否则返回-1；</p>
</li>
<li><p><strong>迭代方法</strong>：有5个迭代方法，参数都是两个，每一项上运行的函数和运行该函数的作用域对象every(someFunction)；对每一项进行函数操作，每一项返回true则最后返回true；filter(someFunction):对每一项运行给定函数，返回每次调用该函数值为true的项的数组forEach(someFunction):对每一项运行给定函数，无返回值map(someFunction):对每一项运行给定函数，返回每次调用该函数结果的数组some(someFunction):对每一项运行给定函数，只要有一项使函数返回true，则整个返回true；</p>
</li>
<li><p><strong>缩小方法</strong>：reduce(给每项调用的函数(可选)，缩小基础初始值)/reduceRight(给每项调用的函数(可选)，缩小基础初始值)，其中，参数中的函数需要传入4个参数，分别是前一项，当前项，项索引，数组对象；reduceRight是相反方向；</p>
</li>
<li><p><strong>禁忌</strong>：[， ， ， ，]不可以出现数组最后一项是，的情况</p>
</li>
</ul>
</li>
</ul>
<h4 id="Date类型"><a href="#Date类型" class="headerlink" title="Date类型"></a>Date类型</h4><ul>
<li><p><strong>定义</strong>：`var now = new Date();</p>
</li>
<li><p><strong>特定时间</strong>：<code>var someDate = new Date(Date.parse(“May 25, 2004”));</code>但是不用date.parse也会默认转换；</p>
</li>
<li><p><strong>继承的方法</strong></p>
<ul>
<li>toLocaleString()按照与浏览器设置的地区相适应的格式返回日期和时间</li>
<li>toString()返回带有时区信息的日期和时间</li>
<li>valueOf()返回日期的毫秒表示</li>
</ul>
</li>
<li><p><strong>日期格式化方法</strong></p>
<ul>
<li>toDateString():以特定于实现的格式返回日年月日星期</li>
<li>toTimeString():以特定于实现的格式返回时区、时分秒</li>
<li>toLocaleDateString():以特定于地区的格式返回年月日星期</li>
<li>toLocaleTimeString():以特定于实现的格式显示时分秒</li>
<li>toUTCString():以特定于实现的格式完整的UTC日期</li>
</ul>
</li>
<li>日期时间组件方法<br>￼ ￼</li>
</ul>
<h4 id="RegExp类型："><a href="#RegExp类型：" class="headerlink" title="RegExp类型："></a>RegExp类型：</h4><ul>
<li><strong>定义</strong>：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> pattern = <span class="keyword">new</span> <span class="title class_">RegExp</span>(“pattern”, “flags”);</span><br><span class="line"><span class="keyword">var</span> expression = <span class="regexp">/pattern/</span> flags;</span><br></pre></td></tr></table></figure>
<p>其中flags有一下几种模式：</p>
<ul>
<li>_g_:全剧模式，模式应用于所有字符串而非在发现第一个匹配项时立即停止；</li>
<li>_i_:不区分大小写模式；</li>
<li><p>_m_:多行模式，到达一行末尾还会查找下一行是否有匹配项</p>
<p><em>元字符转义：( { [ \ ^ $ | ) ? </em> + . ] }</p>
<p>e.g.匹配第一个”[bc]at”:<code>var patter = /\[bc\]at/i;</code></p>
</li>
<li><p>实例属性</p>
<ul>
<li>.global:bool是否设置g标志</li>
<li>.ignoreCase:bool是否设置了i标志</li>
<li>.lastIndex:整数，表示开始搜索下一个匹配项的自负位置，0算起</li>
<li>.multiline:bool，表示是否设置了m标志</li>
<li>.source:该正则的字符串表示</li>
</ul>
</li>
<li><p>实例方法：.exec()</p>
<ul>
<li>构造函数属性：</li>
<li>模式的局限性：</li>
</ul>
</li>
</ul>
<h4 id="Function类型"><a href="#Function类型" class="headerlink" title="Function类型"></a>Function类型</h4><p>函数是对象，因此函数名实际上也是一个指向函数对象的指针，可以使用多个指针指向同一个函数。</p>
<ul>
<li><p><strong>声明</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">sum</span>(<span class="params">num1, num2</span>)&#123;</span><br><span class="line">  <span class="keyword">return</span> num1 + num2;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>特征</strong></p>
<ul>
<li><p><strong>没有重载</strong>:因为JS中的函数名就像指针一样，所以当为一个函数名（就是一个变量）赋予新的函数时，会覆盖之前的函数；</p>
</li>
<li><p><strong>声明位置和表达式</strong>:</p>
<p>JS中有函数声明提升过程，所以声明和调用不强调先后顺序。<br><strong>但是！如果把函数放进一个初始化语句中而非函数声明时,会出现先后问题的错误；</strong></p>
</li>
<li><p><strong>作为值的函数</strong>:函数名可作为变量像值一样传入另一个函数；函数内也可以返回一个函数的计算结果等等；</p>
</li>
<li><p><strong>函数属性和方法</strong>:</p>
<ul>
<li><p>函数作为对象有两个属性:</p>
<ul>
<li>length（表示函数希望接受的命名参数的个数）</li>
<li>prototype（保存引用类型的所有实例的方法，是不可枚举的）</li>
</ul>
</li>
<li><p>函数作为对象有两个方法:</p>
<ul>
<li>apply()：接受两个参数，一个是其中运行函数的作用域，另一个是参数数组（可以使arguments对象或者是真实的一个数组）</li>
<li>call()：接受this参数以及来自函数的每一个参数都要写进去而非传入数组），这两个函数都是在特定的作用域中调用函数，相当于设置函数体内this对象的值</li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li><p><strong>函数内部对象及环境的属性</strong></p>
<ul>
<li><p>两个特殊对象</p>
<ul>
<li><p>arguments：包含传入函数的所有参数，并且有一个callee的属性指向拥有这个arguments对象的函数，其实就是这个函数，另一个caller的属性返回引用本函数的函数</p>
</li>
<li><p>this：引用的是函数执行的环境对象，网页的全局就是window</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="基本包装类型"><a href="#基本包装类型" class="headerlink" title="基本包装类型"></a>基本包装类型</h4><p>String、Boolean、Number（Object、Null、Undefined、Array）</p>
<ul>
<li><p>String：</p>
<ul>
<li><p>方法：</p>
<ul>
<li>下标相关<ul>
<li>所指的单字符串charAt()</li>
<li>所指字符的编码charCodeAte()</li>
<li>字符串位置indexOf(substr)</li>
<li>最后一个字符串位置lastIndexOf(substr)</li>
</ul>
</li>
<li><p>字符串操作</p>
<ul>
<li>连接concat(String str1, String str2)</li>
<li>截取slice(startPos(, endPos))</li>
<li>截取<strong>substr</strong>(startPos(, length))</li>
<li>截取substring(startPos(, endPos))，当输入负数的index时，slice将负数和字符串长度相加，substr将第一个负参数加字符串长度第二个负参数转换为0，substring将所有负参数转化为0；</li>
</ul>
</li>
<li><p>格式化字符串方法trim(string)创建一个副本并删除前后所有空格</p>
</li>
<li><p>大小写相关</p>
<ul>
<li>转小写toLowerCase()</li>
<li>转本地语言toLocaleLowerCase()</li>
<li>toUpperCase()</li>
<li>toLocaleUpperCase()</li>
</ul>
</li>
<li><p>字符串模式匹配方法match()详见正则表达式，search()返回第一个匹配项索引否则返回-1</p>
</li>
<li><p><strong>replace(新字符串，需要替换的字符串)</strong></p>
</li>
<li><p>split(simbol)，由simbol分隔字符串，返回一个字符串数组</p>
</li>
<li><p>string1.localeCompare(string2)返回：string1在string2之前-1，相等0，之后1</p>
</li>
<li><p>fromCharCode()静态方法，接收一或多个字符编码，然后转换成一个字符串</p>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="单体内置对象"><a href="#单体内置对象" class="headerlink" title="单体内置对象"></a>单体内置对象</h4><ul>
<li><p>Global对象</p>
<p>在js中，不属于任何其他对象的属性和方法，都是它的属性和方法。所有在全局作用域中定义的属性和函数都是它的属性和函数。例如isNaN()/isFinite()/parseInt()/parseFloat()/encodeURI()只编码空格/encodeURIComponet()编码所有非字母数字字符/decodeURI()/decodeURICompoment()/eval()接收一个js语句字符串，若是函数则不会被提升，需要按照声明顺序进行调用，严格模式下外部不能访问该函数中的函数或变量</p>
</li>
</ul>
<h3 id="第六章-面向对象的程序设计"><a href="#第六章-面向对象的程序设计" class="headerlink" title="第六章 面向对象的程序设计"></a>第六章 面向对象的程序设计</h3><p>对象就是无序属性的集合，其属性可以包含基本值、对象或者函数。创建对象的基础方法：</p>
<ul>
<li><p>创建Object实例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = <span class="keyword">new</span> <span class="title class_">Object</span>();</span><br><span class="line">person.<span class="property">name</span> = <span class="string">&quot;Nicholas&quot;</span>;</span><br><span class="line">person.<span class="property">age</span> = <span class="number">29</span>;</span><br><span class="line">person.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="title function_">alert</span>(<span class="string">&quot;this.name&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>对象字面量语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;</span><br><span class="line">  person.<span class="property">name</span> = <span class="string">&quot;Nicholas&quot;</span>;</span><br><span class="line">  person.<span class="property">age</span> = <span class="number">29</span>;</span><br><span class="line">  person.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;this.name&quot;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="理解对象"><a href="#理解对象" class="headerlink" title="理解对象"></a>理解对象</h4><ul>
<li>属性类型：</li>
</ul>
<p>ECMAScript中有两种属性：<strong>数据属性</strong> 和 <strong>访问器属性</strong>。</p>
<ul>
<li><p><strong>数据属性</strong>：包含一个数据值的位置，在此位置可读取和写入值，有以下特性：</p>
<ul>
<li>[[Configurable]]默认值为true，表示能否通过delete删除属性</li>
<li>[[Enumerable]]默认值为true，表示能否通过for-in循环返回属性</li>
<li>[[Writable]]默认值为true，表示能否修改属性的值</li>
<li><p>[[Value]]默认值为undefined，包含这个属性的数据值</p>
<p>要修改上述属性，必须通过<em>Object.defineProperty</em>(属性所在对象，属性名，描述符对象)方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> person = &#123;&#125;;</span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(person, <span class="string">&quot;name&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">writable</span>: <span class="literal">false</span>,</span><br><span class="line">  <span class="attr">value</span>: <span class="string">&quot;Nicholas&quot;</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p><strong>访问器属性</strong>：不包含数据值，只包含getter和setter函数（这两个函数并非必需），访问器属性具有如下特征：</p>
<ul>
<li>[[Configurable]]默认值为true，表示能否通过delete删除属性</li>
<li>[[Enumerable]]默认值为true，表示能否通过for-in循环返回属性</li>
<li>[[Get]]默认值为undefined，读取属性时调用的函数</li>
<li>[[Set]]默认值为undefined，写入属性时调用的函数</li>
<li><p>访问器属性也必须使用Object.defineProperty()方法来进行定义：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> book = &#123;</span><br><span class="line">  <span class="attr">_year</span>: <span class="number">2004</span>,</span><br><span class="line">  <span class="attr">edition</span>: <span class="number">1</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(book, <span class="string">&quot;year&quot;</span>, &#123;</span><br><span class="line">  <span class="attr">get</span>: <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">_year</span>;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">set</span>: <span class="keyword">function</span>(<span class="params">newValue</span>)&#123;</span><br><span class="line">    <span class="keyword">if</span>(newValue &gt; <span class="number">2004</span>)&#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">_year</span> = newValue;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">edition</span> = newValue - <span class="number">2004</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">//若想定义多个属性或对应的方法时，可以这样写：</span></span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">defineProperty</span>(book, &#123;</span><br><span class="line">  <span class="comment">//这里定义了两个数据属性</span></span><br><span class="line">  <span class="attr">_year</span>: &#123;</span><br><span class="line">    <span class="attr">value</span>: <span class="number">2004</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">edition</span>: &#123;</span><br><span class="line">    <span class="attr">value</span>: <span class="number">1</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">//这里定义了一个访问器属性</span></span><br><span class="line">  <span class="attr">year</span>: &#123;</span><br><span class="line">    <span class="attr">get</span>: <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">_year</span>;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">set</span>: <span class="keyword">function</span>(<span class="params">newValue</span>)&#123;</span><br><span class="line">      <span class="keyword">if</span>(newValue &gt; <span class="number">2004</span>)&#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">_year</span> = newValue;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">edition</span> = newValue - <span class="number">2004</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果想读取属性的特征，则要通过Object.getOwnPropertyDescriptor()函数来访问：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> descriptor = <span class="title class_">Object</span>.<span class="title function_">getOwnPropertyDescriptor</span>(book, <span class="string">&quot;_year&quot;</span>);</span><br><span class="line"><span class="title function_">alert</span>(descriptor.<span class="property">value</span>);</span><br><span class="line"><span class="title function_">alert</span>(<span class="keyword">typeof</span> descriptor.<span class="property">get</span>); <span class="comment">//&quot;function&quot;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h4 id="创建对象"><a href="#创建对象" class="headerlink" title="创建对象"></a>创建对象</h4><p>创建类和对象名要使用大写字母开头，构造函数也是，而非构造函数则由小写字母开头。</p>
<ul>
<li><p><strong>工厂模式</strong>：抽象了创建具体对象的过程，用函数来封装以特定接口创建对象的细节：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createPerson</span>(<span class="params">name, age, job</span>)&#123;</span><br><span class="line">  <span class="keyword">var</span> o = <span class="keyword">new</span> <span class="title class_">Object</span>();</span><br><span class="line">  o.<span class="property">name</span> = name;</span><br><span class="line">  o.<span class="property">age</span> = age;</span><br><span class="line">  o.<span class="property">job</span> = job;</span><br><span class="line">  o.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> o;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> person1 = <span class="title function_">createPerson</span>(<span class="string">&quot;Nicholas&quot;</span>, <span class="number">29</span>, <span class="string">&quot;Software Engineer&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>其实就是把一个完整的由Object实例来创建对象的过程放在一个函数中；</p>
</li>
<li><p><strong>构造函数模式</strong>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params">name, age, job</span>)&#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">name</span> = name;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">age</span> = age;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">job</span> = job;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> person1 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Nicholas&quot;</span>, <span class="number">29</span>, <span class="string">&quot;Software Engineer&quot;</span>);</span><br></pre></td></tr></table></figure>
<p>可以看到，构造函数模式中 <strong>没有显式地创建对象</strong>，<strong>直接将属性和方法赋给了this对象</strong>，<strong>没有return语句</strong> 。</p>
<p>构造函数模式相较于工厂模式的一个优点是，它创建的构造函数可以将它的实例标识为一种特定的类型，即对象也是Object也是Person的实例。</p>
<p>将构造函数当做函数： 任何函数，只要通过new操作符来调用，就可以作为构造函数。</p>
</li>
<li><p><strong>原型模式</strong></p>
<p>JavaScript中并没有提供类的实现，虽然在ES2015/ES6之中引入了class关键字，但是JavaScript仍然是基于原型的。而JavaScript中创建对象的方法有：new Object()方法、字面量方法、工厂模式、构造函数方法和<strong>原型模式</strong>。其中，使用原型模式创建对象可以令所有实例共享方法，减少内存消耗，有利于对象继承。</p>
<p>JavaScript中的<strong>继承</strong>则是体现在一种结构上——对象，所有的对象都是由Object衍生的对象，所有的对象都继承了Object.prototype的方法和属性（也有可能被覆盖）。每一个对象都有一个内部链接到另一个对象，这个对象成为它的原型（prototype）。而且，该原型对象也有自己的原型，直到追溯到一个以null为原型的对象，因为null是没有原型的，所以可以作为这个<strong>原型链（prototype chain)</strong> 中的最终链接。</p>
<p>只要创建了一个新函数，就会为该函数创建一个prototype属性，这个属性指向函数的原型对象。所有原型对象都自动获得一个constructor属性，这个属性包含一个指向prototype属性所在函数的指针。</p>
<ul>
<li><p>原型对象的创建：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params">name, age, job</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">name</span> = name;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">age</span> = age;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">job</span> = job;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">var</span> person1 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Nicholas&quot;</span>, <span class="number">29</span>, <span class="string">&quot;Lawyer&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> person2 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Katie&quot;</span>, <span class="number">30</span> <span class="string">&quot;Account&quot;</span>);</span><br><span class="line"><span class="keyword">var</span> person3 = <span class="keyword">new</span> <span class="title class_">Person</span>(<span class="string">&quot;Nicholas&quot;</span>, <span class="number">29</span>, <span class="string">&quot;Lawyer&quot;</span>);</span><br><span class="line">person1.<span class="title function_">sayName</span>();     <span class="comment">//&quot;Nicholas&quot;</span></span><br><span class="line">person2.<span class="title function_">sayName</span>();     <span class="comment">//&quot;Katie&quot;</span></span><br><span class="line"><span class="title function_">alert</span>(person1.<span class="property">sayName</span> == person3.<span class="property">sayName</span>);    <span class="comment">//true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>更加简单的原型对象创建语法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params"></span>) &#123;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123;</span><br><span class="line">  name : <span class="string">&quot;Nicholas&quot;</span>,</span><br><span class="line">  age : <span class="number">29</span>,</span><br><span class="line">  job : <span class="string">&quot;Software Engineer&quot;</span>,</span><br><span class="line">  sayName : <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p><strong>寄生构造函数模式</strong></p>
<p>与工厂模式没有本质上的区别，只是在创建新的实例的时候，工厂模式不需要new，而寄生构造函数需要加new。这种模式不能够以来instanceof操作符来确定对象类型，所以建议在能够使用其他模式创建对象时尽量不要使用这种模式。</p>
</li>
<li><p><strong>稳妥构造函数模式</strong></p>
<p>JavaScript中的稳妥对象是指，没有公共属性，而且其方法也不引用this的对象。稳妥对象最适合在一些禁止使用this和new的安全环境中使用，防止被Mashup等的应用程序改动。</p>
<p>稳妥构造函数遵循与寄生构造函数类似的模式，但有两点不同：一是新创建的对象的实例方法不引用this；二是不适用new操作符调用构造函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params">name, age, job</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> o = <span class="keyword">new</span> <span class="title class_">Object</span>();</span><br><span class="line">  o.<span class="property">sayName</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(name);</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">return</span> o;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h4><h5 id="原型链"><a href="#原型链" class="headerlink" title="原型链"></a>原型链</h5><ul>
<li><p>代码搜索某属性的路径（原型链）：</p>
<p>上面代码中出现了Person.prototype,通过该形式可以获得原型对象，并且可以为其添加属性和方法。这段代码也展示了对象属性和方法搜索的路径：每当代码读取某个对象的某个属性时，进行目标为该属性的名字，搜索首先从实例（person1）本身开始，若有该名字的属性则返回该属性，若没有则继续搜索该实例中原型指针指向的原型对象，在原型对象中查找该名字的属性。所以说<strong>实例是共享原型中保存的属性和方法</strong>。</p>
</li>
<li><p>实例的属性，原型的属性？</p>
<p>一个对象的所有实例尽管可以共享该原型对象的所有属性和方法，但是却无法重写原型对象中的属性和方法，只能通过利用同名属性和方法来覆盖和屏蔽原型对象中的属性和方法；若想把实例中覆盖的属性还原，可以通过delete操作。使用hasOwnProperty()方法可以检测一个属性是存在于实例中还是存在于原型中。</p>
</li>
<li><p>怎样获得原型对象</p>
<p>下面的代码展示了Object.getPrototypeOf(obj)方法和<strong>proto</strong>属性的使用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Object</span>.<span class="title function_">getPrototypeOf</span>(person1) === <span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>;    <span class="comment">//true</span></span><br><span class="line">person1.<span class="property">__proto__</span> === <span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>;    <span class="comment">//true</span></span><br></pre></td></tr></table></figure>
<p>可以看到，Object.getPrototypeOf(obj)是获取obj对象的原型对象的方法，这个方法将在<strong>利用原型实现继承的情况中发挥非常重要的作用</strong>。obj.<strong>proto</strong> 也是如此，是每一个对象都拥有的属性，但是<strong>proto</strong>并不是一个规范的属性（当使用Object.create()方法创建对象时，<strong>proto</strong> 并不能指向该对象的原型对象），其对应的标准属性应当是[[Prototype]]。</p>
</li>
<li><p>原型对象和构造函数</p>
<p>对于对象Person来说，它的构造函数是Person()，它的原型对象为Person.prototype；而Person.prototype.constructor又会指回Person。而对象Person的实例person1和person2都包含有一个属性[[Prototype]]（也就是上面提到的<strong>proto</strong>）它们都指向Person.prototype；同时，person1和person2也可以通过isprototypeOf(）方法来确定是否与确定对象之间有这种关系：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">alert</span>(<span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="title function_">isPrototypeOf</span>(person1));    <span class="comment">//true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>原型与in操作符</p>
<ul>
<li><p>in操作符单独使用的情况</p>
<p>in操作符单独使用会在通过对象能够访问给定属性时返回true，无论该属性存在于实例中还是在原型中：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接上面的代码</span></span><br><span class="line"><span class="keyword">var</span> person4 = <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line"><span class="title function_">alert</span>(person4.<span class="title function_">hasOwnproperty</span>(<span class="string">&quot;name&quot;</span>));    <span class="comment">//由于person4实例没有覆盖原型中的name所以返回false</span></span><br><span class="line"><span class="title function_">alert</span>(<span class="string">&quot;name&quot;</span> <span class="keyword">in</span> person4);    <span class="comment">//true</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>for-in循环</p>
<p>该种方式是返回所有能够通过对象访问的、可枚举的属性，包括实例和原型中的属性，并且屏蔽了原型中不可枚举（[[Enumerable]]）的属性（仅在IE8及更早版本中有不可枚举的属性）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> o = &#123;</span><br><span class="line">  toString : <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&quot;my Object&quot;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">var</span> prop <span class="keyword">in</span> o) &#123;</span><br><span class="line">  <span class="keyword">if</span> (prop == <span class="string">&quot;toString&quot;</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Found toString&quot;</span>);      <span class="comment">//IE中无法显示</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>若想获取对象中所有可枚举的实例属性，可以使用Object.keys(),该函数接受一个对象或实例作为参数（当然也可以是原型对象），返回一个包含所有可枚举属性的字符串数组。示例代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接Person对象代码</span></span><br><span class="line"><span class="keyword">var</span> keys = <span class="title class_">Object</span>.<span class="title function_">keys</span>(person.<span class="property"><span class="keyword">prototype</span></span>);</span><br><span class="line"><span class="keyword">var</span> person5 = <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line">person5.<span class="property">name</span> = <span class="string">&quot;Rob&quot;</span>;</span><br><span class="line">person5.<span class="property">age</span> = <span class="number">31</span>;</span><br><span class="line"><span class="title function_">alert</span>(keys);</span><br><span class="line"><span class="title function_">alert</span>(<span class="title class_">Object</span>.<span class="title function_">keys</span>(person5));</span><br><span class="line"><span class="keyword">var</span> keys2 = <span class="title class_">Object</span>.<span class="title function_">getOwnPropertyNames</span>(<span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>);    </span><br><span class="line"><span class="title function_">alert</span>(keys2);</span><br></pre></td></tr></table></figure>
<p>上述代码中出现的Object.getOwnPropertyNames(Person.prototype)方法可以得到所有的可枚举/不可枚举的实例属性（列入constructor）。</p>
</li>
</ul>
<h5 id="原型的动态性"><a href="#原型的动态性" class="headerlink" title="原型的动态性"></a>原型的动态性</h5><p>由于从原型中查找值的过程试一次搜索，因此对原型对象所做的任何修改都能从实例上反映出来。即，可以先创建实例，再修改原型对象中的属性或函数，实例依旧可以访问该属性和函数：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接person对象代码</span></span><br><span class="line"><span class="keyword">var</span> friend = <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">sayHi</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line"> <span class="title function_">alert</span>(<span class="string">&quot;hi!&quot;</span>);</span><br><span class="line">&#125;;</span><br><span class="line">friend.<span class="title function_">sayHi</span>();  <span class="comment">//返回”hi”</span></span><br></pre></td></tr></table></figure>
<p>尽管像以上代码中所看到的可以为原型添加属性或方法，但是如果重写整个原型对象，就会切断实例的构造函数与最初原型对象之间的联系。这是因为实例中的指针仅指向原型，而非构造函数。</p>
<h5 id="原生对象的原型"><a href="#原生对象的原型" class="headerlink" title="原生对象的原型"></a>原生对象的原型</h5><p>不仅是自定义的对象，JavaScript中所有原生的引用类型，都是采用这种模式创建的原生对象。通过原生对象的原型可以取得所有默认方法的引用，也可以自己添加新的方法。</p>
<h4 id="原型对象的缺点"><a href="#原型对象的缺点" class="headerlink" title="原型对象的缺点"></a>原型对象的缺点</h4><ul>
<li><p>由于原型中所有属性都是可以被实例共享，而对于原型中含有引用类型值的属性来说就会发生问题：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//接person对象代码</span></span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">friends</span> = [<span class="string">&quot;Shelby&quot;</span>, <span class="string">&quot;Court&quot;</span>];</span><br><span class="line"><span class="keyword">var</span> person6 = <span class="keyword">new</span> <span class="title class_">Person</span>();</span><br><span class="line">person6.<span class="property">friends</span>.<span class="title function_">push</span>(<span class="string">&quot;Van&quot;</span>);</span><br><span class="line"><span class="title function_">alert</span>(person6.<span class="property">friends</span>);</span><br><span class="line"><span class="title function_">alert</span>(person1.<span class="property">friends</span>);    <span class="comment">//此时两个Person的实例拥有相同的朋友，很可能发生错误</span></span><br></pre></td></tr></table></figure>
<p>可以看到，由于原型的属性可以被共享的这一特性，原型对象中包含的引用类型值很可能被修改之后导致实例的属性也发生错误，必须要在实例属性中覆盖才可以。所以为了解决这个问题，提出了以下组合构造函数模式和原型模式的方法来创建对象：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//改写上面的Person对象</span></span><br><span class="line"><span class="comment">//首先使用构造函数模式创建对象Person并加入容易被修改的属性name，age，job</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Person</span>(<span class="params">name, age, job</span>) &#123;</span><br><span class="line"> <span class="variable language_">this</span>.<span class="property">name</span> = name;</span><br><span class="line"> <span class="variable language_">this</span>.<span class="property">age</span> = age;</span><br><span class="line"> <span class="variable language_">this</span>.<span class="property">job</span> = job;</span><br><span class="line"> <span class="variable language_">this</span>.<span class="property">friends</span> = [<span class="string">&quot;Shelby&quot;</span>, <span class="string">&quot;Court&quot;</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//再使用原型模式创建构造函数和不容易被修改的属性和函数，这样可以发挥原型的共享机制并减少内存消耗</span></span><br><span class="line"><span class="title class_">Person</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123;</span><br><span class="line"> constructor : <span class="title class_">Person</span>,</span><br><span class="line"> sayName : <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">   <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="第七章-函数表达式"><a href="#第七章-函数表达式" class="headerlink" title="第七章 函数表达式"></a>第七章 函数表达式</h3><p>函数的定义有两种方法：一种是函数声明，另一种是<strong>函数表达式</strong> 。</p>
<ul>
<li>函数声明：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">functionName</span>(<span class="params">arg0, arg1, arg2</span>) &#123;</span><br><span class="line">  <span class="comment">//函数体\</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Firefox、Safari、Chrome和Opera都给函数定义了一个非标准的name属性，可以访问到给函数指定的名字alert(functionName.name);同时我们还需要注意的是，函数有函数声明提升的重要特征，所以函数声明可以放在函数调用语句之后。</p>
<ul>
<li>函数表达式：</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> functionName = <span class="keyword">function</span>(<span class="params">arg0, arg1, arg2</span>) &#123;</span><br><span class="line">  <span class="comment">//函数体</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这种情况下创建的函数叫做<strong>匿名函数</strong>f，所以函数的name属性是空字符串，而且根据赋值语句的特性，调用一定要在函数赋值语句之后。既然函数可以赋值给变量，同时函数也可以作为其他函数的返回值。</p>
<h4 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h4><ul>
<li><p>概念</p>
<p>递归函数是在一个函数通过名字调用自身的情况下构成的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">factorial</span>(<span class="params">num</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (num &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">return</span> num * <span class="title function_">factorial</span>(num - <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是一个经典递归阶乘函数，但是存在以下的问题：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> anotherFactorial = factorial;</span><br><span class="line">factorial = <span class="literal">null</span>;</span><br><span class="line"><span class="title function_">alert</span>(<span class="title function_">anotherFactorial</span>(<span class="number">4</span>));   <span class="comment">//出错</span></span><br></pre></td></tr></table></figure>
<p>上面调用出错的原因是，当factorial的引用被替换之后，在anotherFactorial调用时还是会用到factorial，这时候就会发生错误。这种情况可以使用arguments.callee可以解决这个问题。</p>
<p>arguments.callee是一个指向正在执行的函数的指针。所以用这个指针对递归调用能够解决这个问题，例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">factorial</span>(<span class="params">num</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span>(num &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> num * <span class="variable language_">arguments</span>.<span class="title function_">callee</span>(num - <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果是严格模式下，callee模式会导致错误，最好使用命名函数表达式来达到相同的目的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> factorial = (<span class="keyword">function</span> <span class="title function_">f</span>(<span class="params">num</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span>(num &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> num * <span class="title function_">f</span>(num - <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h4><ul>
<li><p>概念：闭包是指有权访问另一个函数作用域中的变量的函数。</p>
</li>
<li><p>创建闭包：常见方式是在一个函数内部创建另一个函数。</p>
</li>
<li><p>理解闭包</p>
<p>要理解闭包的细节，必须理解如何创建作用域链以及作用域链的作用以及理解函数第一次被调用的时候发生了什么。</p>
<p>当某函数第一次被调用时，会创建一个执行环境以及相应的作用域链，并把作用域链赋值给一个特殊的内部属性（[[Scope]])，然后可以使用this、arguments和其他命名参数的值来初始化函数的活动对象。在作用域链中，外部函数的活动对象始终处于第二位，外部函数的外部函数活动对象处于第三位，直至作为作用域链重点的全局执行环境。</p>
<p>所以说，当函数执行完毕之后，据不活动对象（this、arguments）就会被销毁，内存中也仅保存全局作用域。而引入闭包之后情况就会好转：</p>
<p>在另一个函数内部定义的函数会将包含函数（外部函数）的活动对象添加到它的作用域链中。当调用该函数时，函数执行完毕之后，其活动对象也不会被销毁，因为其中的匿名函数的作用域链仍然在引用这个活动对象，除非使用null来销毁该函数解除对匿名函数的引用，释放内存。</p>
</li>
<li><p>闭包与变量</p>
<p>作用域链的机制有一个副作用：闭包只能取得包含函数中任何变量的最后一个值。闭包所保存的是整个变量的对象，而不是某个特殊的变量。</p>
<p>这是因为匿名函数始终都在内存中保存着作用域链中该函数的活动对象，所以其实所有新建的函数都使用着同一个变量。除非在该匿名函数中再写一个匿名函数才能强制让闭包的行为符合预期。</p>
</li>
<li><p>this对象</p>
<p>在闭包中使用this对象也会出现问题：<strong>匿名函数的执行环境具有全局性</strong>，因此匿名函数的this对象通常指向window（全局变量）。这是因为内部匿名函数在搜索变量时，根据作用域链进行搜索，所以存在在对象属性的变量是无法访问到的（只有函数才有作用域，还是因为外部函数没有显示声明this对象？），内部函数只能找到全局，所以如果想要闭包的函数中访问到外部函数的this对象，必须要将this对象保存在外部函数中。</p>
</li>
<li><p>内存泄漏</p>
<p>IE9之前的版本中，如果闭包的作用域链中保存着一个HTML元素，那么该元素无法被销毁。</p>
<p>避免发生”循环引用“的情况。</p>
</li>
</ul>
<h4 id="模仿块级作用域"><a href="#模仿块级作用域" class="headerlink" title="模仿块级作用域"></a>模仿块级作用域</h4><p>JavaScript中没有块级作用域的概念，所有的块语句中定义的变量都存在于函数中的作用域。所以以下代码是可以正常访问的：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">outputNumbers</span>(<span class="params">count</span>)&#123;</span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">var</span> i = <span class="number">0</span>;i &lt; count;i++)&#123;</span><br><span class="line">    <span class="title function_">alert</span>(i);  </span><br><span class="line">  &#125;</span><br><span class="line">  <span class="title function_">alert</span>(i);   <span class="comment">//返回count计数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>就算在最后一个alert(i)之前重新定义声明变量i也不会改变i的值。所以JS从来不会提醒是否多次声明了同一个变量，有可能会导致问题的产生。所以这时候需要匿名函数来模仿块级作用域来避免这个问题:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="comment">//这里就是块级作用域</span></span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>
<p>以上代码定义并立即调用了一个匿名函数，前一个括号是函数表达式声明，后一个括号是指立即调用这个函数。需要注意的是，函数表达式后面可以跟圆括号，而函数声明后面是不可以加圆括号的。</p>
<h4 id="私有变量"><a href="#私有变量" class="headerlink" title="私有变量"></a>私有变量</h4><p>JS中没有私有成员的概念，对象属性和函数都是共有的，但是有一个私有变量的概念。<strong>任何在函数中定义的变量，都可以认为是私有变量</strong>。所以利用函数的闭包就可以创建用于访问私有变量的公有方法。该方法成为”特权方法”，创建方式有以下两种：</p>
<ul>
<li>基本模式</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">MyObject</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="keyword">var</span> privateVariable = <span class="number">10</span>;</span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">privateFunction</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">publicMethod</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    privateVariable++;</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">privateFunction</span>();</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="静态私有变量"><a href="#静态私有变量" class="headerlink" title="静态私有变量"></a>静态私有变量</h5><p>在构造函数中定义特权方法也有一个缺点，那就是必须使用构造函数模式来达到该目的，可是构造函数模式的缺点就是每个实例都会创建同样一组新方法。而使用静态私有变量来实现特权方法就可以避免这个问题。</p>
<p>通过在私有作用域中定义私有变量或函数，同样也可以创建特权方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="keyword">var</span> privateVariable = <span class="number">10</span>;</span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">privateFunction</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="title class_">MyObject</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  &#125;;    <span class="comment">//严格模式下未经声明的变量赋值会导致错误</span></span><br><span class="line">  <span class="title class_">MyObject</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">publicMethod</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    privateVariable++;</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">privateFunction</span>();</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;)();</span><br></pre></td></tr></table></figure>
<h5 id="模块模式"><a href="#模块模式" class="headerlink" title="模块模式"></a>模块模式</h5><p><strong>单例</strong> 是指只有一个实例的对象，通常JavaScript是用对象字面量的方式来创建单例对象。</p>
<p>而模块模式正是专门为单例创建私有变量和特权方法的方式：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> singleton = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="keyword">var</span> privateVariaable = <span class="number">10</span>;</span><br><span class="line">  <span class="keyword">function</span> <span class="title function_">privateFunction</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">publicProperty</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">publicMethod</span>: <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">      privateVariable++;</span><br><span class="line">      <span class="keyword">return</span> <span class="title function_">privateFunction</span>();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;();</span><br></pre></td></tr></table></figure>
<p>这种模式在需要对单例进行某些初始化，同时又需要维护其私有变量时时非常有用的。单例经常被用在Web应用程序中来管理应用程序级的信息。</p>
<p>另外还有 <strong>增强的模块模式</strong></p>
<h3 id="第八章-BOM"><a href="#第八章-BOM" class="headerlink" title="第八章 BOM"></a>第八章 BOM</h3><p>如果在Web中使用JavaScript，那么BOM才是真正的核心。</p>
<h4 id="window对象"><a href="#window对象" class="headerlink" title="window对象"></a>window对象</h4><p>BOM的核心对象是window，它表示浏览器的一个实例。在浏览器中，window对象不仅是通过JavaScript访问浏览器窗口的一个接口，又是ECMAScript规定的Global对象。</p>
<ul>
<li>全局作用域</li>
</ul>
<p>在JavaScript中，全局作用域中声明的变量、函数都会变成window对象的属性和方法。然而定义全局变量和在window对象上直接定义属性仍有一点区别：全局变量无法通过delete操作符删除（IE&lt;9抛错，其他浏览器返回false），而window对象上的属性可以。</p>
<ul>
<li>窗口关系及框架</li>
</ul>
<p>如果页面中包含frame，则每一个框架都拥有自己的window对象，并保存在frames集合中。</p>
<ul>
<li>窗口位置</li>
</ul>
<p>用来确定和修改window对象位置的属性和方法：</p>
<p>IE、Safari、Opera和Chrome都提供screenLeft和screenTop属性；Chrome和Safari以及Firefox则支持screenX和screenY属性：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> leftPos = (<span class="keyword">typeof</span> <span class="variable language_">window</span>.<span class="property">screenLeft</span> == <span class="string">&quot;number&quot;</span>) ? <span class="variable language_">window</span>.<span class="property">screenLeft</span> : <span class="variable language_">window</span>.<span class="property">screenX</span>;</span><br><span class="line"><span class="keyword">var</span> topPos = (<span class="keyword">typeof</span> <span class="variable language_">window</span>.<span class="property">screenTop</span> == <span class="string">&quot;number&quot;</span>) ? <span class="variable language_">window</span>.<span class="property">screenTop</span> : <span class="variable language_">window</span>.<span class="property">screenY</span>;</span><br></pre></td></tr></table></figure>
<p>上述代码对浏览器进行选择并获取窗口的位置。默认返回左上角原点（0，0）。</p>
<p>通过winow.moveBy(横坐标长度，纵坐标长度)，window.moveTo(横坐标，纵坐标)，可以移动窗口，但是可能会被浏览器禁用。</p>
<ul>
<li>窗口大小</li>
</ul>
<p>IE9+/Firefox/Safari/Opera/Chrome均提供4个属性：innerWidth/innerHeight/outerWidth/outerHeight。其中在IE9+/Safari/Firefox中，outerwidth和outerHeight返回浏览器窗口本身吃葱，Opera中则是表示页面视图容器的大小。而innerWidth、innerHeight是表示该容器中页面视图去的大小。Chrome中内外两对属性返回相同值-viewport大小。</p>
<p>另外，还可以使用window.resizeTo(width,height)/window.resizeBy(widthtoformer,lengthtoformer)</p>
<ul>
<li>导航和打开窗口</li>
</ul>
<p>window.open()方法可以导航到特定URL或打开新浏览器窗口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">window</span>.<span class="title function_">open</span>(<span class="string">&quot;http://www.wrox.com/&quot;</span>,topFrame,<span class="string">&quot;someString&quot;</span>,<span class="string">&quot;1&quot;</span>)</span><br><span class="line"><span class="comment">//四个参数分别是网站的url、窗口目标（或者_self/_parent/_top/_blank），特性字符串，是否取代当前页面的bool</span></span><br></pre></td></tr></table></figure>
<p>如果该方法中第二个参数不存在，就会根据在第三个参数为止传入的字符串创建一个新窗口，有“fullscreen=yes、height=100”等参数，该参数字符串中不允许出现空格。该方法可以按照下面的方式赋值给变量并进行一些操作：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> wroxWin = <span class="variable language_">window</span>.<span class="title function_">open</span>(<span class="string">&quot;http://www.wrox.com/&quot;</span>,<span class="string">&quot;wroxWindow&quot;</span>,<span class="string">&quot;height=400,width=400,top=10,left=10,resizable=yes&quot;</span>);</span><br><span class="line"><span class="title function_">alert</span>(wroxWin.<span class="property">opener</span> == <span class="variable language_">window</span>);    <span class="comment">//true</span></span><br><span class="line">wroxWin.<span class="title function_">resizeTo</span>(<span class="number">500</span>,<span class="number">500</span>);</span><br><span class="line">wroxWin.<span class="title function_">moveTo</span>(<span class="number">20</span>,<span class="number">20</span>);</span><br><span class="line">wroxWin.<span class="property">close</span>;</span><br><span class="line"><span class="title function_">alert</span>(wroxWin.<span class="property">closed</span>);    <span class="comment">//true</span></span><br></pre></td></tr></table></figure>
<p>鉴于有很多网站有很多弹窗广告的窗口，所以为了屏蔽这些窗口，网站的代码中可以</p>
<ul>
<li>间歇调用和超时调用</li>
</ul>
<p>JS是单线程语言，但是允许通过设置超时值和间歇时间值（毫秒）来调度代码在特定时刻执行。其中，超时值是在指定时间过后执行代码，后者是每隔指定时间就执行一次代码。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">setTimeout</span>(<span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="title function_">alert</span>(<span class="string">&quot;Hello world!&quot;</span>);</span><br><span class="line">&#125;, <span class="number">1000</span>);</span><br><span class="line"><span class="comment">//第一个参数可以是字符串格式的代码，但是不推荐使用，会导致性能损失</span></span><br><span class="line"><span class="built_in">setInterval</span>(<span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">  <span class="title function_">alert</span>(<span class="string">&quot;Hello World!&quot;</span>);</span><br><span class="line">&#125;,<span class="number">10000</span>);</span><br><span class="line"><span class="comment">//每10000毫秒执行一次代码</span></span><br></pre></td></tr></table></figure>
<p>该方法也可以赋值给变量，然后通过clearTimeout(timeoutVar)/clearinterval(intervalVaar)</p>
<ul>
<li>系统对话框</li>
</ul>
<p>浏览器通过alert(),confirm(),prompt()方法可以调用系统对话框向用户显示消息。其中confirm()方法有OK和cancel两个按钮，prompt()会提示用户输入一个参数。另外还有window.print()打印对话框和window.find()查找对话框。</p>
<h4 id="location对象"><a href="#location对象" class="headerlink" title="location对象"></a>location对象</h4><p>location是最有用的BOM对象之一，提供了与当前窗口中加载的文档有关的信息和导航功能，还将URL解析为独立片段来通过不同属性访问这些片段。</p>
<p>location既是window对象的一个属性，又是document对象的属性，即window.location=document.location。下面是location的所有属性：</p>
<p>hash（#contents）、host（<a href="http://www.wrox.com:80）、hostname（www.wrox.com）、href（[http://www.wrox.com）、pathname（/WileyCDA）、port（8080）、protocol（http:）、search（?q=javascript）](http://www.wrox.com）、pathname（/WileyCDA）、port（8080）、protocol（http:）、search（?q=javascript）)">www.wrox.com:80）、hostname（www.wrox.com）、href（[http://www.wrox.com）、pathname（/WileyCDA）、port（8080）、protocol（http:）、search（?q=javascript）](http://www.wrox.com）、pathname（/WileyCDA）、port（8080）、protocol（http:）、search（?q=javascript）)</a>;</p>
<ul>
<li><p>查询字符串参数（对search属性进行字符串解析）</p>
</li>
<li><p>位置操作</p>
<p>通过<code>location.assign(&quot;http://www.wrox.com&quot;);</code>可以打开新的URL，与<code>window.location=&quot;http://www.wrox.com&quot;;</code>以及<code>location.href=&quot;http://www.wrox.com&quot;;</code>相同</p>
<p>如果要禁止将页面存入浏览记录（防止后退到前一个页面），可以使用location.replace(URL);</p>
</li>
</ul>
<h4 id="navigator对象"><a href="#navigator对象" class="headerlink" title="navigator对象"></a>navigator对象</h4><ul>
<li><p>检测插件</p>
<p>navigator.plugins是一个数组，其中每一项都包括name, description, filename, length</p>
<p>但是IE中检测插件需要使用ActiveXObject类型：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">hasIEPlugin</span>(<span class="params">name</span>) &#123;</span><br><span class="line">    <span class="keyword">try</span>&#123;</span><br><span class="line">        <span class="keyword">new</span> <span class="title class_">ActivXOBject</span>(name);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">catch</span>(e) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_">alert</span>(<span class="title function_">hasIEPlugin</span>(<span class="string">&quot;shockwaveFlash.ShockwaveFlash&quot;</span>));</span><br><span class="line"></span><br></pre></td></tr></table></figure>
</li>
<li><p>注册处理程序</p>
<p>navigator对象有registerContentHandler()和registerProtocolHandler()方法；</p>
<p>registerContentHandler()接受三个参数：要处理的协议（mailto或ftp），处理该协议的页面URL和应用程序的名称：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">navigator.<span class="title function_">registerProtocolHandler</span>(<span class="string">&quot;mailto&quot;</span>, <span class="string">&quot;http://www.somemailclient.com?cmd=%s&quot;</span>, <span class="string">&quot;Some Mail Client&quot;</span>);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="screen对象"><a href="#screen对象" class="headerlink" title="screen对象"></a>screen对象</h4><p>对于变成来说作用不大，用来标明客户端的能力，包括浏览器窗口外部的显示其信息等；</p>
<h4 id="history对象"><a href="#history对象" class="headerlink" title="history对象"></a>history对象</h4><p>使用history.go()接受参数整数，一个url（跳到最近的该url页面）<br>另外也可以使用history.back()/.forward();</p>
<h3 id="第九章-客户端检测"><a href="#第九章-客户端检测" class="headerlink" title="第九章 客户端检测"></a>第九章 客户端检测</h3><h4 id="能力检测"><a href="#能力检测" class="headerlink" title="能力检测"></a>能力检测</h4><p>目标是识别浏览器的能力，</p>
<ul>
<li><p>基本模式如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(object.<span class="property">propertyInQuestion</span>) &#123;</span><br><span class="line">    <span class="comment">//使用object.propertyInQuestion</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用方案：先检测达成目的的最常用的特性（提升效率），必须测试实际要用到的特性（判断条件要够详细）</p>
<p>如果是想要判断对象是否有某个属性或者支持某种方法，尽量使用typeof</p>
</li>
</ul>
<h4 id="怪癖（bug）检测"><a href="#怪癖（bug）检测" class="headerlink" title="怪癖（bug）检测"></a>怪癖（bug）检测</h4><p>目标是识别浏览器的特殊行为。</p>
<h4 id="用户代理检测"><a href="#用户代理检测" class="headerlink" title="用户代理检测"></a>用户代理检测</h4><p>通过检测用户代理字符串来确定实际使用的浏览器。用户代理字符串作为响应头部发送，也可以通过navigator.userAgent属性访问</p>
<p>用户代理字符串检测技术：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> client = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> engine = &#123;</span><br><span class="line">        <span class="attr">ie</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">gecko</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">webkit</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">khtml</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">opera</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">ver</span>: <span class="literal">null</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">var</span> browser = &#123;</span><br><span class="line">        <span class="attr">ie</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">firefox</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">safari</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">konq</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">opera</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">chrome</span>: <span class="number">0</span>,</span><br><span class="line">        <span class="attr">ver</span>: <span class="literal">null</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">var</span> system = &#123;</span><br><span class="line">        <span class="attr">win</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">mac</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">x11</span>: <span class="literal">false</span>，</span><br><span class="line">        iPhone：<span class="literal">false</span>,</span><br><span class="line">        <span class="attr">ipod</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">ipad</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">ios</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">android</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">nokiaN</span>: <span class="literal">false</span>,</span><br><span class="line">        <span class="attr">winMobile</span>: <span class="literal">false</span></span><br><span class="line">    &#125;; <span class="comment">//navigator.platFORM</span></span><br><span class="line">    <span class="comment">//再次检测呈现引擎、平台和设备</span></span><br><span class="line">    <span class="keyword">var</span> ua = navigator.<span class="property">userAgent</span>;</span><br><span class="line">    <span class="keyword">if</span>(<span class="variable language_">window</span>.<span class="property">opera</span>) &#123;</span><br><span class="line">        engine.<span class="property">ver</span> = <span class="variable language_">window</span>.<span class="property">opera</span>.<span class="title function_">version</span>();</span><br><span class="line">        engine.<span class="property">opera</span> = <span class="built_in">parseFloat</span>(engine.<span class="property">ver</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span>(<span class="regexp">/AppleWebKit\/(\S+)/</span>.<span class="title function_">test</span>(ua)) &#123;</span><br><span class="line">        engine.<span class="property">ver</span> = <span class="title class_">RegExp</span>[<span class="string">&quot;$1&quot;</span>];</span><br><span class="line">        engine.<span class="property">webkit</span> = <span class="built_in">parseFloat</span>(engine.<span class="property">ver</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span>(（/<span class="variable constant_">KHTML</span>\/(\S+).<span class="title function_">test</span>(ua)) &#123;</span><br><span class="line">        engine.<span class="property">ver</span> = <span class="title class_">RegExp</span>[<span class="string">&quot;$1&quot;</span>];</span><br><span class="line">        engine.<span class="property">khtml</span> = <span class="built_in">parseFloat</span>(enging.<span class="property">ver</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span>(<span class="regexp">/rv:([^\)]+)\) Gecho\/d&#123;8&#125;/</span>.<span class="title function_">test</span>(ua)) &#123;</span><br><span class="line">        engine.<span class="property">ver</span> = <span class="title class_">RegExp</span>[<span class="string">&quot;$1&quot;</span>];</span><br><span class="line">        engine.<span class="property">gecko</span> = <span class="built_in">parseFloat</span>(engine.<span class="property">ver</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span>(<span class="regexp">/MSIE ([^;]+)/</span>.<span class="title function_">test</span>(ua)) &#123;</span><br><span class="line">        engine.<span class="property">ver</span> = <span class="title class_">RegExp</span>[<span class="string">&quot;$1&quot;</span>];</span><br><span class="line">        engine.<span class="property">ie</span> = <span class="built_in">parseFloat</span>(engine.<span class="property">ver</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        engine：engine,</span><br><span class="line">        <span class="attr">browser</span>: browser,</span><br><span class="line">        <span class="attr">system</span>: system</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;();</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>可以检测用户代理、浏览器的类型和版本、系统类型和版本、移动设备</p>
<h3 id="第十章-DOM"><a href="#第十章-DOM" class="headerlink" title="第十章 DOM"></a>第十章 DOM</h3><h4 id="节点层次"><a href="#节点层次" class="headerlink" title="节点层次"></a>节点层次</h4><p>HTML页面中，文档元素始终都是元素。</p>
<h4 id="节点类型"><a href="#节点类型" class="headerlink" title="节点类型"></a>节点类型</h4><p>DOM1级定义了Node借口，由DOM中的所有节点类型实现。在JS中作为Node类型实现，<strong>除了IE</strong>，其他所有浏览器中都可以访问到这个类型。节点类型一共有12种。</p>
<p>使用方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(someNode.<span class="property">nodeType</span> == <span class="title class_">Node</span>.<span class="property">ELEMENT_NODE</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Node is an element.&quot;</span>);   <span class="comment">//不适用于IE</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span>(someNode.<span class="property">nodeType</span> == <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Node is an element.&quot;</span>);   <span class="comment">//全部适用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li><p>nodeName和nodeValue属性</p>
<p>对于元素节点，nodeName保存元素的标签名，nodeValue中则为null</p>
</li>
<li><p>节点关系</p>
<p>可以使用.childNode[]数组属性、item()方法来访问子节点：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> firstChild = someNode.<span class="property">childNodes</span>[<span class="number">0</span>];</span><br><span class="line"><span class="keyword">var</span> secondChild = someNode.<span class="property">childNodes</span>.<span class="title function_">item</span>(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">var</span> count = someNode.<span class="property">childeNodes</span>.<span class="property">length</span>;</span><br></pre></td></tr></table></figure>
<p>可以使用parentNode属性访问父节点，使用previousSibling和nextSibling属性访问兄弟节点，但是<strong>列表中第一个节点的previousSibling属性值为null，最后一个节点的nextSibling属性值也为null</strong></p>
<p>可以使用firstChild和lastChild属性访问childNodes列表中第一个和最后一个节点。</p>
</li>
<li><p>操作节点</p>
<p>appendChild()方法新增节点：<code>someNode.appendChild(newNode)</code>，该方法返回新增的节点，若传入的参数节点已经存在，则该方法对该节点进行转移</p>
<p>insertBefore()方法插入特定位置，接受两个参数，一个是要插入的节点，第二个是参照节点</p>
<p>replaceChild()方法替换节点</p>
<p>removeChild()方法移除节点</p>
<p>cloneNode(一个bool参数表示是否执行深复制)、normalize()支持所有类型节点</p>
</li>
</ul>
<h4 id="Document类型"><a href="#Document类型" class="headerlink" title="Document类型"></a>Document类型</h4><p>JS中通过Document类型表示文档，document对象是HTMLDocument（继承自Document类型）的一个实例，表示整个HTML页面，也是window对象的一个属性。Document节点具有下列特征：</p>
<ul>
<li>nodeType值为9</li>
<li>nodeName的值”document“</li>
<li>parentNode的值为null</li>
<li>nodeValue的值为null</li>
<li>ownerDocument的值为null</li>
</ul>
<h5 id="文档的子节点"><a href="#文档的子节点" class="headerlink" title="文档的子节点"></a>文档的子节点</h5><p>可以通过<strong>documentElement属性</strong>（始终指向\&lt;html>元素）和childNodes列表访问文档元素。&lt;/html></p>
<p>可以通过**document.body来访问\&lt;body>元素，这两个属性所有浏览器均支持；&lt;/body></p>
<p>可以通过**document.doctype访问\&lt;!DOCTYPE&gt;的引用，但是IE8之前不支持，而其他浏览器这个属性不存在在childNodes中</p>
<p>\&lt;html>标签之外的注释也算文档的子节点，但是IE8/SAFARI/OPERA/CHROME均只为第一条注释创建节点&lt;/html></p>
<h5 id="文档信息"><a href="#文档信息" class="headerlink" title="文档信息"></a>文档信息</h5><h5 id="查找元素（最最常用）"><a href="#查找元素（最最常用）" class="headerlink" title="查找元素（最最常用）"></a>查找元素（最最常用）</h5><p>可以通过getElementById(“”)访问指定元素,ID大小写在IE7之前有要求。如果多个相同ID的元素出现，只返回第一个元素（IE7会在name等于指定id时且在这个id元素之前出现，返回有该name的元素）</p>
<p>可以通过getElementsByTagName(“”)访问指定元素列表，返回HTMLCollection对象，可以使用数组的下标访问，也可以通过item()方法进行访问，<strong>也可以通过该对象的namedItem(name)方法返回相同tag元素列表中指定的name元素</strong>。</p>
<p>可以通过getElementsByName()访问</p>
<h5 id="特殊集合"><a href="#特殊集合" class="headerlink" title="特殊集合"></a>特殊集合</h5><ul>
<li>document.anchors</li>
<li>document.applets</li>
<li>document.forms</li>
<li>document.images</li>
<li>document.links</li>
</ul>
<h5 id="DOM一致性检测"><a href="#DOM一致性检测" class="headerlink" title="DOM一致性检测"></a>DOM一致性检测</h5><h5 id="文档写入"><a href="#文档写入" class="headerlink" title="文档写入"></a>文档写入</h5><p>write()/writeln()/open()/close()是document对象的IO功能，这些函数是在加载过程当中写入的，所以加载完再运行write会导致整个页面消失：</p>
<p><code>document.write(&quot;&lt;script type=\&quot;text/javascript\&quot; src=\&quot;file.js\&quot;&gt;&quot; + &quot;&lt;\/script&gt;&quot;);</code></p>
<h4 id="Element类型"><a href="#Element类型" class="headerlink" title="Element类型"></a>Element类型</h4><p>要访问元素的标签名，最好使用tagName属性，但是注意这里tagName返回的是<strong>大写</strong>！</p>
<ul>
<li><p>HTML元素（取得特性）</p>
<p>由HTMLElement类型表示，继承自Element类型，拥有id/title/lang/dir/className等属性</p>
</li>
<li><p>操作特性</p>
<p>操作特性的DOM方法主要有：div.getAttribute(“属性名除了style和onclick”)/div.setAttribute(“属性名”,”属性值”)/div.removeAttribute(“属性名”)</p>
</li>
<li><p>attributes属性</p>
</li>
<li><p>创建元素</p>
<p>使用document.createElement()方法创建新元素，只接受一个参数为要创建的标签名</p>
</li>
</ul>
<h4 id="Text类型"><a href="#Text类型" class="headerlink" title="Text类型"></a>Text类型</h4><h3 id="第十三章-事件"><a href="#第十三章-事件" class="headerlink" title="第十三章 事件"></a>第十三章 事件</h3><h4 id="事件流"><a href="#事件流" class="headerlink" title="事件流"></a>事件流</h4><p>事件流描述的是从页面中接收事件的顺序。</p>
<ul>
<li><p>事件冒泡</p>
<p>IE的事件流叫做事件冒泡：时间开始时由最具体的元素（嵌套层次最深的那个节点）接收，然后逐级向上传播直到文档根节点（window对象）。所有现代浏览器都支持事件冒泡。</p>
</li>
<li><p>事件捕获</p>
<p>从不太具体的节点开始接收事件，逐级到最具体的节点最后接收到事件。很少使用事件捕获。</p>
</li>
<li><p>DOM事件流</p>
<p>包括三个阶段：事件捕获阶段、处于目标阶段、事件冒泡阶段。</p>
</li>
</ul>
<h4 id="事件处理程序"><a href="#事件处理程序" class="headerlink" title="事件处理程序"></a>事件处理程序</h4><ul>
<li><p>HTML事件处理程序</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">method</span>=<span class="string">&quot;post&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;text&quot;</span> <span class="attr">name</span>=<span class="string">&quot;username&quot;</span> <span class="attr">value</span>=<span class="string">&quot;&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">&quot;button&quot;</span> <span class="attr">value</span>=<span class="string">&quot;Echo Username&quot;</span> <span class="attr">onclick</span>=<span class="string">&quot;alert(username.value)&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>缺点：如果调用的函数在引用之后定义，页面解析该函数之前就进行了操作会引发错误（时差问题）；不同JS引擎遵循的标识符解析规则略有差异，很可能会在访问非限定对象成员时出错。</p>
</li>
<li><p>DOM0级事件处理程序</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="property">onclick</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Clicked&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>DOM2级事件处理程序</p>
<p>定义了addEventListener()和removeEventListener()用于添加和删除监听器的函数。都接受三个参数：处理事件名，事件处理函数，和一个bool值（该值为true捕获阶段调用，false冒泡阶段调用）。但是add方法添加的程序 只能用remove来删除，所以当add一个匿名函数时就无法被删除。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="title function_">addEventListener</span>(<span class="string">&quot;click&quot;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">id</span>);</span><br><span class="line">    &#125;, <span class="literal">false</span>);</span><br></pre></td></tr></table></figure>
<p>可添加多个事件，按顺序出发。</p>
</li>
<li><p>IE事件处理程序</p>
<p>IE中使用attachEvent()，事件处理程序会在<strong>全局作用域</strong>中进行，所以函数中出现的this等于window，而DOM0中的函数中出现的this就是局部作用于内运行。也有detachEvent()移除函数，所以道理与DOM2级事件处理程序一样。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="title function_">attachEvent</span>(<span class="string">&quot;onclick&quot;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Clicked&quot;</span>);</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
</li>
<li><p>跨浏览器的事件处理程序</p>
<ol>
<li>创建addHandler()方法，接受三个参数：要操作的元素、事件名称和事件处理程序函数；目的是判断情况应该使用DOM0/DOM2/IE的哪种方法，该方法属于EventUtil对象。</li>
</ol>
</li>
</ul>
<h4 id="事件对象"><a href="#事件对象" class="headerlink" title="事件对象"></a>事件对象</h4><ul>
<li><p>DOM中的事件对象</p>
<p>兼容DOM的浏览器会将一个event对象传入到事件处理程序中。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="property">onclick</span> = <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(event.<span class="property">type</span>);      <span class="comment">//&quot;click&quot;</span></span><br><span class="line">&#125;;</span><br><span class="line">btn.<span class="title function_">addEventListener</span>(<span class="string">&quot;click&quot;</span>, <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(event.<span class="property">type</span>);      <span class="comment">//&quot;click&quot;</span></span><br><span class="line">    &#125;, <span class="literal">false</span>)</span><br></pre></td></tr></table></figure>
<p>事件处理程序内部的this永远都是currentTarget，也就是现在事件作用的目标（按钮、window等）；</p>
<p>一个函数处理多个事件，可以使用switch(event.type)函数；阻止特定时间的默认行为，可以使用preventDefault()方法；停止事件在DOM层次中传播停止事件的捕获和冒泡，可以调用stopPropagation()：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="property">onclick</span> = <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Clicked&quot;</span>);</span><br><span class="line">    event.<span class="title function_">stopPropagation</span>();    <span class="comment">//避免了下面body上的事件触发</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="variable language_">document</span>.<span class="property">body</span>.<span class="property">onclick</span> = <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="string">&quot;Body clicked&quot;</span>);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>事件对象的eventPhase属性可以用来确定事件当前正位于事件流的哪个阶段。捕获阶段调用程序为1，程序正在目标对象上为2，在冒泡阶段调用程序为3</p>
</li>
<li><p>IE中的事件对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="property">onclick</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> event = <span class="variable language_">window</span>.<span class="property">event</span>;</span><br><span class="line">    <span class="title function_">alert</span>(event.<span class="property">type</span>);      <span class="comment">//&quot;click&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>由于事件处理程序的作用域在IE下是全局，在DOM下是局部作用域，所以提供event.srcElement来判断this的指向：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> btn = <span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;myBtn&quot;</span>);</span><br><span class="line">btn.<span class="property">onclick</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">window</span>.<span class="property">event</span>.<span class="property">srcElement</span> === <span class="variable language_">this</span>);        <span class="comment">//true</span></span><br><span class="line">&#125;;</span><br><span class="line">btn.<span class="title function_">attachEvent</span>(<span class="string">&quot;onclick&quot;</span>, <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(event.<span class="property">srcElement</span> === <span class="variable language_">this</span>);               <span class="comment">//false</span></span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>
<p>若想要取消时间的默认行为，可以通过设置<code>window.event.returnValue = false</code>进行；</p>
<p>若想要阻止时间的冒泡（IE只支持事件冒泡），可以通过设置<code>window.event.cancelBubble = true;</code></p>
</li>
<li><p>跨浏览器的事件对象</p>
</li>
</ul>
<h4 id="事件类型"><a href="#事件类型" class="headerlink" title="事件类型"></a>事件类型</h4><ul>
<li><p>UI事件</p>
<ul>
<li><strong>load</strong>(html中可以给标签添加onload</li>
<li>unload</li>
<li>abort</li>
<li>error</li>
<li>select</li>
<li>resize</li>
<li>scroll</li>
</ul>
</li>
<li><p>焦点事件</p>
<ul>
<li><strong>blur</strong></li>
<li><strong>focus</strong></li>
<li>focusin</li>
<li>focusout</li>
</ul>
</li>
<li><p>鼠标与滚轮事件</p>
<ul>
<li>click</li>
<li>dbclick</li>
<li>mousedown</li>
<li>mouseenter</li>
<li>mouseleave</li>
<li>mousemove</li>
<li>mouseout</li>
<li>mouseover</li>
<li>mouseup</li>
</ul>
</li>
<li><p>键盘与文本事件</p>
<ul>
<li>keydown</li>
<li>keypress</li>
<li>keyup</li>
</ul>
</li>
<li><p>复合事件</p>
<ul>
<li>compositionstart</li>
<li>compositionupdate</li>
<li>compositionend</li>
</ul>
</li>
<li><p>HTML5事件</p>
<ul>
<li>contextmenu</li>
<li>beforeunload</li>
<li>DOMContentLoaded</li>
<li>readystatechange</li>
<li>pageshow/pagehide</li>
<li>hashchange</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>LESS官方文档学习笔记</title>
    <url>/2017/08/02/2017-05-10-LESS-document-note.html</url>
    <content><![CDATA[<p>LESS是一种动态样式语言。<br>LESS将CSS赋予了动态语言的特性，如变量，继承，运算，函数。LESS既可以在客户端上运行 (支持IE 6+，Webkit， Firefox)，也可以借助Node.js或者Rhino在服务端运行。</p>
<span id="more"></span>
<h3 id="使用方式"><a href="#使用方式" class="headerlink" title="使用方式"></a>使用方式</h3><ul>
<li><p>客户端使用</p>
<p>在页面顶端加入下面代码，在服务器环境下可以使用less文件：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet/less&quot;</span> <span class="attr">type</span>=<span class="string">&quot;text/css&quot;</span> <span class="attr">href</span>=<span class="string">&quot;styles.less&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;less.js&quot;</span> <span class="attr">type</span>=<span class="string">&quot;text/javascript&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>注意，LESS文件引入时rel的值为<em>stylesheet/less</em>；less.js一定要在LESS文件引用之后的位置。</p>
<ul>
<li><p>在服务器端使用</p>
<p>使用npm工具管理器在服务器端安装LESS，之后可以在Node中调用编译器，类似于：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> less = <span class="built_in">require</span>(<span class="string">&#x27;less&#x27;</span>);</span><br><span class="line">less.<span class="title function_">render</span>(<span class="string">&#x27;.class &#123; width: 1 + 1 &#125;&#x27;</span>, <span class="keyword">function</span> (<span class="params">e, css</span>) &#123;</span><br><span class="line">	    <span class="variable language_">console</span>.<span class="title function_">log</span>(css);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>可以通过向解析器传递参数对LESS进行配置：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> parser = <span class="title function_">new</span>(less.<span class="property">Parser</span>)(&#123;</span><br><span class="line">    <span class="attr">paths</span>: [<span class="string">&#x27;.&#x27;</span>, <span class="string">&#x27;./lib&#x27;</span>], <span class="comment">// Specify search paths for @import directives</span></span><br><span class="line">    <span class="attr">filename</span>: <span class="string">&#x27;style.less&#x27;</span> <span class="comment">// Specify a filename, for better error messages</span></span><br><span class="line">&#125;);</span><br><span class="line">parser.<span class="title function_">parse</span>(<span class="string">&#x27;.class &#123; width: 1 + 1 &#125;&#x27;</span>, <span class="keyword">function</span> (<span class="params">e, tree</span>) &#123;</span><br><span class="line">    tree.<span class="title function_">toCSS</span>(&#123; <span class="attr">compress</span>: <span class="literal">true</span> &#125;); <span class="comment">// Minify CSS output</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>也可以通过命令行的方式调用LESS解析器对LESS文件进行解析或编译：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">$ lessc styles.<span class="property">less</span></span><br><span class="line">$ lessc styles.<span class="property">less</span> &gt; styles.<span class="property">css</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><h4 id="注释"><a href="#注释" class="headerlink" title="注释"></a>注释</h4><p>完全支持CSS中的注释方法。</p>
<h4 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h4><p>LESS中的变量是完全的“常量”，由@符号引导，且只能够定义<strong>一次</strong>：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@nice-blue</span>: #<span class="number">5</span>B83AD;</span><br><span class="line"><span class="keyword">@light-blue</span>: @nice-blue + #<span class="number">111</span>;</span><br><span class="line">	</span><br><span class="line"><span class="keyword">@fnord</span>: <span class="string">&quot;I am fnord.&quot;</span>;</span><br><span class="line"><span class="keyword">@var</span>: <span class="string">&#x27;fnord&#x27;</span>;</span><br><span class="line"><span class="selector-class">.text</span>&#123;</span><br><span class="line">	<span class="attribute">color</span>: light-blue;</span><br><span class="line">	<span class="attribute">content</span>: @@var;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="混合"><a href="#混合" class="headerlink" title="混合"></a><a href="#混合" title="混合"></a>混合</h4><p>LESS中定义好通用的class属性集之后，可以在另一个属性集中调用：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.bordered</span> &#123;</span><br><span class="line">  <span class="attribute">border-top</span>: dotted <span class="number">1px</span> black;</span><br><span class="line">  <span class="attribute">border-bottom</span>: solid <span class="number">2px</span> black;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.post</span> <span class="selector-tag">a</span> &#123;</span><br><span class="line">  <span class="attribute">color</span>: red;</span><br><span class="line">  <span class="selector-class">.bordered</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>带参数混合</strong></p>
<p>可以像函数的参数一样为样式属性集合名称中添加带<strong>默认值的参数</strong>（参数可设置多个，使用逗号进行分割，可以使用@arguments代替所有参数出现在样式属性集合体之中），该混合方法最适合隐藏某属性集合但需要引用该属性集合时使用。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*带有参数的混合方法*/</span></span><br><span class="line"><span class="selector-class">.border-radius</span> (<span class="keyword">@radius</span>: <span class="number">5px</span>) &#123;</span><br><span class="line">  <span class="attribute">border-radius</span>: @radius;</span><br><span class="line">  -moz-<span class="attribute">border-radius</span>: @radius;</span><br><span class="line">  -webkit-<span class="attribute">border-radius</span>: @radius;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#header</span> &#123;</span><br><span class="line">  <span class="selector-class">.border-radius</span>(<span class="number">4px</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  使用@arguments代替所有参数进行表达</span></span><br><span class="line"><span class="comment">  其中传参个数可以少于参数数量，但是参</span></span><br><span class="line"><span class="comment">  数的位置必须对应，中间参数位置不可空</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="selector-class">.box-shadow</span> (<span class="keyword">@x</span>: <span class="number">0</span>, @<span class="attribute">y</span>: <span class="number">0</span>, @<span class="attribute">blur</span>: <span class="number">1px</span>, @<span class="attribute">color</span>: #<span class="number">000</span>) &#123;</span><br><span class="line">  <span class="attribute">box-shadow</span>: @arguments;</span><br><span class="line">  -moz-<span class="attribute">box-shadow</span>: @arguments;</span><br><span class="line">  -webkit-<span class="attribute">box-shadow</span>: @arguments;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.special-box-shadow</span> &#123;</span><br><span class="line">	<span class="selector-class">.box-shadow</span>(<span class="number">2px</span>, <span class="number">5px</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="模式匹配和导引表达是"><a href="#模式匹配和导引表达是" class="headerlink" title="模式匹配和导引表达是"></a><a href="#模式匹配和导引表达是" title="模式匹配和导引表达是"></a>模式匹配和导引表达是</h4><ul>
<li><p>模式匹配</p>
<p>如果想根据传入的参数来改变混合的默认呈现，可以使用模式匹配方式（相当于将第一个参数设置为可变参数，该参数可以设置switch的选项来启动相应的模式）：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.mixin</span> (dark, <span class="keyword">@color</span>) &#123;</span><br><span class="line">  <span class="attribute">color</span>: <span class="built_in">darken</span>(@color, <span class="number">10%</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.mixin</span> (light, <span class="keyword">@color</span>) &#123;</span><br><span class="line">  <span class="attribute">color</span>: <span class="built_in">lighten</span>(@color, <span class="number">10%</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*接受任意值，相当于默认值*/</span></span><br><span class="line"><span class="selector-class">.mixin</span> (<span class="keyword">@_</span>, @<span class="attribute">color</span>) &#123;</span><br><span class="line">  <span class="attribute">display</span>: block;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">@switch</span>: light;</span><br><span class="line">	</span><br><span class="line"><span class="selector-class">.someClass</span> &#123;</span><br><span class="line">	<span class="selector-class">.mixin</span>(<span class="keyword">@switch</span>, #<span class="number">888</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>引导</p>
<p>当我们想根据表达式进行匹配，而非根据值和参数匹配时，导引就显得非常有用。如果你对函数式编程非常熟悉，那么你很可能已经使用过导引。</p>
<p>导引中可以用的比较运算符有：&gt;、&gt;=、=（这里相当于==）、=&lt;、&lt;</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  使用when语句判断条件，lightness()是一个</span></span><br><span class="line"><span class="comment">     判断颜色亮度的函数。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="selector-class">.mixin</span> (<span class="keyword">@a</span>) when (lightness(@a) &gt;= <span class="number">50%</span>) &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: black;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.mixin</span> (<span class="keyword">@a</span>) when (lightness(@a) &lt; <span class="number">50%</span>) &#123;</span><br><span class="line">    <span class="attribute">background-color</span>: white;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.mixin</span> (<span class="keyword">@a</span>) &#123;</span><br><span class="line">    <span class="attribute">color</span>: @a;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>when后面所跟的条件，值为布尔true时才会执行解析，而LESS中的除去上面的<strong>比较结果返回值</strong>以及<strong>关键词true</strong>以外的值都视为布尔false：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*下面两个语句含义相同*/</span></span><br><span class="line"><span class="selector-class">.truth</span> (<span class="keyword">@a</span>) when (@a) &#123; <span class="attribute">color</span>: black; &#125;</span><br><span class="line"><span class="selector-class">.truth</span> (<span class="keyword">@a</span>) when (@a = true) &#123; <span class="attribute">color</span>: black; &#125;</span><br><span class="line"><span class="comment">/*下面一句是不会匹配任何条件，也不执行*/</span></span><br><span class="line"><span class="selector-class">.someClass</span> &#123; <span class="selector-class">.truth</span>(<span class="number">255</span>); &#125;</span><br></pre></td></tr></table></figure>
<p>导引之后可以有<strong>多个条件</strong>，一般使用<code>(condition1),(condition2),...</code>的形式来表示。符合其中的任意一个条件，匹配就可以成功，属性集才会被执行，这个形式相当于使用关键词<em>or</em>来连接条件实现或门。另外，可以使用关键词<em>and</em>实现与门，使用<em>when not</em>关键词实现非门。</p>
<p>导引语句when之后的判断条件中可以是<strong>不含参数</strong>的判断条件，也可以是对多个参数的比较等：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.mixin</span> (<span class="keyword">@a</span>) when (@media = mobile) &#123; ... &#125;</span><br><span class="line"><span class="selector-class">.mixin</span> (<span class="keyword">@a</span>) when (@media = desktop) &#123; ... &#125;</span><br><span class="line"><span class="selector-class">.max</span> (<span class="keyword">@a</span>, @b) when (@a &gt; @b) &#123; <span class="attribute">width</span>: @a &#125;</span><br><span class="line"><span class="selector-class">.max</span> (<span class="keyword">@a</span>, @b) when (@a &lt; @b) &#123; <span class="attribute">width</span>: @b &#125;</span><br></pre></td></tr></table></figure>
<p>其中判断语句中可以使用一些判断函数，如常见的类型判断：<code>iscolor()</code>，<code>isnumber()</code>，<code>isstring()</code>，<code>iskeyword()</code>，<code>isurl()</code>；常见的单位量判断：<code>ispixel()</code>，<code>ispercentage()</code>，<code>isem()</code>。</p>
</li>
</ul>
<h4 id="嵌套规则"><a href="#嵌套规则" class="headerlink" title="嵌套规则"></a>嵌套规则</h4><p>此处的嵌套，指的是对上级重复的派生选择器进行嵌套方式的简写，例如：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#header</span> &#123; <span class="attribute">color</span>: black; &#125;</span><br><span class="line"><span class="selector-id">#header</span> <span class="selector-class">.navigation</span> &#123;</span><br><span class="line">  <span class="attribute">font-size</span>: <span class="number">12px</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#header</span> <span class="selector-class">.logo</span> &#123; </span><br><span class="line">  <span class="attribute">width</span>: <span class="number">300px</span>; </span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-id">#header</span> <span class="selector-class">.logo</span><span class="selector-pseudo">:hover</span> &#123;</span><br><span class="line">  <span class="attribute">text-decoration</span>: none;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以简写为：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#header</span> &#123; </span><br><span class="line">	<span class="attribute">color</span>: black; </span><br><span class="line">	<span class="selector-class">.navigation</span> &#123;</span><br><span class="line">  		<span class="attribute">font-size</span>: <span class="number">12px</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="selector-class">.logo</span> &#123; </span><br><span class="line">  		<span class="attribute">width</span>: <span class="number">300px</span>; </span><br><span class="line">  		&amp;<span class="selector-pseudo">:hover</span>: text-decoration: none;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里需要注意的是，对于<code>:hover</code>和<code>:focus</code>这样的伪类等，需要使用串联选择器而非后代选择器时（串联选择器之间无空格，后代选择器有）应该使用_&amp;_符号置于前面：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.bordered</span> &#123;</span><br><span class="line">  &amp;<span class="selector-class">.float</span> &#123;</span><br><span class="line">    <span class="attribute">float</span>: left; </span><br><span class="line">  &#125;					<span class="comment">/*输出的是.bordered.float选择器*/</span></span><br><span class="line">  <span class="selector-class">.top</span> &#123;</span><br><span class="line">    <span class="attribute">margin</span>: <span class="number">5px</span>; </span><br><span class="line">  &#125;					<span class="comment">/*输出的是.bordered .top选择器*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="运算"><a href="#运算" class="headerlink" title="运算"></a>运算</h4><p>数字、颜色或者变量都可以参与运算，且支持并向下兼容带有单位的数值：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@base</span>: <span class="number">5%</span>;</span><br><span class="line"><span class="keyword">@filler</span>: @base * <span class="number">2</span>;				<span class="comment">/*返回10%*/</span></span><br><span class="line"><span class="keyword">@other</span>: @base + @filler;		<span class="comment">/*返回15%*/</span></span><br><span class="line"><span class="attribute">color</span>: <span class="number">#888</span> / <span class="number">4</span>;</span><br><span class="line"><span class="attribute">background-color</span>: @base-color + <span class="number">#111</span>;</span><br><span class="line"><span class="attribute">height</span>: <span class="number">100%</span> / <span class="number">2</span> + @filler;</span><br><span class="line"><span class="keyword">@var</span>: <span class="number">1px</span> + <span class="number">5</span>;					<span class="comment">/*输出结果是6px*/</span></span><br><span class="line"><span class="attribute">width</span>: (@var + <span class="number">5</span>) * <span class="number">2</span>;</span><br></pre></td></tr></table></figure>
<h4 id="函数"><a href="#函数" class="headerlink" title="函数"></a><a href="#函数" title="函数"></a>函数</h4><p>LESS中提供了非常丰富且方便使用的函数，包括有color函数、字符串函数、数学函数等等，下面主要介绍一下color函数，其余的函数可以在官方的API中查询使用<a href="http://less.bootcss.com/functions/#functions-overview">^1</a></p>
<h5 id="Color函数"><a href="#Color函数" class="headerlink" title="Color函数"></a><a href="#Color函数" title="Color函数"></a>Color函数</h5><p>LESS提供的颜色运算函数，本质是先把RGB格式转化成HSL（就是色调、饱和度和亮度三维空间）色彩空间，然后再通道级别进行操作：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">hue(<span class="keyword">@color</span>);        		<span class="comment">/* 返回该颜色的hue通道值 */</span></span><br><span class="line">saturation(<span class="keyword">@color</span>);  		<span class="comment">/* 返回该颜色的饱和度值 */</span></span><br><span class="line">lightness(<span class="keyword">@color</span>);  		<span class="comment">/* 返回颜色的亮度值 */</span></span><br><span class="line"></span><br><span class="line">lighten(<span class="keyword">@color</span>, <span class="number">10%</span>);     <span class="comment">/* 颜色亮度提高10% */</span></span><br><span class="line">darken(<span class="keyword">@color</span>, <span class="number">10%</span>);      <span class="comment">/* 颜色亮度降低10% */</span></span><br><span class="line"></span><br><span class="line">saturate(<span class="keyword">@color</span>, <span class="number">10%</span>);    <span class="comment">/* 颜色饱和度提高10% */</span></span><br><span class="line">desaturate(<span class="keyword">@color</span>, <span class="number">10%</span>);  <span class="comment">/* 颜色饱和度降低10% */</span></span><br><span class="line"></span><br><span class="line">fadein(<span class="keyword">@color</span>, <span class="number">10%</span>);      <span class="comment">/* 颜色透明度降低10% */</span></span><br><span class="line">fadeout(<span class="keyword">@color</span>, <span class="number">10%</span>);     <span class="comment">/* 颜色透明度提高10% */</span></span><br><span class="line">fade(<span class="keyword">@color</span>, <span class="number">50%</span>);        <span class="comment">/* 颜色透明度变为50% */</span></span><br><span class="line"></span><br><span class="line">spin(<span class="keyword">@color</span>, <span class="number">10</span>);         <span class="comment">/* 在色轮上顺时针旋转10°</span></span><br><span class="line"><span class="comment">spin(@color, -10);        /* 在色轮上逆时针旋转10°</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">mix(@color1, @color2);    /* 两种颜色的混色</span></span><br></pre></td></tr></table></figure>
<h4 id="命名空间"><a href="#命名空间" class="headerlink" title="命名空间"></a><a href="#命名空间" title="命名空间"></a>命名空间</h4><p>为了对CSS进行更好的封装，可以将一些变量或混合模式打包起来，如：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#bundle</span> &#123;</span><br><span class="line">  <span class="selector-class">.button</span> () &#123;</span><br><span class="line">    <span class="attribute">display</span>: block;</span><br><span class="line">    <span class="attribute">border</span>: <span class="number">1px</span> solid black;</span><br><span class="line">    <span class="attribute">background-color</span>: grey;</span><br><span class="line">    &amp;<span class="selector-pseudo">:hover</span> &#123; <span class="attribute">background-color</span>: white &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="selector-class">.tab</span> &#123; ... &#125;</span><br><span class="line">  <span class="selector-class">.citation</span> &#123; ... &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 使用&gt;符号来进行引入 */</span></span><br><span class="line"><span class="selector-id">#header</span> <span class="selector-tag">a</span> &#123;</span><br><span class="line">  <span class="attribute">color</span>: orange;</span><br><span class="line">  <span class="selector-id">#bundle</span> &gt; <span class="selector-class">.button</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h4><p>LESS中的作用域类似于JS中原型链，首先会从本地查找变量或者混合模块，如果没找到的话会去父级作用域中查找，直到找到为止.</p>
<h4 id="importing"><a href="#importing" class="headerlink" title="importing"></a>importing</h4><p>可以通过@import “less-file-name.less”，其中.less的后缀是可选的。</p>
<h4 id="字符串插值"><a href="#字符串插值" class="headerlink" title="字符串插值"></a>字符串插值</h4><p>变量可以通过使用@{someName}的方式来将变量放入字符串中，例如：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@base-url</span>: <span class="string">&quot;http://assets.fnord.com&quot;</span>;</span><br><span class="line"><span class="attribute">background-image</span>: <span class="built_in">url</span>(<span class="string">&quot;@&#123;base-url&#125;/images/bg.png&quot;</span>);</span><br></pre></td></tr></table></figure>
<h4 id="避免编译"><a href="#避免编译" class="headerlink" title="避免编译"></a>避免编译</h4><p>当我们需要让一些语句失去作用，避免编译这些语句时，可以在语句字符串前面加上~，例如：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.class</span> &#123;</span><br><span class="line">  <span class="attribute">filter</span>: ~<span class="string">&quot;ms:alwaysHasItsOwnSyntax.For.Stuff()&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="在LESS中使用JavaScript"><a href="#在LESS中使用JavaScript" class="headerlink" title="在LESS中使用JavaScript"></a>在LESS中使用JavaScript</h4><p>通过反单引号<code></code>的形式包围：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="keyword">@str</span>: <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="keyword">@var</span>: ~`<span class="string">&quot;@&#123;str&#125;&quot;</span>.toUpperCase() + <span class="string">&#x27;!&#x27;</span>`;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Leaflet插件在浏览器加载地图</title>
    <url>/2017/06/12/2017-06-12-using-leaflet-to-load-map.html</url>
    <content><![CDATA[<p>在公司实习的第一个项目就与地图和数据可视化相关，这让从来没有接触过地图API、加载工具以及相关开发的我一开始都有些手足无措。之后技术路线慢慢定下来，在浏览器中加载腾讯地图的瓦片地图，并在该地图上添加热力图等可视化图表。</p>
<p>在经过一番简单的调研之后，在浏览器加载地图无非是采用相应地图公司开发的JavaScript API，或者加载在线/离线瓦片地图。后者由于要使用第三方的插件或者自己开发的工具进行加载，所以实现起来较为复杂，而前者实现起来就比较傻瓜了。本文中主要使用腾讯地图作为实例使用地图，同时也会简单提及其他公司开发的地图。</p>
<span id="more"></span>
<h4 id="使用JavaScript-API加载腾讯地图"><a href="#使用JavaScript-API加载腾讯地图" class="headerlink" title="使用JavaScript API加载腾讯地图"></a>使用JavaScript API加载腾讯地图</h4><p>使用<a href="http://lbs.qq.com/javascript_v2/index.html">JavaScript API</a>在浏览器加载腾讯地图，方式非常简单，最重要就是在页面中加载相应的API的js文件，并初始化地图组件即可：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">&quot;Content-Type&quot;</span> <span class="attr">content</span>=<span class="string">&quot;text/html; charset=utf-8&quot;</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span>简单地图<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">&quot;text/css&quot;</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">			<span class="selector-id">#container</span>&#123;</span></span><br><span class="line"><span class="language-css">			    <span class="attribute">min-width</span>:<span class="number">600px</span>;</span></span><br><span class="line"><span class="language-css">			    <span class="attribute">min-height</span>:<span class="number">767px</span>;</span></span><br><span class="line"><span class="language-css">			&#125;</span></span><br><span class="line"><span class="language-css">		</span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">script</span> <span class="attr">charset</span>=<span class="string">&quot;utf-8&quot;</span> <span class="attr">src</span>=<span class="string">&quot;http://map.qq.com/api/js?v=2.exp&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">		<span class="variable language_">window</span>.<span class="property">onload</span> = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span></span><br><span class="line"><span class="language-javascript">			<span class="keyword">function</span> <span class="title function_">init</span>(<span class="params"></span>) &#123;	<span class="comment">//初始化地图函数，自定义函数名init</span></span></span><br><span class="line"><span class="language-javascript">		        <span class="comment">//定义map变量，调用qq.maps.Map()构造函数，获取地图显示容器</span></span></span><br><span class="line"><span class="language-javascript">		    	<span class="keyword">var</span> map = <span class="keyword">new</span> qq.<span class="property">maps</span>.<span class="title class_">Map</span>(<span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&quot;container&quot;</span>), &#123;</span></span><br><span class="line"><span class="language-javascript">             		<span class="comment">// 地图的中心地理坐标：</span></span></span><br><span class="line"><span class="language-javascript">             		<span class="attr">center</span>: <span class="keyword">new</span> qq.<span class="property">maps</span>.<span class="title class_">LatLng</span>(<span class="number">39.916527</span>,<span class="number">116.397128</span>),</span></span><br><span class="line"><span class="language-javascript">              	<span class="attr">zoom</span>:<span class="number">8</span>                                                 </span></span><br><span class="line"><span class="language-javascript">             &#125;);</span></span><br><span class="line"><span class="language-javascript">		   &#125;</span></span><br><span class="line"><span class="language-javascript">	      <span class="comment">//调用初始化函数地图</span></span></span><br><span class="line"><span class="language-javascript">	      <span class="title function_">init</span>();</span></span><br><span class="line"><span class="language-javascript">		&#125;</span></span><br><span class="line"><span class="language-javascript">		</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--定义地图显示容器--&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;container&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h4 id="瓦片地图"><a href="#瓦片地图" class="headerlink" title="瓦片地图"></a>瓦片地图</h4><p>目前的流行的互联网瓦片地图种类有两种，一种是栅格地图瓦片，另一种是矢量地图瓦片。</p>
<p>栅格地图瓦片较为传统，是将矢量数据渲染成为256×256像素大小的图片，例如百度地图、高德地图、天地图和腾讯地图等，这些地图可以使用leaflet框架进行瓦片的加载。矢量地图瓦片是以json格式分块传输，在浏览器端利用前端框架将地图展示出来，mapbox和腾讯地图中的3D地图就是矢量地图瓦片，支持三维旋转等。</p>
<h4 id="使用leaflet-js"><a href="#使用leaflet-js" class="headerlink" title="使用leaflet.js"></a>使用leaflet.js</h4><p>各个地图公司的产品本身拥有还比较完善的API和相关的插件和工具，比如除了地图加载之外的标志叠加、热力图叠加等。但是地图瓦片的加载目前比较流行和完善的工具就是<a href="http://leafletjs.com/">leaflet.js</a>工具,它通过下载特定地图产品的瓦片（一般是256x256px大小），将相应的x、y坐标对应起来，展示在html文档的容器中。之前在网上搜资料，腾讯地图瓦片的资料也是凤毛麟角，跟公司地图部门的同学了解之后便开始了这个地图应用的开发。下面就用leaflet工具加载腾讯地图瓦片作为例子来展示如何进行地图瓦片的操作。</p>
<p>废话不多说，先上代码：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">http-equiv</span>=<span class="string">&quot;Content-Type&quot;</span> <span class="attr">content</span>=<span class="string">&quot;text/html; charset=utf-8&quot;</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span>简单地图<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">&quot;text/css&quot;</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">			<span class="selector-id">#container</span>&#123;</span></span><br><span class="line"><span class="language-css">			    <span class="attribute">min-width</span>:<span class="number">600px</span>;</span></span><br><span class="line"><span class="language-css">			    <span class="attribute">min-height</span>:<span class="number">767px</span>;</span></span><br><span class="line"><span class="language-css">			&#125;</span></span><br><span class="line"><span class="language-css">		</span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">script</span> <span class="attr">charset</span>=<span class="string">&quot;utf-8&quot;</span> <span class="attr">src</span>=<span class="string">&quot;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--定义地图显示容器--&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;mapid&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">			<span class="keyword">var</span> mymap = L.<span class="title function_">map</span>(<span class="string">&#x27;mapid&#x27;</span>).<span class="title function_">setView</span>([<span class="number">39.90469</span>, <span class="number">116.40717</span>], <span class="number">12</span>);</span></span><br><span class="line"><span class="language-javascript">			L.<span class="title function_">tileLayer</span>(<span class="string">&#x27;https://rt&#123;s&#125;.map.gtimg.com/realtimerender?z=&#123;z&#125;&amp;x=&#123;x&#125;&amp;y=&#123;y&#125;&amp;type=vector&amp;style=0&#x27;</span>, &#123;</span></span><br><span class="line"><span class="language-javascript">	          <span class="attr">subdomains</span>: [<span class="string">&quot;0&quot;</span>,<span class="string">&quot;1&quot;</span>,<span class="string">&quot;2&quot;</span>,<span class="string">&quot;3&quot;</span>],</span></span><br><span class="line"><span class="language-javascript">	          <span class="attr">tms</span>: <span class="literal">true</span>,</span></span><br><span class="line"><span class="language-javascript">	          <span class="attr">maxZoom</span>:<span class="number">18</span>,</span></span><br><span class="line"><span class="language-javascript">	          <span class="attr">minZoom</span>:<span class="number">1</span>,</span></span><br><span class="line"><span class="language-javascript">	          <span class="attr">detectRetina</span>: <span class="literal">true</span>,</span></span><br><span class="line"><span class="language-javascript">	          <span class="attr">attribution</span>: <span class="string">&#x27;&amp;copy 滴滴出行&#x27;</span></span></span><br><span class="line"><span class="language-javascript">	   		&#125;).<span class="title function_">addTo</span>(mymap);</span></span><br><span class="line"><span class="language-javascript">		</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>上面代码中展示的就是在div容器中加载地图瓦片的实例，其中，最重要的就是腾讯地图瓦片的下载地址<strong><a href="https://rt{s}.map.gtimg.com/realtimerender?z={z}&amp;x={x}&amp;y={y}&amp;type=vector&amp;style=0">https://rt{s}.map.gtimg.com/realtimerender?z={z}&amp;x={x}&amp;y={y}&amp;type=vector&amp;style=0</a></strong>，其中{s}是指subdomains，是地图瓦片的集群编号，可以传入字符串或数组，此处我们传入的是[“0”, “1”, “2”, “3”]；其次是{x}、{y}、{z}三个参数分别指的是瓦片的横坐标、纵坐标以及缩放等级。地图实例通过L.map(容器ID)来进行声明，通过L.tileLayer().addTo(地图实例)方法来进行加载，该方法中需要传入一个地图瓦片URL和一个参数对象作为参数。其中参数对象中的参数配置可以参考leaflet官网的文档进行配置。</p>
<p>地图加载完毕之后，可以在加载好的地图图层上继续添加所需要的标志、热力图等等。我们这里展示leaflet工具自带的热力图加载方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">L.<span class="title function_">heatLayer</span>(data,&#123;</span><br><span class="line">	<span class="attr">minOpacity</span>: <span class="number">0.6</span>,</span><br><span class="line">	<span class="attr">maxZoom</span>: <span class="number">18</span>,</span><br><span class="line">	<span class="attr">max</span>: <span class="number">500</span>,</span><br><span class="line">	<span class="attr">radius</span>: <span class="number">12</span>,</span><br><span class="line">	<span class="attr">blur</span>: <span class="number">0</span>,</span><br><span class="line">	<span class="attr">gradient</span>: &#123;</span><br><span class="line">	  <span class="number">1</span>: <span class="string">&#x27;#00ffe4&#x27;</span>, </span><br><span class="line">	  <span class="number">0.7</span>: <span class="string">&#x27;#0078ff&#x27;</span>, </span><br><span class="line">	  <span class="number">0.5</span>:<span class="string">&#x27;#4285ff&#x27;</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;).<span class="title function_">addTo</span>(mymap);</span><br></pre></td></tr></table></figure>
<p>同样是在一样的容器里添加图层，参数有热力数据和参数对象两个，其中热力数据的数据结构是[纬度, 经度, 热度]该热力图可以通过设置透明度（minOpacity）、渐变色（gradient）、扩展度（blur）来改变热力图的样式。</p>
<p>在本文最基础的使用leaflet工具来加载腾讯地图瓦片之后，相信会接触更多有关地图网页应用的项目，之后也会不断地更新地图相关技术博客文章。</p>
]]></content>
      <categories>
        <category>Toolkit</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Leaflet</tag>
        <tag>Map</tag>
        <tag>腾讯地图</tag>
      </tags>
  </entry>
  <entry>
    <title>ES6入门必须了解的知识（上）</title>
    <url>/2017/08/02/2017-08-02-ES6-Must-Know-Keypoint-Note.html</url>
    <content><![CDATA[<p>最近一段时间的实习期间，团队进行了一次ESLint规范的调整和文档改进，里面涉及到了非常多ES6的内容，而在之前热力图项目中也用到了像Promise对象、async函数等的知识，所以专门参考阮一峰写的<a href="http://es6.ruanyifeng.com/">《ECMAScript6入门》</a>一书对ES6的一些重要知识点进行一次整理。<br><span id="more"></span></p>
<h5 id="Babel转码器"><a href="#Babel转码器" class="headerlink" title="Babel转码器"></a>Babel转码器</h5><p>Babel是可以将ES6代码转为ES5代码的转码器。</p>
<ul>
<li><p>配置文件 <strong>.babelrc</strong></p>
<p>该配置文件是使用Babel工具和模块必须的，存放在项目的根目录下，用来设置转码<strong>规则</strong>和<strong>插件</strong>，基本格式如下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;presets&quot;</span>: [],</span><br><span class="line">    <span class="string">&quot;plugins&quot;</span>: []</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>其中presets字段设定转码规则，官方提供以下的规则集，你可以根据需要安装。一般安装最新的的规则集 <code>npm install --save-dev babel-preset-latest</code> ，然后，配置文件应该如下</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;presets&quot;</span>: [</span><br><span class="line">      <span class="string">&quot;latest&quot;</span>,</span><br><span class="line">      <span class="string">&quot;react&quot;</span>,</span><br><span class="line">      <span class="string">&quot;stage-2&quot;</span></span><br><span class="line">    ],</span><br><span class="line">    <span class="string">&quot;plugins&quot;</span>: []</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>命令行转码<strong>babel-cli</strong></p>
<ol>
<li>安装命令： <code>npm install --global babel-cli</code></li>
<li>基本用法（全局环境下）：<ul>
<li>转码结果输出到标准输出： <code>babel example.js</code></li>
<li>转码结果写入一个文件： <code>babel example.js --out-file compiled.js</code></li>
<li>整个目录转码： <code>babel src --out-dir lib</code></li>
</ul>
</li>
</ol>
</li>
<li><p>建议将babel-cli安装在项目目录下</p>
<ol>
<li>安装命令： <code>npm install --save-dev babel-cli</code>;</li>
<li><p>改写package.json</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">	<span class="string">&quot;devDependencies&quot;</span>: &#123;</span><br><span class="line">		<span class="string">&quot;babel-cli&quot;</span>: <span class="string">&quot;^6.0.0&quot;</span></span><br><span class="line">	&#125;,</span><br><span class="line">	<span class="string">&quot;scripts&quot;</span>: &#123;</span><br><span class="line">		<span class="string">&quot;build&quot;</span>: <span class="string">&quot;babel src -d lib&quot;</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>转码命令（在项目目录下）： <code>npm run build</code>;</p>
</li>
</ol>
</li>
<li><p>ES6的REPL环境<strong>babel-node</strong>：</p>
<p>babel-node随babel-cli工具安装，执行 <code>babel-node</code> 进入REPL环境可以直接运行命令了</p>
</li>
<li><p>babel-register</p>
<p>babel-register木块改写require命令，可以使文档中使用require加载的.js/.jsx/.es/.es6后缀的文件先进行babel转码：</p>
<ol>
<li>局部安装： <code>npm install --save-dev babel-register</code></li>
<li><p>使用（前必须先加载babel-register）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">require</span>(<span class="string">&quot;babel-register&quot;</span>);</span><br><span class="line"><span class="built_in">require</span>(<span class="string">&quot;./index.js&quot;</span>);</span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p>babel-core：当代码需要调用Babel的API进行转码时</p>
<ol>
<li>安装： <code>npm install babel-core --save</code></li>
<li><p>项目中调用：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> babel = <span class="built_in">require</span>(<span class="string">&#x27;babel-core&#x27;</span>);</span><br><span class="line">   </span><br><span class="line"><span class="comment">// 字符串转码</span></span><br><span class="line">babel.<span class="title function_">transform</span>(<span class="string">&#x27;code();&#x27;</span>, options);</span><br><span class="line"><span class="comment">// =&gt; &#123; code, map, ast &#125;</span></span><br><span class="line">   </span><br><span class="line"><span class="comment">// 文件转码（异步）</span></span><br><span class="line">babel.<span class="title function_">transformFile</span>(<span class="string">&#x27;filename.js&#x27;</span>, options, <span class="keyword">function</span>(<span class="params">err, result</span>) &#123;</span><br><span class="line"> result; <span class="comment">// =&gt; &#123; code, map, ast &#125;</span></span><br><span class="line">&#125;);</span><br><span class="line">   </span><br><span class="line"><span class="comment">// 文件转码（同步）</span></span><br><span class="line">babel.<span class="title function_">transformFileSync</span>(<span class="string">&#x27;filename.js&#x27;</span>, options);</span><br><span class="line"><span class="comment">// =&gt; &#123; code, map, ast &#125;</span></span><br><span class="line">   </span><br><span class="line"><span class="comment">// Babel AST转码</span></span><br><span class="line">babel.<span class="title function_">transformFromAst</span>(ast, code, options);</span><br><span class="line"><span class="comment">// =&gt; &#123; code, map, ast &#125;</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p>babel-polyfill</p>
<p>Babel 默认只转换新的 JavaScript 句法（syntax），而不转换新的API，比如Iterator、Generator、Set、Maps、Proxy、Reflect、Symbol、Promise等全局对象，以及一些定义在全局对象上的方法（比如Object.assign）都不会转码。如果想让这个方法运行，必须使用<strong>babel-polyfill</strong></p>
</li>
<li><p>Traceus转码器</p>
<ul>
<li>直接插入网页</li>
<li>在线转换</li>
<li>命令行转换</li>
</ul>
</li>
</ul>
<h5 id="常量和变量"><a href="#常量和变量" class="headerlink" title="常量和变量"></a>常量和变量</h5><p>除了ES5提出的var和function两种声明变量的方法，ES6提出了let和const命令，以及import命令和class命令。所以ES6中一共有六种声明变量的方法。</p>
<h5 id="let和const命令"><a href="#let和const命令" class="headerlink" title="let和const命令"></a>let和const命令</h5><ul>
<li><p>let命令</p>
<p>ES6新增了let命令，用来声明变量。它的用法类似于var，但是所声明的变量，只在let命令所在的<strong>代码块</strong>内有效。</p>
<ul>
<li><p>基本用法：特别适合在for循环计数器中使用，而且循环体是循环语句的自作用域：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = [];</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">	a[i] = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">	  <span class="variable language_">console</span>.<span class="title function_">log</span>(i);</span><br><span class="line">	&#125;;</span><br><span class="line">&#125;</span><br><span class="line">a[<span class="number">6</span>](); <span class="comment">// 可以避免闭包中只能读取最后一个值</span></span><br><span class="line">	</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">	<span class="keyword">let</span> i = <span class="string">&#x27;abc&#x27;</span>;</span><br><span class="line">	<span class="variable language_">console</span>.<span class="title function_">log</span>(i);</span><br><span class="line">&#125;		<span class="comment">// abc</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>特点</p>
<ul>
<li>不存在变量提升（变量提升是指变量在声明之前可以使用<strong>但是值为undefined</strong>）</li>
<li>暂时性死区：只要块中有let声明变量语句，该变量在该块中就必须服从let语句的规则（const也是如此），所以typeof也是不安全的</li>
<li>不允许重复声明：包括在相同作用域内禁止重复声明同一个变量（只要出现let就不允许，包括var），以及函数内部重新声明参数</li>
</ul>
</li>
<li><p>块级作用域</p>
<p>由于内层变量可能会覆盖外层变量，用来计数的循环变量泄露成为全局变量。</p>
<p>let实际上为JavaScript新增了块级作用域：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">f1</span>(<span class="params"></span>) &#123;</span><br><span class="line">	<span class="keyword">let</span> n = <span class="number">5</span>;</span><br><span class="line">	<span class="keyword">if</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">	  <span class="keyword">let</span> n = <span class="number">10</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="variable language_">console</span>.<span class="title function_">log</span>(n); <span class="comment">// 5</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li><p>特点</p>
<ul>
<li>块级作用域可以任意嵌套，但是外层作用域无法读取内层作用域的变量</li>
<li>内层作用域可以定义外层作用域的同名变量</li>
<li>实际上让立即执行函数表达式不再必要（直接将需要立即执行的函数内部代码写在{}块级作用域中即可）</li>
</ul>
</li>
<li><p>块级作用域与函数声明</p>
<p>  ES6明确允许在块级作用域中声明函数（在ES5中函数声明只能在函数作用域或全局作用域中进行），且块级作用域中的函数声明行为类似于let，，但必须在有大括号的情况下成立，且在块级作用域外不可引用。</p>
<p>  但是浏览器不遵守上述规定，有自己的行为方式：</p>
<ul>
<li>允许在块级作用域内声明函数</li>
<li>函数声明类似于var，即会提升到全局作用域或函数作用域的头部</li>
<li><p>同时，函数声明还会提升到所在的块级作用域的头部</p>
<p>所以<strong>尽量不要</strong>在块级作用域内声明函数，若非要声明，也应该写成<strong>函数表达式</strong>而非函数声明语句。</p>
</li>
</ul>
</li>
<li><p>do表达式</p>
<p>  块级作用域内的let声明的变量无法在作用域外被访问，所以提出了do表达式，可以使块级作用域表现为表达式并有返回值：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> x = <span class="keyword">do</span> &#123;</span><br><span class="line">	<span class="keyword">let</span> t = <span class="title function_">f</span>();</span><br><span class="line">	t * t + <span class="number">1</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li><p>const命令</p>
<p>声明一个只读的常量，声明之后该值不能改变。所以一旦声明必须赋值！</p>
<p>特点：</p>
<ol>
<li>不会被提升；</li>
<li>存在暂时性死区</li>
<li><p>将一个引用变量声明为常量需要小心（该常量指向引用变量的地址，但是里面的内容是可以修改的，所以对象常量不可以被重新赋值为新的引用变量）</p>
<p>如果想让对象以及对象里的内容只读，那么需要使用 <code>const foo = Object.freeze(&#123;&#125;);</code></p>
</li>
</ol>
</li>
<li><p>顶层对象的属性</p>
<p>顶层对象，在浏览器环境指的是<strong>window对象</strong>，在Node指的是<strong>global对象</strong>。ES5之中，顶层对象的属性与全局变量是等价的。但是这样存在很多问题：</p>
<ol>
<li>没法在编译时报出变量未声明的错误（只能运行时报错）</li>
<li>容易莫名其妙创建全局变量</li>
<li><p>顶层对象可以随处读写，不利于模块化编程</p>
<p>所以ES6提出了：</p>
</li>
<li><p>var命令和function命令声明的全局变量依旧是顶层对象的属性</p>
</li>
<li><p>let命令、const命令和class命令声明的全局变量不属于顶层对象的属性：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> b = <span class="number">1</span>;</span><br><span class="line"><span class="variable language_">window</span>.<span class="property">b</span> <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
</li>
<li><p>global对象</p>
<p>ES5中的顶层对象，在浏览器中是window，浏览器和WebWorker里面self也指向顶层对象，在Node里面是global</p>
<p>对于this变量来说，ES5全局环境this返回顶层对象；Node和ES6模块返回当前模块；函数作为对象的方法运行指向对象，否则指向顶层对象，严格模式下返回undefined；</p>
</li>
</ul>
<h4 id="变量的解构赋值"><a href="#变量的解构赋值" class="headerlink" title="变量的解构赋值"></a>变量的解构赋值</h4><ul>
<li><p>数组的解构赋值</p>
<p>用法： <code>let[a, b, c] = [1, 2, 3];</code></p>
<p>只要两边的数组结构模式相同，那么就可以使用这种数组解构赋值。</p>
<p>两种特殊情况：</p>
<ul>
<li>左边模式变量多于右边数组元素变量，解构不成功，返回undefined</li>
<li>左边模式变量少于右边数组元素变量，解构成功</li>
<li><p>等号右边不是数组（严格讲，可遍历的数据结构），报错</p>
<p>事实上，只要具有Iterator解构的数据结构，都可以采用数组的形式进行解构赋值，例如 <code>let [x, y, z] = Set([&#39;a&#39;, &#39;b&#39;, &#39;c&#39;]);</code> ,</p>
<p><strong>默认值</strong>：解构赋值允许指定默认值，使用表达式来赋值，但是默认值对应的数组中的位置如果不是严格等于undefined，默认值不会生效：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [x, y = <span class="string">&#x27;b&#x27;</span>] = [<span class="string">&#x27;a&#x27;</span>]; <span class="comment">// x=&#x27;a&#x27;, y=&#x27;b&#x27;</span></span><br><span class="line"><span class="keyword">let</span> [x, y = <span class="string">&#x27;b&#x27;</span>] = [<span class="string">&#x27;a&#x27;</span>, <span class="literal">undefined</span>]; <span class="comment">// x=&#x27;a&#x27;, y=&#x27;b&#x27;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<p>默认值是一个惰性求值的<strong>表达式</strong>，只有右边数组中没有或者undefined时才会求值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">f</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;aaa&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> [x = <span class="title function_">f</span>()] = [<span class="number">1</span>];    <span class="comment">//此处f()不会求值，因为右边已经赋值成功</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">let</span> [x = <span class="number">1</span>, y = x] = [];     <span class="comment">// x=1; y=1</span></span><br><span class="line"><span class="keyword">let</span> [x = <span class="number">1</span>, y = x] = [<span class="number">2</span>];    <span class="comment">// x=2; y=2</span></span><br><span class="line"><span class="keyword">let</span> [x = <span class="number">1</span>, y = x] = [<span class="number">1</span>, <span class="number">2</span>]; <span class="comment">// x=1; y=2</span></span><br><span class="line"><span class="keyword">let</span> [x = y, y = <span class="number">1</span>] = [];     <span class="comment">// ReferenceError</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p>对象的解构赋值</p>
<p>按照对象的<strong>同名</strong>属性可以进行正确得解构赋值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; bar, foo &#125; = &#123; <span class="attr">foo</span>: <span class="string">&quot;aaa&quot;</span>, <span class="attr">bar</span>: <span class="string">&quot;bbb&quot;</span> &#125;;</span><br><span class="line">foo     <span class="comment">// &quot;aaa&quot;</span></span><br><span class="line">bar    <span class="comment">// &quot;bbb&quot;</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">let</span> &#123; baz &#125; = &#123; <span class="attr">foo</span>: <span class="string">&quot;aaa&quot;</span>, <span class="attr">bar</span>: <span class="string">&quot;bbb&quot;</span> &#125;;</span><br><span class="line">baz <span class="comment">// undefined</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>其真正的机制为： <code>let &#123; foo: foo, bar: bar &#125; = &#123; foo: &quot;aaa&quot;, bar: &quot;bbb&quot; &#125;;</code> 注意冒号后面的才是真正的变量</p>
<p>所以 <code>let &#123; foo: baz &#125; = &#123; foo: &quot;aaa&quot;, bar: &quot;bbb&quot; &#125;;</code> 中baz才被赋值</p>
<p>这里需要注意的是由let进行声明，不可以重新声明，可以使用var替换，也可以试用一下的方式：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> foo;</span><br><span class="line">(&#123;foo&#125; = &#123;<span class="attr">foo</span>: <span class="number">1</span>&#125;); <span class="comment">// 成功，圆括号必须有，否则会认为是一个代码块而非赋值语句</span></span><br><span class="line"><span class="keyword">let</span> baz;</span><br><span class="line">(&#123;<span class="attr">bar</span>: baz&#125; = &#123;<span class="attr">bar</span>: <span class="number">1</span>&#125;); <span class="comment">// 成功</span></span><br><span class="line"><span class="comment">// 错误的写法</span></span><br><span class="line"><span class="keyword">let</span> x;</span><br><span class="line">&#123;x&#125; = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;;</span><br><span class="line"><span class="comment">// SyntaxError: syntax error, &#123;&#125;会被理解成一个代码块</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">// 正确的写法</span></span><br><span class="line">(&#123;x&#125; = &#123;<span class="attr">x</span>: <span class="number">1</span>&#125;);</span><br></pre></td></tr></table></figure>
<p>解构赋值失败会返回undefined，与数组相同，对象也可以用于嵌套结构，牢记<strong>冒号前的一般为模式</strong>，要分清模式和真正的变量；对象也可以有默认值，默认值的表达式也是惰性求值（任何非undefined的值都会对默认值进行覆盖），常用例子： <code>let &#123; log, sin, cos &#125; = Math;</code></p>
<p>数组本质是特殊的对象，所以对象解构赋值等号右边可以是一个数组。</p>
<ul>
<li><p>字符串的解构赋值</p>
<ul>
<li>字符串分解为数组： <code>const [a, b, c, d, e] = &#39;hello&#39;;</code></li>
<li>字符串（是一个对象）属性解构赋值：<code>let &#123;length : len&#125; = &#39;hello&#39;;</code></li>
<li><p>数值和布尔值的解构赋值</p>
<p>由于Number和Bool值都是值类型，所以解构赋值前需要转换为引用类型，比如可以使用toString方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123;<span class="attr">toString</span>: s&#125; = <span class="number">123</span>;</span><br><span class="line">s === <span class="title class_">Number</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">toString</span> <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
<p>由于undefined和null无法转为对象，所以无法进行解构赋值</p>
</li>
</ul>
</li>
<li><p>函数参数的解构赋值</p>
<p>函数参数的解构赋值就是在函数的参数位置进行解构赋值，可以使用上面提到的数组解构赋值、对象解构赋值、字符串解构赋值等，支持默认值（默认值是当解构失败会返回的值），也可以直接传入数组、对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">move</span>(<span class="params">&#123;x = <span class="number">0</span>, y = <span class="number">0</span>&#125; = &#123;&#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> [x, y];</span><br><span class="line">&#125;   				<span class="comment">//该处为x,y指定默认值</span></span><br><span class="line">    </span><br><span class="line"><span class="title function_">move</span>(&#123;<span class="attr">x</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">8</span>&#125;); <span class="comment">// [3, 8]</span></span><br><span class="line"><span class="title function_">move</span>(&#123;<span class="attr">x</span>: <span class="number">3</span>&#125;); 		<span class="comment">// [3, 0]</span></span><br><span class="line"><span class="title function_">move</span>(&#123;&#125;); 			<span class="comment">// [0, 0]</span></span><br><span class="line"><span class="title function_">move</span>(); 			<span class="comment">// [0, 0]</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">function</span> <span class="title function_">move</span>(<span class="params">&#123;x, y&#125; = &#123; x: <span class="number">0</span>, y: <span class="number">0</span> &#125;</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> [x, y];</span><br><span class="line">&#125;   				<span class="comment">//该处为函数的参数指定默认值，这里要进行深入研究</span></span><br><span class="line">    </span><br><span class="line"><span class="title function_">move</span>(&#123;<span class="attr">x</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">8</span>&#125;); <span class="comment">// [3, 8]</span></span><br><span class="line"><span class="title function_">move</span>(&#123;<span class="attr">x</span>: <span class="number">3</span>&#125;); 		<span class="comment">// [3, undefined]</span></span><br><span class="line"><span class="title function_">move</span>(&#123;&#125;); 			<span class="comment">// [undefined, undefined]</span></span><br><span class="line"><span class="title function_">move</span>(); 			<span class="comment">// [0, 0]</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>圆括号问题：尽量不要再解构赋值的模式（等号左边的一堆）中放置圆括号</p>
<p><strong>不能使用圆括号</strong>的三种情况</p>
<ul>
<li>变量声明语句中模式不能带有圆括号</li>
<li>函数参数中模式不能带有圆括号</li>
<li><p>赋值语句中，不能在模式外/嵌套模式中的一层套圆括号</p>
<p><strong>可以使用圆括号</strong>的情况</p>
</li>
<li><p>只有一种：赋值语句的非模式部分可以使用圆括号</p>
</li>
</ul>
</li>
<li><p>解构赋值的用途</p>
<ul>
<li><p>交换变量的值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> y = <span class="number">2</span>;</span><br><span class="line">[x, y] = [y, x];</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<ul>
<li><p>从函数返回多个值</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回一个数组</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">example</span>(<span class="params"></span>) &#123;</span><br><span class="line"><span class="keyword">return</span> [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> [a, b, c] = <span class="title function_">example</span>();</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 返回一个对象</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">example</span>(<span class="params"></span>) &#123;</span><br><span class="line"><span class="keyword">return</span> &#123;</span><br><span class="line">  <span class="attr">foo</span>: <span class="number">1</span>,</span><br><span class="line">  <span class="attr">bar</span>: <span class="number">2</span></span><br><span class="line">&#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> &#123; foo, bar &#125; = <span class="title function_">example</span>();</span><br></pre></td></tr></table></figure>
</li>
<li><p>函数参数的定义</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 参数是一组有次序的值</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">f</span>(<span class="params">[x, y, z]</span>) &#123; <span class="comment">/*...*/</span> &#125;</span><br><span class="line"><span class="title function_">f</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 参数是一组无次序的值</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">f</span>(<span class="params">&#123;x, y, z&#125;</span>) &#123; <span class="comment">/*...*/</span> &#125;</span><br><span class="line"><span class="title function_">f</span>(&#123;<span class="attr">z</span>: <span class="number">3</span>, <span class="attr">y</span>: <span class="number">2</span>, <span class="attr">x</span>: <span class="number">1</span>&#125;);</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>提取JSON数据</strong>（都支持嵌套结构）</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> jsonData = &#123;</span><br><span class="line">  <span class="attr">id</span>: <span class="number">42</span>,</span><br><span class="line">  <span class="attr">status</span>: <span class="string">&quot;OK&quot;</span>,</span><br><span class="line">  <span class="attr">data</span>: [<span class="number">867</span>, <span class="number">5309</span>]</span><br><span class="line">&#125;;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">let</span> &#123; id, status, <span class="attr">data</span>: number &#125; = jsonData;</span><br><span class="line">    </span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(id, status, number);</span><br><span class="line"><span class="comment">// 42, &quot;OK&quot;, [867, 5309]</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>函数参数默认值</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">jQuery.<span class="property">ajax</span> = <span class="keyword">function</span> (<span class="params">url, &#123;</span></span><br><span class="line"><span class="params">  <span class="keyword">async</span> = <span class="literal">true</span>,</span></span><br><span class="line"><span class="params">  beforeSend = <span class="keyword">function</span> () &#123;&#125;,</span></span><br><span class="line"><span class="params">  cache = <span class="literal">true</span>,</span></span><br><span class="line"><span class="params">  complete = <span class="keyword">function</span> () &#123;&#125;,</span></span><br><span class="line"><span class="params">  crossDomain = <span class="literal">false</span>,</span></span><br><span class="line"><span class="params">  <span class="variable language_">global</span> = <span class="literal">true</span>,</span></span><br><span class="line"><span class="params">  // ... more config</span></span><br><span class="line"><span class="params">&#125;</span>) &#123;</span><br><span class="line">  <span class="comment">// ... do stuff</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>遍历Map结构</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> map = <span class="keyword">new</span> <span class="title class_">Map</span>();</span><br><span class="line">map.<span class="title function_">set</span>(<span class="string">&#x27;first&#x27;</span>, <span class="string">&#x27;hello&#x27;</span>);</span><br><span class="line">map.<span class="title function_">set</span>(<span class="string">&#x27;second&#x27;</span>, <span class="string">&#x27;world&#x27;</span>);</span><br><span class="line">    </span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key, value] <span class="keyword">of</span> map) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(key + <span class="string">&quot; is &quot;</span> + value);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// first is hello</span></span><br><span class="line"><span class="comment">// second is world</span></span><br><span class="line">  </span><br><span class="line"><span class="comment">// 获取键名</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [key] <span class="keyword">of</span> map) &#123;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 获取键值</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> [,value] <span class="keyword">of</span> map) &#123;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>输入模块的指定方法：<code>const &#123; SourceMapConsumer, SourceNode &#125; = require(&quot;source-map&quot;);</code></p>
</li>
</ul>
<h5 id="字符串的扩展"><a href="#字符串的扩展" class="headerlink" title="字符串的扩展"></a>字符串的扩展</h5><ul>
<li><p>字符的Unicode表示法</p>
<p>ES5只限制采用码点在\u0000~\uFFFF之间的字符，ES6规定只要将码点放入大括号，就能正确解读（最后JS有z \z \172 \x7A \u007A \u{7A}六种表示字符方法）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;\u&#123;20BB7&#125;&quot;</span></span><br><span class="line"><span class="comment">// &quot;𠮷&quot;</span></span><br><span class="line">    </span><br><span class="line"><span class="string">&quot;\u&#123;41&#125;\u&#123;42&#125;\u&#123;43&#125;&quot;</span></span><br><span class="line"><span class="comment">// &quot;ABC&quot;</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">let</span> hello = <span class="number">123</span>;</span><br><span class="line">hell\u&#123;6F&#125; <span class="comment">// 123</span></span><br><span class="line">    </span><br><span class="line"><span class="string">&#x27;\u&#123;1F680&#125;&#x27;</span> === <span class="string">&#x27;\uD83D\uDE80&#x27;</span></span><br><span class="line"><span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>codePointAt()</p>
<p>JS内部字符以UTF-16的格式储存，每个字符固定为2个字节。大于2个字节的字符，charAt()和charCodeAt()都不能读取整个字符，所以引入了codePointAt()，返回十进制码点。想返回十六进制，可以.toString(16)</p>
</li>
<li><p>String.fromCodePoint()</p>
<p>可识别大于0xFFFF的字符，与上面的codePointAt()方法正好相反</p>
</li>
<li><p>字符串遍历器接口</p>
<p>ES6可以使用for a of b结构对字符串循环遍历：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> text = <span class="title class_">String</span>.<span class="title function_">fromCodePoint</span>(<span class="number">0x20BB7</span>);</span><br><span class="line">  </span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; text.<span class="property">length</span>; i++) &#123;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(text[i]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 无法识别0xFFFF的码点</span></span><br><span class="line">  </span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">let</span> i <span class="keyword">of</span> text) &#123;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(i);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// &quot;𠮷&quot;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>.at()</p>
<p>使用垫片库实现<code>&#39;𠮷&#39;.at(0)</code>对0xFFFF字符进行charAt()操作</p>
</li>
<li><p>.normalize()</p>
<p>用来将字符的不同表示方法统一为同样的形式，这称为Unicode正规化。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;\u01D1&#x27;</span>.<span class="title function_">normalize</span>() === <span class="string">&#x27;\u004F\u030C&#x27;</span>.<span class="title function_">normalize</span>()</span><br><span class="line"><span class="comment">// true</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">/*该函数可以接收NFC(合成形式)/NFD(分解形式)/NFKC/NFKD作为参数*/</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>.includes(), .startsWith(), .endsWith()</p>
<p>都是找字符串中是否是包含、以…开始、以…结束的，均返回bool值，接收两个参数，第一个参数为寻找的子串，第二个参数为开始（end是结束）寻找的父串的下标：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">&#x27;Hello world!&#x27;</span>;</span><br><span class="line">  </span><br><span class="line">s.<span class="title function_">startsWith</span>(<span class="string">&#x27;world&#x27;</span>, <span class="number">6</span>) <span class="comment">// true，这里要注意</span></span><br><span class="line">s.<span class="title function_">endsWith</span>(<span class="string">&#x27;Hello&#x27;</span>, <span class="number">5</span>) <span class="comment">// true</span></span><br><span class="line">s.<span class="title function_">includes</span>(<span class="string">&#x27;Hello&#x27;</span>, <span class="number">6</span>) <span class="comment">// false</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>.repeat()</p>
<p>接收一个参数作为重复次数n（n&gt;-1），返回一个重复n次的新字符串：</p>
</li>
<li><p>.padStart(), .padEnd()</p>
<p>接收两个参数，一个是指定字符串长度，第二个是用来补全的”补丁字符串”（若补丁字符串超出长度则截取，若第二个参数省略用空格补全，不想要的话使用.trim()）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="string">&#x27;x&#x27;</span>.<span class="title function_">padStart</span>(<span class="number">5</span>, <span class="string">&#x27;ab&#x27;</span>) <span class="comment">// &#x27;ababx&#x27;</span></span><br><span class="line"><span class="string">&#x27;x&#x27;</span>.<span class="title function_">padEnd</span>(<span class="number">4</span>, <span class="string">&#x27;ab&#x27;</span>) <span class="comment">// &#x27;xaba&#x27;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>模板字符串</p>
<p>ES6引入模板字符串，使用反引号（`）来标识（可嵌套），可以当做普通字符串，也可以用来定义多行字符串（多行字符串中的空格和缩进都会保留在输出之中）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 普通字符串</span></span><br><span class="line"><span class="string">`In JavaScript &#x27;\n&#x27; is a line-feed.`</span></span><br><span class="line">  </span><br><span class="line"><span class="comment">// 多行字符串</span></span><br><span class="line"><span class="string">`In JavaScript this is</span></span><br><span class="line"><span class="string">not legal.`</span></span><br><span class="line">  </span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`string text line 1</span></span><br><span class="line"><span class="string">string text line 2`</span>);</span><br><span class="line">  </span><br><span class="line"><span class="comment">// 字符串中嵌入变量使用$&#123;表达式、对象属性甚至调用函数等&#125;，牢记&#123;&#125;中最后返回的都是字符串</span></span><br><span class="line"><span class="keyword">var</span> name = <span class="string">&quot;Bob&quot;</span>, time = <span class="string">&quot;today&quot;</span>;</span><br><span class="line"><span class="string">`Hello <span class="subst">$&#123;name&#125;</span>, how are you <span class="subst">$&#123;time&#125;</span>?`</span></span><br><span class="line">    </span><br><span class="line"><span class="comment">//字符串里出现`需要转义</span></span><br><span class="line"><span class="keyword">var</span> greeting = <span class="string">`\`Yo\` World!`</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>模板编译</p>
</li>
<li><p>标签模板</p>
<p>模板字符串可以紧跟在一个函数名后面，该函数将被调用来处理这个模板字符串。这被称为“标签模板”功能（tagged template）。“标签”指的就是<strong>函数</strong>，紧跟在后面的模板字符串就是它的参数。</p>
<ul>
<li><p>当模板字符串里有变量，会将模板字符串先处理成多个参数，再调用函数</p>
<p>处理结果是(stringArray, value1, value2)，处理过程是将所有非变量的部分存在一个字符串数组中，剩下的变量依次一个个排在后面:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> total = <span class="number">30</span>;</span><br><span class="line"><span class="keyword">var</span> msg = passthru<span class="string">`The total is <span class="subst">$&#123;total&#125;</span> (<span class="subst">$&#123;total*<span class="number">1.05</span>&#125;</span> with tax)`</span>;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">function</span> <span class="title function_">passthru</span>(<span class="params">literals</span>) &#123;</span><br><span class="line"><span class="keyword">var</span> result = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> i = <span class="number">0</span>;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">while</span> (i &lt; literals.<span class="property">length</span>) &#123;</span><br><span class="line">  result += literals[i++];</span><br><span class="line">  <span class="keyword">if</span> (i &lt; <span class="variable language_">arguments</span>.<span class="property">length</span>) &#123;</span><br><span class="line">    result += <span class="variable language_">arguments</span>[i];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line">  </span><br><span class="line">msg <span class="comment">// &quot;The total is 30 (31.5 with tax)&quot;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<p>“标签模板”重要应用：</p>
<ol>
<li>过滤HTML字符串，防止用户输入恶意内容</li>
<li>多语言转换（国际化处理）</li>
<li>嵌入其他语言</li>
</ol>
<ul>
<li><p>String.raw()</p>
<p>用来充当模板字符串的处理函数，返回一个斜杠都被转义（斜杠前面再加一个斜杠）的字符串，对应于替换变量后的模板字符串，源码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">String</span>.<span class="property">raw</span> = <span class="keyword">function</span> (<span class="params">strings, ...values</span>) &#123;</span><br><span class="line">	<span class="keyword">var</span> output = <span class="string">&quot;&quot;</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">var</span> index = <span class="number">0</span>; index &lt; values.<span class="property">length</span>; index++) &#123;</span><br><span class="line">		output += strings.<span class="property">raw</span>[index] + values[index];</span><br><span class="line">	&#125;</span><br><span class="line">	  </span><br><span class="line">	output += strings.<span class="property">raw</span>[index]</span><br><span class="line">	<span class="keyword">return</span> output;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>该函数接受第一个参数是有raw数组属性的的对象</p>
</li>
<li><p>模板字符串的限制：默认会将字符串转义，所以无法嵌入其他语言（例如LaTeX），但是标签模板可以</p>
</li>
</ul>
<h5 id="正则的扩展"><a href="#正则的扩展" class="headerlink" title="正则的扩展"></a>正则的扩展</h5><ul>
<li><p>RegExp构造函数</p>
<p>ES5中，RegExp构造函数如下：</p>
<ul>
<li>参数是字符串，这时第二个参数表示正则表达式的修饰符（flag）<code>var regex = new RegExp(&#39;xyz&#39;, &#39;i&#39;);</code></li>
<li><p>参数是一个正则表示式，这时会返回一个原有正则表达式的拷贝<code>var regex = new RegExp(/xyz/i);</code></p>
<p>ES6允许有第二个参数（flag）：<code>var regex = new RegExp(/xyz/ig, &#39;i&#39;);</code>，且第二个参数会覆盖表达式中的修饰符</p>
</li>
</ul>
</li>
<li><p>字符串的正则方法：</p>
<p>字符串对象共有4个方法，可以使用正则表达式：match()、replace()、search()和split()。ES6将这4个方法，在语言内部全部调用RegExp的实例方法，从而做到所有与正则相关的方法，全都定义在RegExp对象上。</p>
<ul>
<li><code>String.prototype.match</code> 调用 <code>RegExp.prototype[Symbol.match]</code>，以此类推…</li>
</ul>
</li>
<li><p>u修饰符</p>
<p>含义为Unicode模式，处理大于\uFFFF的Unicode字符</p>
<ul>
<li><p>点字符：对于码点大于0xFFFF的Unicode字符，点字符不能识别，必须加上u修饰符</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">&#x27;𠮷&#x27;</span>;</span><br><span class="line">  </span><br><span class="line"><span class="regexp">/^.$/</span>.<span class="title function_">test</span>(s) <span class="comment">// false</span></span><br><span class="line">/^.<span class="property">$</span>/u.<span class="title function_">test</span>(s) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>Unicode字符表示法：使用大括号表示的unicode字符需要加u修饰符<code>/\u&#123;20BB7&#125;/u.test(&#39;𠮷&#39;)</code></p>
</li>
<li>预定义模式</li>
<li>i修饰符：添加u才能够识别非规范的字母</li>
</ul>
</li>
<li><p>y修饰符</p>
<p>粘连修饰符，y修饰符的作用与g修饰符类似，也是全局匹配，后一次匹配都从上一次匹配成功的下一个位置开始。不同之处在于，g修饰符只要剩余位置中存在匹配就可，而y修饰符确保匹配必须从剩余的第一个位置开始，这也就是“粘连”的涵义。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">&#x27;aaa_aa_a&#x27;</span>;</span><br><span class="line"><span class="keyword">var</span> r1 = <span class="regexp">/a+/g</span>;</span><br><span class="line"><span class="keyword">var</span> r2 = <span class="regexp">/a+/y</span>;</span><br><span class="line">  </span><br><span class="line">r1.<span class="title function_">exec</span>(s) <span class="comment">// [&quot;aaa&quot;]</span></span><br><span class="line">r2.<span class="title function_">exec</span>(s) <span class="comment">// [&quot;aaa&quot;]</span></span><br><span class="line">  </span><br><span class="line">r1.<span class="title function_">exec</span>(s) <span class="comment">// [&quot;aa&quot;]</span></span><br><span class="line">r2.<span class="title function_">exec</span>(s) <span class="comment">// null</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>sticky属性</p>
</li>
<li><p>flags属性</p>
</li>
<li><p>RegExp.escape()</p>
</li>
<li><p>s修饰符：dotAll模式</p>
</li>
<li><p>后行断言</p>
</li>
<li><p>Unicode属性类</p>
</li>
</ul>
<h5 id="数值的扩展"><a href="#数值的扩展" class="headerlink" title="数值的扩展"></a>数值的扩展</h5><ul>
<li><p>二进制和八进制的表示法</p>
<p>二进制0b/0B，八进制0o/0O</p>
</li>
<li><p>Number.isFinite(Number), Number.isNaN(Number)</p>
<p>分别检查一个数值是否为有限的、是否为NaN</p>
</li>
<li><p>Number.parseInt(Number), Number.parseFloat(Number)</p>
<p>分别将参数（字符串）转换为int和float数据类型</p>
</li>
<li><p>Number.isInteger()</p>
<p>用来判断一个值是否为整数（3和3.0都可以返回true）</p>
</li>
<li><p>Number.EPSILON()</p>
<p>一个极小的常量，用来设置一个误差范围来精确浮点数计算：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">withinErrorMargin</span> (left, right) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="title class_">Math</span>.<span class="title function_">abs</span>(left - right) &lt; <span class="title class_">Number</span>.<span class="property">EPSILON</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_">withinErrorMargin</span>(<span class="number">0.1</span> + <span class="number">0.2</span>, <span class="number">0.3</span>)		<span class="comment">// true</span></span><br><span class="line"><span class="title function_">withinErrorMargin</span>(<span class="number">0.2</span> + <span class="number">0.2</span>, <span class="number">0.3</span>)		<span class="comment">// false</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>安全证书和Number.isSafeInteger()</p>
<p>JavaScript能够准确表示的整数范围在-2^53到2^53之间（不含两个端点），超过这个范围，无法精确表示这个值。Number.isSafeInteger()则是用来判断一个整数是否落在这个范围之内。<strong>验证运算结果是否落在安全整数的范围内，不要只验证运算结果，而要同时验证参与运算的每个值。</strong></p>
</li>
<li><p>Math对象的扩展</p>
<ul>
<li>Math.trunc方法用于去除一个数的小数部分，返回整数部分,可以接收数字和数字字符串</li>
<li>Math.sign方法用来判断一个数到底是正数、负数、还是零</li>
<li>Math.cbrt方法用于计算一个数的立方根</li>
<li>Math.clz32方法返回一个数的32位无符号整数形式有多少个前导0</li>
<li>Math.imul方法返回两个数以32位带符号整数形式相乘的结果，返回的也是一个32位的带符号整数</li>
<li>Math.fround方法返回一个数的单精度浮点数形式</li>
<li>Math.hypot方法返回所有参数的平方和的平方根</li>
<li>Math.expm1(x)返回ex - 1，即Math.exp(x) - 1</li>
<li>Math.log1p(x)方法返回1 + x的自然对数，即Math.log(1 + x)</li>
<li>Math.log10(x)返回以10为底的x的对数</li>
<li><p>Math.log2(x)返回以2为底的x的对数。如果x小于0，则返回NaN</p>
</li>
<li><p>ES6新增了6个三角函数方法：</p>
<ul>
<li>Math.sinh(x) 返回x的双曲正弦（hyperbolic sine）</li>
<li>Math.cosh(x) 返回x的双曲余弦（hyperbolic cosine）</li>
<li>Math.tanh(x) 返回x的双曲正切（hyperbolic tangent）</li>
<li>Math.asinh(x) 返回x的反双曲正弦（inverse hyperbolic sine）</li>
<li>Math.acosh(x) 返回x的反双曲余弦（inverse hyperbolic cosine）</li>
<li>Math.atanh(x) 返回x的反双曲正切（inverse hyperbolic tangent）</li>
</ul>
</li>
</ul>
</li>
<li><p>Math.signbit()</p>
<p>Math.signbit()方法判断一个数的符号位是否设置了</p>
</li>
<li><p>指数运算符</p>
<p>两个乘号代表指数运算符</p>
</li>
</ul>
<h5 id="数组的扩展"><a href="#数组的扩展" class="headerlink" title="数组的扩展"></a>数组的扩展</h5><ul>
<li><p>扩展运算符<br>扩展运算符（spread）是三个点（…），可将一个数组转为用逗号分隔的参数序列。所以该运算符可以用来代替apply调用函数的方式,apply方法是将数组展开为函数的参数，扩展运算符也可以达到此目的。</p>
<p>扩展运算符的应用主要有：</p>
<ol>
<li>合并数组：<code>[...arr1, ...arr2, ...arr3]</code></li>
<li>与解构赋值结合：<code>const [first, ...rest] = [1, 2, 3, 4, 5];</code></li>
<li>函数返回多个值；</li>
<li>将字符串转化为数组：能够正确识别32位的Unicode字符，最好使用扩展运算符；</li>
<li>实现了Iterator接口的对象：</li>
<li>Map、Set解构和Generator函数都可以使用扩展运算符</li>
</ol>
</li>
<li><p>Array.from()</p>
<p>Array.from方法用于将两类对象转为真正的数组：类似数组的<strong>对象</strong>（DOM操作返回的NodeList集合以及函数内部arguments对象）和可遍历（iterable）的对象（包括ES6新增的数据结构<strong>Set</strong>和<strong>Map</strong>），第一个参数接受一个类数组对象，第二个参数接受一个类map方法对对象进行操作。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// arguments对象</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params"></span>) &#123;</span><br><span class="line">	<span class="keyword">var</span> args = <span class="title class_">Array</span>.<span class="title function_">from</span>(<span class="variable language_">arguments</span>);</span><br><span class="line">	<span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="string">``</span><span class="string">`    </span></span><br><span class="line"><span class="string">   </span></span><br><span class="line"><span class="string">   也可以用来进行对数组的复制`</span>arrayCopy = <span class="title class_">Array</span>.<span class="title function_">from</span>([<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>]);<span class="string">`</span></span><br><span class="line"><span class="string">   </span></span><br><span class="line"><span class="string">   扩展运算符（...）也可以将某些数据结构转为数组（必须是部署了Symbol.iterator遍历器接口的对象）：</span></span><br><span class="line"><span class="string">   </span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>javascript</span><br><span class="line"><span class="comment">// arguments对象</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params"></span>) &#123;</span><br><span class="line">	<span class="keyword">var</span> args = [...<span class="variable language_">arguments</span>];</span><br><span class="line">&#125;</span><br><span class="line">  </span><br><span class="line"><span class="comment">// NodeList对象</span></span><br><span class="line">[...<span class="variable language_">document</span>.<span class="title function_">querySelectorAll</span>(<span class="string">&#x27;div&#x27;</span>)]</span><br></pre></td></tr></table></figure>
</li>
<li><p>Array.of()</p>
<p>ES5中，Array方法没有参数、一个参数、三个参数时，返回结果都不一样。只有当参数个数不少于2个时，Array()才会返回由参数组成的新数组。参数个数只有一个时，实际上是指定数组的长度。</p>
<p>Array.of方法用于将一组值，转换为数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Array</span>.<span class="title function_">of</span>() 			<span class="comment">// []</span></span><br><span class="line"><span class="title class_">Array</span>.<span class="title function_">of</span>(<span class="literal">undefined</span>) <span class="comment">// [undefined]</span></span><br><span class="line"><span class="title class_">Array</span>.<span class="title function_">of</span>(<span class="number">1</span>) 		<span class="comment">// [1]</span></span><br><span class="line"><span class="title class_">Array</span>.<span class="title function_">of</span>(<span class="number">1</span>, <span class="number">2</span>) 		<span class="comment">// [1, 2]</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>数组实例的copyWithin()</p>
<p>它接受三个参数，start和end指定要复制的子串，target指定从哪里开始替换:<code>[1, 2, 3, 4, 5].copyWithin(0, 3) // [4, 5, 3, 4, 5]</code></p>
<ul>
<li>target（必需）：从该位置开始替换数据。</li>
<li>start（可选）：从该位置开始读取数据，默认为0。如果为负值，表示倒数（加数组长度即可）。</li>
<li>end（可选）：到该位置<strong>前</strong>停止读取数据，默认等于数组长度。如果为负值，表示倒数。</li>
</ul>
</li>
<li><p>数组实例的find()和findIndex()</p>
<p>数组实例的find方法，用于找出第一个符合条件的数组成员。它的参数是一个回调函数，所有数组成员依次执行该回调函数，直到找出第一个返回值为true的成员，然后返回该成员。如果没有符合条件的成员，则返回undefined。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[<span class="number">1</span>, <span class="number">5</span>, <span class="number">10</span>, <span class="number">15</span>].<span class="title function_">find</span>(<span class="keyword">function</span>(<span class="params">value, index, arr</span>) &#123;</span><br><span class="line">	<span class="keyword">return</span> value &gt; <span class="number">9</span>;</span><br><span class="line">&#125;) <span class="comment">// 10</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">findIndex()与find()用法相同，返回寻找到的成员下标</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<ul>
<li><p>数组实例的fill()</p>
<p>fill方法使用给定值，填充一个数组：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">[<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>].<span class="title function_">fill</span>(<span class="number">7</span>)			<span class="comment">// [7, 7, 7]</span></span><br><span class="line">[<span class="string">&#x27;a&#x27;</span>, <span class="string">&#x27;b&#x27;</span>, <span class="string">&#x27;c&#x27;</span>].<span class="title function_">fill</span>(<span class="number">7</span>, <span class="number">1</span>, <span class="number">2</span>)	<span class="comment">// [&#x27;a&#x27;, 7, &#x27;c&#x27;]</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>数组实例的entries(), key()和values()</p>
<p>ES6提供三个新的方法——entries()，keys()和values()——用于遍历数组。它们都返回一个遍历器对象（详见《Iterator》一章），可以用for…of循环进行遍历，唯一的区别是keys()是对键名的遍历、values()是对键值的遍历，entries()是对键值对的遍历。</p>
</li>
<li><p>数组实例的includes()</p>
<p>Array.prototype.includes方法返回一个布尔值，表示某个数组是否包含给定的值，与字符串的includes方法类似。该方法属于ES7，但Babel转码器已经支持。</p>
</li>
<li><p>数组的空位</p>
<p>数组的空位指，数组的某一个位置没有任何值。比如，Array构造函数返回的数组都是空位。空位不是undefined，undefined是属于有值的！</p>
</li>
</ul>
<h5 id="函数的扩展"><a href="#函数的扩展" class="headerlink" title="函数的扩展"></a>函数的扩展</h5><ul>
<li><p>函数参数的默认值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Point</span>(<span class="params">x = <span class="number">0</span>, y = <span class="number">0</span></span>) &#123;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">x</span> = x;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">y</span> = y;</span><br><span class="line">&#125;</span><br><span class="line">  </span><br><span class="line"><span class="keyword">var</span> p = <span class="keyword">new</span> <span class="title class_">Point</span>();</span><br><span class="line">p <span class="comment">// &#123; x: 0, y: 0 &#125;</span></span><br></pre></td></tr></table></figure>
<p>如果参数默认值是变量，那么参数就不是传值的，而是每次都重新计算默认值表达式的值。也就是说，参数默认值是惰性求值的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> x = <span class="number">99</span>;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params">p = x + <span class="number">1</span></span>) &#123;</span><br><span class="line">	<span class="variable language_">console</span>.<span class="title function_">log</span>(p);</span><br><span class="line">&#125;</span><br><span class="line">  </span><br><span class="line"><span class="title function_">foo</span>() <span class="comment">// 100</span></span><br><span class="line">  </span><br><span class="line">x = <span class="number">100</span>;</span><br><span class="line"><span class="title function_">foo</span>() <span class="comment">// 101</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li><p>与解构赋值默认值结合使用</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params">&#123;x, y = <span class="number">5</span>&#125;</span>) &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(x, y);</span><br><span class="line">    &#125;</span><br><span class="line"><span class="title function_">foo</span>(&#123;<span class="attr">x</span>: <span class="number">1</span>&#125;) 	<span class="comment">// 1, 5</span></span><br></pre></td></tr></table></figure>
<p>  <strong>在某些情况下，传入undefined时才会触发参数默认值</strong></p>
</li>
<li><p>函数的length属性：指函数预期传入参数的个数，返回<strong>没有指定默认值时</strong>的参数的个数（指定了默认值后length属性失真，从指定默认值的那以为开始失真，而之前没有指定默认值的还是会计入length）：<code>(function (a, b = 1, c) &#123;&#125;).length     // 返回1</code></p>
</li>
<li><p>参数默认值和作用域</p>
<p>  一旦设置了参数的默认值，函数进行声明初始化时，参数会形成一个单独的作用域（context）。等到初始化结束，这个作用域就会消失。这种语法行为，在不设置参数默认值时，是不会出现的。</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">f</span>(<span class="params">x, y = x</span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(y);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_">f</span>(<span class="number">2</span>) 	<span class="comment">// 由于参数初始化自己形成单独作用域，所以y=x=2，不考虑函数外的全局变量</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> a = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">f</span>(<span class="params">b = a</span>) &#123;</span><br><span class="line"><span class="keyword">let</span> a = <span class="number">2</span>;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(b);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_">f</span>() <span class="comment">// 在这个作用域里面，变量x本身没有定义，所以指向外层的全局变量x</span></span><br><span class="line"><span class="string">``</span><span class="string">`   </span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">这个参数默认值的作用域有一个非常不错的应用途径：指定某参数不可忽略，否则抛出错误提示：</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">`</span><span class="string">``</span>javascript</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">throwIfMissing</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;Missing parameter&#x27;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">foo</span>(<span class="params">mustBeProvided = throwIfMissing()</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> mustBeProvided;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_">foo</span>()</span><br></pre></td></tr></table></figure>
</li>
</ul>
<ul>
<li><p>rest参数</p>
<p>rest参数使用…扩展运算符将多余参数放入数组中，形式为(a, …b),所以该函数可以传入任意数目的参数，该参数也可以使用所有数组特有的方法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">sortNumbers</span> = (<span class="params">...numbers</span>) =&gt; numbers.<span class="title function_">sort</span>();</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">push</span>(<span class="params">array, ...items</span>) &#123;</span><br><span class="line">	items.<span class="title function_">forEach</span>(<span class="keyword">function</span>(<span class="params">item</span>) &#123;</span><br><span class="line">		array.<span class="title function_">push</span>(item);</span><br><span class="line">		<span class="variable language_">console</span>.<span class="title function_">log</span>(item);</span><br><span class="line">	&#125;);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> a = [];</span><br><span class="line"><span class="title function_">push</span>(a, <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>需要注意的是，rest参数之后不能再有其他的参数，函数的length属性也不适用于rest参数。</p>
<ul>
<li><p>函数内部严格模式</p>
<p>在ES5中，函数内部可以使用严格模式，在ES2016中则规定只要函数参数使用了<strong>默认值</strong>、<strong>解构赋值</strong>、<strong>扩展运算符</strong>，那么函数内部就不能显式设定严格模式（这是因为函数内部严格模式适用于函数体和函数参数，由于函数执行是先执行函数参数继而执行函数体，所以在函数参数执行的时候无法获知是否使用严格模式，导致报错）。</p>
<p>如果非要在有默认值、解构赋值或扩展运算符在函数参数中出现并需要再函数内设定严格模式，那么可以使用：</p>
<ol>
<li>全局性的严格模式</li>
<li>使用无参数的立即执行函数将所需函数包含进去，并在立即执行函数中使用严格模式</li>
</ol>
</li>
<li><p>函数的name属性：返回该函数的函数名</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> f = <span class="keyword">function</span> (<span class="params"></span>) &#123;&#125;;</span><br><span class="line">f.<span class="property">name</span> 	<span class="comment">// &quot;f&quot;</span></span><br><span class="line"><span class="keyword">const</span> bar = <span class="keyword">function</span> <span class="title function_">baz</span>(<span class="params"></span>) &#123;&#125;;</span><br><span class="line">bar.<span class="property">name</span> 	<span class="comment">// &quot;baz&quot;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>上面代码中显示，函数表达式中如果赋值的是匿名函数，则函数名返回该变量的变量名，若赋值的是具名函数，则返回该具名函数的函数名。</p>
<p>需要注意两点：</p>
<ol>
<li>Function构造函数返回的函数实例，name属性的值为anonymous，<code>(new Function).name // &quot;anonymous&quot;</code></li>
<li>bind返回的函数，name属性值会加上bound前缀<code>(function()&#123;&#125;).bind(&#123;&#125;).name // &quot;bound &quot;</code></li>
</ol>
<ul>
<li><p>箭头函数：<code>var f = v =&gt; console.log(v);</code></p>
<p>箭头函数是定义函数的简单方法，相当于匿名函数，‘=&gt;’符号左边是函数的参数（如果不需要参数或者需要多个参数，可以直接使用()代替参数），右边是函数体（多余一条语句的代码块需要用{}括起来，如果返回的是一个对象，则该对象需要使用()括起来）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//将上面sort函数改进为可以按照大小排序的方法</span></span><br><span class="line"><span class="keyword">var</span> result = values.<span class="title function_">sort</span>(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a - b);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>需要注意的是：</p>
<p>1. 箭头函数体内this对象是定义时所在对象，而非使用时所在对象<br>2. 箭头函数不可以当做构造函数，不可以使用new命令<br>3. 不能使用arguments对象，但是可以使用rest参数代替<br>4. 不能使用yield命令，因此箭头函数不能用作Generator函数</p>
<p>箭头函数还可以进行嵌套：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title function_">pipeline</span> = (<span class="params">...funcs</span>) =&gt; <span class="function"><span class="params">val</span> =&gt;</span> funcs.<span class="title function_">reduce</span>(<span class="function">(<span class="params">a, b</span>) =&gt;</span> <span class="title function_">b</span>(a), val);</span><br><span class="line"><span class="keyword">const</span> <span class="title function_">plus1</span> = a =&gt; a + <span class="number">1</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="title function_">mult2</span> = a =&gt; a * <span class="number">2</span>;</span><br><span class="line"><span class="keyword">const</span> addThenMult = <span class="title function_">pipeline</span>(plus1, mult2);</span><br><span class="line"><span class="title function_">addThenMult</span>(<span class="number">5</span>)</span><br><span class="line"><span class="comment">// 12</span></span><br></pre></td></tr></table></figure>
<p>上述代码显示，箭头前的（输出）作为箭头后的输入</p>
<ul>
<li><p>绑定this：该语法是ES7的一个提案，函数绑定运算符是并排的两个冒号::，左边为一个对象，右边为一个函数，功能是将函数绑定在左边对象的上下文环境中（当前this指向左边的对象）；由于绑定之后返回的仍然是对象，所以可以链式写法：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">foo</span>::bar;</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">bar.<span class="title function_">bind</span>(foo);</span><br><span class="line"><span class="attr">foo</span>::<span class="title function_">bar</span>(...<span class="variable language_">arguments</span>);</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line">bar.<span class="title function_">apply</span>(foo, <span class="variable language_">arguments</span>);</span><br></pre></td></tr></table></figure>
</li>
<li><p>尾调用优化：尾调用是指某个函数的最后一步是调用另一个函数<code>function f(x)&#123;return g(x);&#125;</code></p>
</li>
<li><p>尾递归优化：对于尾递归来说，只存在一个调用帧，永远不会发生“栈溢出”的错误，但是必须在严格模式下才可以生效：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//非尾递归：</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Fibonacci</span> (n) &#123;</span><br><span class="line">  <span class="keyword">if</span> ( n &lt;= <span class="number">1</span> ) &#123;<span class="keyword">return</span> <span class="number">1</span>&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Fibonacci</span>(n - <span class="number">1</span>) + <span class="title class_">Fibonacci</span>(n - <span class="number">2</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//尾递归：</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Fibonacci2</span> (n , ac1 = <span class="number">1</span> , ac2 = <span class="number">1</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span>( n &lt;= <span class="number">1</span> ) &#123;<span class="keyword">return</span> ac2&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="title class_">Fibonacci2</span> (n - <span class="number">1</span>, ac2, ac1 + ac2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>ES6</tag>
      </tags>
  </entry>
  <entry>
    <title>Babel的使用方法</title>
    <url>/2017/09/21/2017-09-21-babel-document-study.html</url>
    <content><![CDATA[<p>Babel是一个广泛使用的转码器，可以将ES6代码转为ES5代码，从而在现有环境执行。</p>
<span id="more"></span>
<h5 id="使用命令行转码"><a href="#使用命令行转码" class="headerlink" title="使用命令行转码"></a>使用命令行转码</h5><ul>
<li><p>安装babel-cli：<code>npm install --global babel-cli</code></p>
</li>
<li><p>使用：</p>
<ul>
<li>转码结果输出到标准输出：<code>babel compiled.js</code></li>
<li>转码结果写入一个文件：<code>babel source.js -o compiled.js</code></li>
<li>整个目录转码：<code>babel src -d lib</code></li>
<li>生成source map文件：<code>babel src -d lib -s</code></li>
</ul>
</li>
<li><p>在package.json中使用babel-cli</p>
<ul>
<li>在项目中安装babel-cli：<code>npm install --save-dev babel-cli</code></li>
<li><p>在package.json中进行配置（使用npm run build就可以进行babel转码）：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="string">&quot;devDependencies&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;babel-cli&quot;</span>: <span class="string">&quot;^6.0.0&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;scripts&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;build&quot;</span>: <span class="string">&quot;babel src -d lib&quot;</span></span><br><span class="line">  &#125;,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h5 id="安装和注册"><a href="#安装和注册" class="headerlink" title="安装和注册"></a>安装和注册</h5><ul>
<li><p>安装</p>
<ul>
<li><p>ES2015转码规则<br>  npm install –save-dev babel-preset-es2015</p>
</li>
<li><p>react转码规则<br>  npm install –save-dev babel-preset-react</p>
</li>
<li><p>ES7不同阶段语法提案的转码规则（共有4个阶段），选装一个<br>  npm install –save-dev babel-preset-stage-0<br>  npm install –save-dev babel-preset-stage-1<br>  npm install –save-dev babel-preset-stage-2<br>  npm install –save-dev babel-preset-stage-3</p>
<p>  如果是想使用比较新的语法时，比如ES7中的async/await语法，选择安装stage-3是比较稳妥的。在安装好之后，需要在Babel的配置文件中进行注册，在项目的根目录下新建.babelrc文件并进行配置：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;presets&quot;</span>: [</span><br><span class="line">      <span class="string">&quot;es2015&quot;</span>,</span><br><span class="line">      <span class="string">&quot;react&quot;</span>,</span><br><span class="line">      <span class="string">&quot;stage-2&quot;</span></span><br><span class="line">    ],</span><br><span class="line">    <span class="string">&quot;plugins&quot;</span>: []</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h5 id="webpack配置babel"><a href="#webpack配置babel" class="headerlink" title="webpack配置babel"></a>webpack配置babel</h5><ul>
<li>安装：安装方式仍如上一步npm安装方式</li>
<li><p>配置：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//webpack1.0</span></span><br><span class="line"><span class="attr">module</span>: &#123;</span><br><span class="line">    <span class="attr">loaders</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">test</span>: <span class="regexp">/\.js$/</span>,</span><br><span class="line">            <span class="attr">exclude</span>: <span class="regexp">/(node_modules|bower_components)/</span>,</span><br><span class="line">            <span class="attr">loader</span>: <span class="string">&quot;babel&quot;</span>,</span><br><span class="line">            <span class="attr">query</span>: &#123;</span><br><span class="line">              <span class="attr">presets</span>: [<span class="string">&#x27;es2015&#x27;</span>, <span class="string">&#x27;stage-3&#x27;</span>]</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//webpack3.0，preset如果已经在package.json中指定（`babel:&#123;&quot;presets&quot;: [&quot;lastest&quot;]&#125;`），这里可以不用配置</span></span><br><span class="line"><span class="attr">module</span>: &#123;</span><br><span class="line">	<span class="attr">rules</span>: &#123;</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="attr">test</span>: <span class="regexp">/\.js$/</span>,</span><br><span class="line">			<span class="attr">use</span>: <span class="string">&#x27;babel-loader&#x27;</span>,</span><br><span class="line">			<span class="attr">exclude</span>: <span class="regexp">/(node_modules|bower_components)/</span>,</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h5 id="开发环境使用babel-register"><a href="#开发环境使用babel-register" class="headerlink" title="开发环境使用babel-register"></a>开发环境使用babel-register</h5><p>babel-register的作用是在模块使用<strong>require</strong>命令进行引入时，对该命令引入的模块进行babel的转码（只要是js/jsx/es/es6类型文件）：</p>
<ul>
<li>安装babel-register：<code>npm install --save-dev babel-register</code></li>
<li>加载babel-register：<code>require(&quot;babel-register&quot;);require(&quot;./index.js&quot;);</code>，即可对index.js进行转码（<strong>注意：register不会对当前js文件进行转码</strong>）</li>
</ul>
<h5 id="使用babel-core的API进行转码"><a href="#使用babel-core的API进行转码" class="headerlink" title="使用babel-core的API进行转码"></a>使用babel-core的API进行转码</h5><ul>
<li>安装：<code>npm install babel-core --save</code></li>
<li><p>使用：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> babel = <span class="built_in">require</span>(<span class="string">&#x27;babel-core&#x27;</span>);</span><br><span class="line"><span class="comment">// 字符串转码</span></span><br><span class="line">babel.<span class="title function_">transform</span>(<span class="string">&#x27;code();&#x27;</span>, options);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h5 id="babel-polyfill"><a href="#babel-polyfill" class="headerlink" title="babel-polyfill"></a>babel-polyfill</h5><p>Babel默认只转换新的JavaScript句法（syntax），而不转换新的API，比如Iterator、Generator、Set、Maps、Proxy、Reflect、Symbol、Promise等全局对象，以及一些定义在全局对象上的方法（比如Object.assign）都不会转码。<br>举例来说，ES6在Array对象上新增了Array.from方法。Babel就不会转码这个方法。如果想让这个方法运行，必须使用babel-polyfill，为当前环境提供一个垫片。[^1]</p>
<p>例如之前在学习koa2的过程中，遇到了async/await的情况，就必须安装polyfill才能够正常加载。</p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>Babel</tag>
      </tags>
  </entry>
  <entry>
    <title>形状文法简介</title>
    <url>/2017/10/08/2017-10-08-shape-grammar-learning-notes.html</url>
    <content><![CDATA[<p>形状文法（shape grammar）最早由George Stiny和James Gips在1972年提出，是一种用带符号的形状作为基本要素，用语法结构分析和产生新的形状的设计推理方法，是一种以运算规律为主的设计方法。最早运用于绘画、雕塑和建筑设计领域，后被推广到工业设计领域。我的毕设开题之后，是实验室和阿里一起合作的项目中的一个系统实现，其中大量地使用到了形状文法的方法，所以有必要对该研究进行一些了解和知识点的整理。</p>
<span id="more"></span>
<h4 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h4><p>更加简洁的概念：形状文法是一种计算机辅助设计方法，它可以按照人们的设计思想和要求，依据一定的规则产生新的形状。形状是指有限直线段的组合，并且带有符号标记，以指示形状生成的方向；规则是指对形状进行改变的规定。广义地来讲，形状文法是对已有产品特征的分析、描述和归纳；再现和发展原有产品特征，形成新的风格</p>
<ul>
<li><p>形状文法的定义：</p>
<p>  形状文法定义为四元组，即SG＝(S, L, R, I)。其中，Ｓ为形状的有限集合；Ｌ为符号的有限集合；Ｒ为规则的有限集合，规则的基本形式为α → β，α是一个带符号形状，α∈(S, L)+，β也是一个带符号形状，β∈(S, L)，(S, L)＋是一个形状和符号所组成的集合，（S, L）则是（S, L）+∪（SΦ, L）；Ｉ为初始形状，I∈(S, L)+．一个形状文法定义的形状集合称为语言，这个语言包含所有的由形状文法生成的无符号形状或带符号形状，其中的每一个形状都是通过应用形状规则从初始形状派生出来的，如下图所示：</p>
<p>  <img src="https://img.alicdn.com/imgextra/i3/O1CN01x8tVcl1G7qyHzKkRn_!!6000000000576-0-tps-1566-1204.jpg" alt=""></p>
<p>  基础的形状文法规则有生成性规则和衍生性（修改性）规则，衍生性规则是在生成性规则的基础上进行更多参数和更多步骤的规则变化，能够生成更加复杂的纹样和图案。</p>
</li>
<li><p>形状文法应用流程：</p>
<p>  以纹样的形状文法生成为例来说，最基本的步骤分为三步：</p>
<ol>
<li><p>纹样研究，对传统纹样及相关文化背景进行研究以锁定目标纹样。目标纹样应为传统纹样的代表，能够反映特定的文化特色。再针对目标纹样进行特征分析，包括纹样主题纹样构图规律、骨骼形式、纹样元素组织方式等信息。</p>
</li>
<li><p>形状演变，对第一部分得到的典型图形运用形状文法的规则进行演变，生成基本图形单元。再对基本图形单元进行派生，以得到多种创新纹样元素。</p>
</li>
<li><p>方案生成，设计师对派生出来的多种纹样元素进行筛选，再以纹样设计文法为约束对纹样元素进行合理组织、重构，以生成新的纹样设计方案。</p>
</li>
</ol>
</li>
</ul>
<h4 id="形状文法实施过程"><a href="#形状文法实施过程" class="headerlink" title="形状文法实施过程"></a>形状文法实施过程</h4><ul>
<li><p>问题定义：即对即将要使用形状文法的客体进行研究问题的确定，一般从逻辑推理的方法得出对问题的定义；</p>
</li>
<li><p>初始形状确定：</p>
<ol>
<li><p>收集样本：选定某个领域的客体实例作为研究对象，广泛收集该领域各个发展阶段的图片，将这些图片进行整理、分类，去除类型接近的图片，找出代表性的图片，将其制作成可供调查的样本，并以一定的方式编号（labeling）。</p>
</li>
<li><p>专家访谈：邀集相关的专家进行访谈，一般应包括资深设计师和深度使用者。访谈的主要目的首先在于对影响该纹理特征进行分类，然后对各类特征的重要程度做出主观评价，以确定各个因子的权重．再根据纹理分类和专家的直观经验，列举出该领域实例主要的可识别特征，为进行下一步设计形态分析做准备。</p>
</li>
<li><p>设计形态分析：设计形态分析法是由荷兰学者Ware，具体过程是：</p>
<ul>
<li>将选取的产品样本图片和专家列举的产品显性特征制成调查表，的受试者选择面要广，包括专家、设计师、一般用户等，年龄、性别和职业要分布合理。</li>
<li>由受试者将产品样本一一对照各个形态特征进行判断，以“强”、“弱”、“无”作为标准，并对应以不同分数。</li>
<li>对调查结果进行统计分析，判断并提取产品的风格特征。</li>
</ul>
</li>
</ol>
</li>
<li><p>形状规则的确定：</p>
<ul>
<li><p>生成性规则：生成性规则的形式可分两种：<strong>添加</strong>，即从无到有；<strong>置换</strong>，以一个完全不同的全新形态代替原形态．</p>
</li>
<li><p>修改性规则：“修改”就是指对原有形态的改变、变形、调整．由于生成性规则是对纹理本质的“变异”，因而修改性规则应侧重于“遗传”，否则纹理的显性特征变得面目全非，品牌的延续将无从谈起。例如，可以使用仿射变换（affine transform）进行变形，或者对整体轮廓曲线采用贝塞尔曲线（Bezier curve）进行描述并进行非精确性变形。</p>
</li>
</ul>
</li>
<li><p>设计要求与约束规则：</p>
<p>  在完成以上两项初始形状和文法规则的确立之后，还要对于已确定问题的边界和约束进行规定。</p>
</li>
</ul>
<h4 id="目前形状文法落实进展："><a href="#目前形状文法落实进展：" class="headerlink" title="目前形状文法落实进展："></a>目前形状文法落实进展：</h4><ul>
<li><p>形状生成：Tranglify算法生成随机背景</p>
</li>
<li><p>形状布局：通过布局风格（平铺/随机）、布局区域（全局/环绕）、形状旋转和形状缩放的python代码实现</p>
</li>
<li><p>颜色渲染：图片颜色整个色盘选择替换</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Research</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>形状文法</tag>
      </tags>
  </entry>
  <entry>
    <title>JS语言精粹中的精粹</title>
    <url>/2017/10/10/2017-10-10-The-Good-Part-of-JavaScripts-The-Good-Part.html</url>
    <content><![CDATA[<p>《JavaScript语言精粹》这本书可谓是JavaScript这门语言的几乎所有知识点的总结，相当于考试之前要画的重点。尽管书中对于各个知识点的介绍比较少，但是知识点覆盖很全，可以进行反复阅读和积累。下面就对这本书中的知识点进行一个整理和总结。</p>
<span id="more"></span>
<h4 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h4><p>这一部分主要总结JavaScript中的语言结构和相关语法知识点。</p>
<ul>
<li>空白：空格一般是可以省略，但是对于<code>var that = this;</code>这种语句var和that之间的空格是必须保留的，而且为了代码美观，也要适当使用空格。</li>
<li>注释：使用<code>/* */</code>这种风格的注释在遇到正则表达式中出现*/时很容易出错，所以<strong>建议使用<code>//</code>的注释方式</strong>。</li>
<li>标识符：只能使用字母、$、_开头，其中也只能使用字母、数字或下划线。<strong>避免使用保留字作为标识符</strong>。</li>
<li>数字：JS中只有一个数字类型，内部表示为64位浮点数，一定程度可以避免溢出问题；<strong>NaN是一个数值，不等于任何值（包括自己）</strong>，有isNaN(number)方法；Infinity表示无限大；数字拥有方法，也可以使用Math对象的方法对数字进行操作。</li>
<li>字符串：由单引号/双引号包括，\开头是转义字符。在ES5中，JS中所有字符都是<strong>16位</strong>Unicode；字符串有.length属性，不可变，但是可以通过一些方法对其进行操作。</li>
<li><p>语句：switch、while、for、do可有前置标签label，配合break语句使用</p>
<ul>
<li>if语句中可以作为假值的条件有：false、null、undefined、’’、0、NaN，其他都作为真值条件；</li>
<li>for语句中可以使用for in语句对对象或者类对象的属性/键名进行枚举，但是要确保该属性是否来自该对象还是原型链，使用<code>if(object.hasOwnProperty(variable))</code>进行确定；</li>
</ul>
</li>
<li><p>表达式：注意运算符的优先级；typeof运算符可产生六个值（字符串）number、string、boolean、undefined、function和object；</p>
</li>
<li>字面量：</li>
<li>函数：</li>
</ul>
<h4 id="对象"><a href="#对象" class="headerlink" title="对象"></a>对象</h4><p>JavaScript中的简单数据类型有Number、String、Bool、Null、undefined，这些类型都不可变（但是数字、字符串和布尔值有一些方法），其他的数据类型都属于对象Object。</p>
<ul>
<li><p>对象字面量：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> stooge = &#123;</span><br><span class="line">  <span class="string">&quot;first-name&quot;</span>: <span class="string">&quot;Jerome&quot;</span>, <span class="comment">//不合法的属性名要用&quot;&quot;包括</span></span><br><span class="line">  <span class="attr">last_name</span>: <span class="string">&quot;Howard&quot;</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>检索：对象的属性检索方式有两种，一种是</p>
</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>从输入URL到页面加载发生了什么之进阶篇</title>
    <url>/2018/03/22/2018-03-22-what-happend-during-opening-url.html</url>
    <content><![CDATA[<p>文章来源：<a href="https://segmentfault.com/a/1190000006879700">https://segmentfault.com/a/1190000006879700</a><br>文章优点：采用总分总的形式进行撰写，结构和章节明确，发散较广。<br>本文从打开网站的完整流程进行分析：DNS解析，TCP连接，发送HTTP请求，服务器处理请求并返回HTTP报文，浏览器解析渲染页面，连接结束。</p>
<span id="more"></span>
<h4 id="文章首先指出整个过程分为六大步骤"><a href="#文章首先指出整个过程分为六大步骤" class="headerlink" title="文章首先指出整个过程分为六大步骤"></a>文章首先指出整个过程分为六大步骤</h4><ul>
<li>DNS解析</li>
<li>TCP连接</li>
<li>发送HTTP请求</li>
<li>服务器处理请求并返回HTTP报文</li>
<li>浏览器解析渲染页面</li>
<li>连接结束</li>
</ul>
<h4 id="DNS解析"><a href="#DNS解析" class="headerlink" title="DNS解析"></a>DNS解析</h4><p>将网站的【姓名】翻译成【身份证号】，是一个翻译的过程。<br>这个过程是将网址从右到左进行解析和查询的过程。.=&gt;com=&gt;google.com<br>为了查询效率，DNS解析简历起多级缓存（浏览器缓存，系统缓存，路由器缓存，IPS服务器缓存，根域名服务器缓存，顶级域名服务器缓存，主域名服务器缓存）<br>为了均衡地发送和处理请求，DNS进行负载均衡（DNS重定向），CDN也是利用了该技术，根据每台机器负载量、地理位置距离等返回给用户最优的一个IP地址<br>没有使用CDN时请求服务器资源：localDNS-rootDNS-逐级查询到域名授权的DNS记录-使用查询到的ip去请求内容<br>使用了CDN之后：localDNS-rootDNS-逐级查询到域名授权的DNS记录-向DNS服务器请求最优ip地址-使用该ip去CDN节点服务器返回对应的资源</p>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN01CyEdCz1VBG9VdQ4xB_!!6000000002614-0-tps-800-518.jpg" alt="DNS解析流程图"><br><img src="https://img.alicdn.com/imgextra/i1/O1CN01IDCe7X1qbSk8Ue29Z_!!6000000005514-0-tps-2584-1460.jpg" alt="DNS解析流程图"></p>
<h4 id="TCP连接"><a href="#TCP连接" class="headerlink" title="TCP连接"></a>TCP连接</h4><p>网站请求大部分使用http请求，http协议是使用tcp作为其传输层协议，（应用http、传输tcp、网络ip、数据、物理），http由tcp报文中提取。<br>由于http报文为明文存在一定风险，所以在http报文包裹入tcp报文之前使用ssl对其进行加密。<br>https在原有tcp握手的基础上需要提前进行一个解密握手。https会有时间损耗，需要在安全和性能两者做出权衡。（公钥加密报文，私钥加密公钥）</p>
<h4 id="发送HTTP请求"><a href="#发送HTTP请求" class="headerlink" title="发送HTTP请求"></a>发送HTTP请求</h4><p>请求报文：请求行（请求方法、内容、协议），请求报头（Accept、Content-Type、Cookie、User-Agent）、请求正文（POST/PUT时会有）<br>响应报文：状态码、响应报头、相应报文<br>状态码：<br>1xx：指示信息–表示请求已接收，继续处理。<br>2xx：成功–表示请求已被成功接收、理解、接受。<br>3xx：重定向–要完成请求必须进行更进一步的操作。4xx：客户端错误–请求有语法错误或请求无法实现。5xx：服务器端错误–服务器未能实现合法的请求。</p>
<h4 id="浏览器解析渲染页面"><a href="#浏览器解析渲染页面" class="headerlink" title="浏览器解析渲染页面"></a>浏览器解析渲染页面</h4><p>文档来源：<a href="https://www.html5rocks.com/en/tutorials/internals/howbrowserswork">https://www.html5rocks.com/en/tutorials/internals/howbrowserswork</a><br>主要文章：<a href="https://www.html5rocks.com/en/tutorials/internals/howbrowserswork/#The_rendering_engine">https://www.html5rocks.com/en/tutorials/internals/howbrowserswork/#The_rendering_engine</a><br>总流程：<br><img src="https://img.alicdn.com/imgextra/i3/O1CN01Qnfdal1fBOQSgeKkv_!!6000000003968-0-tps-600-66.jpg" alt="浏览器解析渲染页面总流程"></p>
<p>没有脚本的流程：<br><img src="https://img.alicdn.com/imgextra/i1/O1CN016XZiQ31QdaZTG2t5w_!!6000000001999-0-tps-624-289.jpg" alt="没有脚本的流程"></p>
<p>HTML Parsing: 输出DOM tree<br>HTML不是一种context free grammar，是因为它不同于XML，它允许一些类似标签缺失等的容错<br>分为两部分：tokenize和tree construction<br>Tokenize：状态机实现，初始状态为DATA，遇到&lt;状态改为TAGOPEN，之后遇到第一个a-z的字符状态改为TAGNAME，之后遇到&gt;状态改为DATA，直到&lt;/&gt;，tokenize就commit一个token，<br>Tree Construction：状态机实现。Document Object在token的parser创建时同时创建，每当一个token被commited，constroctor会判断该token跟哪个dom元素相关，新建该token的元素，并添加到开放元素栈（这个栈用于检查标签闭合以及未正常闭合标签）中：<br><img src="https://img.alicdn.com/imgextra/i2/O1CN01V0qNep1cVb3Fykjrr_!!6000000003606-0-tps-532-769.jpg" alt="DOM tree 生成"></p>
<p>css和js的parse过程：<br>Firefox blocks all scripts when there is a style sheet that is still being loaded and parsed.<br>WebKit blocks scripts only when they try to access certain style properties that may be affected by unloaded style sheets.<br>文章的优点：<br>从dom tree的构建的词法分析层面指出一些标签错误以及兼容方式，从语法分析层面指出了类似<table><table></table></table>这种情况应当怎样处理。<br>render tree构建过程中，构建方法对head标签以及display:none的标签不予插入树结构中，而hidden则会存在在结构当中。</p>
<p>Web优化<br>雅虎35条军规：<a href="https://juejin.im/post/5b73ef38f265da281e048e51">https://juejin.im/post/5b73ef38f265da281e048e51</a><br><img src="https://img.alicdn.com/imgextra/i4/O1CN01BU8Wr61Ywq9E8e29w_!!6000000003124-0-tps-1668-1230.jpg" alt="35条军规"></p>
]]></content>
      <categories>
        <category>Reading</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>HTTP</tag>
      </tags>
  </entry>
  <entry>
    <title>源码阅读 - Vue的数据响应式原理</title>
    <url>/2019/12/01/2019-12-01-Vue-source-code-reading-data-binding.html</url>
    <content><![CDATA[<p>没有像 Vue 和 React 这些具有数据响应式特性的前端框架之前，我们从服务端提供过的接口获取到数据要渲染在 html 页面上时，抑或是需要获取表单的值进行计算回显到页面上时，都需要建立很多 DOM 事件监听器，并进行许多的 DOM 操作。举个最简单的例子：用户在 html 页面的表单中输入两个加数 a 和 b ，计算得到两个加数之和，并展示到页面上。这时我们需要监听表单中 a 和 b 两个输入框的变化，拿到变化值相加然后通过修改指定 id 或者 class 的选择器对应的 DOM 来修改两数之和。但是当输入表单不止两个时，就会令人捉急。</p>
<p>而 Vue 的数据响应式大大地优化了这一整套的流程。面对大量的 UI 交互和数据更新，数据响应式让我们做到从容不迫——我们需要去了解数据的改变是怎样被触发，更新触发后的数据改变又是怎样反馈到视图。接下来我们通过对部分 Vue 源码的简单分析和学习来深入了解 Vue 中的数据响应式是怎样实现的。</p>
<span id="more"></span>
<h3 id="什么是数据响应式"><a href="#什么是数据响应式" class="headerlink" title="什么是数据响应式"></a>什么是数据响应式</h3><blockquote>
<p>Vue.js 一个核心思想是数据驱动。所谓数据驱动，是指视图是由数据驱动生成的，我们对视图的修改，不会直接操作 DOM，而是通过修改数据。——<a href="https://ustbhuangyi.github.io/vue-analysis/v2/data-driven/">《Vue.js技术揭秘》</a></p>
</blockquote>
<p>没有像 Vue 和 React 这些具有数据响应式特性的前端框架之前，我们从服务端提供过的接口获取到数据要渲染在 html 页面上时，抑或是需要获取表单的值进行计算回显到页面上时，都需要建立很多 DOM 事件监听器，并进行许多的 DOM 操作。举个最简单的例子：用户在 html 页面的表单中输入两个加数 a 和 b ，计算得到两个加数之和，并展示到页面上。这时我们需要监听表单中 a 和 b 两个输入框的变化，拿到变化值相加然后通过修改指定 id 或者 class 的选择器对应的 DOM 来修改两数之和。但是当输入表单不止两个时，就会令人捉急。</p>
<p>而 Vue 的数据响应式大大地优化了这一整套的流程。面对大量的 UI 交互和数据更新，数据响应式让我们做到从容不迫——我们需要去了解数据的改变是怎样被触发，更新触发后的数据改变又是怎样反馈到视图。接下来我们通过对部分 Vue 源码的简单分析和学习来深入了解 Vue 中的数据响应式是怎样实现的。</p>
<p>我们将这一部分的代码分析大致分为三部分：让数据变成响应式、依赖收集和派发更新。</p>
<h3 id="数据响应式的中心思想"><a href="#数据响应式的中心思想" class="headerlink" title="数据响应式的中心思想"></a>数据响应式的中心思想</h3><p><img src="https://img.alicdn.com/imgextra/i3/O1CN01cFZ8g61hW7aEmNpAt_!!6000000004284-0-tps-1080-483.jpg" alt="5de7af21d4c2de951720c006f84b98fc.png"></p>
<p>该原理图源自 Vue 官方教程的<a href="https://cn.vuejs.org/v2/guide/reactivity.html">深入响应式原理</a>，这张图向我们展示了 Vue 的数据响应式的中心思想。</p>
<p>我们先来介绍图中的四个模块，<strong>黄色部分</strong>是 Vue 的渲染方法，视图初始化和视图更新时都会调用 <code>vm._render</code> 方法进行重新渲染。渲染时不可避免地会 touch 到每个需要展示到视图上的数据（<strong>紫色部分</strong>），触发这些数据的 get 方法从而收集到本次渲染的所有依赖。收集依赖和更新派发都是基于<strong>蓝色部分</strong>的 Watcher 观察者。而当我们在修改这些收集到依赖的数据时，会触发数据中的 set 属性方法，该方法会修改数据的值并 notify 到依赖到它的观察者，从而触发视图的重新渲染。<strong>绿色部分</strong>是渲染过程中生成的 Virtual DOM Tree，这棵树不仅关系到视图渲染，更是 Vue 优化视图更新过程的基础。</p>
<h3 id="让数据变成响应式"><a href="#让数据变成响应式" class="headerlink" title="让数据变成响应式"></a>让数据变成响应式</h3><p>基本上大家都了解 Vue 的数据响应式原理是由 JS标准内置对象方法 <strong>Object.defineProperty</strong> 来实现的，而这个方法是<strong>不兼容IE8和FF22及以下版本</strong>的浏览器的，所以 Vue 也只能在这些版本之上的浏览器中才能正常使用。这个方法的作用是直接在一个对象上定义一个新属性，或者修改一个对象的现有属性， 并返回这个对象。那么数据响应式用这个方法为<strong>什么对象</strong>添加或修改了<strong>什么属性</strong>呢？我们从 Vue 的初始化讲起。</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN01o7s5Vq1LmDxgNOdI0_!!6000000001341-0-tps-1080-328.jpg" alt="99d9a00af95016a8e9b0b558fd4249ab.png"></p>
<h4 id="Vue-的初始化"><a href="#Vue-的初始化" class="headerlink" title="Vue 的初始化"></a>Vue 的初始化</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Vue</span> (options) &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">_init</span>(options)</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_">initMixin</span>(<span class="title class_">Vue</span>)     </span><br><span class="line"><span class="title function_">stateMixin</span>(<span class="title class_">Vue</span>)     </span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title class_">Vue</span></span><br></pre></td></tr></table></figure>
<p>在 <a href="https://github.com/DQFE/vue/blob/dev/src/core/index.js#L1"><em>src/core/index.js</em></a> 中我们可以看到一个 Vue 被导出，这个Vue是在 <em>src/core/instance/index</em> 中定义的，Vue 是一个方法，参数是options，我们大胆猜想这个 Vue 就是进行 vue 实例化的方法，而 options 就是我们传入的 data、computed、methods 等属性。这个 Vue 方法中主要调用了 _init 方法，这个方法是在 initMixin 方法调用时定义在 Vue 原型上的一个方法，_init 方法中对当前传入的 options 进行了一些处理（主要是判断当前实例化的是否为组件，使用 mergeOptions 方法对 options 进行加工，此处不做赘述），然后又调用了一系列方法进行了生命周期、事件、渲染器的初始化，我们主要来关注 <strong>initState</strong> 这个方法（<a href="https://github.com/DQFE/vue/blob/dev/src/core/instance/state.js#L48"><em>src/core/instance/state.js</em></a>）:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">initState</span> (<span class="attr">vm</span>: <span class="title class_">Component</span>) &#123;</span><br><span class="line">    vm.<span class="property">_watchers</span> = []</span><br><span class="line">    <span class="keyword">const</span> opts = vm.<span class="property">$options</span></span><br><span class="line">    <span class="keyword">if</span> (opts.<span class="property">props</span>) <span class="title function_">initProps</span>(vm, opts.<span class="property">props</span>)</span><br><span class="line">    <span class="keyword">if</span> (opts.<span class="property">methods</span>) <span class="title function_">initMethods</span>(vm, opts.<span class="property">methods</span>)</span><br><span class="line">    <span class="comment">// a*</span></span><br><span class="line">    <span class="keyword">if</span> (opts.<span class="property">data</span>) &#123;</span><br><span class="line">        <span class="title function_">initData</span>(vm)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="title function_">observe</span>(vm.<span class="property">_data</span> = &#123;&#125;, <span class="literal">true</span> <span class="comment">/* asRootData */</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (opts.<span class="property">computed</span>) <span class="title function_">initComputed</span>(vm, opts.<span class="property">computed</span>)</span><br><span class="line">    <span class="keyword">if</span> (opts.<span class="property">watch</span> &amp;&amp; opts.<span class="property">watch</span> !== nativeWatch) &#123;</span><br><span class="line">        <span class="title function_">initWatch</span>(vm, opts.<span class="property">watch</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h4 id="将-Vue-实例上的-data-响应式化"><a href="#将-Vue-实例上的-data-响应式化" class="headerlink" title="将 Vue 实例上的 data 响应式化"></a>将 Vue 实例上的 data 响应式化</h4><p>上面的方法中对props、methods、data、computed和watch进行了初始化，这些都是Vue实例化方法中传入参数options对象的一些属性，这些属性都需要被<strong>响应式化</strong>。而针对于data的初始化分了两种情况[a*]，一种是options中没有data属性的，该方法会给data赋值一个空对象并进行observe（该方法之后我们会详细讲述），如果有data属性，则调用<a href="https://github.com/DQFE/vue/blob/dev/src/core/instance/state.js#L112"><strong>initData</strong></a>方法进行初始化。我们主要通过对data属性的初始化来分析Vue中的数据响应式原理。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">initData</span> (<span class="attr">vm</span>: <span class="title class_">Component</span>) &#123;</span><br><span class="line">    <span class="keyword">let</span> data = vm.<span class="property">$options</span>.<span class="property">data</span></span><br><span class="line">    <span class="comment">// a*</span></span><br><span class="line">    data = vm.<span class="property">_data</span> = <span class="keyword">typeof</span> data === <span class="string">&#x27;function&#x27;</span></span><br><span class="line">    ? <span class="title function_">getData</span>(data, vm)</span><br><span class="line">    : data || &#123;&#125;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="title function_">isPlainObject</span>(data)) &#123;</span><br><span class="line">        data = &#123;&#125;</span><br><span class="line">        process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span> &amp;&amp; <span class="title function_">warn</span>(</span><br><span class="line">          <span class="string">&#x27;data functions should return an object:\n&#x27;</span> +</span><br><span class="line">          <span class="string">&#x27;https://vuejs.org/v2/guide/components.html#data-Must-Be-a-Function&#x27;</span>,</span><br><span class="line">          vm</span><br><span class="line">        )</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// b*</span></span><br><span class="line">    <span class="keyword">const</span> keys = <span class="title class_">Object</span>.<span class="title function_">keys</span>(data)</span><br><span class="line">    <span class="keyword">const</span> props = vm.<span class="property">$options</span>.<span class="property">props</span></span><br><span class="line">    <span class="keyword">const</span> methods = vm.<span class="property">$options</span>.<span class="property">methods</span></span><br><span class="line">    <span class="keyword">let</span> i = keys.<span class="property">length</span></span><br><span class="line">    <span class="keyword">while</span> (i--) &#123;</span><br><span class="line">        <span class="comment">// c*</span></span><br><span class="line">        <span class="keyword">const</span> key = keys[i]</span><br><span class="line">        <span class="keyword">if</span> (process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span>) &#123;</span><br><span class="line">          <span class="keyword">if</span> (methods &amp;&amp; <span class="title function_">hasOwn</span>(methods, key)) &#123;</span><br><span class="line">            <span class="title function_">warn</span>(</span><br><span class="line">              <span class="string">`Method &quot;<span class="subst">$&#123;key&#125;</span>&quot; has already been defined as a data property.`</span>,</span><br><span class="line">              vm</span><br><span class="line">            )</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (props &amp;&amp; <span class="title function_">hasOwn</span>(props, key)) &#123;</span><br><span class="line">          process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span> &amp;&amp; <span class="title function_">warn</span>(</span><br><span class="line">            <span class="string">`The data property &quot;<span class="subst">$&#123;key&#125;</span>&quot; is already declared as a prop. `</span> +</span><br><span class="line">            <span class="string">`Use prop default value instead.`</span>,</span><br><span class="line">            vm</span><br><span class="line">          )</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!<span class="title function_">isReserved</span>(key)) &#123;</span><br><span class="line">          <span class="title function_">proxy</span>(vm, <span class="string">`_data`</span>, key)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// d*</span></span><br><span class="line">    <span class="title function_">observe</span>(data, <span class="literal">true</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>initData 方法对 options 中的 data 进行处理，主要是有<strong>两个目的</strong>:</p>
<ul>
<li>一方面<strong>将 data 代理到 Vue 实例上</strong>，同时检查 data 中是否使用了 Vue 中的保留字、是否与 props、methods 属性中的数据同名</li>
<li>使用 <strong>observe</strong> 方法将data中的属性变成响应式的</li>
</ul>
<p>之前我们提到在调用 initState 方法之前会有对 Vue 实例化传入的 options 参数进行处理的环节（mergeOptions），这个过程会将 data 处理成一个函数（例如我们在 vue 组件中的 data 属性），之后会调用 <code>callHook(vm, &#39;beforeCreate&#39;)</code> 方法来触发 beforeCreate 的生命周期钩子，这个方法的调用可能会对 data 属性进行进一步处理，所以方法一开始会<strong>对 data 统一做处理使其成为一个function</strong>[a*]。</p>
<p>接下来会对 data 中的每一个数据进行遍历[b*]，遍历过程将会使用  <code>hasOwn(methods, key)</code>、<code>hasOwn(props, key)</code>、<code>!isReserved(key)</code> 方法对该数据是否占用保留字、是否与 props 和 methods 中的属性重名进行判断，然后调用 proxy 方法将其代理到 Vue 实例上。此处需要注意的是：<strong>如果 data 中的属性与 props、methods 中的属性重名，那么在 Vue 实例上调用这个属性时的优先级顺序是 props &gt; methods &gt; data</strong>。</p>
<p>最后对每一个 data 中的属性调用 <code>observe</code> 方法，该方法的功能是赋予 data 中的属性可被监测的特性。这个方法和其中使用到的 Observe 类是在<a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/index.js#L37"><em>src/core/observer/index.js</em></a>文件中，observe 方法主要是调用<strong>Observer类构造方法</strong>，将每一个 data中的 value 变成可观察、响应式的：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">constructor</span> (<span class="attr">value</span>: any) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">value</span> = value</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">dep</span> = <span class="keyword">new</span> <span class="title class_">Dep</span>()</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">vmCount</span> = <span class="number">0</span></span><br><span class="line">    </span><br><span class="line">    <span class="title function_">def</span>(value, <span class="string">&#x27;__ob__&#x27;</span>, <span class="variable language_">this</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="title class_">Array</span>.<span class="title function_">isArray</span>(value)) &#123;</span><br><span class="line">      <span class="comment">//...</span></span><br><span class="line">      <span class="variable language_">this</span>.<span class="title function_">observeArray</span>(value)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="title function_">walk</span>(value)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><br>可以看到该构造函数有以下几个目的：</p>
<ul>
<li>针对当前的数据对象新建一个订阅器；</li>
<li>为每个数据的value都添加一个<strong>ob</strong>属性，该属性不可枚举并指向自身；</li>
<li>针对数组类型的数据进行单独处理（包括赋予其数组的属性和方法，以及 observeArray 进行的数组类型数据的响应式）；</li>
<li>this.walk(value)，遍历对象的 key 调用 defineReactive 方法；</li>
</ul>
<p><code>defineReactive</code>是真正为数据添加 get 和 set 属性方法的方法，它将 data 中的数据定义一个响应式对象，并给该对象设置 get 和 set 属性方法，其中 get 方法是对依赖进行收集， set 方法是当数据改变时通知 Watcher 派发更新。</p>
<h3 id="依赖收集"><a href="#依赖收集" class="headerlink" title="依赖收集"></a>依赖收集</h3><p>依赖收集的原理是：当视图被渲染时，会触发渲染中所使用到的数据的 get 属性方法，通过 get 方法进行依赖收集。</p>
<p>其真实的逻辑我们举例来说明：当前正在渲染组件 ComponentA，会将当前全局唯一的监听器置为这个 Watcher，这个组件中使用到了数据 <code>data () &#123; return &#123; a: b + 1&#125; &#125;</code>，此时触发b的 getter 会将当前的 watcher 添加到b的订阅者列表 subs 中。也就是说如果 ComponentA 依赖 b，则将该组件的渲染 Watcher 作为订阅者加入b的订阅者列表中。</p>
<p><img src="https://img.alicdn.com/imgextra/i3/O1CN01zHQHVX1ya6UYKS6gb_!!6000000006594-0-tps-1080-427.jpg" alt="1e9dfeb2187b782e42b78196a723f860.jpeg"></p>
<p>我们先看下 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/index.js#L160">get</a> 属性方法：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">get</span>: <span class="keyword">function</span> <span class="title function_">reactiveGetter</span> () &#123;</span><br><span class="line">  <span class="keyword">const</span> value = getter ? getter.<span class="title function_">call</span>(obj) : val</span><br><span class="line">  <span class="keyword">if</span> (<span class="title class_">Dep</span>.<span class="property">target</span>) &#123;</span><br><span class="line">    dep.<span class="title function_">depend</span>()</span><br><span class="line">    <span class="keyword">if</span> (childOb) &#123;</span><br><span class="line">      childOb.<span class="property">dep</span>.<span class="title function_">depend</span>()</span><br><span class="line">      <span class="keyword">if</span> (<span class="title class_">Array</span>.<span class="title function_">isArray</span>(value)) &#123;</span><br><span class="line">        <span class="title function_">dependArray</span>(value)</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> value</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h4 id="Dep-订阅器"><a href="#Dep-订阅器" class="headerlink" title="Dep - 订阅器"></a>Dep - 订阅器</h4><p>数据对象中的 get 方法主要使用 depend 方法进行依赖收集，该方法是 Dep 类中的属性方法，继续来看 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/dep.js#L13">Dep 类</a>是怎样实现的：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="keyword">class</span> <span class="title class_">Dep</span> &#123;</span><br><span class="line">    <span class="comment">// a*</span></span><br><span class="line">    <span class="keyword">static</span> <span class="attr">target</span>: ?<span class="title class_">Watcher</span>;</span><br><span class="line">    <span class="attr">id</span>: number;</span><br><span class="line">    <span class="attr">subs</span>: <span class="title class_">Array</span>&lt;<span class="title class_">Watcher</span>&gt;;</span><br><span class="line">    <span class="title function_">constructor</span> () &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">id</span> = uid++</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">subs</span> = []</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    addSub (<span class="attr">sub</span>: <span class="title class_">Watcher</span>) &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">subs</span>.<span class="title function_">push</span>(sub)</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    depend () &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="title class_">Dep</span>.<span class="property">target</span>) &#123;</span><br><span class="line">            <span class="title class_">Dep</span>.<span class="property">target</span>.<span class="title function_">addDep</span>(<span class="variable language_">this</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title class_">Dep</span>.<span class="property">target</span> = <span class="literal">null</span></span><br><span class="line"><span class="keyword">const</span> targetStack = []</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">pushTarget</span> (<span class="attr">target</span>: ?<span class="title class_">Watcher</span>) &#123;</span><br><span class="line">    targetStack.<span class="title function_">push</span>(target)</span><br><span class="line">    <span class="title class_">Dep</span>.<span class="property">target</span> = target</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">popTarget</span> () &#123;</span><br><span class="line">    targetStack.<span class="title function_">pop</span>()</span><br><span class="line">    <span class="title class_">Dep</span>.<span class="property">target</span> = targetStack[targetStack.<span class="property">length</span> - <span class="number">1</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>Dep 类中有三个属性：<code>target</code>、<code>id</code> 和 <code>subs</code>，分别表示当前全局唯一的静态数据依赖的监听器 Watcher、该属性的 id 以及订阅这个属性数据的订阅者列表[a*]。另外还提供了将订阅者添加到订阅者列表的 <code>addSub方法</code>、从订阅者列表删除订阅者的 <code>removeSub</code> 方法。</p>
<p><code>Dep.target</code> 是当前全局唯一的订阅者，这是因为同一时间只允许一个订阅者被处理。target 的意思就是指当前正在处理的目标订阅者，而这个订阅者是有一个订阅者栈 <code>targetStack</code>，当某一个组件执行到某个生命周期的 hook 时（例如 mountComponent），会将当前目标订阅者 target 置为这个 watcher，并将其压入 <code>targetStack</code> 栈顶。</p>
<p>接下来是添加当前数据依赖的 <code>depend方法</code>，<code>Dep.target</code> 对象是一个 Watcher 类的实例，调用 Watcher 类的 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/watcher.js#L128">addDep 方法</a>，我们看下 addDep 方法将当前的依赖 Dep 实例放在了哪里：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">addDep (<span class="attr">dep</span>: <span class="title class_">Dep</span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> id = dep.<span class="property">id</span></span><br><span class="line">    <span class="keyword">if</span> (!<span class="variable language_">this</span>.<span class="property">newDepIds</span>.<span class="title function_">has</span>(id)) &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">newDepIds</span>.<span class="title function_">add</span>(id)</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">newDeps</span>.<span class="title function_">push</span>(dep)</span><br><span class="line">        <span class="keyword">if</span> (!<span class="variable language_">this</span>.<span class="property">depIds</span>.<span class="title function_">has</span>(id)) &#123;</span><br><span class="line">            dep.<span class="title function_">addSub</span>(<span class="variable language_">this</span>)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>我们稍后会对 Watcher 类进行分析，目前先来简单看下 addDep 这个属性方法做了什么。可以看到入参是一个 Dep 类实例，这个实例实际上是当前全局唯一的订阅者，newDepIds 是当前数据依赖 dep 的 id 列表，newDeps 是当前数据依赖 dep 列表，depsId 则是上一个 tick 的数据依赖的 id 列表。这个方法主要的逻辑就是调用当前数据依赖 dep 的类方法 <code>addSub</code>，而这个方法在上面Dep 类方法中可以看到，就是将当前全局唯一的 watcher 实例放入这个数据依赖的订阅者列表中。</p>
<h4 id="target-Watcher的产生"><a href="#target-Watcher的产生" class="headerlink" title="target Watcher的产生"></a>target Watcher的产生</h4><p>我们以生命周期中的 <a href="https://github.com/DQFE/vue/blob/dev/src/core/instance/lifecycle.js#L141">mountComponent</a> 这一个阶段为例来分析上面提到的Dep.target 这个全局唯一的当前订阅者 Watcher 是怎样产生的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">mountComponent</span> (</span><br><span class="line">    <span class="attr">vm</span>: <span class="title class_">Component</span>, </span><br><span class="line">    <span class="attr">el</span>: ?<span class="title class_">Element</span>, hydrating?: boolean</span><br><span class="line">): <span class="title class_">Component</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">let</span> updateComponent</span><br><span class="line">    <span class="keyword">if</span> (process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span> &amp;&amp; config.<span class="property">performance</span> &amp;&amp; mark) &#123; </span><br><span class="line">        updateComponent = <span class="function">() =&gt;</span> &#123; <span class="comment">// 首次渲染视图会进入，生成虚拟Node，更新DOM&#125;</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        updateComponent = <span class="function">() =&gt;</span> &#123; <span class="comment">// 数据更新时会进入，调用渲染函数对视图进行更新</span></span><br><span class="line">        vm.<span class="title function_">_update</span>(vm.<span class="title function_">_render</span>(), hydrating)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">new</span> <span class="title class_">Watcher</span>(vm, updateComponent, noop, &#123;</span><br><span class="line">        before () &#123;</span><br><span class="line">            <span class="keyword">if</span> (vm.<span class="property">_isMounted</span> &amp;&amp; !vm.<span class="property">_isDestroyed</span>) &#123;</span><br><span class="line">                <span class="title function_">callHook</span>(vm, <span class="string">&#x27;beforeUpdate&#x27;</span>)</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;, <span class="literal">true</span> <span class="comment">/* isRenderWatcher */</span>)</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> vm</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过mountComponent的代码可以看到在触发了<code>beforeMount</code>生命周期钩子后，紧接着创建了一个用于渲染Watcher实例。我们通过Watcher类的构造函数来看创建<a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/watcher.js#L45">Watcher</a>实例时做了哪些工作：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title function_">constructor</span> (</span><br><span class="line">    <span class="attr">vm</span>: <span class="title class_">Component</span>,              <span class="comment">// 当前渲染的组件</span></span><br><span class="line">    <span class="attr">expOrFn</span>: string | <span class="title class_">Function</span>,  <span class="comment">// 当前Watcher实例的getter属性方法</span></span><br><span class="line">    <span class="attr">cb</span>: <span class="title class_">Function</span>,               <span class="comment">// 回调函数</span></span><br><span class="line">    options?: ?<span class="title class_">Object</span>,          <span class="comment">// 手动传入watch时的option</span></span><br><span class="line">    isRenderWatcher?: boolean    <span class="comment">// 是否为渲染watcher的标识</span></span><br><span class="line">  ) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">vm</span> = vm</span><br><span class="line">    <span class="keyword">if</span> (isRenderWatcher) &#123;</span><br><span class="line">        vm.<span class="property">_watcher</span> = <span class="variable language_">this</span></span><br><span class="line">    &#125;</span><br><span class="line">    vm.<span class="property">_watchers</span>.<span class="title function_">push</span>(<span class="variable language_">this</span>)</span><br><span class="line">    <span class="comment">// 此处处理我们传入的watcher参数（在组件内手动新建watch属性）</span></span><br><span class="line">    <span class="keyword">if</span> (options) &#123;&#125; <span class="keyword">else</span> &#123;&#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="comment">// parse expression for getter</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> expOrFn === <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">getter</span> = expOrFn</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">getter</span> = <span class="title function_">parsePath</span>(expOrFn)</span><br><span class="line">        <span class="keyword">if</span> (!<span class="variable language_">this</span>.<span class="property">getter</span>) &#123;</span><br><span class="line">            <span class="variable language_">this</span>.<span class="property">getter</span> = noop</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">value</span> = <span class="variable language_">this</span>.<span class="property">lazy</span> ? <span class="literal">undefined</span> : <span class="variable language_">this</span>.<span class="title function_">get</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>通过 Watcher 类的构造函数传参可以看到 mountComponent 生命周期中创建的 Watcher 是一个渲染 Watcher，将当前的 getter 设置为了 updateComponent 方法（也就是重新渲染视图的方法），最后调用了 get 属性方法，我们接下来看 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/watcher.js#L101">get 方法</a>做了什么：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">get () &#123;</span><br><span class="line">    <span class="title function_">pushTarget</span>(<span class="variable language_">this</span>)</span><br><span class="line">    <span class="keyword">let</span> value</span><br><span class="line">    <span class="keyword">const</span> vm = <span class="variable language_">this</span>.<span class="property">vm</span></span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        value = <span class="variable language_">this</span>.<span class="property">getter</span>.<span class="title function_">call</span>(vm, vm)</span><br><span class="line">    &#125; <span class="keyword">catch</span> (e) &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">user</span>) &#123;</span><br><span class="line">            <span class="title function_">handleError</span>(e, vm, <span class="string">`getter for watcher &quot;<span class="subst">$&#123;<span class="variable language_">this</span>.expression&#125;</span>&quot;`</span>)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; <span class="keyword">throw</span> e &#125;</span><br><span class="line">    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="variable language_">this</span>.<span class="property">deep</span>) &#123;</span><br><span class="line">            <span class="title function_">traverse</span>(value)</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="title function_">popTarget</span>()</span><br><span class="line">        <span class="variable language_">this</span>.<span class="title function_">cleanupDeps</span>()</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> value</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>get 方法首先调用了在 <code>Dep类</code>文件中定义的全局方法 pushTarget，将 Dep.target 置为当前正在执行的渲染 Watcher，并将这个 watcher 压到了 targetStack。接下来调用 wathcer 的 getter 方法：由于此时的 getter 就是 updateComponent 方法，执行 updateComponent 方法，也就是 <code>vm._update(vm._render(), hydrating)</code> 进行渲染，渲染过程中必然会 touch 到 data 中相关依赖的属性，此时就会触发各个属性中的 get 方法（也就是将当前的渲染 Watcher 添加到所有渲染中使用到的数据的依赖列表 subs 中）。</p>
<p>渲染完之后会将当前的渲染 Watcher 从 targetStack 推出，进行下一个 watcher 的任务。最后会进行<em>依赖的清空</em>，每次数据的变化都会引起视图重新渲染，每一次渲染又会进行依赖收集，又会触发 data 中每个属性的 get 方法，这时会有一种情况发生：<code>&lt;div v-if=&quot;someBool&quot;&gt;&#123;&#123; a &#125;&#125;&lt;/div&gt;&lt;div v-else&gt;&#123;&#123; b &#125;&#125;&lt;/div&gt;</code> 第一次当 someBool 为真时，进行渲染，当我们把 someBool 的值 update 为 false 时，这时候属性 a 不会被使用，所以如果不进行依赖清空，会导致不必要的依赖通知。依赖清空主要是对 newDeps 进行更新，通过对比本次收集的依赖和之前的依赖进行对比，把不需要的依赖清除。</p>
<h3 id="派发更新"><a href="#派发更新" class="headerlink" title="派发更新"></a>派发更新</h3><p><img src="evernotecid://954D5E79-AC85-4E89-863E-EB8C8DEE61D9/appyinxiangcom/23187233/ENResource/p11" alt="6ff6878b57427d1b0115d669089d54d3.png"></p>
<p>当我们修改一个存在 data 属性上的值时，会触发数据中的 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/index.js#L173">set 属性方法</a>，首先会判断并修改该属性数据的值，并触发自身的 dep.notify 方法开始更新派发：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="attr">set</span>: <span class="keyword">function</span> <span class="title function_">reactiveSetter</span> (newVal) &#123;</span><br><span class="line">  <span class="keyword">const</span> value = getter ? getter.<span class="title function_">call</span>(obj) : val</span><br><span class="line">  <span class="keyword">if</span> (newVal === value || (newVal !== newVal &amp;&amp; value !== value)) &#123;</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  childOb = !shallow &amp;&amp; <span class="title function_">observe</span>(newVal)</span><br><span class="line">  dep.<span class="title function_">notify</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>接下来让我们进入<a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/dep.js#L37"><code>dep.notify</code></a>这个方法：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">notify () &#123;</span><br><span class="line">    <span class="keyword">const</span> subs = <span class="variable language_">this</span>.<span class="property">subs</span>.<span class="title function_">slice</span>()</span><br><span class="line">    <span class="keyword">if</span> (process.<span class="property">env</span>.<span class="property">NODE_ENV</span> !== <span class="string">&#x27;production&#x27;</span> &amp;&amp; !config.<span class="property">async</span>) &#123;</span><br><span class="line">        subs.<span class="title function_">sort</span>(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a.<span class="property">id</span> - b.<span class="property">id</span>)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>, l = subs.<span class="property">length</span>; i &lt; l; i++) &#123;</span><br><span class="line">      subs[i].<span class="title function_">update</span>()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure><br>notify 这个方法是每一个数据在响应式化之后自己内部闭包的 dep 实例的方法，还记得之前讲过的 subs 保存着所有订阅该数据的订阅者，所以在 notify 方法中首先对 subs 这个订阅者序列按照其 id 进行了排序。接下来就是调用每一个订阅者 watcher 实例的 <code>update</code> 方法进行更新的派发。update 方法是在 Watcher 类文件中，使用了队列的方式来管理订阅者的更新派发，其中主要调用了 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/scheduler.js#L164"><code>queueWatcher</code></a> 这个方法来实现该逻辑：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">queueWatcher</span> (<span class="attr">watcher</span>: <span class="title class_">Watcher</span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> id = watcher.<span class="property">id</span></span><br><span class="line">  <span class="keyword">if</span> (has[id] == <span class="literal">null</span>) &#123;    <span class="comment">// a*</span></span><br><span class="line">    has[id] = <span class="literal">true</span></span><br><span class="line">    <span class="keyword">if</span> (!flushing) &#123;        <span class="comment">// b*</span></span><br><span class="line">      queue.<span class="title function_">push</span>(watcher)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">let</span> i = queue.<span class="property">length</span> - <span class="number">1</span></span><br><span class="line">      <span class="keyword">while</span> (i &gt; index &amp;&amp; queue[i].<span class="property">id</span> &gt; watcher.<span class="property">id</span>) &#123;</span><br><span class="line">        i--</span><br><span class="line">      &#125;</span><br><span class="line">      queue.<span class="title function_">splice</span>(i + <span class="number">1</span>, <span class="number">0</span>, watcher)</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!waiting) &#123;         <span class="comment">// c*</span></span><br><span class="line">      waiting = <span class="literal">true</span></span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">      <span class="title function_">nextTick</span>(flushSchedulerQueue)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><br>进入该方法后，首先使用一个名为 has 的 Map 来保证每一个 watcher 仅推入队列一次[a<em>]。<code>flushing</code> 这个标识符表示目前这个队列是否正在进行更新派发，如果是，那么将这个 id 对应的订阅者进行替换，如果已经路过这个 id，那么就立刻将这个 id 对应的 watcher 放在下一个排入队列[b</em>]。接下来根据<code>waiting</code> 这个标识符来表示当前是否正在对这个队列进行更新派发[c*]，如果没有的话，就可以调用 <a href="https://github.com/DQFE/vue/blob/dev/src/core/observer/scheduler.js#L71"><code>nextTick(flushSchedulerQueue)</code></a> 方法进行真正的更新派发了。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">flushSchedulerQueue</span> () &#123;</span><br><span class="line">  flushing = <span class="literal">true</span></span><br><span class="line">  <span class="keyword">let</span> watcher, id</span><br><span class="line"></span><br><span class="line">  queue.<span class="title function_">sort</span>(<span class="function">(<span class="params">a, b</span>) =&gt;</span> a.<span class="property">id</span> - b.<span class="property">id</span>)</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">for</span> (index = <span class="number">0</span>; index &lt; queue.<span class="property">length</span>; index++) &#123;</span><br><span class="line">    watcher = queue[index]</span><br><span class="line">    <span class="keyword">if</span> (watcher.<span class="property">before</span>) &#123;</span><br><span class="line">      watcher.<span class="title function_">before</span>()</span><br><span class="line">    &#125;</span><br><span class="line">    id = watcher.<span class="property">id</span></span><br><span class="line">    has[id] = <span class="literal">null</span></span><br><span class="line">    watcher.<span class="title function_">run</span>()</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>这个方法首先对队列中的 watcher 按照其 id 进行了排序，排序的主要目的有三：</p>
<ul>
<li>组件的更新由父到子；因为父组件的创建过程是先于子的，所以 watcher 的创建也是先父后子，执行顺序也应该保持先父后子。</li>
<li>用户的自定义 watcher 要优先于渲染 watcher 执行；因为用户自定义 watcher 是在渲染 watcher 之前创建的。</li>
<li>如果一个组件在父组件的 watcher 执行期间被销毁，那么它对应的 watcher 执行都可以被跳过，所以父组件的 watcher 应该先执行。<br>排序结束之后，便对这个队列进行遍历，并执行<code>watcher.run()</code>方法去实现数据更新的通知，run方法的逻辑是：<ul>
<li>新的值与老的值不同时会触发通知；</li>
<li>但是当值是对象或者deep为true时无论如何都会进行通知</li>
</ul>
</li>
</ul>
<p>所以 watcher 有两个功能，一个是将属性的值进行更新，另一个就是可以执行 watch 中的回调函数 handler(newVal, oldVal)，这也是为何我们可以在 watcher 中拿到新旧两个值的原因。</p>
<p>至此，数据的更新派发也通知到了各个组件，便又可以进行视图的更新渲染，收集依赖，派发更新……</p>
]]></content>
      <categories>
        <category>SourceCodeReading</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>源码学习</tag>
        <tag>VUE</tag>
        <tag>数据响应</tag>
      </tags>
  </entry>
  <entry>
    <title>Clean Code规范整理</title>
    <url>/2019/12/21/2019-12-21-clean-code-note.html</url>
    <content><![CDATA[<p>本文针对 Clean Code 一书进行了一些关键内容和提纲的总结。</p>
<span id="more"></span>
<h4 id="变量"><a href="#变量" class="headerlink" title="变量"></a>变量</h4><pre><code>1.变量名有意义
2.不使用魔法数字
3.参数用变量而不是数字
4.mapping中的iterator变量命名清楚
5.属性中不要出现类名/对象名
6.方法参数使用默认值检查参数是否为undefined
</code></pre><h4 id="方法"><a href="#方法" class="headerlink" title="方法"></a>方法</h4><pre><code>1.使用一到两个形参（或方法复杂、使用对象类型参数、形参的解构赋值）
2.一个方法应该只做一件事！
3.方法名要能自我解释
4.如果出现重复的代码要精练和抽象
5.使用Object.assign给对象赋值
6.（如果方法中出现flag，应当分成两个方法处理）
7.尽量避免方法的副作用（更改全局变量、多个方法写一个文件、公用一个状态等）
8.处理数组、对象时最好处理其复本
9.修改原生对象和方法最好使用class新建
10.尽量使用函数式编程代替命令式编程
11.存在多个判断条件时用方法来包括，避免使用非类型条件
12.避免使用条件语句，使用多态性（class）来代替
13.避免类型判断
</code></pre><h4 id="对象与数据结构"><a href="#对象与数据结构" class="headerlink" title="对象与数据结构"></a>对象与数据结构</h4><pre><code>1.使用class来实现继承
2.使用链式方法调用
3.谨慎使用继承（仅在以下三种情况使用）：
    a.关系为is-a而不是has-a的时候
    b.基类可以被复用时
    c.想通过更改基类来进行全局属性的改变时
</code></pre><h4 id="SOLID原则"><a href="#SOLID原则" class="headerlink" title="SOLID原则"></a>SOLID原则</h4><pre><code>1.SRP（单一职责）：一个类要足够内聚，负责尽量单一的功能，减少类的改动；
2.OCP（开闭原则）：对扩展开放，对修改封闭；
3.LSP（里式替换）：父类特性在子类中要完全继承无变化，即在父类对象被替换为子类对象时无影响；
4.ISP（接口隔离）：不同接口之间需要进行隔离；
5.DIP（依赖倒置）：高层模块不能依赖底层模块，应都依赖于抽象；
</code></pre><h4 id="并发"><a href="#并发" class="headerlink" title="并发"></a>并发</h4><pre><code>1.用Promises，不用回调函数callbacks（用async/await更好）
</code></pre><h4 id="错误处理"><a href="#错误处理" class="headerlink" title="错误处理"></a>错误处理</h4><pre><code>1.不确定的代码需要try...catch...
2.不要忘了catch里的错误处理、reject里的错误处理
</code></pre><h4 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h4><pre><code>1.驼峰、下划线、大小写都需要保持一致
</code></pre>]]></content>
      <categories>
        <category>Reading</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>规范</tag>
      </tags>
  </entry>
  <entry>
    <title>怎样做一个Pragmatic Programmer</title>
    <url>/2020/01/09/2020-01-09-how-to-be-a-pragmatic-programmer.html</url>
    <content><![CDATA[<p>怎样成为一个务实的程序员？本文针对原作进行了一些重点的摘录。</p>
<span id="more"></span>
<h4 id="What-maks-a-pragmatic-programmer"><a href="#What-maks-a-pragmatic-programmer" class="headerlink" title="What maks a pragmatic programmer"></a>What maks a pragmatic programmer</h4><p>1.Early adopter/faster adapter: 善于学习新的技术并能够尝试将其集成在自己的知识库中;<br>2.Inquisitive：善于提问、保持好奇心；<br>3.Critical thinker：辩证地看待问题而非一味接受前人的话语；<br>4.Realistic：发觉问题的本质，认识问题的难度并能估计解决该问题的时间；<br>5.Jack of all trades：能够转移领域之后快速适应，通过掌握核心基础知识；<br>6.We who cut mere stones must always be envisioning cathedrals.<br>7.持续以上过程</p>
<h4 id="TIPs"><a href="#TIPs" class="headerlink" title="TIPs"></a>TIPs</h4><p>Tip1.在乎你所做的事情<br>Tip2.不断思考所做的事情，不断从big picture进行思考<br>Tip3.提供解决方案而非各种借口<br>Tip4.“不留一扇破窗”，一旦发现问题及时修复<br>Tip5.做项目的催化剂，吸引资源到一个可预见成功的项目中<br>Tip6.时刻牢记“蓝图”<br>Tip7.将产品质量作为需求之一进行考量<br>Tip8.定期对知识进行投资<br>Tip9.批判地看待自己的所见所闻</p>
<h4 id="持续进行知识投资"><a href="#持续进行知识投资" class="headerlink" title="持续进行知识投资"></a>持续进行知识投资</h4><p>1.定期地进行知识投资<br>2.注重知识的多样性<br>3.在2的基础上注意控制风险<br>4.在一个技术还未成熟前进行学习<br>5.时常对知识库进行review</p>
<h5 id="e-g"><a href="#e-g" class="headerlink" title="e.g."></a>e.g.</h5><p>1.一年至少学习一种新的语言<br>2.一个季度看一本技术书籍（甚至一个月、可涉及非技术例如用户等）<br>3.上课<br>4.参与到用户群体中<br>5.在不同环境中进行试验<br>6.跟上潮流：订阅杂志或期刊</p>
]]></content>
      <categories>
        <category>Reading</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>规范</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript模块化</title>
    <url>/2020/01/12/2020-01-12-Javascript-module-history.html</url>
    <content><![CDATA[<p>JavaScript 模块化是怎么产生，又是如何发展的？现在的模块化是怎样的构成，怎样使用模块更好地组织我们的代码？本文进行了一些简单的介绍和总结。</p>
<span id="more"></span>
<h4 id="名词定义先行"><a href="#名词定义先行" class="headerlink" title="名词定义先行"></a>名词定义先行</h4><p>模块化主要解决以下三个问题：</p>
<ul>
<li>代码分离</li>
<li>不同模块间的依赖定义</li>
<li>代码到执行环境的传递</li>
</ul>
<p>能够解决其中一两个点的解决方案我们称之为“<strong>模式</strong>”，能够解决全部三个问题的方案我们称之为“<strong>模块系统</strong>”；</p>
<p>我们将封装好的导出实例（对象、方法等）和引入的实例称为“<strong>模块格式</strong>”；用“分离依赖定义（detached dependency definitions，即用不同文件存储不同文件）”来描述模块系统中可被使用的独立依赖。</p>
<h4 id="关于模块化所解决的问题"><a href="#关于模块化所解决的问题" class="headerlink" title="关于模块化所解决的问题"></a>关于模块化所解决的问题</h4><ul>
<li>命名冲突</li>
</ul>
<p>在1995-1999年间，使用var来定义全局变量是非常方便的，因为当时的JavaScript就是用来编写脚本处理小微任务，而随着应用的代码量上升，全局变量的缺点就越来越明显，因此我们甚至不能引用第三方脚本。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file greeting.js</span></span><br><span class="line"><span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">writeHello</span>(<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(helloInLang[lang]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file hello.js</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">writeHello</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(<span class="string">&#x27;The script is broken&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>支持大量代码仓库</li>
</ul>
<p>由于编写应用的代码量越来越大，我们常常需要将代码分为多个脚本文件逐一引入，这导致了越来越多数量的脚本文件需要我们手动维护，而且还要考虑脚本引入的顺序！这真是太令人头痛！</p>
<h4 id="Directly-Defined-Dependencies"><a href="#Directly-Defined-Dependencies" class="headerlink" title="Directly Defined Dependencies"></a>Directly Defined Dependencies</h4><p>1999年提出的首次对于独立依赖的模式“直接定义依赖”。该模式是由 Erik Arvidsson 在1999年提出。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file greeting.js</span></span><br><span class="line">dojo.<span class="title function_">provide</span>(<span class="string">&quot;app.greeting&quot;</span>);</span><br><span class="line"></span><br><span class="line">app.<span class="property">greeting</span>.<span class="property">helloInLang</span> = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">app.<span class="property">greeting</span>.<span class="property">sayHello</span> = <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> app.<span class="property">greeting</span>.<span class="property">helloInLang</span>[lang];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file hello.js</span></span><br><span class="line">dojo.<span class="title function_">provide</span>(<span class="string">&quot;app.hello&quot;</span>);</span><br><span class="line"></span><br><span class="line">dojo.<span class="built_in">require</span>(<span class="string">&#x27;app.greeting&#x27;</span>);</span><br><span class="line"></span><br><span class="line">app.<span class="property">hello</span> = <span class="keyword">function</span>(<span class="params">x</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(app.<span class="property">greeting</span>.<span class="title function_">sayHello</span>(<span class="string">&#x27;es&#x27;</span>));</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>代码使用dojo 1.6编写，其中dojo.provide用来定义模块，获取模块时需要使用dojo.require</p>
<h4 id="命名空间模式"><a href="#命名空间模式" class="headerlink" title="命名空间模式"></a>命名空间模式</h4><p>在JavaScript中，函数是<code>first class citizens</code>，意味着可以被赋给变量，也可以被函数返回。命名空间模式是由 Erik Arvidsson于2002年发明的互联网应用开发工具Bindows开始形成。该模式类似于：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file app.js</span></span><br><span class="line"><span class="keyword">var</span> app = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file greeting.js</span></span><br><span class="line">app.<span class="property">helloInLang</span> = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file hello.js</span></span><br><span class="line">app.<span class="property">writeHello</span> = <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(app.<span class="property">helloInLang</span>[lang]);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>可见，所有的逻辑和数据都被放在了app对象的属性中，所以不会污染其他全局变量。尽管该模式看似让代码组织有了一定的规律，但是很明显上面的数据和逻辑仍然没有被隔离，我们很轻易就可以修改这些代码。</p>
<h4 id="模块模式"><a href="#模块模式" class="headerlink" title="模块模式"></a>模块模式</h4><p>模块模式的主旨是将数据和逻辑代码放在闭包中并提供一些公用方法作为对外接口对这些资源进行访问<a href="http://www.adequatelygood.com/JavaScript-Module-Pattern-In-Depth.html">JavaScript Module Pattern: In-Depth</a>。例如：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> greeting = (<span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> <span class="variable language_">module</span> = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">        <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">        <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">        <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">module</span>.<span class="property">getHello</span> = <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> helloInLang[lang];</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">module</span>.<span class="property">writeHello</span> = <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">        <span class="variable language_">document</span>.<span class="title function_">write</span>(<span class="variable language_">module</span>.<span class="title function_">getHello</span>(lang))</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">module</span>;</span><br><span class="line">&#125;());</span><br></pre></td></tr></table></figure>
<p>这种将数据和逻辑闭包在立即调用方法中的方式在2008年Douglas Crockford发表的《JavaScript the Good Parts》一书中被称作“模块”。</p>
<h4 id="模板定义依赖（Template-Defined-Dependencies"><a href="#模板定义依赖（Template-Defined-Dependencies" class="headerlink" title="模板定义依赖（Template Defined Dependencies)"></a>模板定义依赖（Template Defined Dependencies)</h4><p>当有多个script文件依赖时，我们可以使用一个模板来将这些依赖进行顺序管理：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file app.tmp.js</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*borschik:include:../lib/main.js*/</span></span><br><span class="line"><span class="comment">/*borschik:include:../lib/helloInLang.js*/</span></span><br><span class="line"><span class="comment">/*borschik:include:../lib/writeHello.js*/</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// file main.js</span></span><br><span class="line"><span class="keyword">var</span> app = &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file helloInLang.js</span></span><br><span class="line">app.<span class="property">helloInLang</span> = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file writeHello.js</span></span><br><span class="line">app.<span class="property">writeHello</span> = <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(app.<span class="property">helloInLang</span>[lang]);</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h4 id="注释定义依赖"><a href="#注释定义依赖" class="headerlink" title="注释定义依赖"></a>注释定义依赖</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file helloInLang.js</span></span><br><span class="line"><span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file sayHello.js</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*! lazy require scripts/app/helloInLang.js */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">sayHello</span>(<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> helloInLang[lang];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file hello.js</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*! lazy require scripts/app/sayHello.js */</span></span><br><span class="line"></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">write</span>(<span class="title function_">sayHello</span>(<span class="string">&#x27;en&#x27;</span>));</span><br></pre></td></tr></table></figure>
<p>这种依赖的工作方式是：首先下载这些依赖文件，并进行文件内容解析，解析到有依赖存在的注释时迭代下载、解析……</p>
<h4 id="外部定义依赖"><a href="#外部定义依赖" class="headerlink" title="外部定义依赖"></a>外部定义依赖</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file deps.json</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;files&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;main.js&quot;</span>: [<span class="string">&quot;sayHello.js&quot;</span>],</span><br><span class="line">        <span class="string">&quot;sayHello.js&quot;</span>: [<span class="string">&quot;helloInLang.js&quot;</span>],</span><br><span class="line">        <span class="string">&quot;helloInLang.js&quot;</span>: []</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file helloInLang.js</span></span><br><span class="line"><span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file sayHello.js</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">sayHello</span>(<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> helloInLang[lang];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file main.js</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="title function_">sayHello</span>(<span class="string">&#x27;en&#x27;</span>));</span><br></pre></td></tr></table></figure>
<p>deps.json文件就是我们定义所有依赖的外部上下文依赖文件。当运行这个应用时，加载器会获取到这个文件，将这个文件中的依赖按照数组的正确顺序进行读取和加载。lodash就是使用的这种方法进行依赖加载的。</p>
<h4 id="沙箱模式"><a href="#沙箱模式" class="headerlink" title="沙箱模式"></a>沙箱模式</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file sandbox.js</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">Sandbox</span>(<span class="params">callback</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> modules = [];</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i <span class="keyword">in</span> <span class="title class_">Sandbox</span>.<span class="property">modules</span>) &#123;</span><br><span class="line">        modules.<span class="title function_">push</span>(i);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; modules.<span class="property">length</span>; i++) &#123;</span><br><span class="line">        <span class="variable language_">this</span>[modules[i]] = <span class="title class_">Sandbox</span>.<span class="property">modules</span>[modules[i]]();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="title function_">callback</span>(<span class="variable language_">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file greeting.js</span></span><br><span class="line"><span class="title class_">Sandbox</span>.<span class="property">modules</span> = <span class="title class_">Sandbox</span>.<span class="property">modules</span> || &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="title class_">Sandbox</span>.<span class="property">modules</span>.<span class="property">greeting</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">        <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">        <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">        <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="attr">sayHello</span>: <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> helloInLang[lang];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file app.js</span></span><br><span class="line"><span class="keyword">new</span> <span class="title class_">Sandbox</span>(<span class="keyword">function</span>(<span class="params">box</span>) &#123;</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(box.<span class="property">greeting</span>.<span class="title function_">sayHello</span>(<span class="string">&#x27;es&#x27;</span>));</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>该模式的关键是使用一个全局的构造函数来代替全局对象，依赖模块可以被定义为这个构造函数的属性。</p>
<h4 id="依赖注入"><a href="#依赖注入" class="headerlink" title="依赖注入"></a>依赖注入</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file greeting.js</span></span><br><span class="line">angular.<span class="title function_">module</span>(<span class="string">&#x27;greeter&#x27;</span>, [])</span><br><span class="line">    .<span class="title function_">value</span>(<span class="string">&#x27;greeting&#x27;</span>, &#123;</span><br><span class="line">        <span class="attr">helloInLang</span>: &#123;</span><br><span class="line">            <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">            <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">            <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">        &#125;,</span><br><span class="line"></span><br><span class="line">        <span class="attr">sayHello</span>: <span class="keyword">function</span>(<span class="params">lang</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">helloInLang</span>[lang];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// file app.js</span></span><br><span class="line">angular.<span class="title function_">module</span>(<span class="string">&#x27;app&#x27;</span>, [<span class="string">&#x27;greeter&#x27;</span>])</span><br><span class="line">    .<span class="title function_">controller</span>(<span class="string">&#x27;GreetingController&#x27;</span>, [<span class="string">&#x27;$scope&#x27;</span>, <span class="string">&#x27;greeting&#x27;</span>, <span class="keyword">function</span>(<span class="params">$scope, greeting</span>) &#123;</span><br><span class="line">        $scope.<span class="property">phrase</span> = greeting.<span class="title function_">sayHello</span>(<span class="string">&#x27;en&#x27;</span>);</span><br><span class="line">    &#125;]);</span><br></pre></td></tr></table></figure>
<h4 id="CommonJS-Modules"><a href="#CommonJS-Modules" class="headerlink" title="CommonJS Modules"></a>CommonJS Modules</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file greeting.js</span></span><br><span class="line"><span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">    <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">    <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">    <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> sayHello = <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> helloInLang[lang];</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">module</span>.<span class="property">exports</span>.<span class="property">sayHello</span> = sayHello;</span><br><span class="line"></span><br><span class="line"><span class="comment">// file hello.js</span></span><br><span class="line"><span class="keyword">var</span> sayHello = <span class="built_in">require</span>(<span class="string">&#x27;./lib/greeting&#x27;</span>).<span class="property">sayHello</span>;</span><br><span class="line"><span class="keyword">var</span> phrase = <span class="title function_">sayHello</span>(<span class="string">&#x27;en&#x27;</span>);</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(phrase);</span><br></pre></td></tr></table></figure>
<p>CommonJS使用require和module两个标志来表示依赖的引入和导出。</p>
<h4 id="AMD"><a href="#AMD" class="headerlink" title="AMD"></a>AMD</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// file lib/greeting.js</span></span><br><span class="line"><span class="title function_">define</span>(<span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> helloInLang = &#123;</span><br><span class="line">        <span class="attr">en</span>: <span class="string">&#x27;Hello world!&#x27;</span>,</span><br><span class="line">        <span class="attr">es</span>: <span class="string">&#x27;¡Hola mundo!&#x27;</span>,</span><br><span class="line">        <span class="attr">ru</span>: <span class="string">&#x27;Привет мир!&#x27;</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">        <span class="attr">sayHello</span>: <span class="keyword">function</span> (<span class="params">lang</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> helloInLang[lang];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// file hello.js</span></span><br><span class="line"><span class="title function_">define</span>([<span class="string">&#x27;./lib/greeting&#x27;</span>], <span class="keyword">function</span>(<span class="params">greeting</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> phrase = greeting.<span class="title function_">sayHello</span>(<span class="string">&#x27;en&#x27;</span>);</span><br><span class="line">    <span class="variable language_">document</span>.<span class="title function_">write</span>(phrase);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Reading</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>读书笔记</tag>
        <tag>规范</tag>
      </tags>
  </entry>
  <entry>
    <title>Express源码阅读-router相关</title>
    <url>/2020/03/15/2020-03-05-source-code-reading-express-router.html</url>
    <content><![CDATA[<p>Express 是一种保持最低程度规模的灵活 Node.js Web 应用程序框架，为 Web 和移动应用程序提供一组强大的功能。本文针对该框架进行一些简单的源码解读。</p>
<span id="more"></span>
<h3 id="Express中的app"><a href="#Express中的app" class="headerlink" title="Express中的app"></a>Express中的app</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// express.js</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">createApplication</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="comment">// typeof app === &#x27;function&#x27;，同时它身上还绑定着无数属性和方法</span></span><br><span class="line">    <span class="keyword">var</span> app = <span class="keyword">function</span>(<span class="params">req, res, next</span>) &#123;</span><br><span class="line">        app.<span class="title function_">handle</span>(req, res, next);</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">// app同时继承了事件监听/触发机制和application.js中的各种方法</span></span><br><span class="line">    <span class="title function_">mixin</span>(app, <span class="title class_">EventEmitter</span>.<span class="property"><span class="keyword">prototype</span></span>, <span class="literal">false</span>);</span><br><span class="line">    <span class="title function_">mixin</span>(app, proto, <span class="literal">false</span>);</span><br><span class="line">    <span class="comment">// 此处暴露了request和response两个类</span></span><br><span class="line">    app.<span class="property">request</span> = <span class="title class_">Object</span>.<span class="title function_">create</span>(req, &#123; ... &#125;);</span><br><span class="line">    app.<span class="property">response</span> = <span class="title class_">Object</span>.<span class="title function_">create</span>(res, &#123; ... &#125;);</span><br><span class="line">    <span class="comment">// 调用application.js中的init方法，主要调用的是defaultConfiguration方法</span></span><br><span class="line">    app.<span class="title function_">init</span>();</span><br><span class="line">    <span class="keyword">return</span> app;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 这里需要注意的是exports和module.exports的不同：当module.exports被赋值后，exports就不同于module.exports了，所以需要这么做；当然也可以只用module.exports；</span></span><br><span class="line"><span class="built_in">exports</span> = <span class="variable language_">module</span>.<span class="property">exports</span> = createApplication;</span><br></pre></td></tr></table></figure>
<p>可以看出，调用express()返回的app其实是一个函数，调用app.listen()其实执行的是http.createServer(app).listen()。因此，app其实就是一个请求处理函数，作为http.createServer的参数。而express其实是一个工厂函数，用来生成请求处理函数。</p>
<h3 id="中间件"><a href="#中间件" class="headerlink" title="中间件"></a>中间件</h3><blockquote>
<p>An Express application is essentially a series of middleware calls.<br>一个Express应用实际上就是一系列中间件的调用。</p>
</blockquote>
<p>中间件大致可分为两种，一种是普通中间件，通过 <code>app.use(&#39;/user&#39;)</code> 方法进行注册，该方法中的路径是匹配所有以 <code>/user</code> 开头的路径；另外一种是路由中间件，通过 <code>app.METHOD()</code> 方法进行注册，这种方式精确匹配路径，且只能处理确定请求方法的请求。</p>
<p>针对app.use部分的代码如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">app.<span class="property">use</span> = <span class="keyword">function</span> <span class="title function_">use</span>(<span class="params">fn</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> offset = <span class="number">0</span>;</span><br><span class="line">  <span class="comment">// 默认设置路径为根路径，同时判断中间件是方法还是方法所在路径</span></span><br><span class="line">  <span class="keyword">var</span> path = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> fn !== <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> arg = fn;</span><br><span class="line">    <span class="keyword">while</span> (<span class="title class_">Array</span>.<span class="title function_">isArray</span>(arg) &amp;&amp; arg.<span class="property">length</span> !== <span class="number">0</span>) &#123;</span><br><span class="line">      arg = arg[<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> arg !== <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">      offset = <span class="number">1</span>;</span><br><span class="line">      path = fn;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// router初始化</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="title function_">lazyrouter</span>();</span><br><span class="line">  <span class="keyword">var</span> router = <span class="variable language_">this</span>.<span class="property">_router</span>;</span><br><span class="line">  </span><br><span class="line">  <span class="keyword">var</span> fns = <span class="title function_">flatten</span>(slice.<span class="title function_">call</span>(<span class="variable language_">arguments</span>, offset));</span><br><span class="line">  fns.<span class="title function_">forEach</span>(<span class="keyword">function</span> (<span class="params">fn</span>) &#123;</span><br><span class="line">    <span class="comment">// non-express app</span></span><br><span class="line">    <span class="keyword">if</span> (!fn || !fn.<span class="property">handle</span> || !fn.<span class="property">set</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> router.<span class="title function_">use</span>(path, fn);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">debug</span>(<span class="string">&#x27;.use app under %s&#x27;</span>, path);</span><br><span class="line">    fn.<span class="property">mountpath</span> = path;</span><br><span class="line">    fn.<span class="property">parent</span> = <span class="variable language_">this</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// restore .app property on req and res</span></span><br><span class="line">    router.<span class="title function_">use</span>(path, <span class="keyword">function</span> <span class="title function_">mounted_app</span>(<span class="params">req, res, next</span>) &#123;</span><br><span class="line">      <span class="keyword">var</span> orig = req.<span class="property">app</span>;</span><br><span class="line">      fn.<span class="title function_">handle</span>(req, res, <span class="keyword">function</span> (<span class="params">err</span>) &#123;</span><br><span class="line">        <span class="title function_">setPrototypeOf</span>(req, orig.<span class="property">request</span>)</span><br><span class="line">        <span class="title function_">setPrototypeOf</span>(res, orig.<span class="property">response</span>)</span><br><span class="line">        <span class="title function_">next</span>(err);</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// mounted an app</span></span><br><span class="line">    fn.<span class="title function_">emit</span>(<span class="string">&#x27;mount&#x27;</span>, <span class="variable language_">this</span>);</span><br><span class="line">  &#125;, <span class="variable language_">this</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></p>
<p>针对app.METHODS的代码如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">methods.<span class="title function_">forEach</span>(<span class="keyword">function</span>(<span class="params">method</span>)&#123;</span><br><span class="line">  app[method] = <span class="keyword">function</span>(<span class="params">path</span>)&#123;</span><br><span class="line">    <span class="keyword">if</span> (method === <span class="string">&#x27;get&#x27;</span> &amp;&amp; <span class="variable language_">arguments</span>.<span class="property">length</span> === <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="comment">// app.get(setting)</span></span><br><span class="line">      <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="title function_">set</span>(path);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 路由初始化</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">lazyrouter</span>();</span><br><span class="line">    <span class="keyword">var</span> route = <span class="variable language_">this</span>.<span class="property">_router</span>.<span class="title function_">route</span>(path);</span><br><span class="line">    route[method].<span class="title function_">apply</span>(route, slice.<span class="title function_">call</span>(<span class="variable language_">arguments</span>, <span class="number">1</span>));</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p>
<h3 id="路由详解"><a href="#路由详解" class="headerlink" title="路由详解"></a>路由详解</h3><p>在上面两种中间件的实现代码中，都调用了 <code>this.lazyrouter()</code> 方法，所有涉及到路由的方法都会调用这个方法，作用是初始化一个应用的内部路由。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">app.<span class="property">lazyrouter</span> = <span class="keyword">function</span> <span class="title function_">lazyrouter</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (!<span class="variable language_">this</span>.<span class="property">_router</span>) &#123;</span><br><span class="line">    <span class="comment">// 生成一个路由实例</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">_router</span> = <span class="keyword">new</span> <span class="title class_">Router</span>(&#123;</span><br><span class="line">        <span class="attr">caseSensitive</span>: <span class="variable language_">this</span>.<span class="title function_">enabled</span>(<span class="string">&#x27;case sensitive routing&#x27;</span>),</span><br><span class="line">        <span class="attr">strict</span>: <span class="variable language_">this</span>.<span class="title function_">enabled</span>(<span class="string">&#x27;strict routing&#x27;</span>)</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">_router</span>.<span class="title function_">use</span>(<span class="title function_">query</span>(<span class="variable language_">this</span>.<span class="title function_">get</span>(<span class="string">&#x27;query parser fn&#x27;</span>)));</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">_router</span>.<span class="title function_">use</span>(middleware.<span class="title function_">init</span>(<span class="variable language_">this</span>));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><br>针对第一个默认路由，可以看下具体调用了哪些方法，最终实现了什么逻辑：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">this</span>.<span class="title function_">get</span>(<span class="string">&#x27;query parser fn&#x27;</span>);</span><br><span class="line"><span class="comment">// 上面的 get 方法同 app.get，可以在 methods.forEach 循环处理时，如果 get 方法的入参长度为1时，会调用 this.set(arg)</span></span><br><span class="line"><span class="comment">// 在set方法中，有针对 &#x27;query parser&#x27; 的处理</span></span><br><span class="line"><span class="comment">// 最终，query 方法实现了将 req.query 进行 querystring.parse 的一个解析过程</span></span><br><span class="line"><span class="keyword">case</span> <span class="string">&#x27;query parser&#x27;</span>:</span><br><span class="line">  <span class="variable language_">this</span>.<span class="title function_">set</span>(<span class="string">&#x27;query parser fn&#x27;</span>, <span class="title function_">compileQueryParser</span>(val));</span><br><span class="line">  <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure></p>
<p>针对第二个默认路由 init，是给 app 上的暴露出的 req、res 继承 node 原生的 request 和 response 的一些属性。</p>
<p>之所以不在 <code>defaultConfiguration</code> 方法中进行这一步路由的初始化，原因在于设置路由的相关参数需要调用app.set方法，这个方法明显需要有app实例，如果在获取app实例的时候就初始化了一个路由，这个路由的参数就没办法配置了。因此，在获取app实例后，必须先对路由参数进行配置，然后再调用对应的app.use等方法。</p>
<h4 id="app-Methods"><a href="#app-Methods" class="headerlink" title="app.Methods"></a>app.Methods</h4><p>我们先以 <code>app.get</code> 为例，通过断点调试的方式来查看 <code>app.get</code> 这种路由的 <code>_router</code> 对象是什么结构。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN0155CNRA1Gm9p9PNuL4_!!6000000000664-0-tps-1011-1542.jpg" alt="702ca38d0160d8b82ab5d1b7a552bec1.png"></p>
<p>上面的截图是当我们初始化一个 express 实例，并设置了一个 <code>app.get()</code> 的路由后，在 app.listen 处添加断点进行调试时的 app 实例的属性。可以看到在 <code>app._router</code> 中有一个 stack，里面按顺存放着三个 layer 对象，分别是初始化时的 query 和 init 两个路由，和第三个则是我们所设置的 get 路由。每一个 layer 中包含路由处理的回调函数 <code>handle</code>，路由对象 <code>route&lt;Route&gt;</code>，该对象中还包含一个存有 Layer 对象的栈（stack)，就和 <code>app._router.stack</code> 相同。</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN01HG9tIS1lp8UOWG4zm_!!6000000004867-0-tps-1220-654.jpg" alt="81b68f825db7a3f5bf1ff7d87c09496d.png"></p>
<p>根据上面对于 <code>app.get</code> 这种路由的结构分析，我们可以先猜想它的实现流程：</p>
<ul>
<li><p>首先根据传入的路径封装一个Route对象，再对传入的回调函数封装成Layer对象，接着把这个Layer对象push到Route.stack里面去</p>
</li>
<li><p>再创建一个默认Layer(跟app.use里面Layer的同级)，把步骤一中的Route挂到这个Layer.route属性上面，而这个Layer对象，会被push到app._router.stack里面</p>
</li>
</ul>
<h4 id="app-use"><a href="#app-use" class="headerlink" title="app.use"></a>app.use</h4><p>接下来我们来看 <code>app.use</code> 这种路由又有什么不同。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN01xkM27Y1WTrrERnA8w_!!6000000002790-0-tps-1058-1713.jpg" alt="08bdd22fe221f77141d8af6afb5ac8bc.png"></p>
<p>可以看到，在这种路由中，除了 <code>query</code> 和 <code>init</code> 两个初始化路由的方法外，第三个真正的回调方法被封装成的 <code>Layer</code> 对象中，<code>route</code> 属性的值为 <code>undefined</code>。<br>所以我们可以猜想：在 <code>app.use</code> 这种路由里，传入的参数（路径、回调函数）会被封装成 <code>Layer</code> 对象（其中 <code>route</code> 属性为 <code>undefined</code>），压入 <code>app._router.stack</code> 栈中。</p>
<h4 id="两种路由的源码实现"><a href="#两种路由的源码实现" class="headerlink" title="两种路由的源码实现"></a>两种路由的源码实现</h4><p>接下来我们通过源码来分析我们写的 <code>app.use(&#39;/main&#39;, someFun())</code> 是如何成为一个 layer 对象并压入 <code>app._router</code> 的路由栈中的。</p>
<p><strong>首先来看 <code>app.use</code>：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">app.<span class="property">use</span> = <span class="keyword">function</span> <span class="title function_">use</span>(<span class="params">fn</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> path = <span class="string">&#x27;/&#x27;</span>;   <span class="comment">// 设置一个默认的路由方法路径</span></span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="comment">// 设置路由，同样也是由lazyrouter进行路由对象的初始化：this._router = new Router(&#123;&#125;)</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="title function_">lazyrouter</span>();</span><br><span class="line">  <span class="keyword">var</span> router = <span class="variable language_">this</span>.<span class="property">_router</span>;</span><br><span class="line"></span><br><span class="line">  fns.<span class="title function_">forEach</span>(<span class="keyword">function</span> (<span class="params">fn</span>) &#123;</span><br><span class="line">    <span class="comment">// router.use是接下来重点要看的方法</span></span><br><span class="line">    router.<span class="title function_">use</span>(path, <span class="keyword">function</span> <span class="title function_">mounted_app</span>(<span class="params">req, res, next</span>) &#123;</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">  &#125;, <span class="variable language_">this</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>接下来看 Router 对象的 use 方法实现：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">proto.<span class="property">use</span> = <span class="keyword">function</span> <span class="title function_">use</span>(<span class="params">fn</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> path = <span class="string">&#x27;/&#x27;</span>;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  <span class="keyword">var</span> callbacks = <span class="title function_">flatten</span>(slice.<span class="title function_">call</span>(<span class="variable language_">arguments</span>, offset));</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; callbacks.<span class="property">length</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">var</span> fn = callbacks[i];</span><br><span class="line">    <span class="comment">// 针对传入路由中的每一个回调方法，都包装成一个 Layer 对象并压入</span></span><br><span class="line">    <span class="keyword">var</span> layer = <span class="keyword">new</span> <span class="title class_">Layer</span>(path, &#123;</span><br><span class="line">      <span class="attr">sensitive</span>: <span class="variable language_">this</span>.<span class="property">caseSensitive</span>,</span><br><span class="line">      <span class="attr">strict</span>: <span class="literal">false</span>,</span><br><span class="line">      <span class="attr">end</span>: <span class="literal">false</span></span><br><span class="line">    &#125;, fn);</span><br><span class="line">    layer.<span class="property">route</span> = <span class="literal">undefined</span>;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">stack</span>.<span class="title function_">push</span>(layer);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>从代码中可以看到，与我们的猜想一致，<code>app.use</code> 这种路由的实现，是将传入的一个个路径或回调方法等参数封装成 <code>Layer</code> 对象压入 <code>_router.stack</code> 栈中。</p>
<p><strong>然后来看 <code>app.get</code> 这种路由的代码实现：</strong></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// methods 中包含各种请求类型等，get 就包含在其中</span></span><br><span class="line">methods.<span class="title function_">forEach</span>(<span class="keyword">function</span>(<span class="params">method</span>)&#123;</span><br><span class="line">  app[method] = <span class="keyword">function</span>(<span class="params">path</span>)&#123;</span><br><span class="line">    <span class="keyword">if</span> (method === <span class="string">&#x27;get&#x27;</span> &amp;&amp; <span class="variable language_">arguments</span>.<span class="property">length</span> === <span class="number">1</span>) &#123;</span><br><span class="line">      <span class="comment">// app.get(setting)</span></span><br><span class="line">      <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="title function_">set</span>(path);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">lazyrouter</span>();  <span class="comment">// 路由对象 Router 初始化</span></span><br><span class="line">    <span class="keyword">var</span> route = <span class="variable language_">this</span>.<span class="property">_router</span>.<span class="title function_">route</span>(path);</span><br><span class="line">    route[method].<span class="title function_">apply</span>(route, slice.<span class="title function_">call</span>(<span class="variable language_">arguments</span>, <span class="number">1</span>));</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>上面代码中最终返回的是 <code>app</code>，那么这端代码对 <code>app</code> 进行了什么样的操作呢？我们可以将目光聚焦在 <code>route[method].apply()</code> 这行代码上。接下来我们可以看下 <code>route</code> 到底是个什么东西。<code>this._router.route</code> 的代码实现：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">proto.<span class="property">route</span> = <span class="keyword">function</span> <span class="title function_">route</span>(<span class="params">path</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> route = <span class="keyword">new</span> <span class="title class_">Route</span>(path);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">var</span> layer = <span class="keyword">new</span> <span class="title class_">Layer</span>(path, &#123;</span><br><span class="line">    <span class="attr">sensitive</span>: <span class="variable language_">this</span>.<span class="property">caseSensitive</span>,</span><br><span class="line">    <span class="attr">strict</span>: <span class="variable language_">this</span>.<span class="property">strict</span>,</span><br><span class="line">    <span class="attr">end</span>: <span class="literal">true</span></span><br><span class="line">  &#125;, route.<span class="property">dispatch</span>.<span class="title function_">bind</span>(route));</span><br><span class="line"></span><br><span class="line">  layer.<span class="property">route</span> = route;</span><br><span class="line"></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">stack</span>.<span class="title function_">push</span>(layer);</span><br><span class="line">  <span class="keyword">return</span> route;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>这里可以看到返回的 <code>route</code> 是一个 <code>Route</code> 对象，在这个对象中将参数封装成了 <code>Layer</code> 对象推入了 <code>app._router.stack</code> 中。</p>
<p>接下来我们看一下 <code>Route</code> 对象中的一些实现代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Route</span>(<span class="params">path</span>) &#123;</span><br><span class="line">  <span class="comment">// Route对象初始化时添加了一个空数组stack属性</span></span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">path</span> = path;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">stack</span> = [];</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">methods</span> = &#123;&#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 对于app.METHODS这种路由，将路由参数（路径、回调方法）包装成Layer对象压入Route.stack中</span></span><br><span class="line">methods.<span class="title function_">forEach</span>(<span class="keyword">function</span>(<span class="params">method</span>)&#123;</span><br><span class="line">  <span class="title class_">Route</span>.<span class="property"><span class="keyword">prototype</span></span>[method] = <span class="keyword">function</span>(<span class="params"></span>)&#123;</span><br><span class="line">    <span class="keyword">var</span> handles = <span class="title function_">flatten</span>(slice.<span class="title function_">call</span>(<span class="variable language_">arguments</span>));</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; handles.<span class="property">length</span>; i++) &#123;</span><br><span class="line">      <span class="comment">// ...</span></span><br><span class="line">      <span class="keyword">var</span> layer = <span class="title class_">Layer</span>(<span class="string">&#x27;/&#x27;</span>, &#123;&#125;, handle);</span><br><span class="line">      layer.<span class="property">method</span> = method;</span><br><span class="line"></span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">methods</span>[method] = <span class="literal">true</span>;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">stack</span>.<span class="title function_">push</span>(layer);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">this</span>;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>至此，在调用 <code>route[method].apply()</code> 时就会为 <code>app.METHODS</code> 这种类型的路由的 <code>Layer</code> 中添加 <code>route&lt;Route&gt;</code> 属性。</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN01bP9D781hvJMo0r6nj_!!6000000004339-0-tps-1183-691.jpg" alt="e6778eb14b0b4f77fc6bc60d49e9a053.png"></p>
]]></content>
      <categories>
        <category>SourceCodeReading</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>源码学习</tag>
        <tag>Express</tag>
        <tag>NodeJS</tag>
      </tags>
  </entry>
  <entry>
    <title>EggJS源码阅读-启动流程与代码实现</title>
    <url>/2020/05/23/2020-05-30-source-code-reading-Egg-starting.html</url>
    <content><![CDATA[<p>egg是阿里开源的一个框架，为企业级框架和应用而生，相较于express和koa，有更加严格的目录结构和规范。不同的团队可以基于egg，根据自己的需求封装出适合团队业务的更上层框架。本文对 egg.js 源码进行一个简要的入门阅读和解析。</p>
<span id="more"></span>
<h3 id="egg如何启动"><a href="#egg如何启动" class="headerlink" title="egg如何启动"></a>egg如何启动</h3><p><img src="https://img.alicdn.com/imgextra/i4/O1CN01gbgC311e0ZRz21oLP_!!6000000003809-0-tps-1268-1002.jpg" alt="fca8379caeffb9c194aa5369db1c563b.png"></p>
<p>根据<code>package.json</code>中的<code>script</code>命令，可以看到执行的直接是<code>egg-bin dev</code>的命令。找到<code>egg-bin</code>文件夹中的<code>dev.js</code>,会看到里面会去执行外层的<code>start-cluster</code>文件:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">require(options.framework).startCluster(options);</span><br></pre></td></tr></table></figure>
<p>此处的<code>options.framework</code>其实指的就是<code>egg</code>框架，然后调用了<code>egg-cluster</code>包中的<code>startCluster</code>方法，egg正式迈出了启动的第一步。</p>
<p>[源码流程]</p>
<p><code>egg</code>框架采用了<code>master-agent-worder</code>的集群模式，如果所示，官方文档中也对于这三种进程的启动和通信给出了较为详细的说明：</p>
<ul>
<li>Master 启动后先 fork Agent 进程</li>
<li>Agent 初始化成功后，通过 IPC 通道通知 Master</li>
<li>Master 再 fork 多个 App Worker</li>
<li>App Worker 初始化成功，通知 Master</li>
<li>所有的进程初始化成功后，Master 通知 Agent 和 Worker 应用启动成功</li>
</ul>
<p>我们尝试从源码中大致将这个流程实现出来：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// egg-cluster/lib/master.js</span></span><br><span class="line">ready.<span class="title function_">mixin</span>(<span class="variable language_">this</span>);              <span class="comment">// 将 ready 方法挂在 Master 上</span></span><br><span class="line"><span class="variable language_">this</span>.<span class="title function_">detectPorts</span>().<span class="title function_">then</span>(<span class="function">() =&gt;</span> &#123;  <span class="comment">// 检测端口</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">forkAgentWorker</span>();      <span class="comment">// 启动 agent 进程，发送 agent-start 消息给 master 进程</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// master 进程一旦接收到 agent-start 消息后，开始创建 worker 进程</span></span><br><span class="line"><span class="variable language_">this</span>.<span class="title function_">once</span>(<span class="string">&#x27;agent-start&#x27;</span>, <span class="variable language_">this</span>.<span class="property">forkAppWorkers</span>.<span class="title function_">bind</span>(<span class="variable language_">this</span>));</span><br></pre></td></tr></table></figure>
<p>[源码流程]</p>
<h3 id="Agent与AppWorker的实现"><a href="#Agent与AppWorker的实现" class="headerlink" title="Agent与AppWorker的实现"></a>Agent与AppWorker的实现</h3><h4 id="AgentWorker与AppWorker进程的启动"><a href="#AgentWorker与AppWorker进程的启动" class="headerlink" title="AgentWorker与AppWorker进程的启动"></a>AgentWorker与AppWorker进程的启动</h4><p><img src="https://img.alicdn.com/imgextra/i4/O1CN01Fyk5k31EQVsRROl7M_!!6000000000346-0-tps-1337-1029.jpg" alt="de59fd88a438492231f38fb2032ffd5b.png"></p>
<p>在启动<code>AgentWorker</code>和<code>AppWorker</code>时，会分别加载<code>agent_worker.js</code>和<code>app_worker.js</code>两个文件并创建进程，其中<code>agent_worker.js</code>中会创建<code>Agent</code>类的实例，而<code>app_worker.js</code>中会创建<code>Application</code>类的实例。</p>
<p>两种进程在启动时都会调用<code>this.loader.load()</code>方法来加载自己相应的一些插件和自定义的扩展。</p>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN01aX0q421ZQbtFSpqUz_!!6000000003189-0-tps-1335-716.jpg" alt="18d21732106d0293791bf03d6481784b.png"></p>
<p>基于<code>egg_loader</code>实现了<code>AppWorkerLoader</code>和 <code>AgentWorkerLoader</code>，上层框架基于这两个类来扩展，<code>Loader</code>的扩展只能在框架进行。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">AppWorkerLoader</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;egg&#x27;</span></span><br><span class="line"><span class="keyword">import</span> common <span class="keyword">from</span> <span class="string">&#x27;./common&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyAppWorkerLoader</span> <span class="keyword">extends</span> <span class="title class_ inherited__">AppWorkerLoader</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params">opt</span>) &#123;</span><br><span class="line">    <span class="variable language_">super</span>(opt);</span><br><span class="line">        <span class="comment">// 自定义初始化</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">loadConfig</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>.<span class="title function_">loadConfig</span>();</span><br><span class="line">        <span class="comment">// 对 config 进行处理</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_">load</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="variable language_">super</span>.<span class="title function_">load</span>();</span><br><span class="line">        <span class="comment">// 自定义加载其他目录</span></span><br><span class="line">        <span class="comment">// 或对已加载的文件进行处理</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">Object</span>.<span class="title function_">assign</span>(<span class="title class_">MyAppWorkerLoader</span>.<span class="property"><span class="keyword">prototype</span></span>, &#123;</span><br><span class="line">    <span class="comment">// 可以根据自己的需求，重写一些获取配置的方法</span></span><br><span class="line">    <span class="title function_">getServerEnv</span>(<span class="params"></span>)&#123;&#125;;</span><br><span class="line">    <span class="title function_">_preloadAppConfig</span>(<span class="params"></span>)&#123;&#125;</span><br><span class="line">    <span class="title function_">getTypeFiles</span>(<span class="params"></span>)&#123;&#125;</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="title class_">MyAppWorkerLoader</span></span><br></pre></td></tr></table></figure>
<h4 id="Agent如何实现"><a href="#Agent如何实现" class="headerlink" title="Agent如何实现"></a>Agent如何实现</h4><p><img src="https://img.alicdn.com/imgextra/i1/O1CN01zkrj4o1OUliCxYhjc_!!6000000001709-0-tps-1093-760.jpg" alt="f2ac3732452f487f21e11c0de480f3df.png"></p>
<p><code>Agent</code>对象在<code>egg-cluster</code>创建环节中被创建出来，继承自<code>egg.Agent</code>对象，该对象继承<code>EggApplication</code>,且<code>loader</code>为<code>./lib/loader/agent_worker_loader.js</code>文件，继承自<code>egg-core.eggLoader</code>对象，整体继承链如上图。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// egg/lib/agent.js</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Agent</span> <span class="keyword">extends</span> <span class="title class_ inherited__">EggApplication</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">options = &#123;&#125;</span>) &#123;</span><br><span class="line">    options.<span class="property">type</span> = <span class="string">&#x27;agent&#x27;</span>;</span><br><span class="line">    <span class="comment">// 1.完成父类构建</span></span><br><span class="line">    <span class="variable language_">super</span>(options);</span><br><span class="line">    <span class="comment">// 2.驱动loader执行load方法</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">loader</span>.<span class="title function_">load</span>();</span><br><span class="line">    <span class="comment">// 3.dump相关配置文件进入./run目录下</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">dumpConfig</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> &#123;&#125;, <span class="number">24</span> * <span class="number">60</span> * <span class="number">60</span> * <span class="number">1000</span>);</span><br><span class="line">    <span class="comment">// 4.监听异常事件</span></span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">_uncaughtExceptionHandler</span> = <span class="variable language_">this</span>.<span class="property">_uncaughtExceptionHandler</span>.<span class="title function_">bind</span>(<span class="variable language_">this</span>);</span><br><span class="line">    process.<span class="title function_">on</span>(<span class="string">&#x27;uncaughtException&#x27;</span>, <span class="variable language_">this</span>.<span class="property">_uncaughtExceptionHandler</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>Agent</code>类的实现中，主要是实例化了<code>EggApplication</code>，调用了<code>this.loader.load()</code>方法来加载各种文件和配置。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// egg/lib/egg.js</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">EggApplication</span> <span class="keyword">extends</span> <span class="title class_ inherited__">EggCore</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params">options = &#123;&#125;</span>) &#123;</span><br><span class="line">        options.<span class="property">mode</span> = options.<span class="property">mode</span> || <span class="string">&#x27;cluster&#x27;</span>;</span><br><span class="line">        <span class="comment">// 1.原型EggCore构建</span></span><br><span class="line">        <span class="variable language_">super</span>(options);</span><br><span class="line">        <span class="comment">// 2.调用loadConfig，在agent的实现中，指向的并不是egg-core.loader，而是agent_worker_loader</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">loader</span>.<span class="title function_">loadConfig</span>();</span><br><span class="line">        <span class="comment">// 3.ready事件</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="title function_">ready</span>(<span class="function">() =&gt;</span> process.<span class="title function_">nextTick</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">const</span> dumpStartTime = <span class="title class_">Date</span>.<span class="title function_">now</span>();</span><br><span class="line">          <span class="variable language_">this</span>.<span class="title function_">dumpConfig</span>();</span><br><span class="line">          <span class="variable language_">this</span>.<span class="title function_">dumpTiming</span>();</span><br><span class="line">        &#125;));</span><br><span class="line">        <span class="comment">// 监听unhandleRejection事件</span></span><br><span class="line">        <span class="comment">// 4.cluster初始化</span></span><br><span class="line">        <span class="variable language_">this</span>[<span class="variable constant_">CLUSTER_CLIENTS</span>] = [];</span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">cluster</span> = <span class="function">(<span class="params">clientClass, options</span>) =&gt;</span> &#123;&#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在<code>EggApplication</code>类的实现中，我们可以看到是继承自<code>EggCore</code>类，在父类构建好之后，会调用<code>this.loader.loadConfig()</code>方法，该方法的<code>loader</code>实例实际指向了<code>AgentWorkerLoader（agent_worker_loader.js）</code>：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// egg/lib/loader/agent_worker_loader.js</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">AgentWorkerLoader</span> <span class="keyword">extends</span> <span class="title class_ inherited__">EggLoader</span> &#123;</span><br><span class="line">  <span class="title function_">loadConfig</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">loadPlugin</span>();  <span class="comment">// loadPlugin from egg-core/lib/loader/mixin/plgin.js</span></span><br><span class="line">    <span class="variable language_">super</span>.<span class="title function_">loadConfig</span>(); <span class="comment">// loadConfig from egg-core/lib/loader/mixin/config.js</span></span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="title function_">load</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">loadAgentExtend</span>();</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">loadContextExtend</span>();</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">loadCustomAgent</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>loadPlugin</code>方法会加载三种插件：</p>
<ul>
<li><code>eggPlugins</code>，从eggjs框架配置的插件，也就是<code>egg/config/plugins.js</code>文件中egg框架自带的插件；</li>
<li><code>appPlugins</code>，每个应用自己配置的插件，也就是<code>myproject/config/plugins.js</code>，用户可以自定义配置一些特殊的插件；</li>
<li><code>customPlugins</code>，应用启动命令中参数EGG_PLUGINS值所代表的插件；</li>
</ul>
<p>最后会将这三种插件都挂在app实例上：<code>this.plugins = enablePlugins</code>;</p>
<p><code>loadConfig</code>方法会加载三种配置：</p>
<ul>
<li><code>appConfig</code>，每个应用自己独有的配置，其中会按顺序加载两个配置，一个是默认配置<code>config.default</code>，另一个是当前环境的配置<code>config.$&#123;this.serverEnv&#125;</code>，也就是<code>myproject/config</code>下的一些配置文件加载</li>
<li>加载自定义添加的<code>plugin</code>插件的配置</li>
<li>加载框架<code>egg</code>的配置，即<code>egg/config</code></li>
<li>重新加载应用<code>app</code>的配置，即<code>myproject/config</code>下的</li>
</ul>
<p>最后将合并的配置挂载在<code>app</code>实例上<code>this.config = target</code>;</p>
<h4 id="Application如何实现"><a href="#Application如何实现" class="headerlink" title="Application如何实现"></a>Application如何实现</h4><p>上面提到，<code>AppWorker</code>在实例化的过程中，会调用<code>this.loader.load()</code>。进入具体这个<code>Application</code>所对应的<code>loader.load</code>方法，可以发现<code>Application</code>的实现比<code>Agent</code>的实现多调用了很多加载的方法：</p>
<ul>
<li><code>this.loadApplicationExtend();</code>，该方法的调用会给应用加载扩展方法，加载路径为<code>app\extend\application.js</code>, 会将对应的对象挂载在app 应用上。</li>
<li><code>this.loadRequestExtend();</code>，加载<code>app\extend\request.js</code></li>
<li><code>this.loadResponseExtend();</code>，加载<code>app\extend\response.js</code></li>
<li><code>this.loadContextExtend();</code>，加载<code>app\extend\context.js</code></li>
<li><code>this.loadHelperExtend();</code>，加载<code>app\extend\helper.js</code></li>
<li><code>this.loadService()</code></li>
<li><code>this.loadMiddleware()</code></li>
<li><code>this.loadController()</code></li>
<li><code>this.loadRouter()</code></li>
</ul>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ol>
<li><p>egg启动服务集群，采用了<code>master-agent-worker</code>模式，<code>AgentWorker</code>和<code>AppWorker</code>都由Application(egg/lib/applicaton.js) -&gt; EggApplication(egg/lib/egg.js) -&gt; EggCore(egg-core/lib/egg.js) -&gt; KoaApplication(koa)原型链进行继承</p>
</li>
<li><p><code>Agent</code>和<code>Application</code>在实例化的过程中，都会调用相应的Loader去加载自己所需的插件和配置，且加载顺序严格按照<strong>插件plugin-框架framework-应用application</strong>这样一个顺序</p>
</li>
</ol>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN015m0LOC1ZwfcF0EBGW_!!6000000003259-2-tps-888-2235.png" alt="0464781205f05b89c1b8807280114dd8.png"></p>
]]></content>
      <categories>
        <category>SourceCodeReading</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>源码学习</tag>
        <tag>NodeJS</tag>
        <tag>EggJS</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 基于事件循环的并发模型</title>
    <url>/2020/06/03/2020-06-03-advanced-eventloop-in-javascript.html</url>
    <content><![CDATA[<p>不去了解 JavaScript 的事件循环就相当于没有入门 JavaScript 这门语言程序，本文第一次对事件循环进行了了解和学习。</p>
<span id="more"></span>
<p>runtime（运行时刻）是指一个程序在运行的状态。JavaScript 的运行时刻有一个基于事件循环的并发模型，事件循环负责执行代码、收集和处理事件以及执行队列中的子任务。</p>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN01JTr8Wt1WAApPxCxng_!!6000000002747-55-tps-295-271.svg" alt="The_Javascript_Runtime_Environment_Example"></p>
<p>JavaScript 中的函数调用会形成一个由若干帧组成的栈，当一个函数中存在闭包函数时，外层函数会先压入栈中，其中包含参数和局部变量；闭包函数跟着压入栈中，也包含了自己的参数和局部变量。闭包函数先从栈中弹出，执行完了之后外层函数从栈中弹出执行，栈就被清空了。</p>
<p>对象是被分配在堆中。</p>
<p>一个 JavaScript 运行时包含了一个待处理消息的消息队列。每一个消息都关联着一个用以处理这个消息的回调函数。</p>
<h2 id="事件循环"><a href="#事件循环" class="headerlink" title="事件循环"></a>事件循环</h2><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (queue.<span class="title function_">waitForMessage</span>()) &#123;</span><br><span class="line">  queue.<span class="title function_">processNextMessage</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>JavaScript 中每一个函数都是需要完整执行完成才会继续进行下一步（单线程语言：one thread ⇒ one call stack ⇒ one thing at a time），所以 JavaScript 的代码很容易出现一个函数执行时间过长的情况。为了实现程序的并发执行，JavaScript 引入了事件循环机制。</p>
<p>在上面所提到的函数调用栈之外，还会有一个任务队列。当 runtime 执行代码过程中遇到了像 setTimeout 或 DOM 事件绑定的回调函数，会首先调用 WebAPIs 去进行执行——例如 setTimeout 会产生一个 timer 计时器，当计时器到达指定时间后，会将这个事件推入任务队列。任务队列就是由一个个异步操作的回调函数所组成的队列。事件循环简单来说就是：当 runtime 函数调用栈清空后，查看当前任务队列，并把队列第一个任务压入 runtime 函数调用栈。</p>
<p>浏览器的 render 刷新最快是 16.6ms 一次（浏览器刷新一般是 60次/s ），这是在主线程为空的时候（所以任务队列里的任务是不会阻塞浏览器渲染的）。<strong>如果想在浏览器渲染任务队列中添加任务，可以使用 requestAnimationFrame(callback)方法来进行添加</strong>。如果函数调用栈不为空，则 render 会被阻塞：主线程中的死循环会阻塞浏览器渲染，但是看下面的代码： </p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">loop</span>(<span class="params"></span>) &#123;</span><br><span class="line">	<span class="built_in">setTimeout</span>(loop, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_">loop</span>();</span><br></pre></td></tr></table></figure>
<p>这个无限自我递归调用也会形成一个循环，但是由于 setTimeout 方法只是不停地往任务队列里面添加任务，而主线程还是每次取一个任务去执行，主线程不会被阻塞，所以浏览器渲染也不会被阻塞。</p>
<p>到目前为止，在事件循环中，我们认识到了主线程、任务队列和渲染任务队列。当主线程任务全部执行完成清空后，会从非空的任务队列中拿第一个任务到主线程尽心执行，每次循环都是只取一个任务队列中的任务。而渲染任务则是一次性执行完成的。</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN01QUc4TE1iugRDgt0iq_!!6000000004473-2-tps-1536-636.png" alt="O1CN01QUc4TE1iugRDgt0iq_"></p>
<p>实际上，浏览器总还有一个 microTask queue，这个队列中一般我们遇到的 99.9% 应该都是以 promise 形式出现的，microTask 执行的时间是每一段 JS 代码执行完毕（主线程任务清空、任务队列取得第一个任务到主线程并执行完、浏览器渲染任务执行完）都会进行执行，而且一旦执行，microTask queue 中无论有多少个任务（包括同时添加进来的任务）都会一直执行完。所以，microTask queue 是会产生阻塞的。</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN01bpN7Ky1eCw92MQLdq_!!6000000003836-2-tps-1535-746.png" alt="O1CN01bpN7Ky1eCw92MQLdq_"></p>
<p>如上图所示，我们所知的浏览器的事件循环有以下的几种队列机制：</p>
<ul>
<li><p>主线程任务队列</p>
<p>  接受 JS 程序中按顺序的同步任务，在每次事件循环中都会执行任务直至队列清空</p>
</li>
<li><p>异步回调任务队列</p>
<p>  接受像 setTimeout、ajax 等的异步回调函数任务，在每次时间循环中主线程任务队列清空后，会拿出该队列的头部任务放入主线程进行执行</p>
</li>
<li><p>microtask 任务队列</p>
<p>  接受 promise.then 的回调函数任务，在事件循环中，每一段 JS 代码执行完毕（主线程任务清空、任务队列取得第一个任务到主线程并执行完、浏览器渲染任务执行完）都会执行，且每次执行都会将该队列清空，包括同时入队的任务也会一并清空</p>
</li>
<li><p>动画帧回调任务队列</p>
<p>  接受 requestAnimationFrame 的回调函数任务，在每次浏览器渲染任务执行之前进行执行（目前仅在 chrome 中是该顺序），每次执行都清空当前队列，如有新的任务添加则等待下次渲染前执行</p>
</li>
<li><p>浏览器渲染任务队列</p>
<p>  每当上面主线程、异步任务队列、microTask 任务队列执行完毕，且距离上一次渲染时长大于 16.6ms 时，就会进行任务执行，每一次执行都会将当前队列任务全部执行完毕</p>
</li>
</ul>
<p>用一个伪代码来进行上面流程的表述，如下：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">	<span class="keyword">while</span> (mainThread.<span class="title function_">hasTasks</span>()) &#123;</span><br><span class="line">		<span class="title function_">execute</span>(mainThread.<span class="title function_">getNextTask</span>());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	queue = <span class="title function_">getNextQueue</span>();</span><br><span class="line">	task = queue.<span class="title function_">pop</span>();</span><br><span class="line">	<span class="title function_">execute</span>(task);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">while</span>(microTask.<span class="title function_">hasTasks</span>()) &#123;</span><br><span class="line">		<span class="title function_">doMicroTask</span>();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (<span class="title function_">isRepaintTime</span>()) &#123;</span><br><span class="line">		animationTasks = animationQueue.<span class="title function_">copyTasks</span>();</span><br><span class="line">		<span class="keyword">for</span> (task <span class="keyword">in</span> animationTasks) &#123;</span><br><span class="line">			<span class="title function_">doAnimationTask</span>();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="title function_">repaint</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="Node-中的事件循环"><a href="#Node-中的事件循环" class="headerlink" title="Node 中的事件循环"></a>Node 中的事件循环</h2><p>NodeJS 的事件循环基本上与浏览器中的事件循环是一致的，只不过浏览器使用的是 WebAPIs，而 NodeJS 中使用的是 C++ APIs。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN01Xv7ETw1Q5EroGIgBS_!!6000000001924-2-tps-1383-703.png" alt="O1CN01Xv7ETw1Q5EroGIgBS_"></p>
<p>主线程与异步任务队列执行以及 microTask 任务队列都与浏览器中的事件循环相同，我们可以看到上图中多了三个没有见到过的任务队列：</p>
<ul>
<li><p>check phase 任务队列</p>
<p>  接受使用 setImmediate 方法传入的回调函数任务，每次在异步任务队列执行完成后，清空该队列</p>
</li>
<li><p>timer phase 任务队列</p>
<p>  接受 setTimeout、setInterval 传入的回调函数，每次在 check phase 任务队列清空后，清空该任务队列</p>
</li>
<li><p>nextTick 任务队列</p>
<p>  与 microTask 任务队列相同， 接受 process.nextTick 传入的回调函数，该队列优先级高于 microTask </p>
</li>
</ul>
<p>用伪代码进行上述流程的模拟，如下：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="title function_">tasksAreWaiting</span>()) &#123;</span><br><span class="line">	queue = <span class="title function_">getNextQueue</span>();</span><br><span class="line">	<span class="keyword">while</span> (queue.<span class="title function_">hasTasks</span>()) &#123;</span><br><span class="line">		task = queue.<span class="title function_">pop</span>();</span><br><span class="line">		<span class="title function_">execute</span>(task);</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">while</span> (nextTickQueue.<span class="title function_">hasTasks</span>()) &#123;</span><br><span class="line">			<span class="title function_">doNextTickTask</span>();</span><br><span class="line">		&#125;</span><br><span class="line">		</span><br><span class="line">		<span class="keyword">while</span> (promiseQueue.<span class="title function_">hasTasks</span>()) &#123;</span><br><span class="line">			<span class="title function_">doPromiseTask</span>();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue 中容易被忽视的小功能</title>
    <url>/2020/07/22/2020-07-22-things-need-to-be-aware-of-in-vue.html</url>
    <content><![CDATA[<p>在使用 Vue 2.0 开发项目的过程中，会发现有很多容易忽略或者令人迷惑的一些小方法和概念，本文针对这些”遗珠“进行了一个整理，方便分享和以后查阅。</p>
<span id="more"></span>
<h2 id="组件通信"><a href="#组件通信" class="headerlink" title="组件通信"></a>组件通信</h2><h3 id="Prop相关"><a href="#Prop相关" class="headerlink" title="Prop相关"></a>Prop相关</h3><p>prop 是父组件向子组件传递数据的一种方式，在父组件中使用 kebab-case 的方式传入数据，在子组件中可以添加 props 属性来获取传入的数据。这是一个单向的通信过程。</p>
<p>可以通过 v-bind 传入动态变量，也可以直接传入字符串值到子组件。</p>
<p>如果子组件中 props 属性中没有声明某些值，那么父组件传入的就是<strong>非 prop 的 attribute</strong>，对于绝大多数 attribute 来说，<strong>从外部提供给组件的值会替换掉组件内部设置好的值</strong>。所以如果传入 type=”text” 就会替换掉 type=”date” 并把它破坏！庆幸的是，<strong>class 和 style attribute</strong> 会稍微智能一些，即两边的值<strong>会被合并（merge）起来，从而得到最终的值</strong>。</p>
<p>可以使用 vm.$attr 属性来获取除 class 和 style 之外的非 prop 的 attribute，可以在父组件中使用 <code>v-bind=&quot;$attr&quot;</code> 传入，还可以通过子组件中的 <code>inheritAttrs: true</code> 字段来控制是否接受父组件传入的非 prop 的 attribute。</p>
<h2 id="事件"><a href="#事件" class="headerlink" title="事件"></a>事件</h2><h3 id="自定义事件"><a href="#自定义事件" class="headerlink" title="自定义事件"></a>自定义事件</h3><p>使用 v-on 绑定事件时，<strong>最好使用 kebab-case 命名法来命名事件</strong>，因为 v-on 事件监听器在 DOM 模板中会被自动转换为全小写 （因为 HTML 是大小写不敏感的）。</p>
<p>可以使用 <code>v-on=&quot;$listeners&quot;</code> 来将所有的事件监听器指向这个组件的某个特定的子元素，它是一个对象，里面包含了作用在这个组件上所有的监听器（监听事件）。这样，在子组件上可以绑定 vm.$listeners 中所包含的监听事件。</p>
<h3 id="事件监听"><a href="#事件监听" class="headerlink" title="事件监听"></a>事件监听</h3><p>除了 $emit 和 v-on 这一对好基友之外，vue 还提供了类似 总线（bus）机制的观察者模式的事件监听方案：</p>
<ul>
<li><code>$on(eventName, eventHandler)</code> 侦听一个事件</li>
<li><code>$once(eventName, eventHandler)</code> 一次性侦听一个事件</li>
<li><code>$off(eventName, eventHandler)</code> 停止侦听一个事件</li>
</ul>
<h3 id="修饰符"><a href="#修饰符" class="headerlink" title="修饰符"></a>修饰符</h3><ul>
<li>sync<ul>
<li>作用：对 prop 进行简单的“双向绑定”，可以实现简单的父子数据同步</li>
<li>使用：在父组件需要双向绑定的数据上添加 .sync 修饰符，例如 <code>&lt;div&gt;&lt;child :some-prop.sync=&quot;someProp&quot;&gt;&lt;/child&gt;&lt;/div&gt;</code> ；在子组件上使用 vm.$emit 触发数据更新，例如 <code>this.$emit(&#39;update:some-prop&#39;, val)</code></li>
</ul>
</li>
</ul>
<h2 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h2><h3 id="动态组件"><a href="#动态组件" class="headerlink" title="动态组件"></a>动态组件</h3><p>动态组件是通过 <component :is="componentName"></component> 来实现的，但是在切换组件时，会将每个组件的原有状态丢失，这时需要添加一个 keep-alive 标签，例如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">keep-alive</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">component</span> <span class="attr">v-bind:is</span>=<span class="string">&quot;currentTabComponent&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">component</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">keep-alive</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="异步组件"><a href="#异步组件" class="headerlink" title="异步组件"></a>异步组件</h3><p>Vue 允许你以一个工厂函数的方式定义你的组件，这个工厂函数会异步解析你的组件定义。Vue 只有在这个组件需要被渲染的时候才会触发该工厂函数，且会把结果缓存起来供未来重渲染。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="title function_">component</span>(</span><br><span class="line">  <span class="string">&#x27;async-webpack-example&#x27;</span>,</span><br><span class="line">  <span class="comment">// 这个动态导入会返回一个 `Promise` 对象。</span></span><br><span class="line">  <span class="function">() =&gt;</span> <span class="keyword">import</span>(<span class="string">&#x27;./my-async-component&#x27;</span>)</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<h2 id="Slot"><a href="#Slot" class="headerlink" title="Slot"></a>Slot</h2><h3 id="编译作用域"><a href="#编译作用域" class="headerlink" title="编译作用域"></a>编译作用域</h3><p>父级模板里的所有内容都是在父级作用域中编译的；子模板里的所有内容都是在子作用域中编译的。</p>
<h3 id="default内容"><a href="#default内容" class="headerlink" title="default内容"></a>default内容</h3><p>可以使用 <code>&lt;slot&gt; Default content &lt;/slot&gt;</code> 来设置分发内容的默认值，当没有内容传入时会显示该内容。</p>
<h3 id="具名插槽"><a href="#具名插槽" class="headerlink" title="具名插槽"></a>具名插槽</h3><p>有多个插槽需要进行内容分发时，可以使用具名插槽，具体用法如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 子元素设置具名插槽 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">header</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">slot</span> <span class="attr">name</span>=<span class="string">&quot;header&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">slot</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">header</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 父元素设置分发内容，由 v-slot 属性来指明具体的插槽名 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">template</span> <span class="attr">v-slot:header</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Here might be a page title<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h3 id="作用域插槽"><a href="#作用域插槽" class="headerlink" title="作用域插槽"></a>作用域插槽</h3><p>绑定在 <slot> 元素上的 attribute 被称为插槽 prop，为了让父元素能够访问到子组件中的数据，可以通过 v-bind 进行作用域的绑定，具体代码如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 子元素中需要在 slot 中使用 v-bind 绑定具体变量 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">slot</span> <span class="attr">v-bind:user</span>=<span class="string">&quot;user&quot;</span>&gt;</span></span><br><span class="line">    &#123;&#123; user.lastName &#125;&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">slot</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 父元素通过 v-slot 来指明子元素所 bind 的所有数据 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">child</span>&gt;</span></span><br><span class="line">	  <span class="tag">&lt;<span class="name">template</span> <span class="attr">v-slot</span>=<span class="string">&quot;slotProps&quot;</span>&gt;</span></span><br><span class="line">		    &#123;&#123; slotProps.user.firstName &#125;&#125;</span><br><span class="line">	  <span class="tag">&lt;/<span class="name">template</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">child</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong><em>v-slot 的缩写为 #</em></strong>，这个在具名插槽中有</p>
<h3 id="结构插槽Prop"><a href="#结构插槽Prop" class="headerlink" title="结构插槽Prop"></a>结构插槽Prop</h3><p>作用域插槽的内部工作原理是将你的插槽内容包裹在一个拥有单个参数的函数里，这意味着 v-slot 的值实际上可以是任何能够作为函数定义中的参数的 JavaScript 表达式。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- prop 重命名 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">current-user</span> <span class="attr">v-slot</span>=<span class="string">&quot;&#123; user: person &#125;&quot;</span>&gt;</span></span><br><span class="line">	  &#123;&#123; person.firstName &#125;&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">current-user</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 为 prop 指定默认值 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">current-user</span> <span class="attr">v-slot</span>=<span class="string">&quot;&#123; user = &#123; firstName: &#x27;Guest&#x27; &#125; &#125;&quot;</span>&gt;</span></span><br><span class="line">	  &#123;&#123; user.firstName &#125;&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">current-user</span>&gt;</span></span><br></pre></td></tr></table></figure>
<h2 id="边界情况处理"><a href="#边界情况处理" class="headerlink" title="边界情况处理"></a>边界情况处理</h2><h3 id="访问元素-amp-组件"><a href="#访问元素-amp-组件" class="headerlink" title="访问元素&amp;组件"></a>访问元素&amp;组件</h3><ul>
<li><p>访问根实例</p>
<p>  一般不建议触达另一个组件实例内部或手动操作 DOM 元素</p>
<p>  如果必须这么做，可以通过 this.$root 来访问根实例中的 data、computed 和 methods 等属性</p>
</li>
<li><p>访问父级元素</p>
<p>  同访问根实例，父级元素的访问是通过 this.$parent</p>
</li>
<li><p>访问子元素</p>
<p>  你可以通过 ref 这个 attribute 为子组件赋予一个 ID 引用，父组件可以通过 他会 this.$refs.someId 来访问子元素的所有属性</p>
</li>
<li><p>依赖注入</p>
<p>  当出现了多层嵌套的组件时，使用 $parent 属性无法很好获取到特定父级元素，这时需要采用依赖注入的方法 provide 和 inject，相当于爷组件和孙组件之间的 props：</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 爷组件中添加 provide 属性</span></span><br><span class="line"><span class="attr">provide</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">getMap</span>: <span class="variable language_">this</span>.<span class="property">getMap</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 孙组件中添加 inject 属性</span></span><br><span class="line"><span class="attr">inject</span>: [<span class="string">&#x27;getMap&#x27;</span>]</span><br></pre></td></tr></table></figure>
</li>
<li><p>循环引用</p>
<ul>
<li>组件递归引用要避免</li>
<li><p>不同组件间相互引用，如果产生循环引用（例如 Tree 文件系统或级联系统，模块系统会不知道到底哪个是初始组件），可以采用如下两种方式来解决：</p>
<ul>
<li><p>在生命周期钩子 beforeCreate 中进行组件的引入：</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="attr">beforeCreate</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">$options</span>.<span class="property">components</span>.<span class="property">TreeFolderContents</span> = <span class="built_in">require</span>(<span class="string">&#x27;./tree-folder-contents.vue&#x27;</span>).<span class="property">default</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>引用组件时采用异步方式：</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="attr">components</span>: &#123;</span><br><span class="line">  <span class="title class_">TreeFolderContents</span>: <span class="function">() =&gt;</span> <span class="keyword">import</span>(<span class="string">&#x27;./tree-folder-contents.vue&#x27;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="过渡-amp-动画"><a href="#过渡-amp-动画" class="headerlink" title="过渡&amp;动画"></a>过渡&amp;动画</h2><h3 id="单元素-组件的过渡"><a href="#单元素-组件的过渡" class="headerlink" title="单元素/组件的过渡"></a>单元素/组件的过渡</h3><p>使用 <transition name="someTransition"></transition> 封装所要添加过渡的元素。这种方式适用于以下四种情况（都是单个组件显隐）：</p>
<ul>
<li>条件渲染 (使用 <code>v-if</code>)</li>
<li>条件展示 (使用 <code>v-show</code>)</li>
<li>动态组件</li>
<li>组件根节点</li>
</ul>
<p>封装好元素后，还需要提供相应的 css 样式，分别由六种状态的样式名，需要根据 name 属性进行相应的设置：someTransition-enter 表示进入过渡开始状态，someTransition-enter-active 表示进入过渡生效状态，someTransition-enter-to 表示进入过渡结束状态，someTransition-leave，someTransition-leave-active，someTransition-leave-to。</p>
<p>也可以自定义类名，相对应上面六种默认的样式，可以在 transition 标签中添加 enter-class，enter-active-class，enter-to-class，leave-class，leave-active-class，leave-to-class。</p>
<p>transition 标签中可以指定进入和离开过渡的时长，可以通过 <code>:duration=&quot;&#123; enter: 500, leave: 800 &#125;&quot;</code> 实现</p>
<p>transition 标签中可以指定过渡各阶段的钩子，以用来绑定相应的事件，包括 @before-enter、@enter、@after-enter、@enter-cancelled、@before-leave、@leave、@after-leave、@leave-cancelled 八种钩子：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 enter 和 leave 钩子事件，如果没有 css 控制元素过渡，那么一定需要有 done() 回调</span></span><br><span class="line"><span class="comment">// 否则 enter 和 leave 钩子事件会同时完成</span></span><br><span class="line"><span class="attr">beforeEnter</span>: <span class="keyword">function</span> (<span class="params">el</span>) &#123;&#125;,</span><br><span class="line"><span class="attr">enter</span>: <span class="keyword">function</span> (<span class="params">el, done</span>) &#123;</span><br><span class="line">    <span class="title function_">done</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>transition 标签中可以使用 appear 属性来设置元素初始化过渡的样式，用法与 enter 和 leave 相同，可以使用 someTrasition-appear 类或者自定义类名属性，当然也支持钩子的实现。</p>
<h3 id="多元素的过渡"><a href="#多元素的过渡" class="headerlink" title="多元素的过渡"></a>多元素的过渡</h3><p>多元素使用过渡时，可以在最外层使用 transition 标签进行包装，用法与单元素相同，但是有一点需要注意——使用 v-if 或者 v-for 设置多个元素时，一定要在每个元素上绑定自己的 key。</p>
<p>多个元素切换显隐状态时，Vue 提供了一个 mode 属性来进行平滑过渡：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;transition name=<span class="string">&quot;fade&quot;</span> mode=<span class="string">&quot;out-in&quot;</span>&gt;&lt;/transition&gt;</span><br></pre></td></tr></table></figure>
<h3 id="多组件的过渡"><a href="#多组件的过渡" class="headerlink" title="多组件的过渡"></a>多组件的过渡</h3><p>多组件的过渡，使用之前所提到的动态组件即可实现，在过渡时可以在组件外包装一层 transition，指定过渡的样式名 name 属性，就可以使用上面提到的样式或钩子进行过渡样式的实现，当然也可以添加所需要的 mode：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;transition name=<span class="string">&quot;component-fade&quot;</span> mode=<span class="string">&quot;out-in&quot;</span>&gt;</span><br><span class="line">  <span class="language-xml"><span class="tag">&lt;<span class="name">component</span> <span class="attr">v-bind:is</span>=<span class="string">&quot;view&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">component</span>&gt;</span></span></span><br><span class="line">&lt;/transition&gt;</span><br></pre></td></tr></table></figure>
<p>对于列表的过渡，可以使用 <transition-group> 包装 v-for 渲染出的列表，指明过渡样式名 name 属性，即可用上面相同的方式进行过渡的实现。在列表打乱或重新排序时，Vue 提供了 someTransition-move 的类名来实现：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">v-on:click</span>=<span class="string">&quot;shuffle&quot;</span>&gt;</span>Shuffle<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">transition-group</span> <span class="attr">name</span>=<span class="string">&quot;flip-list&quot;</span> <span class="attr">tag</span>=<span class="string">&quot;ul&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span> <span class="attr">v-for</span>=<span class="string">&quot;item in items&quot;</span> <span class="attr">v-bind:key</span>=<span class="string">&quot;item&quot;</span>&gt;</span></span><br><span class="line">	      &#123;&#123; item &#125;&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">transition-group</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="title class_">Vue</span>(&#123;</span><br><span class="line">  <span class="attr">el</span>: <span class="string">&#x27;#flip-list-demo&#x27;</span>,</span><br><span class="line">  <span class="attr">data</span>: &#123;</span><br><span class="line">    <span class="attr">items</span>: [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>]</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">methods</span>: &#123;</span><br><span class="line">    <span class="attr">shuffle</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">items</span> = _.<span class="title function_">shuffle</span>(<span class="variable language_">this</span>.<span class="property">items</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.flip-list-move</span> &#123;</span><br><span class="line">  <span class="attribute">transition</span>: transform <span class="number">1s</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Vue</tag>
        <tag>Framework</tag>
      </tags>
  </entry>
  <entry>
    <title>Vue 高级用法与精粹</title>
    <url>/2020/07/23/2020-07-23-advanced-function-in-vue.html</url>
    <content><![CDATA[<p>本文针对 Vue 2.0 中的复用与组合、自定义指令、渲染函数、插件和过滤器等高级功能进行学习和总结。</p>
<span id="more"></span>
<h2 id="复用与组合"><a href="#复用与组合" class="headerlink" title="复用与组合"></a>复用与组合</h2><h3 id="Mixin"><a href="#Mixin" class="headerlink" title="Mixin"></a>Mixin</h3><p>多重继承大致有两种模式，一种就是按照顺序将各个父类进行继承，例如当你需要一个类继承 A、B、C和D的特性，那么可以采用 A ⇒ B ⇒ C ⇒ D 这样一个继承链去继承，这种继承方式的好处是知道每一步继承的父子关系。而大多数情况我们不需要知道这些，只要像 duck typing 一样拥有这些父类的特性即可，Mixin 混入机制就是来解决这个问题。Vue 中也提供给了这样的机制：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> myMixin = &#123;</span><br><span class="line">  <span class="attr">methods</span>: &#123;</span><br><span class="line">    <span class="attr">hello</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;hello from mixin!&#x27;</span>)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> <span class="title class_">Component</span> = <span class="title class_">Vue</span>.<span class="title function_">extend</span>(&#123;</span><br><span class="line">  <span class="attr">mixins</span>: [myMixin]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>比较好理解的，像 methods、computed 这种本身是对象类型的属性，可以直接进行 merge，所遵循的混合规则如下：</p>
<ul>
<li>数据对象在内部会进行递归合并，并在发生冲突时<strong>以组件数据优先</strong></li>
<li>同名钩子函数将合并为一个数组，数组中的所有函数都将在此钩子生效时执行</li>
</ul>
<p>混入也支持全局注入，但是需要注意！这会影响到之后的每一个 Vue 组件实例（不建议使用）：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="title function_">mixin</span>(&#123;</span><br><span class="line">  <span class="attr">created</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">var</span> myOption = <span class="variable language_">this</span>.<span class="property">$options</span>.<span class="property">myOption</span></span><br><span class="line">    <span class="keyword">if</span> (myOption) &#123;</span><br><span class="line">      <span class="variable language_">console</span>.<span class="title function_">log</span>(myOption)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<h2 id="自定义指令"><a href="#自定义指令" class="headerlink" title="自定义指令"></a>自定义指令</h2><p>除了 v-model 和 v-show 等自带的指令之外，也可以自定义指令。自定义指令可以全局注册，也可以在组件内进行局部注册（指令在标签中必须要以 v- 开头）：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="title function_">directive</span>(<span class="string">&#x27;focus&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">inserted</span>: <span class="keyword">function</span> (<span class="params">el</span>) &#123;</span><br><span class="line">    el.<span class="title function_">focus</span>()</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="attr">directives</span>: &#123;</span><br><span class="line">  <span class="attr">focus</span>: &#123;</span><br><span class="line">    <span class="attr">inserted</span>: <span class="keyword">function</span> (<span class="params">el</span>) &#123;</span><br><span class="line">      el.<span class="title function_">focus</span>()</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面代码中出现的 inserted 是指令所在的钩子，指令的钩子有以下五种：</p>
<ul>
<li><code>bind</code>：只调用一次，指令第一次绑定到元素时调用。在这里可以进行一次性的初始化设置。</li>
<li><code>inserted</code>：被绑定元素插入父节点时调用 (仅保证父节点存在，但不一定已被插入文档中)。</li>
<li><code>update</code>：所在组件的 VNode 更新时调用，<strong>但是可能发生在其子 VNode 更新之前</strong>。指令的值可能发生了改变，也可能没有。但是你可以通过比较更新前后的值来忽略不必要的模板更新 (详细的钩子函数参数见下)。</li>
<li><code>componentUpdated</code>：指令所在组件的 VNode <strong>及其子 VNode</strong> 全部更新后调用。</li>
<li><code>unbind</code>：只调用一次，指令与元素解绑时调用。</li>
</ul>
<p>上述的钩子函数的参数有四个，分别是 el、binding、vnode、oldVnode，其中，指令的值需要从 binding.value 中获取。                                                             </p>
<p>自定义指令还可以添加动态参数，v-someDirection:[arg]，使用 binding.arg 来获取动态的指令：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;dynamicexample&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">p</span> <span class="attr">v-pin:</span>[<span class="attr">direction</span>]=<span class="string">&quot;200&quot;</span>&gt;</span>I am pinned onto the page at 200px to the left.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="title function_">directive</span>(<span class="string">&#x27;pin&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">bind</span>: <span class="keyword">function</span> (<span class="params">el, binding, vnode</span>) &#123;</span><br><span class="line">    el.<span class="property">style</span>.<span class="property">position</span> = <span class="string">&#x27;fixed&#x27;</span></span><br><span class="line">    <span class="keyword">var</span> s = (binding.<span class="property">arg</span> == <span class="string">&#x27;left&#x27;</span> ? <span class="string">&#x27;left&#x27;</span> : <span class="string">&#x27;top&#x27;</span>)</span><br><span class="line">    el.<span class="property">style</span>[s] = binding.<span class="property">value</span> + <span class="string">&#x27;px&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 回调函数可以简写，只支持 bind 和 update 两个钩子同时触发时</span></span><br><span class="line"><span class="title class_">Vue</span>.<span class="title function_">directive</span>(<span class="string">&#x27;pin&#x27;</span>, <span class="keyword">function</span> (<span class="params">el, binding, vnode</span>) &#123;&#125;);</span><br></pre></td></tr></table></figure>
<p>自定义指令也支持传入对象字面量 <code>&lt;div v-demo=&quot;&#123; color: &#39;white&#39;, text: &#39;hello!&#39; &#125;&quot;&gt;&lt;/div&gt;</code></p>
<h2 id="渲染函数"><a href="#渲染函数" class="headerlink" title="渲染函数"></a>渲染函数</h2><p>如果想要使用纯 js 的方式来实现视图，可以使用 Vue 提供的 render 函数属性来进行渲染，下面是一个简单的例子：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Vue</span>.<span class="title function_">component</span>(<span class="string">&#x27;anchored-heading&#x27;</span>, &#123;</span><br><span class="line">  <span class="attr">render</span>: <span class="keyword">function</span> (<span class="params">createElement</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">createElement</span>(</span><br><span class="line">      <span class="string">&#x27;h&#x27;</span> + <span class="variable language_">this</span>.<span class="property">level</span>,   <span class="comment">// 标签名称</span></span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">$slots</span>.<span class="property">default</span> <span class="comment">// 子节点数组</span></span><br><span class="line">    )</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">props</span>: &#123;</span><br><span class="line">    <span class="attr">level</span>: &#123;</span><br><span class="line">      <span class="attr">type</span>: <span class="title class_">Number</span>,</span><br><span class="line">      <span class="attr">required</span>: <span class="literal">true</span></span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>createElement 这个函数可以创建出一个虚拟 Dom，它所包含的信息会告诉 Vue 页面上需要渲染什么样的节点，包括及其子节点的描述信息，所以也被称为 Vnode。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @returns &#123;VNode&#125;</span></span><br><span class="line"><span class="title function_">createElement</span>(</span><br><span class="line">  <span class="comment">// 必填项一个 HTML 标签名、组件选项对象，或者 resolve 了上述任何一种的一个 async 函数</span></span><br><span class="line">  <span class="comment">// &#123;String | Object | Function&#125;</span></span><br><span class="line">  <span class="string">&#x27;div&#x27;</span>,</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 一个与模板中 attribute 对应的**数据对象**，可选项，&#123;Object&#125;。</span></span><br><span class="line">  &#123;&#125;,</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 子级虚拟节点 (VNodes)，由 `createElement()` 构建而成，也可以使用字符串来生成“文本虚拟节点”，可选。</span></span><br><span class="line">	<span class="comment">// &#123;String | Array&#125;</span></span><br><span class="line">  [</span><br><span class="line">    <span class="string">&#x27;先写一些文字&#x27;</span>,</span><br><span class="line">    <span class="title function_">createElement</span>(<span class="string">&#x27;h1&#x27;</span>, <span class="string">&#x27;一则头条&#x27;</span>),</span><br><span class="line">    <span class="title function_">createElement</span>(<span class="title class_">MyComponent</span>, &#123;</span><br><span class="line">      <span class="attr">props</span>: &#123;</span><br><span class="line">        <span class="attr">someProp</span>: <span class="string">&#x27;foobar&#x27;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;)</span><br><span class="line">  ]</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>上面讲到的数据对象，也就是一个节点中的一些属性描述：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="comment">// 与 `v-bind:class` 的 API 相同，</span></span><br><span class="line">  <span class="comment">// 接受一个字符串、对象或字符串和对象组成的数组</span></span><br><span class="line">  <span class="string">&#x27;class&#x27;</span>: &#123;</span><br><span class="line">    <span class="attr">foo</span>: <span class="literal">true</span>,</span><br><span class="line">    <span class="attr">bar</span>: <span class="literal">false</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 与 `v-bind:style` 的 API 相同，</span></span><br><span class="line">  <span class="comment">// 接受一个字符串、对象，或对象组成的数组</span></span><br><span class="line">  <span class="attr">style</span>: &#123;</span><br><span class="line">    <span class="attr">color</span>: <span class="string">&#x27;red&#x27;</span>,</span><br><span class="line">    <span class="attr">fontSize</span>: <span class="string">&#x27;14px&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 普通的 HTML attribute</span></span><br><span class="line">  <span class="attr">attrs</span>: &#123;</span><br><span class="line">    <span class="attr">id</span>: <span class="string">&#x27;foo&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 组件 prop</span></span><br><span class="line">  <span class="attr">props</span>: &#123;</span><br><span class="line">    <span class="attr">myProp</span>: <span class="string">&#x27;bar&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// DOM property</span></span><br><span class="line">  <span class="attr">domProps</span>: &#123;</span><br><span class="line">    <span class="attr">innerHTML</span>: <span class="string">&#x27;baz&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 事件监听器在 `on` 内，</span></span><br><span class="line">  <span class="comment">// 但不再支持如 `v-on:keyup.enter` 这样的修饰器。</span></span><br><span class="line">  <span class="comment">// 需要在处理函数中手动检查 keyCode。</span></span><br><span class="line">  <span class="attr">on</span>: &#123;</span><br><span class="line">    <span class="attr">click</span>: <span class="variable language_">this</span>.<span class="property">clickHandler</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 仅用于组件，用于监听原生事件，而不是组件内部使用</span></span><br><span class="line">  <span class="comment">// `vm.$emit` 触发的事件。</span></span><br><span class="line">  <span class="attr">nativeOn</span>: &#123;</span><br><span class="line">    <span class="attr">click</span>: <span class="variable language_">this</span>.<span class="property">nativeClickHandler</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 自定义指令。注意，你无法对 `binding` 中的 `oldValue`</span></span><br><span class="line">  <span class="comment">// 赋值，因为 Vue 已经自动为你进行了同步。</span></span><br><span class="line">  <span class="attr">directives</span>: [</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="attr">name</span>: <span class="string">&#x27;my-custom-directive&#x27;</span>,</span><br><span class="line">      <span class="attr">value</span>: <span class="string">&#x27;2&#x27;</span>,</span><br><span class="line">      <span class="attr">expression</span>: <span class="string">&#x27;1 + 1&#x27;</span>,</span><br><span class="line">      <span class="attr">arg</span>: <span class="string">&#x27;foo&#x27;</span>,</span><br><span class="line">      <span class="attr">modifiers</span>: &#123;</span><br><span class="line">        <span class="attr">bar</span>: <span class="literal">true</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  ],</span><br><span class="line">  <span class="comment">// 作用域插槽的格式为</span></span><br><span class="line">  <span class="comment">// &#123; name: props =&gt; VNode | Array&lt;VNode&gt; &#125;</span></span><br><span class="line">  <span class="attr">scopedSlots</span>: &#123;</span><br><span class="line">    <span class="attr">default</span>: <span class="function"><span class="params">props</span> =&gt;</span> <span class="title function_">createElement</span>(<span class="string">&#x27;span&#x27;</span>, props.<span class="property">text</span>)</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 如果组件是其它组件的子组件，需为插槽指定名称</span></span><br><span class="line">  <span class="attr">slot</span>: <span class="string">&#x27;name-of-slot&#x27;</span>,</span><br><span class="line">  <span class="comment">// 其它特殊顶层 property</span></span><br><span class="line">  <span class="attr">key</span>: <span class="string">&#x27;myKey&#x27;</span>,</span><br><span class="line">  <span class="attr">ref</span>: <span class="string">&#x27;myRef&#x27;</span>,</span><br><span class="line">  <span class="comment">// 如果你在渲染函数中给多个元素都应用了相同的 ref 名，</span></span><br><span class="line">  <span class="comment">// 那么 `$refs.myRef` 会变成一个数组。</span></span><br><span class="line">  <span class="attr">refInFor</span>: <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="插件"><a href="#插件" class="headerlink" title="插件"></a>插件</h2><p>通过 Vue.use() 方法使用插件，必须要在 new Vue() 实例化之前进行注册。Vue.use 会自动阻止多次注册相同插件，届时即使多次调用也只会注册一次该插件。</p>
<h2 id="过滤器"><a href="#过滤器" class="headerlink" title="过滤器"></a>过滤器</h2><p>Vue.js 允许你自定义过滤器（filters 可作为与 computed 同一级的属性进行配置），可被用于一些常见的文本格式化。过滤器可以用在两个地方：双花括号插值和 v-bind 表达式：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 在双花括号中 --&gt;</span></span><br><span class="line">&#123;&#123; message | capitalize &#125;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 在 `v-bind` 中，过滤器可以串联，前一个的输出会作为下一个的输入 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">v-bind:id</span>=<span class="string">&quot;rawId | capitalize | formatId&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Vue</tag>
        <tag>Framework</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 vue-test-utils 进行测试</title>
    <url>/2020/07/29/2020-07-29-use-vue-test-utils-to-test-components.html</url>
    <content><![CDATA[<p>针对 Vue 2.0 的项目，怎样更简单方便地进行测试？本文对 vue-test-util 工具的使用方法进行了简单总结。</p>
<span id="more"></span>
<h3 id="插件安装"><a href="#插件安装" class="headerlink" title="插件安装"></a>插件安装</h3><p>我们使用 Vue CLI 脚手架进行项目的创建，按照提示一步步进行项目配置，尽量都选择无任何配置，尤其是自带的测试插件：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">vue create vue-test</span><br></pre></td></tr></table></figure>
<p>新建好项目之后，我们需要在项目根目录下<strong>新建一个 test 目录</strong>来放置我们的测试文件。</p>
<p>由于我们是在 Vue 项目中进行测试，采用的是 vue-test-utils 这个测试工具插件进行测试，所以下面几类 npm 插件是必须要安装的：</p>
<h3 id="jest-相关"><a href="#jest-相关" class="headerlink" title="jest 相关"></a>jest 相关</h3><pre><code>jest 是 vue-test-utils 官方推荐的测试运行器，所以这个插件是必要的；要使用 jest 来处理 *.vue 文件，需要安装和配置 vue-jest 插件。

<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">npm i --save-dev jest vue-jest</span><br></pre></td></tr></table></figure>

<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 package.json 中做如下配置</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="string">&quot;scripts&quot;</span>: &#123;</span><br><span class="line">		<span class="string">&quot;test&quot;</span>: <span class="string">&quot;jest&quot;</span></span><br><span class="line">	&#125;,</span><br><span class="line">	<span class="string">&quot;jest&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;moduleFileExtensions&quot;</span>: [</span><br><span class="line">      <span class="string">&quot;js&quot;</span>,</span><br><span class="line">      <span class="string">&quot;json&quot;</span>,</span><br><span class="line">      <span class="string">&quot;vue&quot;</span></span><br><span class="line">    ]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</code></pre><h3 id="babel-相关"><a href="#babel-相关" class="headerlink" title="babel 相关"></a>babel 相关</h3><pre><code>我们一般都不可避免地希望在代码中使用 ES2015 的特性，这时我们需要安装 babel 相关的插件，其中 babel-jest 是测试中的 ES2015 特性编译的插件

<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">npm install --save-dev babel-core babel-preset-env babel-jest</span><br></pre></td></tr></table></figure>

<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 package.json 中做如下配置</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="string">&quot;jest&quot;</span>: &#123;</span><br><span class="line">		<span class="string">&quot;transform&quot;</span>: &#123;</span><br><span class="line">      <span class="string">&quot;.*\\.(vue)$&quot;</span>: <span class="string">&quot;vue-jest&quot;</span>,</span><br><span class="line">      <span class="string">&quot;^.+\\.js$&quot;</span>: <span class="string">&quot;&lt;rootDir&gt;/node_modules/babel-jest&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在 .babelrc 或 babel.config.js 文件中做如下配置</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;presets&quot;</span>: [[<span class="string">&quot;env&quot;</span>, &#123; <span class="string">&quot;modules&quot;</span>: <span class="literal">false</span> &#125;]],</span><br><span class="line">  <span class="string">&quot;env&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;test&quot;</span>: &#123;</span><br><span class="line">      <span class="string">&quot;presets&quot;</span>: [[<span class="string">&quot;env&quot;</span>, &#123; <span class="string">&quot;targets&quot;</span>: &#123; <span class="string">&quot;node&quot;</span>: <span class="string">&quot;current&quot;</span> &#125; &#125;]]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</code></pre><ul>
<li><p>vue-test-utils 依赖浏览器环境，我们使用 JSDOM 在 Node 虚拟浏览器环境运行测试。jest 中有自动设置 JSDOM，如果没有，需要安装以下插件</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">npm install --save-dev jsdom jsdom-<span class="variable language_">global</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>至此，所以前期的插件安装和配置工作就完成了。这时，我们在 test 目录下新建一个 *.test.js 文件，并在 terminal 中运行 npm run test 命令，就可以进行 vue 组件的测试了。</p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>Vue</tag>
        <tag>Test</tag>
      </tags>
  </entry>
  <entry>
    <title>Ontology本体基础知识总结整理</title>
    <url>/2015/09/21/2015-09-21-ontology-concepts-conclusion.html</url>
    <content><![CDATA[<h4 id="本体的概念"><a href="#本体的概念" class="headerlink" title="本体的概念"></a>本体的概念</h4><p>共享概念模型的明确的形式化规范说明。包括四个主要的方面：</p>
<ul>
<li>概念化（conceptualization）：客观世界的现象的抽象模型；</li>
<li>明确（explicit）：概念及它们之间联系都被精确定义；</li>
<li>形式化（formal）：精确的数学描述；</li>
<li>共享（share）：本体中反映的知识是其使用者共同认可的。</li>
</ul>
<span id="more"></span>
<h4 id="本体构成要素"><a href="#本体构成要素" class="headerlink" title="本体构成要素"></a>本体构成要素</h4><ul>
<li>类：集合（sets）、概念、对象类型或者说事物的种类；</li>
<li>关系：关系代表了在领域中类之间的交互作用。形式上定义为n 维笛卡儿乘积的子集： R ： C1 ×C2×⋯×Cn 。 如：子类关系( subclass-of)；</li>
<li>函数： 函数是一类特殊的关系。在这种关系中前n - 1 个元素可以惟一决定第n 个元素。形式化的定义如下： F ： C1 ×C2 ×⋯×Cn-1→Cn 。例如Mother-of 关系就是一个函数，其中Mother-of ( x ， y) 表示y 是x 的母亲，显然x 可以惟一确定他的母亲y；</li>
<li>公理：公理代表永真断言，比如概念乙属于概念甲的范围；</li>
<li>实例：实例代表元素，即某个类中的第一个对象；</li>
</ul>
<h4 id="专家系统"><a href="#专家系统" class="headerlink" title="专家系统"></a>专家系统</h4><ul>
<li>知识库：</li>
<li>推理机：本体按照是否具备推理功能可以分为轻量级本体、中级本体和重量级本体；技术构成库应该属于可以识别一阶谓词逻辑表达式的中级本体</li>
</ul>
<h4 id="本体语言"><a href="#本体语言" class="headerlink" title="本体语言"></a>本体语言</h4><ul>
<li>本体语言目前成为标准的是OWL；</li>
<li>Cyc和loom两种语言具有较强的推理能力；</li>
</ul>
<h4 id="领域本体的知识库构建"><a href="#领域本体的知识库构建" class="headerlink" title="领域本体的知识库构建"></a>领域本体的知识库构建</h4><ul>
<li><p>本体构建原则：清晰、一致、可扩展性、编码偏好程度最小、本体约定最小；</p>
</li>
<li><p>两种模式：</p>
<ol>
<li>利用现有文献和领域专家使用手工的方式创建概念关联；</li>
<li>将已有的叙词表改造成本体，或者采用学习机制，进行自动或自动化的本体构建。</li>
<li><p>本体手工构建方法：骨架法、企业建模法、Methontology、KACTUS、循环获取法、IDEF-5、七步法；</p>
<p>七步法：</p>
<ul>
<li>确定本体的专业领域与范畴；</li>
<li>考查复用现有本体的可能性；</li>
<li>列出本体中的重要术语；</li>
<li>定义类和类的等级关系；</li>
<li>定义类的属性；</li>
<li>定义属性的分面(Facets)；</li>
<li>创建实例。</li>
</ul>
</li>
<li><p>领域本体知识库架构三个层次：</p>
<ul>
<li>表示层：表示层是用户与系统交互的接口，用户通过浏览器或其他界面访问系统，用户界面负责接收查询请求， 并将服务端的检索结果回显给用户； </li>
<li>业务逻辑层：业务逻辑层为主要应用逻辑层，实现系统知识的检索。它由本体管理组件、语义分析组件、推理引擎、查 询组件和信息获取组件5个部分组成；</li>
<li>数据层：数据层包括3个部分：本体库、资源描述库、资源数据库。它是知识库的存储介质，创建并提炼出结构化 的知识本体，是知识检索的直接来源；</li>
</ul>
</li>
</ol>
</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Ontology</tag>
        <tag>基础知识</tag>
        <tag>Knowledge Base</tag>
      </tags>
  </entry>
  <entry>
    <title>前端要学习的http基础知识</title>
    <url>/2017/09/21/2017-10-05-http-primary-knowledge-in-Frontend.html</url>
    <content><![CDATA[<h4 id="HTTP协议基础"><a href="#HTTP协议基础" class="headerlink" title="HTTP协议基础"></a>HTTP协议基础</h4><h5 id="HTTP"><a href="#HTTP" class="headerlink" title="HTTP"></a>HTTP</h5><p>Hyper Text Transfer Protocol(超文本传输协议)，基于TCP/IP通信协议来传递数据，默认端口号80。HTTP协议是前端技术基础中的基础，所以掌握其相关知识也是必要技能之一。</p>
<span id="more"></span>
<h5 id="HTTP工作原理"><a href="#HTTP工作原理" class="headerlink" title="HTTP工作原理"></a>HTTP工作原理</h5><p>工作于C/S架构上，浏览器作为http客户端通过URL向WEB服务器（Apache、IIS）发送所有请求</p>
<h5 id="HTTP的特点"><a href="#HTTP的特点" class="headerlink" title="HTTP的特点"></a>HTTP的特点</h5><ul>
<li>无连接：每次连接只处理一个请求（节省传输时间）</li>
<li>媒体独立：任何类型内容数据都可通过HTTP发送（制定MIME-type内容类型）</li>
<li>无状态：对事务没有记忆能力，若要在当前状态进行后续处理，则必须重传（导致每次连接传送的数据量增大，但是应答比较快）</li>
</ul>
<h5 id="HTTP请求与返回"><a href="#HTTP请求与返回" class="headerlink" title="HTTP请求与返回"></a>HTTP请求与返回</h5><ul>
<li><p>HTTP请求：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable constant_">GET</span> / <span class="variable constant_">HTTP</span>/<span class="number">1.0</span></span><br><span class="line"><span class="title class_">User</span>-<span class="title class_">Agent</span>: <span class="title class_">Mozilla</span>/<span class="number">5.0</span> (<span class="title class_">Macintosh</span>; <span class="title class_">Intel</span> <span class="title class_">Mac</span> <span class="variable constant_">OS</span> X <span class="number">10_10_5</span>)</span><br><span class="line"><span class="title class_">Accept</span>: *<span class="comment">/*</span></span><br></pre></td></tr></table></figure>
<p>  其中第一行为请求命令，命令有POST/GET/PUT/DELETE等等，且请求的资源后面要添加所使用的协议版本，详见附录；第三行中的Accept指定的是可以接受的MIME type，与回应头中的Content-Type字段相对应。</p>
</li>
<li><p>HTTP回应：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="variable constant_">HTTP</span>/<span class="number">1.0</span> <span class="number">200</span> <span class="variable constant_">OK</span> </span><br><span class="line"><span class="title class_">Content</span>-<span class="title class_">Type</span>: text/plain</span><br><span class="line"><span class="title class_">Content</span>-<span class="title class_">Length</span>: <span class="number">137582</span></span><br><span class="line"><span class="title class_">Expires</span>: <span class="title class_">Thu</span>, <span class="number">05</span> <span class="title class_">Dec</span> <span class="number">1997</span> <span class="number">16</span>:<span class="number">00</span>:<span class="number">00</span> <span class="variable constant_">GMT</span></span><br><span class="line"><span class="title class_">Last</span>-<span class="title class_">Modified</span>: <span class="title class_">Wed</span>, <span class="number">5</span> <span class="title class_">August</span> <span class="number">1996</span> <span class="number">15</span>:<span class="number">55</span>:<span class="number">28</span> <span class="variable constant_">GMT</span></span><br><span class="line"><span class="title class_">Server</span>: <span class="title class_">Apache</span> <span class="number">0.84</span></span><br><span class="line"></span><br><span class="line">&lt;html&gt;</span><br><span class="line">  <span class="language-xml"><span class="tag">&lt;<span class="name">body</span>&gt;</span>Hello World<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span></span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>
<p>  回应信息分为两部分，一部分是回应头信息，一部分是回应的数据和内容。其中第一行是”协议版本 + 状态码（status code） + 状态描述”；第二行是用来指定数据格式的”Content-Type字段”。</p>
</li>
</ul>
<h4 id="各版本HTTP协议的区别"><a href="#各版本HTTP协议的区别" class="headerlink" title="各版本HTTP协议的区别"></a>各版本HTTP协议的区别</h4><ul>
<li><p>HTTP/0.9</p>
<p>  只有GET一个请求命令，而且服务器只能回应HTML格式的字符串，服务器发送完毕，TCP连接就关闭。</p>
</li>
<li><p>HTTP/1.0</p>
<p>  完善了POST和HEAD命令，服务器可以回应更多种格式的（MIME type/多种压缩方法）数据，但是需要在回应中有一个header来描述元数据。另外添加了状态吗、权限、缓存等等。</p>
<p>  但是与0.9版本相同的是，每个TCP连接只能发送一个请求，发送完成就关闭连接。所以有一个hack方法：Connection字段设置为keep-alive来实现保持连接和请求复用。</p>
</li>
<li><p>HTTP/1.1</p>
<p>  添加了PUT/PATCH/HEAD/OPTIONS/DELETE请求命令，以及HOST字段指定服务器的域名（该字段可以将请求发往同一台服务器的不同网站，是虚拟主机实现的基础）；</p>
<p>  最大的改动是<strong>默认服务器连接为keep-alive</strong>，不需要声明Connection字段，但是如果主动关闭连接需要添加Connection: close的字段；添加<strong>管道机制</strong>，即在同一个TCP连接里面，客户端可以同时发送多个请求；在管道机制之上需要有区分数据包的机制，该版本提供了Content-Length字段；有了管道机制，就可以实现<strong>分块传输编码</strong>（不使用Content-Length字段的方法），只需要设置Transfer-Encoding字段为chunked即可，每一个返回的内容数据块之前苏姚添加该数据块的长度，最后一个长度需要表示为0来标识回应数据结束。</p>
<p>  1.1版本中唯一的缺点是数据通信单线程按次序进行，非常容易堵塞，所以要求网页设计时减少请求书并多开持久连接；</p>
</li>
<li><p>HTTP/2.0</p>
<p>  该版本主要有四个亮点：</p>
<ul>
<li>二进制协议：头信息和数据块都可以是二进制（被称为头信息帧和数据帧），更加容易解析；</li>
<li>多工通信：在同一个连接里，S/C两端可以同时发送多个请求和回应，避免单线程堵塞；这个技术也为服务器推送奠定了基础；</li>
<li>数据流： 在多工通信的前提下，请求和回应数据必须进行标记（ID），所以提出了数据流概念，奇数表明客户端发出数据流，偶数则为服务器发出；另外客户端还可以指定请求数据流的优先级；</li>
<li>头信息压缩：针对Cookie、User Agent字段的重复和冗余，引入头信息压缩机制，可以使用gzip和compress压缩后再发送，提高速度。</li>
</ul>
</li>
</ul>
<h4 id="附录"><a href="#附录" class="headerlink" title="附录"></a>附录</h4><h5 id="HTTP-header字段（通用头，请求头，响应头，实体头）"><a href="#HTTP-header字段（通用头，请求头，响应头，实体头）" class="headerlink" title="HTTP header字段（通用头，请求头，响应头，实体头）"></a>HTTP header字段（通用头，请求头，响应头，实体头）</h5><ul>
<li><p>通用头：Cache-Control/Connection/Date/Pragma/Transfer-Encoding/Upgrade/Via</p>
</li>
<li><p>请求头：</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>字段名</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>Accept</td>
<td>指定客户端能够接收的内容类型</td>
<td></td>
</tr>
<tr>
<td>Accept-Charset</td>
<td>浏览器可以接受的字符编码集</td>
<td></td>
</tr>
<tr>
<td>Accept-Encoding</td>
<td>指定浏览器可以支持的web服务器返回内容压缩编码类型</td>
<td></td>
</tr>
<tr>
<td>Accept-Language</td>
<td>浏览器可接受的语言</td>
<td></td>
</tr>
<tr>
<td>Accept-Ranges</td>
<td>可以请求网页实体的一个或者多个子范围字段</td>
<td></td>
</tr>
<tr>
<td>Authorization</td>
<td>HTTP授权的授权证书</td>
<td></td>
</tr>
<tr>
<td>Cookie</td>
<td>HTTP请求发送时，会把保存该请求域名下所有cookie发给服务器</td>
<td></td>
</tr>
<tr>
<td>Content-Length</td>
<td>请求的内容长度</td>
<td>Content-Length: 348</td>
</tr>
<tr>
<td>Content-Type</td>
<td>请求的与实体对应的MIME信息</td>
<td></td>
</tr>
<tr>
<td>Expect</td>
<td>请求的特定的服务器行为</td>
<td>Expect: 100-continue</td>
</tr>
<tr>
<td>From</td>
<td>发出请求的用户的Email</td>
<td>From: <a href="mailto:user@email.com">user@email.com</a></td>
</tr>
<tr>
<td>Host</td>
<td>指定请求的服务器的域名和端口号</td>
<td>Host: <a href="http://www.zcmhi.com">www.zcmhi.com</a></td>
</tr>
<tr>
<td>If-Match</td>
<td>只有请求内容与实体相匹配才有效</td>
<td></td>
</tr>
<tr>
<td>If-Modified-Since</td>
<td>若请求部分在指定时间后被修改则请求成功，未被修改则返回304代码</td>
<td></td>
</tr>
<tr>
<td>If-None-Match</td>
<td>如果内容未改变返回304代码</td>
<td></td>
</tr>
<tr>
<td>If-Range</td>
<td>如果实体未改变，服务器发送客户端丢失部分，否则发送整个实体</td>
<td></td>
</tr>
<tr>
<td>If-Unmodified-Since</td>
<td>只在实体在指定时间之后未被修改才请求成功</td>
<td></td>
</tr>
<tr>
<td>Max-Forwards</td>
<td>限制信息通过代理和网关传送的时间</td>
<td>Max-Forwards: 10</td>
</tr>
<tr>
<td>Proxy-Authorization</td>
<td>连接到代理的授权证书</td>
<td></td>
</tr>
<tr>
<td>Range</td>
<td>只请求实体的一部分，指定范围</td>
<td>Range: bytes=500-999</td>
</tr>
<tr>
<td>Referer</td>
<td>先前网页的地址，当前请求网页紧随其后,即来路</td>
<td></td>
</tr>
<tr>
<td>User-Agent</td>
<td>包含发出请求的用户信息</td>
<td>User-Agent: Mozilla/5.0 (Linux; X11)</td>
</tr>
<tr>
<td>Warning</td>
<td>关于消息实体的警告信息</td>
</tr>
</tbody>
</table>
<ul>
<li>响应头：</li>
</ul>
<table>
<thead>
<tr>
<th>字段名</th>
<th>含义</th>
<th>示例</th>
</tr>
</thead>
<tbody>
<tr>
<td>Accept-Ranges</td>
<td>表明服务器是否支持指定范围请求及哪种类型的分段请求</td>
<td></td>
</tr>
<tr>
<td>Age</td>
<td>从原始服务器到代理缓存形成的估算时间（以秒计，非负）</td>
<td>Age: 12</td>
</tr>
<tr>
<td>Allow</td>
<td>对某网络资源的有效的请求行为POST/GET等，不允许则返回405</td>
<td></td>
</tr>
<tr>
<td>Content-Encoding</td>
<td>web服务器支持的返回内容压缩编码类型</td>
<td>Content-Encoding: gzip</td>
</tr>
<tr>
<td>Content-Language</td>
<td>响应体的语言</td>
<td>Content-Language: en,zh</td>
</tr>
<tr>
<td>Content-Length</td>
<td>响应体的长度</td>
<td>Content-Length: 348</td>
</tr>
<tr>
<td>Content-Location</td>
<td>请求资源可替代的备用的另一地址</td>
<td>Content-Location: /index.htm</td>
</tr>
<tr>
<td>Content-MD5</td>
<td>返回资源的MD5校验值</td>
<td></td>
</tr>
<tr>
<td>Content-Range</td>
<td>在整个返回体中本部分的字节位置</td>
<td></td>
</tr>
<tr>
<td>Content-Type</td>
<td>返回内容的MIME类型</td>
<td>Content-Type: text/html; charset=utf-8</td>
</tr>
<tr>
<td>ETag</td>
<td>请求变量的实体标签的当前值</td>
<td></td>
</tr>
<tr>
<td>Expires</td>
<td>响应过期的日期和时间</td>
<td>Expires: Thu, 01 Dec 2010 16:00:00 GMT</td>
</tr>
<tr>
<td>Last-Modified</td>
<td>请求资源的最后修改时间</td>
<td></td>
</tr>
<tr>
<td>Location</td>
<td>用来重定向接收方到非请求URL的位置来完成请求或标识新的资源</td>
<td></td>
</tr>
<tr>
<td>Proxy-Authenticate</td>
<td>它指出认证方案和可应用到代理的该URL上的参数</td>
<td></td>
</tr>
<tr>
<td>Refresh</td>
<td>应用于重定向或一个新的资源被创造，在5秒之后重定向</td>
<td></td>
</tr>
<tr>
<td>Retry-After</td>
<td>如果实体暂时不可取，通知客户端在指定时间之后再次尝试</td>
<td>Retry-After: 120</td>
</tr>
<tr>
<td>Server</td>
<td>web服务器软件名称</td>
<td>Server: Apache/1.3.27 (Unix) (Red-Hat/Linux)</td>
</tr>
<tr>
<td>Set-Cookie</td>
<td>设置Http Cookie</td>
<td></td>
</tr>
<tr>
<td>Trailer</td>
<td>指出头域在分块传输编码的尾部存在</td>
<td>Trailer: Max-Forwards</td>
</tr>
<tr>
<td>Vary</td>
<td>告诉下游代理是使用缓存响应还是从原始服务器请求</td>
<td>Vary: *</td>
</tr>
<tr>
<td>Warning</td>
<td>警告实体可能存在的问题</td>
<td>Warning: 199 Miscellaneous warning</td>
</tr>
<tr>
<td>WWW-Authenticate</td>
<td>表明客户端请求实体应该使用的授权方案</td>
</tr>
</tbody>
</table>
<h5 id="HTTP请求方法"><a href="#HTTP请求方法" class="headerlink" title="HTTP请求方法"></a>HTTP请求方法</h5><p>GET（请求制定的页面信息，并返回实体主体）<br>HEAD（与GET相似，只用于获取报头）<br>POST（向制定资源提交数据进行处理请求，表单或者上传文件等，会导致新资源建立或资源修改）<br>PUT（从客户端向服务器传送的数据取代制定的文档内容）<br>CONNECT（HTTP/1.1协议中预留给能够将连接改为管道方式的代理服务器）<br>OPTIONS（允许客户端查看服务器的性能）<br>TRACE（回显服务器收到的请求，主要用于测试诊断）<br>DELETE（删除删除服务器上的某资源）</p>
<h5 id="常见的MIME-type"><a href="#常见的MIME-type" class="headerlink" title="常见的MIME type"></a>常见的MIME type</h5><p>text/plain<br>text/html<br>text/css<br>image/jpeg<br>image/png<br>image/svg+xml<br>audio/mp4<br>video/mp4<br>application/javascript<br>application/pdf<br>application/zip<br>application/atom+xml</p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>Http</tag>
      </tags>
  </entry>
  <entry>
    <title>理解函数中的 this</title>
    <url>/2020/09/08/2020-09-08-understand-this-in-function.html</url>
    <content><![CDATA[<p>很多人都对 JavaScript 中 function 里的 this 产生疑惑。在我看来，这些疑惑在我们理解了核心的 function 的调用原理后，都会彻底理解。</p>
<span id="more"></span>
<h3 id="核心原理"><a href="#核心原理" class="headerlink" title="核心原理"></a>核心原理</h3><p>首先来看一下 function 的 call 方法。call 方法调用时：</p>
<ul>
<li>可以传入一系列参数</li>
<li>第一个参数就是函数中的 this 值</li>
<li>方法调用时，this 会被置为第一个参数值，后面的参数才会被当做真正的参数使用</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function hello(thing) &#123;</span><br><span class="line">  console.log(this + &quot; says hello &quot; + thing);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">hello.call(&quot;Yehuda&quot;, &quot;world&quot;) //=&gt; Yehuda says hello world</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>所以，很容易理解，this 的值只有在 call 方法被调用时才能够确定，它不是一个不变的值。</p>
<h3 id="简单方法调用"><a href="#简单方法调用" class="headerlink" title="简单方法调用"></a>简单方法调用</h3><p>相比较 Function.call() 这样的调用方式，直接用方法名加括号的形式来调用。同时，这样子会将原有 call 方法的第一个参数的 this 直接设置成为 window：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">function hello(thing) &#123;</span><br><span class="line">	console.log(&quot;Hello &quot; + thing);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// equals to</span><br><span class="line">hello.call(window, &quot;world&quot;);</span><br><span class="line"></span><br><span class="line">// 在 ES5 的严格模式下，window 会被替换为 undefined</span><br><span class="line">hello.call(undefined, &quot;world&quot;);</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="成员方法"><a href="#成员方法" class="headerlink" title="成员方法"></a>成员方法</h3><p>如果函数存在于对象中，那么方法调用时的 this 会被指定为对象本身：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var person = &#123;</span><br><span class="line">  name: &quot;Brendan Eich&quot;,</span><br><span class="line">  hello: function(thing) &#123;</span><br><span class="line">    console.log(this + &quot; says hello &quot; + thing);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// this:</span><br><span class="line">person.hello(&quot;world&quot;)</span><br><span class="line"></span><br><span class="line">// desugars to this:</span><br><span class="line">person.hello.call(person, &quot;world&quot;);</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<h3 id="bind"><a href="#bind" class="headerlink" title="bind"></a>bind</h3><p>那么，如果我们需要一个方法有一个恒定不变的 this 值要怎么操作呢？</p>
<ul>
<li>可以使用另一个方法 B 来封装方法 A，并在调用 A 方法的时候写死 this 的值</li>
<li>可以使用 bind 方法来进行封装：</li>
</ul>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">var B = someFun.bind(A);</span><br><span class="line">B(params);</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>用 JavaScript 实现有限状态机</title>
    <url>/2021/02/14/2021-02-14-state-machine-by-javascript.html</url>
    <content><![CDATA[<p>当一个事物拥有有限多种状态，任一时刻只处于一种状态，而且在某种条件下可以从一种状态转变为另一种状态，满足这三个条件就可以被抽象成为一个有限状态机。当一个对象的状态越多、状态转换的事件越多，就越适合使用状态机来实现。</p>
<p>“javascript-state-machine”是一个很好用的有限状态机函数库。</p>
<span id="more"></span>
<p>该库提供一个全局对象StateMachine，使用该对象的create方法，可以生成有限状态机的实例。生成的时候，需要提供一个参数对象，用来描述实例的性质。比如，交通信号灯（红绿灯）可以这样描述：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> fsm = <span class="title class_">StateMachine</span>.<span class="title function_">create</span>(&#123;</span><br><span class="line">	<span class="attr">initial</span>: <span class="string">&#x27;green&#x27;</span>,</span><br><span class="line">  <span class="attr">events</span>: [</span><br><span class="line">		&#123; <span class="attr">name</span>: <span class="string">&#x27;warn&#x27;</span>,  <span class="attr">from</span>: <span class="string">&#x27;green&#x27;</span>,  <span class="attr">to</span>: <span class="string">&#x27;yellow&#x27;</span> &#125;,</span><br><span class="line">		&#123; <span class="attr">name</span>: <span class="string">&#x27;stop&#x27;</span>, <span class="attr">from</span>: <span class="string">&#x27;yellow&#x27;</span>, <span class="attr">to</span>: <span class="string">&#x27;red&#x27;</span> &#125;,</span><br><span class="line">		&#123; <span class="attr">name</span>: <span class="string">&#x27;ready&#x27;</span>,  <span class="attr">from</span>: <span class="string">&#x27;red&#x27;</span>,    <span class="attr">to</span>: <span class="string">&#x27;yellow&#x27;</span> &#125;,</span><br><span class="line">		&#123; <span class="attr">name</span>: <span class="string">&#x27;go&#x27;</span>, <span class="attr">from</span>: <span class="string">&#x27;yellow&#x27;</span>, <span class="attr">to</span>: <span class="string">&#x27;green&#x27;</span> &#125;</span><br><span class="line">　]</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>initial 参数是初始状态，events 参数是触发状态改变的各种事件。生成实例之后，可以通过以下接口访问当前状态：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">fsm.<span class="property">current</span> <span class="comment">// 返回当前状态。</span></span><br><span class="line">fsm.<span class="title function_">is</span>(s)   <span class="comment">// 返回一个布尔值，表示状态 s 是否为当前状态。</span></span><br><span class="line">fsm.<span class="title function_">can</span>(e)  <span class="comment">// 返回一个布尔值，表示事件 e 是否能在当前状态触发。</span></span><br><span class="line">fsm.<span class="title function_">cannot</span>(e) <span class="comment">// 返回一个布尔值，表示事件 e 是否不能在当前状态触发。</span></span><br></pre></td></tr></table></figure>
<p>其中，每个事件可以指定两个回调函数，一般为事件发生之前和发生之后两个时机触发，<code>onbefore$event/onafter$event</code>。每个状态指定两个回调函数，<code>onleave$status/onenter$status</code>。</p>
]]></content>
      <categories>
        <category>Toolkit</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>What delete really delete?</title>
    <url>/2020/11/01/2020-11-01-what-delete-really-delete.html</url>
    <content><![CDATA[<p>为了引入本文要讨论的内容，先放出一个几乎不会遇到的问题：<code>delete 0</code> 会发生什么？<br>答案是控制台输出 <code>true</code>，其他什么都没发生。<br>那假如我们继续尝试delete ，执行下面的语句会分别发生什么呢？</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* example 1 */</span></span><br><span class="line"><span class="keyword">delete</span> <span class="number">0</span></span><br><span class="line"><span class="comment">/* example 2 */</span></span><br><span class="line"><span class="keyword">const</span> a = &#123;<span class="attr">name</span>: <span class="string">&#x27;z&#x27;</span>&#125;</span><br><span class="line"><span class="keyword">delete</span> a.<span class="property">name</span></span><br><span class="line"><span class="comment">/* example 3 */</span></span><br><span class="line"><span class="keyword">delete</span> y</span><br><span class="line"><span class="comment">/* example 4 */</span></span><br><span class="line"><span class="keyword">delete</span> <span class="literal">null</span></span><br><span class="line"><span class="comment">/* example 5 */</span></span><br><span class="line"><span class="keyword">delete</span> <span class="literal">undefined</span></span><br><span class="line"><span class="comment">/* example 6 */</span></span><br><span class="line"><span class="keyword">const</span> x = <span class="number">1</span></span><br><span class="line"><span class="keyword">delete</span> x</span><br><span class="line"><span class="comment">/* example 7 */</span></span><br><span class="line"><span class="keyword">const</span> map = <span class="keyword">new</span> <span class="title class_">WeakMap</span>();</span><br><span class="line"><span class="keyword">delete</span> map;</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h3 id="输出解析"><a href="#输出解析" class="headerlink" title="输出解析"></a>输出解析</h3><ol>
<li>返回 true，实际上没做任何事情，只是根据 delete 的规则，没有发生异常就必须返回 true</li>
<li>返回 true，删除了 <a href="http://obj.name">obj.name</a> 表达式的值</li>
<li>如果 y 根本不存在，delete y 什么也不做，返回 true；访问不存在的变量 y 报 ReferenceError 错误，其实是对 y 表达式的的 Result 引用做 getValue 的时候报的错误，然后为啥 typeof y 和 delete y 不报错，因为这两个操作没有求值。如果 y 的 configurable 为 false，delete object.y 不能删除掉y属性，返回 false；如果在严格模式下，会报错：TypeError: Cannot delete property ‘c’</li>
<li>返回 true，null是值，相当于delete 0</li>
<li>返回 false</li>
<li>返回 false</li>
<li>返回 false</li>
<li>同 7 的原理一样，都是在删除一个不可设置也就是只读的 global 上的属性，因此返回 false。</li>
</ol>
<p>对于所有情况都是 true，除非属性是一个<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Global_Objects/Object/hasOwnProperty">自身的</a> <a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Errors/Cant_delete">不可配置</a>的属性（configurable 为 false），在这种情况下，非严格模式返回 false。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="title class_">Object</span>.<span class="title function_">getOwnPropertyDescriptor</span>(obj, <span class="string">&#x27;x&#x27;</span>);</span><br></pre></td></tr></table></figure>
<h3 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h3><p>1、delete x 的三种可能：x是一个值、x是一个引用、x是global对象上声明的属性，等同于一个全局变量。</p>
<ul>
<li>删除一个值：delete 0，不代表真的把0从语言当前的运行环境中删除，彷佛“啥也没发生”？</li>
<li>删除一个属性：delete obj.x ？</li>
<li>删除一个变量：delete x ？</li>
</ul>
<p>后两者其实都可以可以理解成，删除对象的属性，x可以理解成global.x。</p>
<p>2、delete 这个操作的正式语法设计并不是“删除某个东西”，而是“删除一个表达式的结果”；</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">delete</span> expression</span><br></pre></td></tr></table></figure>
<p>所以1当中的delete 0，JavaScript 将 0 视为一个表达式，并尝试删除它的求值结果。所以，现在这里的 0，其实不是值（Value）类型的数据，而是一个表达式运算的结果（Result）。而在进一步的删除操作之前，JavaScript 需要检测这个 Result 的类型：</p>
<ul>
<li>如果它是值，则按照传统的 JavaScript 的约定返回 true；</li>
<li>如果它是一个引用，那么对该引用进行分析，以决定如何操作。</li>
</ul>
<p>当 x 是全局对象 global 的属性时，所谓delete x其实只需要返回global.x这个引用就可以了。而当它不是全局对象 global 的属性时，那么就需要从当前环境中找到一个名为x的引用，删除其表达式的结果即可。</p>
<p>3、那么究竟如何去看是值还是引用？</p>
<p>语句的执行结果是一个值（value） 表达式的执行结果是一个result（可能是一个引用，也有可能是一个值），是引用还是值，取决于这是要一个左值还是右值。当你确定了一个Result用作lhs（左手端），那么它就是引用；如果确定它用作rhs（右手端），那么它就是值（将由引擎隐式地调用<code>GetValue()</code>）。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"> x = x; <span class="comment">// x = GetValue(x)</span></span><br><span class="line"><span class="comment">// “把 值x 赋给 引用x”</span></span><br></pre></td></tr></table></figure>
<p>4、“delete x”归根到底，是在删除一个表达式的、引用类型的结果（Result），而不是在删除 x 表达式，或者这个删除表达式的值（Value）。</p>
<p><strong>在 JavaScript 中的delete是一个很罕见的、能直接操作“引用”的语法元素。除了=，在Js中几乎没有其他操作符能直接去操作引用。</strong></p>
<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul>
<li>delete 运算符尝试删除值数据时，会返回 true，用于表示没有错误（Error）。</li>
<li>delete 0 的本质是删除一个表达式的值（Result）。</li>
<li>delete x 与上述的区别只在于 Result 是一个引用（Reference）。</li>
<li>delete 其实只能删除一种引用，即对象的成员（Property）。</li>
</ul>
<p><img src="https://intranetproxy.alipay.com/skylark/lark/0/2022/png/54556479/1653465940596-f2186ba3-a104-4006-82d0-51875255d664.png" alt="https://intranetproxy.alipay.com/skylark/lark/0/2022/png/54556479/1653465940596-f2186ba3-a104-4006-82d0-51875255d664.png"></p>
<h3 id="delete-undefined"><a href="#delete-undefined" class="headerlink" title="delete undefined"></a>delete undefined</h3><p>在早期的JavaScript中，undefined是一个特殊值，是在运行期中通过void运算，或者不返回值的函数，又或者一个声明了但未赋值的变量，等等类似这样的情况来“计算得到”的。所以在JavaScript的早期版本中，你没有办法直接判断“undefined是undefined”，例如无法写出“x === undefined”这样的代码，而你只能写类似“typeof(x) ===’undefined’”这样的代码。</p>
<p>后来（其实也没有太久），规范就约定把undefined作为可以缺省访问的名字，类似于null。但是这个时候就带来了一个矛盾，因为这个undefined很重要，早期的绝大多数框架或引擎都把它作为一个“全局名字”给声明了。也就是说，ECMAScript现在既没有办法将它规范成一个keyword，也没有办法处理成保留字等等，它看起来像null，但又没有办法在规范层面强制它。所以……ECMAScript就搞了一个“奇招”：把undefined声明成全局的属性，怎么样？！！（又一个天坑）</p>
<p>嗯嗯，很好。所以，现在的引擎上面undefined看起来长得跟null值差不多，而且在ECMAScript规范中它们都还是平级的（是原始值），而且它们的作用也很接近，最后他们都还是从最初的JavaScript 1.x中就存在的概念，但是undefined/null两者却在实现上完全不同：undefined是一个全局属性，而null是一个关键字。</p>
<p>由于undefined是全局属性，所以<code>delete undefined</code>其实就是<code>delete global.undefined</code>，是删除引用，而不是删除值。而这个属性是只读的，所以就返回false了。</p>
<h3 id="参考文献"><a href="#参考文献" class="headerlink" title="参考文献"></a>参考文献</h3><ul>
<li><a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Operators/delete">delete 操作符</a></li>
<li><a href="https://time.geekbang.org/column/article/164312">delete 0：JavaScript中到底有什么是可以销毁的</a>（付费）</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 设计模式与类</title>
    <url>/2021/04/08/2021-04-08-design-pattern-with-javascript-class.html</url>
    <content><![CDATA[<p>本文主要针对 JavaScript 类的各种设计模式实现进行简单总结和举例。</p>
<h3 id="字面量"><a href="#字面量" class="headerlink" title="字面量"></a>字面量</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> <span class="variable constant_">BMW</span> = &#123;</span><br><span class="line">	<span class="attr">type</span>: <span class="string">&#x27;Car&#x27;</span>,</span><br><span class="line">	<span class="attr">color</span>: <span class="string">&#x27;White&#x27;</span>,</span><br><span class="line">	<span class="attr">run</span>: <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">		<span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;BMW start!&#x27;</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h3 id="工厂模式"><a href="#工厂模式" class="headerlink" title="工厂模式"></a>工厂模式</h3><p>工厂模式是一种创建型模式，相当于一个封闭的工厂，你只需要通过一个统一接口传入原料，工厂会根据你传入的原料来创建新的对象。工厂模式<strong>不显示使用 new 来进行实例化</strong>。工厂模式不会显式地暴露对象创建的流程和逻辑。</p>
<h4 id="简单工厂模式（静态工厂模式）"><a href="#简单工厂模式（静态工厂模式）" class="headerlink" title="简单工厂模式（静态工厂模式）"></a><strong>简单工厂模式（静态工厂模式）</strong></h4><p>通过一个静态方法来创建对象，只需要传入指定的参数就可以生产对应的产品，而不需要知道具体的生产细节和逻辑判断。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 输入生成对象的原料，可以黑盒生成对象，其中也可以增加一些不可见的逻辑</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">vehicleFactory</span> (type, color) &#123;</span><br><span class="line">	<span class="keyword">function</span> <span class="title function_">Vehicle</span> (type, color) &#123;</span><br><span class="line">		<span class="variable language_">this</span>.<span class="property">type</span> = type;</span><br><span class="line">		<span class="variable language_">this</span>.<span class="property">color</span> = color;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">var</span> vehicle = <span class="keyword">new</span> <span class="title class_">Vehicle</span>(type, color);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (type === <span class="string">&#x27;BMW&#x27;</span>) &#123;</span><br><span class="line">		vehicle.<span class="property">run</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">			<span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;BMW start!&#x27;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125; <span class="keyword">else</span> <span class="keyword">if</span> (type === <span class="string">&#x27;Benz&#x27;</span>) &#123;</span><br><span class="line">		vehicle.<span class="property">talk</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">			<span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Benz start!&#x27;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> vehicle;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> bmw = <span class="title function_">vehicleFactory</span>(<span class="string">&#x27;BMW&#x27;</span>, <span class="string">&#x27;white&#x27;</span>);</span><br></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 用 TypeScript 类实现</span></span><br><span class="line">abstract <span class="keyword">class</span> <span class="title class_">Vehicle</span> &#123;</span><br><span class="line">  abstract <span class="title function_">run</span>(): <span class="keyword">void</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BMW</span> <span class="keyword">extends</span> <span class="title class_ inherited__">Vehicle</span> &#123;</span><br><span class="line">  <span class="title function_">run</span>(): <span class="keyword">void</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;BMW start!&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Benz</span> <span class="keyword">extends</span> <span class="title class_ inherited__">Vehicle</span> &#123;</span><br><span class="line">  <span class="title function_">run</span>(): <span class="keyword">void</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Benz start!&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">VehicleFactory</span> &#123;</span><br><span class="line">  public <span class="keyword">static</span> <span class="title function_">produceVehicle</span>(<span class="attr">model</span>: <span class="string">&quot;BMW&quot;</span> | <span class="string">&quot;Benz&quot;</span>): <span class="title class_">Vehicle</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (model === <span class="string">&quot;BMW&quot;</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title function_">BMW</span>();</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Benz</span>();</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> benz = <span class="title class_">VehicleFactory</span>.<span class="title function_">produceVehicle</span>(<span class="string">&#x27;BMW&#x27;</span>);</span><br></pre></td></tr></table></figure>
<p>简单工厂模式将对象的创建和使用进行了解耦（用户不需要关心对象时怎样创建的），并且用户不需要知道产品的所有信息。缺点是系统扩展困难，如果要添加新产品就要修改工厂逻辑。</p>
<h4 id="工厂方法模式"><a href="#工厂方法模式" class="headerlink" title="工厂方法模式"></a><strong>工厂方法模式</strong></h4><p>是简单工厂模式的升级，也被叫做多台工厂模式。工厂方法模式则是工厂将产品信息判断后，派发给下属分工厂进行生产。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> <span class="title class_">VehicleFactory</span> = <span class="keyword">function</span> (<span class="params">type, color</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="variable language_">this</span> <span class="keyword">instanceof</span> <span class="title class_">VehicleFactory</span>) &#123;</span><br><span class="line">		<span class="keyword">var</span> vehicle = <span class="keyword">new</span> <span class="variable language_">this</span>[type](color);</span><br><span class="line">		<span class="keyword">return</span> vehicle;</span><br><span class="line">	&#125; <span class="keyword">else</span>  &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">VehicleFactory</span>(color);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title class_">VehicleFactory</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123;</span><br><span class="line">	<span class="attr">BMW</span>: <span class="keyword">function</span> (<span class="params">color</span>) &#123;</span><br><span class="line">		<span class="variable language_">this</span>.<span class="property">color</span> = color;</span><br><span class="line">		<span class="variable language_">this</span>.<span class="property">run</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">			<span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;BMW start!&#x27;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;,</span><br><span class="line">	<span class="title class_">Benz</span>: <span class="keyword">function</span> (<span class="params">color</span>) &#123;</span><br><span class="line">		<span class="variable language_">this</span>.<span class="property">color</span> = color;</span><br><span class="line">		<span class="variable language_">this</span>.<span class="property">run</span> = <span class="keyword">function</span> (<span class="params"></span>) &#123;</span><br><span class="line">			<span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Benz start!&#x27;</span>);</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> bmw = <span class="keyword">new</span> <span class="title class_">VehicleFactory</span>(<span class="string">&#x27;BMW&#x27;</span>, <span class="string">&#x27;white&#x27;</span>)</span><br></pre></td></tr></table></figure>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">abstract <span class="keyword">class</span> <span class="title class_">Vehicle</span> &#123;</span><br><span class="line">	abstract <span class="title function_">run</span>(): <span class="keyword">void</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BMW</span> <span class="keyword">extends</span> <span class="title class_ inherited__">Vehicle</span> &#123;</span><br><span class="line">  <span class="title function_">run</span>(): <span class="keyword">void</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;BMW 发动咯&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Benz</span> <span class="keyword">extends</span> <span class="title class_ inherited__">Vehicle</span> &#123;</span><br><span class="line">  <span class="title function_">run</span>(): <span class="keyword">void</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;Benz 发动咯&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line">interface vehicleFactory &#123;</span><br><span class="line">  <span class="title function_">produceVehicle</span>(): <span class="title class_">Vehicle</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BMWFactory</span> implements vehicleFactory &#123;</span><br><span class="line">  <span class="title function_">produceVehicle</span>(): <span class="title class_">Vehicle</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title function_">BMW</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">BenzFactory</span> implements vehicleFactory &#123;</span><br><span class="line">  <span class="title function_">produceVehicle</span>(): <span class="title class_">Vehicle</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Benz</span>();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">const</span> bmwFactory = <span class="keyword">new</span> <span class="title class_">BMWFactory</span>();</span><br><span class="line"><span class="keyword">const</span> bmwCar = bmwFactory.<span class="property">produceVehicle</span>;</span><br></pre></td></tr></table></figure>
<h4 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a><strong>抽象工厂模式</strong></h4><p>通过类的抽象使得业务适用于一个产品类粗的创建，而不负责某一类产品的实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="title class_">VehicleFactory</span> = <span class="keyword">function</span>(<span class="params">subType, superType</span>) &#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="title class_">VehicleFactory</span>[superType] === <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    <span class="keyword">function</span> <span class="title function_">F</span>(<span class="params"></span>) &#123;</span><br><span class="line">      <span class="variable language_">this</span>.<span class="property">type</span> = <span class="string">&#x27;车辆&#x27;</span></span><br><span class="line">    &#125; </span><br><span class="line">    F.<span class="property"><span class="keyword">prototype</span></span> = <span class="keyword">new</span> <span class="title class_">VehicleFactory</span>[superType]()</span><br><span class="line">    subType.<span class="property">constructor</span> = subType</span><br><span class="line">    subType.<span class="property"><span class="keyword">prototype</span></span> = <span class="keyword">new</span> <span class="title function_">F</span>()                <span class="comment">// 因为子类subType不仅需要继承superType对应的类的原型方法，还要继承其对象属性</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;不存在该抽象类&#x27;</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title class_">VehicleFactory</span>.<span class="property">Car</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">type</span> = <span class="string">&#x27;car&#x27;</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">VehicleFactory</span>.<span class="property">Car</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123;</span><br><span class="line">  <span class="attr">getPrice</span>: <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;抽象方法不可使用&#x27;</span>)</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">getSpeed</span>: <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">Error</span>(<span class="string">&#x27;抽象方法不可使用&#x27;</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">BMW</span> = <span class="keyword">function</span>(<span class="params">price, speed</span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">price</span> = price</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">speed</span> = speed</span><br><span class="line">&#125;</span><br><span class="line"><span class="title class_">VehicleFactory</span>(<span class="variable constant_">BMW</span>, <span class="string">&#x27;Car&#x27;</span>)        <span class="comment">// 继承Car抽象类</span></span><br><span class="line"><span class="variable constant_">BMW</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">getPrice</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;        <span class="comment">// 覆写getPrice方法</span></span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`BWM price is <span class="subst">$&#123;<span class="variable language_">this</span>.price&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="variable constant_">BMW</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">getSpeed</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`BWM speed is <span class="subst">$&#123;<span class="variable language_">this</span>.speed&#125;</span>`</span>)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> baomai5 = <span class="keyword">new</span> <span class="title function_">BMW</span>(<span class="number">30</span>, <span class="number">99</span>)</span><br><span class="line">baomai5.<span class="title function_">getPrice</span>()                          <span class="comment">// BWM price is 30</span></span><br><span class="line">baomai5 <span class="keyword">instanceof</span> <span class="title class_">VehicleFactory</span>.<span class="property">Car</span></span><br></pre></td></tr></table></figure>
<h3 id="构造函数（构造器）模式"><a href="#构造函数（构造器）模式" class="headerlink" title="构造函数（构造器）模式"></a>构造函数（构造器）模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Car</span> (model, year, miles) &#123;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">model</span> = model;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">year</span> = year;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">miles</span> = miles;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">toString</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">model</span> + <span class="string">&quot; has done &quot;</span> + <span class="variable language_">this</span>.<span class="property">miles</span> + <span class="string">&quot; miles&quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> civic = <span class="keyword">new</span> <span class="title class_">Car</span>(<span class="string">&quot;Honda Civic&quot;</span>, <span class="number">2009</span>, <span class="number">20000</span>);</span><br></pre></td></tr></table></figure>
<p>想要使用构造函数生成对象实例，必须使用 new 操作符，new 操作符本质上会实现以下几个步骤</p>
<ul>
<li>创建一个空对象： <code>var obj = new Object()</code></li>
<li>设置原型链（实例的内部将包含一个指针（内部属性），指向构造函数的原型对象）： <code>obj.__proto__ = Car.prototype</code></li>
<li>让 Func 中的 this 指向 obj ，并执行 Func 的函数体： <code>var res = Car.call(this, ...arguments)</code></li>
<li><p>判断 Func 的返回值类型：如果是值类型，返回obj。如果是引用类型，就返回这个引用类型的对象：</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="title function_">typeof</span>(res) == <span class="string">&quot;object&quot;</span>)&#123;</span><br><span class="line">  <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span>&#123;</span><br><span class="line">  <span class="keyword">return</span> obj;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="原型模式"><a href="#原型模式" class="headerlink" title="原型模式"></a>原型模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Car</span> () &#123;&#125;;</span><br><span class="line"><span class="title class_">Car</span>.<span class="property"><span class="keyword">prototype</span></span> = &#123;</span><br><span class="line">	<span class="attr">model</span>: <span class="string">&#x27;Honda Civic&#x27;</span>,</span><br><span class="line">	<span class="attr">year</span>: <span class="number">2009</span>,</span><br><span class="line">	<span class="attr">miles</span>: <span class="number">20000</span>,</span><br><span class="line">	<span class="attr">toString</span>: <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">model</span> + <span class="string">&quot; has done &quot;</span> + <span class="variable language_">this</span>.<span class="property">miles</span> + <span class="string">&quot; miles&quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="组合模式（构造-原型）"><a href="#组合模式（构造-原型）" class="headerlink" title="组合模式（构造+原型）"></a>组合模式（构造+原型）</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Car</span> (model, year, miles) &#123;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">model</span> = model;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">year</span> = year;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">miles</span> = miles;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title class_">Car</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">toString</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">model</span> + <span class="string">&quot; has done &quot;</span> + <span class="variable language_">this</span>.<span class="property">miles</span> + <span class="string">&quot; miles&quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="动态原型模式"><a href="#动态原型模式" class="headerlink" title="动态原型模式"></a>动态原型模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">Car</span>(<span class="params">model, year, miles</span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">model</span> = model;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">year</span> = year;</span><br><span class="line">	<span class="variable language_">this</span>.<span class="property">miles</span> = miles;</span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">typeof</span> <span class="variable language_">this</span>.<span class="property">getModelName</span> != <span class="string">&#x27;function&#x27;</span>) &#123;</span><br><span class="line">    <span class="title class_">Car</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">getModelName</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">      <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">name</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="寄生构造函数模式"><a href="#寄生构造函数模式" class="headerlink" title="寄生构造函数模式"></a>寄生构造函数模式</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createCar</span>(<span class="params">model, year, miles</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> car = <span class="keyword">new</span> <span class="title class_">Object</span>();</span><br><span class="line">  car.<span class="property">model</span> = model;</span><br><span class="line">  car.<span class="property">year</span> = year;</span><br><span class="line">  car.<span class="property">miles</span> = miles;</span><br><span class="line">  car.<span class="property">getModelName</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">model</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> car;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> p1 = <span class="keyword">new</span> <span class="title function_">createCar</span>(<span class="string">&#x27;Honda Civic&#x27;</span>, <span class="number">2009</span>, <span class="number">20000</span>)</span><br></pre></td></tr></table></figure>
<h3 id="稳妥构造函数模式"><a href="#稳妥构造函数模式" class="headerlink" title="稳妥构造函数模式"></a>稳妥构造函数模式</h3><p>没有公共属性的工厂模式</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">createCar</span>(<span class="params">model, year, miles</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> car = <span class="keyword">new</span> <span class="title class_">Object</span>();</span><br><span class="line">  car.<span class="property">getModelName</span> = <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="title function_">alert</span>(<span class="variable language_">this</span>.<span class="property">model</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> car;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 中的值</title>
    <url>/2021/04/21/2021-04-21-value-type-of-javascript.html</url>
    <content><![CDATA[<h3 id="JavaScript-中的值的分类"><a href="#JavaScript-中的值的分类" class="headerlink" title="JavaScript 中的值的分类"></a>JavaScript 中的值的分类</h3><p>JavaScript 中的值可以分为<strong>原始类型</strong>和<strong>复杂类型（对象）</strong>：</p>
<ul>
<li>原始类型<ul>
<li>Boolean - <code>true</code> 或 <code>false</code></li>
<li>null - 用 <code>type of</code> 检验 <code>null</code> 数据类型时为 <code>Object</code> ，但它不是对象，这是JS的一个bug</li>
<li>undefined</li>
<li>number - JavaScript中的所有数字都是浮点数，没有整数</li>
<li>string</li>
<li>symbol (ES6)</li>
</ul>
</li>
<li>复杂类型（对象）<ul>
<li>原始类型的包装对象（Boolean、Number、String）</li>
<li>内置对象（Date）</li>
<li>用户创建对象</li>
</ul>
</li>
</ul>
<span id="more"></span>
<p>原始类型和复杂类型之间有一些不同点：</p>
<ul>
<li><p>复杂类型可变，但是原始类型不可变（这里指的是<strong>值的可变性</strong>）：</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 复杂类型</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123; <span class="attr">arr</span>: [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>] &#125;;</span><br><span class="line">obj.<span class="property">arr</span>.<span class="title function_">push</span>(<span class="number">4</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 原始类型，可以改变变量的值，但是不能改变这个值的值</span></span><br><span class="line"><span class="keyword">var</span> str = <span class="string">&#x27;some string&#x27;</span>;</span><br></pre></td></tr></table></figure>
</li>
<li><p>原始类型作为值保存（存放在栈中），复杂类型作为引用保存（存放在堆中，其引用存放在栈中）</p>
</li>
</ul>
<p>原始类型中的 null 和 undefined 是两个比较特殊的类型。两种类型的本质区别是：null 是<strong>程序级正常或预期的值缺失</strong>（例如我们给一个变量赋为空值时就使用null），而 undefined 是系统级的意外和错误的值缺失（没逾期到的情况，比如一个未声明的变量或者命名空间不存在的对象属性）。还有其他一些区别：</p>
<ul>
<li>typeof null === ‘object’，这个现象是原始 JavaScript 语言中的错误（JavaScript 的底层中二进制前三位如果都是 0 的话就被判断为 object 类型，但是 null 二进制表示是全0）</li>
<li>null == undefined 返回 true，但是 null === undefined 返回 false</li>
<li>null 可以进行递增递减（null 相当于一个介于0和1之间的值）</li>
</ul>
<h3 id="怎样判断值的类型"><a href="#怎样判断值的类型" class="headerlink" title="怎样判断值的类型"></a>怎样判断值的类型</h3><h4 id="typeof-运算符"><a href="#typeof-运算符" class="headerlink" title="typeof 运算符"></a>typeof 运算符</h4><ul>
<li><p>使用方法</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">typeof</span> <span class="string">&#x27;someString&#x27;</span>)  <span class="comment">// &#x27;string&#x27;</span></span><br><span class="line"><span class="title function_">typeof</span>(<span class="string">&#x27;someString&#x27;</span>)              <span class="comment">// return &#x27;string&#x27;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>返回值都是字符串类型，有以下几种返回值</p>
<ul>
<li>‘undefined’，所有未定义的变量</li>
<li>‘boolean’，布尔类型</li>
<li>‘string’，字符串类型</li>
<li>‘number’，数字类型</li>
<li>‘function’，函数类型</li>
<li>‘object’，对象类型（包括一些内置对象、数组、正则表达式等，还有 <strong>null</strong>）</li>
</ul>
</li>
<li>原理：不同的对象在底层都表示为二进制，在 Javascript 中二进制前（低）三位存储其类型信息，typeof 根据这三位进行判断</li>
<li><strong>注意点：不能单独判断数组、正则表达式和一些内置对象的类型，null 的类型也会返回 ‘object’</strong></li>
</ul>
<h4 id="instanceof-运算符"><a href="#instanceof-运算符" class="headerlink" title="instanceof 运算符"></a>instanceof 运算符</h4><ul>
<li><p>使用方法： <code>val instanceof Object</code></p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>([] <span class="keyword">instanceof</span> <span class="title class_">Array</span>)      <span class="comment">// true</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="keyword">new</span> <span class="title class_">Date</span> <span class="keyword">instanceof</span> <span class="title class_">Date</span>) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>返回值是布尔值，表示一个值的类型是不是某个对象</p>
</li>
<li><p>原理： <code>a instanceof A</code> 检测 a 的原型链上是否有 A.prototype</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="keyword">function</span>(<span class="params"></span>)&#123;&#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a.<span class="property">__proto__</span> === <span class="title class_">Function</span>.<span class="property"><span class="keyword">prototype</span></span>) <span class="comment">// true</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>注意点只能已知值的类型（原型对象）然后判断是否相等，无法直接返回值的类型</p>
</li>
</ul>
<h4 id="Object-prototype-toString-call"><a href="#Object-prototype-toString-call" class="headerlink" title="Object.prototype.toString.call()"></a>Object.prototype.toString.call()</h4><ul>
<li><p>使用方法</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 比较适用于复杂类型的判断</span></span><br><span class="line"><span class="keyword">var</span> arr=[<span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line"><span class="title class_">Object</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">toString</span>.<span class="title function_">call</span>(arr); <span class="comment">// &quot;[object Array]&quot;</span></span><br><span class="line"><span class="title class_">Object</span>.<span class="property"><span class="keyword">prototype</span></span>.<span class="property">toString</span>.<span class="title function_">call</span>(arr).<span class="title function_">indexOf</span>(<span class="string">&#x27;Array&#x27;</span>) === <span class="number">8</span>; <span class="comment">// true </span></span><br></pre></td></tr></table></figure>
</li>
<li><p>返回值是字符串类型，格式为 <code>&#39;[object Array]&#39;</code> ，后面的是其原型</p>
</li>
<li>原理：JavaScript 中的对象都继承自 Object，而且原始类型也被包装过成为了对象，所以可以使用 Object.prototype.toString.call() 来判断。</li>
<li>注意点：继承过后的对象的 toString 方法已经将最顶端的方法重写，所以普通的 toString 方法是不能返回对象类型的</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Angular 路由</title>
    <url>/2021/07/07/2021-07-07-angular-router.html</url>
    <content><![CDATA[<p>使用 Angular 路由首先要在 index.html 的 <head> 标签下先添加一个 <base> 元素，来告诉路由器该如何合成导航用的 URL。Angular 路由并不在核心库，而是在其自己的 @angular/router 库中，使用时需要进行引入： <code>import &#123; RouterModule, Routes &#125; from &#39;@angular/router&#39;;</code> 。</p>
<span id="more"></span>
<p>下面是一个在根模块的路由实例：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="attr">appRoutes</span>: <span class="title class_">Routes</span> = [</span><br><span class="line">  &#123; <span class="attr">path</span>: <span class="string">&#x27;crisis-center&#x27;</span>, <span class="attr">component</span>: <span class="title class_">CrisisListComponent</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">path</span>: <span class="string">&#x27;hero/:id&#x27;</span>,      <span class="attr">component</span>: <span class="title class_">HeroDetailComponent</span> &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">path</span>: <span class="string">&#x27;heroes&#x27;</span>,</span><br><span class="line">    <span class="attr">component</span>: <span class="title class_">HeroListComponent</span>,</span><br><span class="line">    <span class="attr">data</span>: &#123; <span class="attr">title</span>: <span class="string">&#x27;Heroes List&#x27;</span> &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123; <span class="attr">path</span>: <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">    <span class="attr">redirectTo</span>: <span class="string">&#x27;/heroes&#x27;</span>,</span><br><span class="line">    <span class="attr">pathMatch</span>: <span class="string">&#x27;full&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  &#123; <span class="attr">path</span>: <span class="string">&#x27;**&#x27;</span>, <span class="attr">component</span>: <span class="title class_">PageNotFoundComponent</span> &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line">@<span class="title class_">NgModule</span>(&#123;</span><br><span class="line">  <span class="attr">imports</span>: [</span><br><span class="line">    <span class="title class_">RouterModule</span>.<span class="title function_">forRoot</span>(</span><br><span class="line">      appRoutes,</span><br><span class="line">      &#123; <span class="attr">enableTracing</span>: <span class="literal">true</span> &#125; <span class="comment">// &lt;-- debugging purposes only</span></span><br><span class="line">    )</span><br><span class="line">    <span class="comment">// other imports here</span></span><br><span class="line">  ],</span><br><span class="line">  ...</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">AppModule</span> &#123; &#125;</span><br></pre></td></tr></table></figure>
<p>使用 <code>RouterModule.forRoot()</code> 方法来配置路由器，并添加到 AppModule 的 imports 数组中。</p>
<p>路由 Routes 类有以下的特点：</p>
<ul>
<li>路由都是一个 path （<strong>path不能以斜杠开头！</strong>）对应一个组件；</li>
<li>:id 是一个路由参数的令牌(Token)，例如 ‘/home/323’，就对应 ‘home/:id’ 中的 id；</li>
<li>每个路由对象中都可以传入 data 字段来存放关于该路由的信息；</li>
<li>‘’ 空字符串的 path 表示应用的默认路径，通常作为起点，redirectTo 可以设置重定向的 path；</li>
<li>‘<strong>‘ 表示通配符，一般作为默认路径放在最后，一般用作 404 页面或重定向到其他路由（</strong>先匹配者优先原则**）；</li>
</ul>
<h3 id="路由出口"><a href="#路由出口" class="headerlink" title="路由出口"></a>路由出口</h3><p>上面提到的是路由的详情配置，还需要将这个路由应用在对应的地方。Angular 的路由出口标签为： <code>&lt;router-outlet&gt;&lt;/router-outlet&gt;</code> 。这个标签就表示路由组件在这个地方进行相应的展示和切换。</p>
<h3 id="路由器链接"><a href="#路由器链接" class="headerlink" title="路由器链接"></a>路由器链接</h3><p>除了在浏览器地址栏去输入 url 导航到制定的路由页面，一般还需要一些用户操作进行路由导航，可以使用 routerLink 的指令来指明某个链接对应的跳转路由：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;a routerLink=<span class="string">&quot;/crisis-center&quot;</span> routerLinkActive=<span class="string">&quot;active&quot;</span>&gt;<span class="title class_">Crisis</span> <span class="title class_">Center</span>&lt;/a&gt;</span><br></pre></td></tr></table></figure>
<p>routerLinkActive 指令会根据当前链接对应的路由是否激活来切换所绑定的 css 类。</p>
<h3 id="路由器状态"><a href="#路由器状态" class="headerlink" title="路由器状态"></a>路由器状态</h3><p>可以在应用中的任何地方用 Router 服务（注入的 Router 类实例）及其 routerState 属性来访问当前的 RouterState 值。路由的路径和参数可以通过<strong>注入</strong>进来的一个名叫 ActivatedRoute 的路由服务来获取，其中包括以下有用信息：</p>
<ul>
<li>url：路由路径的 Observable 对象，是一个由路由路径中的各个部分组成的字符串数组。</li>
<li>data：一个 Observable，其中包含提供给路由的 data 对象。也包含由解析守卫（resolve guard）解析而来的值。</li>
<li>paramMap：一个 Observable，其中包含一个由当前路由的必要参数和可选参数组成的 map 对象。用这个 map 可以获取来自同名参数的单一值或多重值（有 has()/get()/getAll()/keys() 等 API）。</li>
<li>queryParamMap：一个 Observable，其中包含一个对所有路由都有效的查询参数组成的map对象。 用这个 map 可以获取来自查询参数的单一值或多重值。</li>
<li>fragment：一个适用于所有路由的 URL 的 fragment（片段）的 Observable。</li>
<li>outlet：要把该路由渲染到的 RouterOutlet 的名字。<strong>对于无名路由，它的路由名是 primary</strong>，而不是空串。</li>
<li>routeConfig：用于该路由的路由配置信息，其中包含原始路径。</li>
<li>parent：当该路由是一个子路由时，表示该路由的父级 ActivatedRoute。</li>
<li>firstChild：包含该路由的子路由列表中的第一个 ActivatedRoute。</li>
<li>children：包含当前路由下所有已激活的子路由。</li>
</ul>
<h3 id="路由事件"><a href="#路由事件" class="headerlink" title="路由事件"></a>路由事件</h3><p>在每次导航中，Router 都会通过 Router.events 属性发布一些导航事件。这些事件的范围涵盖了从开始导航到结束导航之间的很多时间点。</p>
<ul>
<li>NavigationStart：本事件会在导航开始时触发。</li>
<li>RouteConfigLoadStart：本事件会在 Router 惰性加载 某个路由配置之前触发。</li>
<li>RouteConfigLoadEnd：本事件会在惰性加载了某个路由后触发。</li>
<li>RoutesRecognized：本事件会在路由器解析完 URL，并识别出了相应的路由时触发</li>
<li>GuardsCheckStart：本事件会在路由器开始 Guard 阶段之前触发。</li>
<li>ChildActivationStart：本事件会在路由器开始激活路由的子路由时触发。</li>
<li>ActivationStar：本事件会在路由器开始激活某个路由时触发。</li>
<li>GuardsCheckEnd：本事件会在路由器成功完成了 Guard 阶段时触发。</li>
<li>ResolveStart：本事件会在 Router 开始解析（Resolve）阶段时触发。</li>
<li>ResolveEnd：本事件会在路由器成功完成了路由的解析（Resolve）阶段时触发。</li>
<li>ChildActivationEnd：本事件会在路由器激活了路由的子路由时触发。</li>
<li>ActivationEnd：本事件会在路由器激活了某个路由时触发。</li>
<li>NavigationEnd：本事件会在导航成功结束之后触发。</li>
<li>NavigationCancel：本事件会在导航被取消之后触发。 这可能是因为在导航期间某个路由守卫返回了 false。</li>
<li>NavigationError：这个事件会在导航由于意料之外的错误而失败时触发。</li>
<li>Scroll：本事件代表一个滚动事件。</li>
</ul>
<h3 id="子路由配置"><a href="#子路由配置" class="headerlink" title="子路由配置"></a>子路由配置</h3><p>子路由可以在某个子模块的路由文件中进行实现：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="attr">crisisCenterRoutes</span>: <span class="title class_">Routes</span> = [</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">path</span>: <span class="string">&#x27;crisis-center&#x27;</span>,</span><br><span class="line">    <span class="attr">component</span>: <span class="title class_">CrisisCenterComponent</span>,</span><br><span class="line">    <span class="attr">children</span>: [</span><br><span class="line">      &#123;</span><br><span class="line">        <span class="attr">path</span>: <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">        <span class="attr">component</span>: <span class="title class_">CrisisListComponent</span>,</span><br><span class="line">        <span class="attr">children</span>: [</span><br><span class="line">          &#123;</span><br><span class="line">            <span class="attr">path</span>: <span class="string">&#x27;:id&#x27;</span>,</span><br><span class="line">            <span class="attr">component</span>: <span class="title class_">CrisisDetailComponent</span></span><br><span class="line">          &#125;,</span><br><span class="line">          &#123;</span><br><span class="line">            <span class="attr">path</span>: <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">            <span class="attr">component</span>: <span class="title class_">CrisisCenterHomeComponent</span></span><br><span class="line">          &#125;</span><br><span class="line">        ]</span><br><span class="line">      &#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line">@<span class="title class_">NgModule</span>(&#123;</span><br><span class="line">  <span class="attr">imports</span>: [</span><br><span class="line">    <span class="title class_">RouterModule</span>.<span class="title function_">forChild</span>(crisisCenterRoutes)</span><br><span class="line">  ],</span><br><span class="line">  <span class="attr">exports</span>: [</span><br><span class="line">    <span class="title class_">RouterModule</span></span><br><span class="line">  ]</span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">CrisisCenterRoutingModule</span> &#123; &#125;</span><br></pre></td></tr></table></figure>
<p>注意：除了在 app.module.ts 根模块中使用 <code>RouterModule.forRoot()</code> 来配置路由外，其他的路由文件中都必须使用 <code>RouterModule.forChild()</code> 来配置路由。否则多个根路由可能会发生问题。</p>
<h3 id="Secondary-Outlets-Primer"><a href="#Secondary-Outlets-Primer" class="headerlink" title="Secondary Outlets Primer"></a>Secondary Outlets Primer</h3><p>一般情况下， <code>&lt;router-outlet&gt;&lt;/router-outlet&gt;</code> 标签会展示和切换所有路由，但是如果要分区域设置多个 Router Outlets，就需要用到第二路由。那么就要使用以下的方法：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;router-outlet name=<span class="string">&quot;sidebar&quot;</span>&gt;&lt;/router-outlet&gt;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="variable constant_">ROUTES</span> = [</span><br><span class="line">  &#123; <span class="attr">path</span>: <span class="string">&#x27;home&#x27;</span>, <span class="attr">component</span>: <span class="title class_">HomeComponent</span> &#125;,</span><br><span class="line">  &#123; <span class="attr">path</span>: <span class="string">&#x27;chat&#x27;</span>, <span class="attr">component</span>: <span class="title class_">ChatComponent</span>, <span class="attr">outlet</span>: <span class="string">&#x27;sidebar&#x27;</span> &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>
<p>使用了第二路由后，url 可能会出现 <code>[localhost:4200/home(sidebar:chat)](http://localhost:4200/home(sidebar:chat))</code> 这种情况。</p>
<h3 id="路由守卫"><a href="#路由守卫" class="headerlink" title="路由守卫"></a>路由守卫</h3><p>一般情况下，路由是需要有条件地进行跳转（例如某些路由需要登录后的权限）。在路由配置文件的 Routes 类实例中，每个路由对象都可以通过添加对应的守卫来控制路由的访问权限：</p>
<ul>
<li><code>[CanActivate](https://v7.angular.cn/api/router/CanActivate)</code>来处理导航<strong><em>到</em></strong>某路由的情况。</li>
<li><code>[CanActivateChild](https://v7.angular.cn/api/router/CanActivateChild)</code>来处理导航<strong><em>到</em></strong>某子路由的情况。</li>
<li><code>[CanDeactivate](https://v7.angular.cn/api/router/CanDeactivate)</code>来处理从当前路由<strong><em>离开</em></strong>的情况.</li>
<li><code>[Resolve](https://v7.angular.cn/api/router/Resolve)</code>在路由激活<strong><em>之前</em></strong>获取路由数据。</li>
<li><code>[CanLoad](https://v7.angular.cn/api/router/CanLoad)</code>来处理<strong><em>异步</em></strong>导航到某特性模块的情况。</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 配置路由</span></span><br><span class="line"><span class="keyword">const</span> <span class="attr">adminRoutes</span>: <span class="title class_">Routes</span> = [</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">path</span>: <span class="string">&#x27;admin&#x27;</span>,</span><br><span class="line">    <span class="attr">component</span>: <span class="title class_">AdminComponent</span>,</span><br><span class="line">    <span class="attr">canActivate</span>: [<span class="title class_">AuthGuard</span>]</span><br><span class="line">  &#125;</span><br><span class="line">];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现 CanActivate 的守卫实例</span></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">AuthGuard</span> implements <span class="title class_">CanActivate</span> &#123;</span><br><span class="line">  <span class="title function_">canActivate</span>(</span><br><span class="line">    <span class="attr">next</span>: <span class="title class_">ActivatedRouteSnapshot</span>,</span><br><span class="line">    <span class="attr">state</span>: <span class="title class_">RouterStateSnapshot</span>): boolean &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;AuthGuard#canActivate called&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="异步路由"><a href="#异步路由" class="headerlink" title="异步路由"></a>异步路由</h3><p>为了避免路由太多，一次性加载很多组件，可以引入异步路由对组件进行懒加载。异步路由中可以使用 canLoad 来控制是否加载，使用 canActivateChild 来保护子路由权限，loadChildren 设置为module 或 component 的地址，并使用 #moduleName 来添加导出的类名。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="attr">routes</span>: <span class="title class_">Routes</span> = [</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="attr">path</span>: <span class="string">&#x27;personinfo&#x27;</span>,</span><br><span class="line">    <span class="attr">canLoad</span>: [<span class="title class_">AuthGuardService</span>],</span><br><span class="line">    <span class="attr">canActivateChild</span>: [<span class="title class_">AuthGuardService</span>],</span><br><span class="line">    <span class="attr">loadChildren</span>: <span class="string">&#x27;./components/pages/person-info/person-info.module#PersonInfoModule&#x27;</span></span><br><span class="line">	  <span class="attr">data</span>: &#123; <span class="attr">preLoad</span>: <span class="literal">true</span> &#125; <span class="comment">// 预加载开启      </span></span><br><span class="line">	&#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>Angular</tag>
      </tags>
  </entry>
  <entry>
    <title>理解 JavaScript（Node）中的 Cookie 和 Session</title>
    <url>/2021/05/03/2021-05-03-understand-cookie-and-session-in-javascript-node.html</url>
    <content><![CDATA[<p>问题一：为什么网站（网页应用）需要有登录的功能？<br>问题二：Cookie 到底是个什么东西？<br>问题三：Cookie 是怎样告诉服务器”是谁发送的请求“？<br>问题四：Cookie 和 Session 验证之间有什么不同？<br>问题五：Session 又是什么？</p>
<span id="more"></span>
<h3 id="登录-登出功能在网站中的必要性"><a href="#登录-登出功能在网站中的必要性" class="headerlink" title="登录/登出功能在网站中的必要性"></a>登录/登出功能在网站中的必要性</h3><ul>
<li>登录/登出的验证对于开发者来说是最难搞的功能之一</li>
<li>但是为了让网站更可靠，登录/登出功能又是必须的</li>
</ul>
<p><strong>客户端请求有几个非常严肃的问题</strong>：</p>
<ul>
<li>客户端是不知道到底是谁触发的这个请求</li>
<li>当然，我们有 IP 地址和浏览器的一些信息</li>
<li>但是很有可能，不同的用户在不同的电脑上使用的是同一个 IP 地址</li>
<li>所以我们需要登录功能来告诉服务器究竟是谁发送的这个请求，Cookie 和 Session 验证就是解决这个问题的方法之二</li>
</ul>
<h3 id="Cookie-的必要性"><a href="#Cookie-的必要性" class="headerlink" title="Cookie 的必要性"></a>Cookie 的必要性</h3><ul>
<li>如果一个网站在刷新页面的时候不会使用户登出，那么这个网站一定是使用了 cookie 或 session</li>
<li>几乎每一个你所浏览的网站都使用了 cookie 和session</li>
<li>客户端之所以能够发送一些关于你和你的验证信息、状态到服务器，就是用了 cookie</li>
<li>Cookie 就是非常简单的<strong>键值对</strong>： <code>name=gplee</code></li>
<li><p>浏览器将 cookie 保存在内部，并在每一次发送请求的时候把这些 cookie 也一并发送到服务端</p>
<p>  <img src="%5B%E7%BF%BB%E8%AF%91%5D%E7%90%86%E8%A7%A3%20JavaScript%EF%BC%88Node%EF%BC%89%E4%B8%AD%E7%9A%84%20Cookie%20%E5%92%8C%20Session%20821cab22b670445cacf2816c132b0710/0_ZIkc8UzGscPbkxFg.png" alt="%5B%E7%BF%BB%E8%AF%91%5D%E7%90%86%E8%A7%A3%20JavaScript%EF%BC%88Node%EF%BC%89%E4%B8%AD%E7%9A%84%20Cookie%20%E5%92%8C%20Session%20821cab22b670445cacf2816c132b0710/0_ZIkc8UzGscPbkxFg.png"></p>
</li>
</ul>
<ul>
<li>换句话说，cookie 能够记录和追踪你和关于你的信息</li>
<li>这也是为什么你会经常被建议去阶段性地删除 cookie（因为要防止个人信息的泄露）</li>
<li>Cookie 是在 request 和 response 的 header 中进行传输的</li>
</ul>
<h3 id="让我们创建-cookie-并把它发送给浏览器吧"><a href="#让我们创建-cookie-并把它发送给浏览器吧" class="headerlink" title="让我们创建 cookie 并把它发送给浏览器吧"></a>让我们创建 cookie 并把它发送给浏览器吧</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> http = <span class="built_in">require</span>(<span class="string">&#x27;http&#x27;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="title function_">parseCookies</span> = (<span class="params">cookie = <span class="string">&#x27;&#x27;</span></span>) =&gt; cookie.<span class="title function_">split</span>(<span class="string">&#x27;;&#x27;</span>)</span><br><span class="line">	.<span class="title function_">map</span>(<span class="function"><span class="params">v</span> =&gt;</span> v.<span class="title function_">split</span>(<span class="string">&#x27;=&#x27;</span>))</span><br><span class="line">	.<span class="title function_">map</span>(<span class="function">(<span class="params">[k, ...vs]</span>) =&gt;</span> [k, vs.<span class="title function_">join</span>(<span class="string">&#x27;=&#x27;</span>)])</span><br><span class="line">	.<span class="title function_">reduce</span>(<span class="function">(<span class="params">acc, [k, v]</span>) =&gt;</span> &#123;</span><br><span class="line">	    acc[k.<span class="title function_">trim</span>()] = <span class="built_in">decodeURIComponent</span>(v);</span><br><span class="line">	    <span class="keyword">return</span> acc;</span><br><span class="line">	&#125;, &#123;&#125;);</span><br><span class="line"></span><br><span class="line">http.<span class="title function_">createServer</span>(<span class="function">(<span class="params">req, res</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> cookies = <span class="title function_">parseCookies</span>(req.<span class="property">headers</span>.<span class="property">cookie</span>);</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(req.<span class="property">url</span>, cookies);</span><br><span class="line">  res.<span class="title function_">writeHead</span>(<span class="number">200</span>, &#123;<span class="string">&#x27;Set-Cookie&#x27;</span>: <span class="string">&#x27;mycookie=test&#x27;</span>&#125;);</span><br><span class="line">  res.<span class="title function_">end</span>(<span class="string">&#x27;Hello Cookie&#x27;</span>);</span><br><span class="line">&#125;).<span class="title function_">listen</span>(<span class="number">8082</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;listening on port 8082&#x27;</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p><strong>parseCookie 方法</strong></p>
<ul>
<li>该方法将字符串转换为对象</li>
<li>Cookie 是字符串类型，例如 <code>name=gplee;year=1993</code>，通过这个方法转化为一个对象</li>
</ul>
<p><strong>createServer 方法</strong></p>
<ul>
<li>在第一个回调方法中，将请求中的 cookie 字符串（在 req.headers.cookie 中）转化为一个对象</li>
<li>使用 res.writeHead 方法，第一个参数为 200 状态码，第二个参数为响应头的信息（Set-Cookie 字段能够让浏览器保存这个 cookie）</li>
<li>浏览器收到响应之后便将 <code>mycookie=test</code> 这个 cookie 保存下来</li>
</ul>
<p><strong>在浏览器中查看 cookie</strong></p>
<p>运行上面代码，并在浏览器中访问 <a href="http://localhost:8082">http://localhost:8082</a> 这个地址，能够在服务器端的控制台看到</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">➜  test node index.<span class="property">js</span></span><br><span class="line">listening on port <span class="number">8082</span></span><br><span class="line">/ &#123; <span class="string">&#x27;&#x27;</span>: <span class="string">&#x27;&#x27;</span> &#125;</span><br><span class="line">/favicon.<span class="property">ico</span> &#123; <span class="attr">mycookie</span>: <span class="string">&#x27;test&#x27;</span> &#125;</span><br></pre></td></tr></table></figure>
<p>第三行返回的是初始浏览器的第一次请求，该请求中没有任何 cookie，但是在该请求的响应里，服务器已经通知浏览器保存 <code>mycookie=test</code> 这个 cookie。最后一行是当浏览器没有在 HTML 文档中找到 favicon 的信息会再次请求 favicon 的信息，这时就能够看到 mycookie 了。</p>
<p>浏览器端，我们可以看到</p>
<p><img src="%5B%E7%BF%BB%E8%AF%91%5D%E7%90%86%E8%A7%A3%20JavaScript%EF%BC%88Node%EF%BC%89%E4%B8%AD%E7%9A%84%20Cookie%20%E5%92%8C%20Session%20821cab22b670445cacf2816c132b0710/1_WYrTZqQI8PaZjYLZcaLizA.png" alt="%5B%E7%BF%BB%E8%AF%91%5D%E7%90%86%E8%A7%A3%20JavaScript%EF%BC%88Node%EF%BC%89%E4%B8%AD%E7%9A%84%20Cookie%20%E5%92%8C%20Session%20821cab22b670445cacf2816c132b0710/1_WYrTZqQI8PaZjYLZcaLizA.png"></p>
<ul>
<li>General 是通用的头部信息</li>
<li>Request Header 是请求头独有的信息</li>
<li>Response Header 是响应头独有的信息</li>
<li>我们能够在 Request Header 中看到 Cookie 字段，也能在 Response Header 中看到 Set-Cookie 字段</li>
<li>Set-Cookie 字段就是服务器通知浏览器保存这个字段的 cookie 值，在浏览器保存完成之后，每次发送请求到服务器就能够带上 Cookie 字段了</li>
</ul>
<h3 id="HTTP-Header-和-HTTP-Body"><a href="#HTTP-Header-和-HTTP-Body" class="headerlink" title="HTTP Header 和 HTTP Body"></a>HTTP Header 和 HTTP Body</h3><p><img src="%5B%E7%BF%BB%E8%AF%91%5D%E7%90%86%E8%A7%A3%20JavaScript%EF%BC%88Node%EF%BC%89%E4%B8%AD%E7%9A%84%20Cookie%20%E5%92%8C%20Session%20821cab22b670445cacf2816c132b0710/0_i6W-t0A6zjk7-4mP.png" alt="%5B%E7%BF%BB%E8%AF%91%5D%E7%90%86%E8%A7%A3%20JavaScript%EF%BC%88Node%EF%BC%89%E4%B8%AD%E7%9A%84%20Cookie%20%E5%92%8C%20Session%20821cab22b670445cacf2816c132b0710/0_i6W-t0A6zjk7-4mP.png"></p>
<ul>
<li>客户端请求和服务器响应都由 HTTP Header 和 HTTP Body组成（详细点讲，是（请求行+请求头）+空行+（请求内容））</li>
<li>HTTP Header 中包含了请求和相应的一些描述信息</li>
<li>Cookie 保存在 HTTP Header 中</li>
</ul>
<h3 id="将-Cookie-作为请求的标识符"><a href="#将-Cookie-作为请求的标识符" class="headerlink" title="将 Cookie 作为请求的标识符"></a>将 Cookie 作为请求的标识符</h3><p>在上面的代码例子中，我们发送的 cookie 并没有表明是谁发送的。这次我们将在 cookie 中发送一些用户的信息。</p>
<p>网页代码</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">&quot;en&quot;</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">&quot;UTF-8&quot;</span>&gt;</span></span><br><span class="line">		    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Understand Cookie and Session<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		    <span class="tag">&lt;<span class="name">form</span> <span class="attr">action</span>=<span class="string">&quot;/login&quot;</span>&gt;</span></span><br><span class="line">		        <span class="tag">&lt;<span class="name">input</span> <span class="attr">id</span>=<span class="string">&quot;name&quot;</span> <span class="attr">name</span>=<span class="string">&quot;name&quot;</span> <span class="attr">placeholder</span>=<span class="string">&quot;Write your name&quot;</span>&gt;</span></span><br><span class="line">		        <span class="tag">&lt;<span class="name">button</span> <span class="attr">id</span>=<span class="string">&quot;login&quot;</span>&gt;</span>Login<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">		    <span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>服务器代码</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> http = <span class="built_in">require</span>(<span class="string">&#x27;http&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> url = <span class="built_in">require</span>(<span class="string">&#x27;url&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> qs = <span class="built_in">require</span>(<span class="string">&#x27;querystring&#x27;</span>);</span><br><span class="line"></span><br><span class="line">http.<span class="title function_">createServer</span>(<span class="function">(<span class="params">req, res</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">const</span> cookies = <span class="title function_">parseCookies</span>(req.<span class="property">headers</span>.<span class="property">cookie</span>);</span><br><span class="line">  <span class="keyword">if</span>(req.<span class="property">url</span>.<span class="title function_">startsWith</span>(<span class="string">&#x27;/login&#x27;</span>)) &#123;</span><br><span class="line">    <span class="keyword">const</span> &#123; query &#125; = url.<span class="title function_">parse</span>(req.<span class="property">url</span>);</span><br><span class="line">    <span class="keyword">const</span> &#123; name &#125; = qs.<span class="title function_">parse</span>(query);</span><br><span class="line">    <span class="keyword">const</span> expires = <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line">    expires.<span class="title function_">setMinutes</span>(expires.<span class="title function_">getMinutes</span>() + <span class="number">1</span>);</span><br><span class="line">    res.<span class="title function_">writeHead</span>(<span class="number">302</span>, &#123;</span><br><span class="line">      <span class="title class_">Location</span>: <span class="string">&#x27;/&#x27;</span>, </span><br><span class="line">			<span class="string">&#x27;Set-Cookie&#x27;</span>: <span class="string">`name=<span class="subst">$&#123;<span class="built_in">encodeURIComponent</span>(name)&#125;</span>;Expires=<span class="subst">$&#123;expires.toGMTString()&#125;</span>;HttpOnly; Path=/`</span>,</span><br><span class="line">    &#125;);</span><br><span class="line">    res.<span class="title function_">end</span>();</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (cookies.<span class="property">name</span>) &#123;</span><br><span class="line">    res.<span class="title function_">writeHead</span>(<span class="number">200</span>, &#123; <span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;text/html; charset=utf-8&#x27;</span>&#125;);</span><br><span class="line">    res.<span class="title function_">end</span>(<span class="string">`Welcome <span class="subst">$&#123;cookies.name&#125;</span>`</span>);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    fs.<span class="title function_">readFile</span>(<span class="string">&#x27;./index.html&#x27;</span>, <span class="function">(<span class="params">err,data</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">if</span> (err) &#123;</span><br><span class="line">        <span class="keyword">throw</span> err;</span><br><span class="line">      &#125;</span><br><span class="line">      res.<span class="title function_">end</span>(data);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;).<span class="title function_">listen</span>(<span class="number">8083</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;listening on port 8083&#x27;</span>);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>我们直接来看服务器的代码：</p>
<ul>
<li>第一个方法仍然是将 cookie 字符串解析成对象的方法</li>
<li>在 createServer 的回调中我们可以看到如果直接访问 <a href="https://localhost:8083">https://localhost:8083</a> 这个地址，会跳过前两个判断的分支（因为请求地址没有带着 /login 的目录，初始也没有 <a href="http://req.headers.cookie.name">req.headers.cookie.name</a> 这个属性，直接返回 index.html 这个静态文件，在浏览器渲染出一个提交名字的表单</li>
<li>当我们在表单输入名字并点击提交后，会请求 /login，这时服务器会使用 querystring 模块对query 字段进行解析，读出 name 字段。最后服务器会返回 302，并让浏览器在 cookie 中保存了 name 字段，并给 name 设置了一分钟的过期时间，然后重定向到 / 目录。</li>
<li>浏览器收到上面的响应之后，会根据 302 状态码重定向到 <a href="http://localhost:8083">localhost:8083</a></li>
<li>这时浏览器的请求中就有了 <a href="http://cookies.name">cookies.name</a> 字段，服务器会返回 <code>Welcome $&#123;cookies.name&#125;</code></li>
</ul>
<h3 id="Cookie-选项"><a href="#Cookie-选项" class="headerlink" title="Cookie 选项"></a>Cookie 选项</h3><p>cookie 能够使用分号分隔来配置一些属性。</p>
<ul>
<li><code>cookiename=cookievalue</code> ：默认的 cookie 键值对</li>
<li><code>Expires=date</code> ：cookie 的过期时间，默认是浏览器关闭就会被删除</li>
<li><code>Max-age=seconds</code> ：和 Expires 功能一样也是设置过期时间，但是是以秒计数，优先级高于 Expires</li>
<li><code>Domain=DomainName</code> : 设置 cookie 发送时的域名，默认是当前页面域名</li>
<li><code>Path=URL</code> : 指明 cookie 发送的 url 地址，默认为 ‘/‘</li>
<li><code>Secure</code> : 只有在 https 协议下才会发送该 cookie</li>
<li><code>HttpOnly</code> : 禁止客户端 javascript 手动设置 cookie</li>
</ul>
<h3 id="使用-Session-代替-Cookie"><a href="#使用-Session-代替-Cookie" class="headerlink" title="使用 Session 代替 Cookie"></a>使用 Session 代替 Cookie</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> http = <span class="built_in">require</span>(<span class="string">&#x27;http&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">&#x27;fs&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> url = <span class="built_in">require</span>(<span class="string">&#x27;url&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> qs = <span class="built_in">require</span>(<span class="string">&#x27;querystring&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> session = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">http.<span class="title function_">createServer</span>(<span class="function">(<span class="params">req, res</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="keyword">const</span> cookies = <span class="title function_">parseCookies</span>(req.<span class="property">headers</span>.<span class="property">cookie</span>);</span><br><span class="line">    <span class="keyword">if</span>(req.<span class="property">url</span>.<span class="title function_">startsWith</span>(<span class="string">&#x27;/login&#x27;</span>)) &#123;</span><br><span class="line">        <span class="keyword">const</span> &#123; query &#125; = url.<span class="title function_">parse</span>(req.<span class="property">url</span>);</span><br><span class="line">        <span class="keyword">const</span> &#123; name &#125; = qs.<span class="title function_">parse</span>(query);</span><br><span class="line">        <span class="keyword">const</span> expires = <span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line">        expires.<span class="title function_">setMinutes</span>(expires.<span class="title function_">getMinutes</span>() + <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">const</span> randomInt = +<span class="keyword">new</span> <span class="title class_">Date</span>();</span><br><span class="line">        session[randomInt] = &#123;</span><br><span class="line">            name,</span><br><span class="line">            expires</span><br><span class="line">        &#125;;</span><br><span class="line">        res.<span class="title function_">writeHead</span>(<span class="number">302</span>, &#123;</span><br><span class="line">            <span class="title class_">Location</span>: <span class="string">&#x27;/&#x27;</span>,</span><br><span class="line">            <span class="string">&#x27;Set-Cookie&#x27;</span>: <span class="string">`session=<span class="subst">$&#123;randomInt&#125;</span>;Expires=<span class="subst">$&#123;expires.toUTCString()&#125;</span>;HttpOnly;Path=/`</span>,</span><br><span class="line">        &#125;);</span><br><span class="line">        res.<span class="title function_">end</span>();</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (cookies.<span class="property">session</span> &amp;&amp; session[cookies.<span class="property">session</span>].<span class="property">expires</span> &gt; <span class="keyword">new</span> <span class="title class_">Date</span>()) &#123;</span><br><span class="line">        res.<span class="title function_">writeHead</span>(<span class="number">200</span>, &#123; <span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;text/html; charset=utf-8&#x27;</span>&#125;);</span><br><span class="line">        res.<span class="title function_">end</span>(<span class="string">`Welcome <span class="subst">$&#123;session[cookies.session].name&#125;</span>`</span>)</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        fs.<span class="title function_">readFile</span>(<span class="string">&#x27;./server4.html&#x27;</span>, <span class="function">(<span class="params">err,data</span>) =&gt;</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (err) &#123;</span><br><span class="line">                <span class="keyword">throw</span> err;</span><br><span class="line">            &#125;</span><br><span class="line">            res.<span class="title function_">end</span>(data);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;).<span class="title function_">listen</span>(<span class="number">8084</span>, <span class="function">() =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;listening on port 8084&#x27;</span>);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>这个例子中，我们在提交名字后让浏览器设置了一个名为 session 值为一个随机整数的 cookie，并在服务端维护了一个 session 的缓存对象。如果 cookie.session 没有过期，那么我们可以从中获取 session 的值。</p>
<p><strong>Session 是什么？</strong></p>
<ul>
<li>基于 session 的身份验证，用户信息是保存在服务器端的，而浏览器和服务器之间只通过一个 session id 进行交流</li>
<li>有很多实现 session id 的方式，大部分网站使用 cookie 进行实现</li>
<li>服务端的 session 通常是维护在数据库中（redis）</li>
<li>具体实现中，我们不会直接使用一个变量作为 session id，我们经常会使用第三方模块（例如 express-session 或 cookie-parser）。这样更加安全。</li>
</ul>
]]></content>
      <categories>
        <category>Translation</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>JavaScript</tag>
        <tag>NodeJS</tag>
        <tag>浏览器存储</tag>
      </tags>
  </entry>
  <entry>
    <title>Angular 学习笔记</title>
    <url>/2021/06/01/2021-06-01-angular-document-study-note.html</url>
    <content><![CDATA[<h2 id="快速上手"><a href="#快速上手" class="headerlink" title="快速上手"></a>快速上手</h2><p><a href="https://angular.cn/start">Angular - Angular 入门：你的第一个应用</a></p>
<p>首先跟随 Angular 官方文档的快速上手部分进行一个简单项目的代码编写尝试和 Firebase 部署。该示例项目中包括了对</p>
<ul>
<li><strong>模板语法</strong>（的一些基本特性 <em>ngFor、</em>ngIf、差值双花括号、属性绑定[]、事件绑定()）</li>
<li><strong>组件</strong>（由一个组件类、一个 HTML 模板和组件专属样式表组成）</li>
<li>父组件向子组件<strong>输入</strong>信息（@Input）和<strong>输出</strong>（@Output、EventEmitter）</li>
<li><strong>路由</strong>（在 app.module.ts 中注册路由、routerLink 属性、通过 ActivatedRoute 作为构造函数参数来使用 this.route 对象）</li>
<li><strong>管理数据</strong>（通过创建 service 类对数据进行管理，service 在引用后需要作为参数传入构造函数）</li>
<li><strong>数据获取</strong>（使用 Angular 内置的 HttpClientModule 类实例 http 作为参数传入构造函数后，可以使用 this.http 对象请求数据）</li>
<li>表单（使用内置的 FormBuilder 构建表单模型）</li>
<li>构建和部署（ng build、firebase 部署）</li>
</ul>
<span id="more"></span>
<h2 id="Angular-架构概览"><a href="#Angular-架构概览" class="headerlink" title="Angular 架构概览"></a>Angular 架构概览</h2><h3 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h3><p>Angular 是一个用 HTML 和 TypeScript 构建客户端应用的平台与框架。Angular 的<strong>基本构造块是模块 NgModule</strong>，它为组件提供了编译的上下文环境。应用<strong>至少会有一个用于引导应用的根模块 AppModule</strong>，通常还会有很多特性模块。模块通常由组件 Component 构成，组件定义视图（每个应用都至少有一个根组件），组件使用服务（service 提供与视图无关的逻辑功能，服务可注入到组件中）。</p>
<p>模块、组件和服务都是使用装饰器的类。</p>
<p>组件类一般紧跟随在 @Component() 装饰器后。组件提供两种数据绑定类型：事件绑定（处理用户输入的数据）和属性绑定（计算结果插入模板中），也支持双向数据绑定（ngModel），还可以通过<strong>管道</strong>转换要显示的数据。</p>
<p>服务类一般紧跟随在 @Injectable() 装饰器后。该装饰器上的元数据可以将服务作为以来注入到组件中。（服务可以使组件更简洁，复用度更高）</p>
<p>Angular 还提供了 Router 服务来帮助你定义视图之间的导航路径。</p>
<h3 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h3><p>Angular 应用是模块化的，NgModule 是其模块系统。一个 NgModule 就是一个存放内聚代码块的容器，包含一些组件、服务提供者或其它代码文件。</p>
<p>每个 Angular 应用都<strong>至少有一个 NgModule 类</strong>，也就是根模块，它习惯上命名为 <strong>AppModule</strong>，并位于一个名叫 <strong>app.module.ts</strong> 的文件中。引导这个根模块就可以启动你的应用。应用可以有其他特性模块，但是根模块可以包含任意深度、层次的子模块。</p>
<p>NgModule 是一个带有 @NgModule() 装饰器的类。@NgModule() 装饰器是一个函数，它接受一个元数据对象，该对象的属性用来描述这个模块：</p>
<ul>
<li><code>declarations</code>（可声明对象表）：声明属于本 NgModule 的组件、指令、管道。</li>
<li><code>exports</code>（导出表）：那些能在其它模块的<strong>组件模板</strong>中使用的可声明对象的子集。</li>
<li><code>imports</code>（导入表）：本 NgModule 中所需要使用的其他模块。</li>
<li><code>providers</code>：本模块向全局服务中贡献的那些服务的创建器。 这些服务能被本应用中的任何部分使用。（你也可以在组件级别指定服务提供者，这通常是首选方式。）</li>
<li><code>bootstrap</code> —— 应用的主视图，称为<strong>根组件</strong>。它是应用中所有其它视图的宿主。只有<strong>根模块</strong>才应该设置这个 <code>bootstrap</code> 属性</li>
</ul>
<h3 id="组件"><a href="#组件" class="headerlink" title="组件"></a>组件</h3><p><strong>组件元数据</strong></p>
<p>组件是跟在 @Component() 装饰器后的一个 TypeScript 类，装饰器中需要设置一些元数据，其中包括：selector（CSS 选择器）、templateUrl/template（模板文件或模板）、providers（当前组件所需的服务提供者数组）和 styleUrl/styles（样式文件数组或样式数组）等。</p>
<p><strong>模板语法</strong></p>
<p>模板语法很像标准的 HTML 文档，但是有以下几种特性：</p>
<ul>
<li><p>数据绑定（<strong>绑定语法中，等号左边的不是 HTML 的 attr，而是 DOM 的 property 和事件！但是如果一定要设置 attr 且 DOM 的 property 中没有对应属性，可以使用 attr.someAttr 进行绑定</strong>）：</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 单项数据绑定</span></span><br><span class="line"><span class="comment">// 插值</span></span><br><span class="line">&lt;li&gt;&#123;&#123;hero.<span class="property">name</span>&#125;&#125;&lt;/li&gt;  </span><br><span class="line"><span class="comment">// 属性绑定，父组件向子组件传数据  </span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">app-hero-detail</span> [<span class="attr">hero</span>]=<span class="string">&quot;selectedHero&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">app-hero-detail</span>&gt;</span></span> </span><br><span class="line"><span class="comment">// 事件绑定，调用方法进行数据处理     </span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">li</span> (<span class="attr">click</span>)=<span class="string">&quot;selectHero(hero)&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">li</span>&gt;</span></span>           </span><br><span class="line"></span><br><span class="line"><span class="comment">// 双向数据绑定，属性绑定+事件绑定</span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">input</span> [(<span class="attr">ngModel</span>)]=<span class="string">&quot;hero.name&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"></span></span><br><span class="line"><span class="language-xml">// attribute 绑定</span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">tr</span>&gt;</span><span class="tag">&lt;<span class="name">td</span> [<span class="attr">attr.colspan</span>]=<span class="string">&quot;1 + 1&quot;</span>&gt;</span>One-Two<span class="tag">&lt;/<span class="name">td</span>&gt;</span><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">button</span> [<span class="attr">attr.aria-label</span>]=<span class="string">&quot;actionName&quot;</span>&gt;</span>&#123;&#123;actionName&#125;&#125; with Aria<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"></span></span><br><span class="line"><span class="language-xml">// CSS 类绑定使用 [class]时，如果badCurly 有值会替换前面 class 所有类，建议指定某个特定的类</span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;bad curly special&quot;</span> [<span class="attr">class</span>]=<span class="string">&quot;badCurly&quot;</span>&gt;</span>Bad curly<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">div</span> [<span class="attr">class.special</span>]=<span class="string">&quot;isSpecial&quot;</span>&gt;</span>The class binding is special<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">// 使用ngClass 可以设置一个类对象</span></span><br><span class="line"><span class="language-xml">this.currentClasses =  &#123;</span></span><br><span class="line"><span class="language-xml">    &#x27;saveable&#x27;: this.canSave,</span></span><br><span class="line"><span class="language-xml">    &#x27;modified&#x27;: !this.isUnchanged,</span></span><br><span class="line"><span class="language-xml">    &#x27;special&#x27;:  this.isSpecial</span></span><br><span class="line"><span class="language-xml">  &#125;;</span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">div</span> [<span class="attr">ngClass</span>]=<span class="string">&quot;currentClasses&quot;</span>&gt;</span>This div is initially saveable, unchanged, and special<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p>ngClass、ngStyle 都是类似的，将一个类/样式的对象绑定在元素上；</p>
<p>ngModel 可以用来双向绑定，使用时<strong>一定需要引入 FormsModule</strong>（在相应的组件/模块中 imports FormsModule 模块），ngModel 同时还提供了一个 (ngModelChange) 的绑定的输出属性可以处理数据变化时的逻辑。</p>
<p><strong>管道</strong></p>
<p>可以在模板中展示数据时进行一定的逻辑变换（左侧的数据经过右侧管道函数处理得到的值）。<strong>管道可以进行串联，可以传参（管道：参数）</strong>Angular 自带了很多管道，例如 date、currency 等：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span>Today is &#123;&#123;today | date: &#x27;longDate&#x27; | lowercase &#125;&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>自定义管道</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Pipe</span>, <span class="title class_">PipeTransform</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Pipe</span>(&#123;<span class="attr">name</span>: <span class="string">&#x27;exponentialStrength&#x27;</span>&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">ExponentialStrengthPipe</span> implements <span class="title class_">PipeTransform</span> &#123;</span><br><span class="line">  <span class="title function_">transform</span>(<span class="attr">value</span>: number, <span class="attr">exponent</span>: string): number &#123;</span><br><span class="line">    <span class="keyword">let</span> exp = <span class="built_in">parseFloat</span>(exponent);</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Math</span>.<span class="title function_">pow</span>(value, <span class="built_in">isNaN</span>(exp) ? <span class="number">1</span> : exp);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>纯管道与非纯管道</p>
<p>数据更新会触发管道执行数据处理，Angular 只在检测到纯变更（指对<strong>原始类型值的更改</strong>，或对对象<strong>引用的更改</strong>），对对象、数组内部的改变是不会调用纯管道的。非纯管道是可以侦测到引用类型内部变化的，Angular 会在每个组件的变更检测周期执行该管道，成本很高。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Pipe</span>(&#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&#x27;flyingHeroesImpure&#x27;</span>,</span><br><span class="line">  <span class="attr">pure</span>: <span class="literal">false</span> / <span class="literal">true</span> <span class="comment">// 默认为true</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>AsyncPipe，接受一个 Promise 或 Observable，自动订阅这个输入最终返回他们发送的值。 </p>
<p><strong>安全导航操作符 ?. 和非空断言操作符 !.</strong></p>
<p>?. 避免出现TypeError: Cannot read property ‘name’ of null in [null].的错误</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span>The current hero&#x27;s name is &#123;&#123;currentHero?.name&#125;&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>!. 表示.之前的变量一定存在，一般是在 *ngIf 判断后使用</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> *<span class="attr">ngIf</span>=<span class="string">&quot;hero&quot;</span>&gt;</span></span><br><span class="line">  The hero&#x27;s name is &#123;&#123;hero!.name&#125;&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>类型转换函数 $any()</strong></p>
<p>绑定的模板表达式中不清楚某个变量类型，可以强制转换为 any 类型</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>The hero&#x27;s marker is &#123;&#123;$any(hero).marker&#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p><strong>用户输入</strong></p>
<p>一般会在表单中为表单元素绑定用户输入事件，可以通过传入 $event 来获取用户输入。但是 $event 不仅很大而且有时并没有 value 属性。</p>
<p>采用模板引用变量 #someInput 是更好的方法。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// template</span></span><br><span class="line">&lt;input (keyup)=<span class="string">&quot;onKey1($event)&quot;</span>&gt;</span><br><span class="line">&lt;input #input1 (keyup)=&quot;onKey2(input1.value)&quot;&gt;</span><br><span class="line"></span><br><span class="line">// $event bad</span><br><span class="line">onKey1(event: KeyboardEvent) &#123; // with type info</span><br><span class="line">  this.values += (&lt;HTMLInputElement&gt;event.target).value + &#x27; | &#x27;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// #input1 good</span><br><span class="line">onKey1(value: any) &#123; // with type info</span><br><span class="line">  this.values += value + &#x27; | &#x27;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>用户输入事件有很多，有按键事件（keyup、keydown）、失焦事件（blur）和鼠标事件（mouseup）按键事件过滤，采用类似 <code>keyup.enter</code> 表示只有当 enter 键释放时才处理。</p>
<p><strong>指令</strong></p>
<p>Angular 中有<strong>结构型指令</strong>（通过添加、移除或替换 DOM 元素来修改布局，例如 <em>ngIf 和 </em>ngFor）和<strong>属性型指令</strong>（修改现有元素的外观或行为，例如 ngModel）。也可以通过 @Directive() 装饰器自定义一些指令。</p>
<p>*ngIf 与 v-if 类似，是控制元素是否存在，与显隐逻辑不通；</p>
<p>*ngFor 的模板语法是 let…of…，需要带索引时，则需要声明索引变量 let i = index；Angular 中提供了一个 trackBy（是一个函数类型，需要在数据中提供），其作用与 key 相同能够提升性能。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line">trackByHeroes(index: number, hero: Hero): number &#123; return hero.id; &#125;</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> *<span class="attr">ngFor</span>=<span class="string">&quot;let hero of heroes; let i=index; trackBy: trackByHeroes&quot;</span>&gt;</span>&#123;&#123;i + 1&#125;&#125; - &#123;&#123;hero.name&#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>ngSwitch 指令是属性型指令（修改元素行为而非 DOM 结构）由三部分构成：ngSwitch，<em>ngSwitchCase，</em>ngSwitchDefault</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> [<span class="attr">ngSwitch</span>]=<span class="string">&quot;currentHero.emotion&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">app-happy-hero</span>    *<span class="attr">ngSwitchCase</span>=<span class="string">&quot;&#x27;happy&#x27;&quot;</span>    [<span class="attr">hero</span>]=<span class="string">&quot;currentHero&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">app-happy-hero</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">app-sad-hero</span>      *<span class="attr">ngSwitchCase</span>=<span class="string">&quot;&#x27;sad&#x27;&quot;</span>      [<span class="attr">hero</span>]=<span class="string">&quot;currentHero&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">app-sad-hero</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">app-confused-hero</span> *<span class="attr">ngSwitchCase</span>=<span class="string">&quot;&#x27;confused&#x27;&quot;</span> [<span class="attr">hero</span>]=<span class="string">&quot;currentHero&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">app-confused-hero</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">app-unknown-hero</span>  *<span class="attr">ngSwitchDefault</span>           [<span class="attr">hero</span>]=<span class="string">&quot;currentHero&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">app-unknown-hero</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>创建指令，必须使用 @Directive 装饰器（ElementRef 是标签元素引用的类型）：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Directive</span>, <span class="title class_">ElementRef</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Directive</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;[appHighlight]&#x27;</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">HighlightDirective</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params">el: ElementRef</span>) &#123;</span><br><span class="line">       el.<span class="property">nativeElement</span>.<span class="property">style</span>.<span class="property">backgroundColor</span> = <span class="string">&#x27;yellow&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>创建根据事件触发的指令（HostListener 装饰器可以订阅该指令所在的 DOM 元素的事件）：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;p appHighlight [highlightColor]=<span class="string">&quot;color&quot;</span>&gt;<span class="title class_">Highlighted</span> <span class="keyword">with</span> parent component<span class="string">&#x27;s color&lt;/p&gt;</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">import &#123; Directive, ElementRef, HostListener &#125; from &#x27;</span>@angular/core<span class="string">&#x27;;</span></span><br><span class="line"><span class="string">@Directive(&#123;</span></span><br><span class="line"><span class="string">  selector: &#x27;</span>[appHighlight]<span class="string">&#x27;</span></span><br><span class="line"><span class="string">&#125;)</span></span><br><span class="line"><span class="string">export class HighlightDirective &#123;</span></span><br><span class="line"><span class="string">	@Input highlightColor: string;</span></span><br><span class="line"><span class="string">  constructor(el: ElementRef) &#123;&#125;</span></span><br><span class="line"><span class="string">	@HostListener(&#x27;</span>mouseenter<span class="string">&#x27;) onMouseEnter() &#123;</span></span><br><span class="line"><span class="string">	  this.highlight(this.highlightColor);</span></span><br><span class="line"><span class="string">	&#125;</span></span><br><span class="line"><span class="string">	</span></span><br><span class="line"><span class="string">	@HostListener(&#x27;</span>mouseleave<span class="string">&#x27;) onMouseLeave() &#123;</span></span><br><span class="line"><span class="string">	  this.highlight(null);</span></span><br><span class="line"><span class="string">	&#125;</span></span><br><span class="line"><span class="string">	</span></span><br><span class="line"><span class="string">	private highlight(color: string) &#123;</span></span><br><span class="line"><span class="string">	  this.el.nativeElement.style.backgroundColor = color;</span></span><br><span class="line"><span class="string">	&#125;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure>
<p><ng-template> 指令，单独使用时，里面的元素都不会显示（转换为注释）。除非有 ngIf，ngFor 或 ngSwitch 等结构型指令。此时应该使用<a href="是 *ngif 的原始版本，* ngif 会转换成[ngif]*">ngIf</a></p>
<p>可以为 <ng-template> 添加模板引用 #someTemp，然后使用 *ngTemplateOutlet 指令去服用这些模板引用。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;div *ngIf=<span class="string">&quot;hero&quot;</span> &gt;&#123;&#123;hero.<span class="property">name</span>&#125;&#125;&lt;/div&gt;</span><br><span class="line"><span class="comment">// equals to </span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">ng-template</span> [<span class="attr">ngIf</span>]=<span class="string">&quot;hero&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;<span class="name">div</span>&gt;</span>&#123;&#123;hero.name&#125;&#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;/<span class="name">ng-template</span>&gt;</span></span><span class="string">`</span></span><br><span class="line"><span class="string"></span></span><br></pre></td></tr></table></figure>
<p><ng-container> 是一个分组元素，且不会污染样式和布局，不存在 DOM 中。</p>
<p><strong>模板引用变量</strong></p>
<p>用来引用模板中的某个 DOM 元素，它还可以引用 Angular 组件或指令或Web Component。<strong>不要在同一个模板中多次定义同一个变量名!</strong></p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// #phone 代表 input 这个元素</span></span><br><span class="line">&lt;input #phone placeholder=<span class="string">&quot;phone number&quot;</span>&gt;</span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">button</span> (<span class="attr">click</span>)=<span class="string">&quot;callPhone(phone.value)&quot;</span>&gt;</span>Call<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// #heroForm=&quot;ngForm&quot; 可以将引用内容进行修改</span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">form</span> (<span class="attr">ngSubmit</span>)=<span class="string">&quot;onSubmit(heroForm)&quot;</span> #<span class="attr">heroForm</span>=<span class="string">&quot;ngForm&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<p><strong>输入和输出属性</strong></p>
<p>输入和输出属性在父组件上呈现为数据绑定属性，在子组件中需要有 @Input 和 @Output 装饰器声明流入的成员。</p>
<p>输入属性是一个带有 @Input 装饰器的可设置属性，输入属性通常接收数据值。</p>
<p>输出属性是一个带有 @Output 装饰器的可观察对象型的属性。 这个属性几乎总是返回 Angular 的<strong>EventEmitter</strong>，输出属性暴露事件生产者。</p>
<p>输入和输出属性可以使用别名，第一种方式是在装饰器中传入别名字符串，第二种是在元数据的 inputs 和 outputs 数组中用冒号右边表示别名：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Output</span>(<span class="string">&#x27;myClick&#x27;</span>) clicks = <span class="keyword">new</span> <span class="title class_">EventEmitter</span>&lt;string&gt;();</span><br><span class="line">@<span class="title class_">Directive</span>(&#123;</span><br><span class="line">  <span class="attr">outputs</span>: [<span class="string">&#x27;clicks:myClick&#x27;</span>]  <span class="comment">// propertyName:alias</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<h3 id="服务"><a href="#服务" class="headerlink" title="服务"></a>服务</h3><p>服务是一个广义的概念，它包括应用所需的任何值、函数或特性。但是狭义上，服务就是脱离视图的一些定义了用途的类。将组件和服务分开，可以提高模块性和复用性，让代码更加精简。组件的工作只管用户体验，而应该把诸如从服务器获取数据、验证用户输入或直接往控制台中写日志等工作委托给各种服务。</p>
<p><strong>依赖注入</strong></p>
<p>服务是用 @Injectable() 装饰器来提供元数据的类。提供服务需要在 service 类的装饰器 @Injectable() 中注册服务提供者 providedIn 字段。服务还可以在模块 NgModule 和组件级别进行注入，都是在装饰器中添加 providers 字段。</p>
<h2 id="主要概念"><a href="#主要概念" class="headerlink" title="主要概念"></a>主要概念</h2><h3 id="组件-1"><a href="#组件-1" class="headerlink" title="组件"></a>组件</h3><p><strong>组件的构成</strong></p>
<p>每一个组件可以使用一个目录进行包含，其中包括 HTML 模板、一个 TypeScript 类（包含应用数据和逻辑）和一个模板的样式（可选）。其中在 TypeScript 类中需要指明组件的 CSS 选择器（即组件标签名），以及模板文件和样式文件。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// [name].component.ts</span></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-component-overview&#x27;</span>,</span><br><span class="line">  <span class="attr">templateUrl</span>: <span class="string">&#x27;./component-overview.component.html&#x27;</span>,</span><br><span class="line">  <span class="attr">styleUrls</span>: [<span class="string">&#x27;./component-overview.component.css&#x27;</span>]</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>CSS 选择器一般以 app 开头，例如一个组件标签为 <app-hello-world>，那么它的 CSS 选择器（selector）就应当是 app-hello-world，组件 ts 类文件最好命名为 hello-world.component.ts。模板和样式文件亦然。</p>
<p>HTML 模板可以使用外部的 templateUrl，也可以使用 template 属性指定一段 HTML 代码，可以使用模板字符串来实现。<strong>templateUrl 和 template 两个字段不能同时出现</strong>。</p>
<p>styleUrls 字段以<strong>字符串数组</strong>形式指定模板的样式文件，也可以使用 styles 字段（字符串数组类型）来内置样式。</p>
<p><strong>组件生命周期和钩子</strong></p>
<p>Angular 中生命周期钩子由 TypeScript 的 Interface 实现，在组件或指令类实现中，需要 implements 相应的钩子接口，每个接口都有唯一的一个钩子方法，方法名即接口名前加 on。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Directive</span>()</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">PeekABooDirective</span> implements <span class="title class_">OnInit</span> &#123;</span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">private logger: LoggerService</span>) &#123; &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// implement OnInit&#x27;s `ngOnInit` method</span></span><br><span class="line">  <span class="title function_">ngOnInit</span>(<span class="params"></span>) &#123; <span class="variable language_">this</span>.<span class="title function_">logIt</span>(<span class="string">`OnInit`</span>); &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">logIt</span>(<span class="params">msg: string</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">logger</span>.<span class="title function_">log</span>(<span class="string">`#<span class="subst">$&#123;nextId++&#125;</span> <span class="subst">$&#123;msg&#125;</span>`</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>生命周期顺序如下：</p>
<ul>
<li>ngOnChanges()：设置或重新设置数据绑定的输入属性时响应。 该方法接受当前和上一属性值的 SimpleChanges 对象，该钩子频繁发生，钩子中的操作会十分影响性能</li>
<li>ngOnInit()：第一次显示数据绑定和设置指令/组件的输入属性之后，初始化指令/组件。在第一轮 ngOnChanges() 完成之后调用，只调用一次</li>
<li>ngDoCheck()：检测，并在发生 Angular 无法或不愿意自己检测的变化时作出反应</li>
<li>ngAfterContentInit()：把外部内容投影进组件视图或指令所在的视图之后调用</li>
<li>ngAfterContentChecked()：检查完被投影到组件或指令中的内容之后调用</li>
<li>ngAfterViewInit()：初始化完组件视图及其子视图或包含该指令的视图之后调用</li>
<li>ngAfterViewChecked()：每当 Angular 做完组件视图和子视图或包含该指令的视图的变更检测之后调用</li>
<li>ngOnDestroy()：每当 Angular 每次销毁指令/组件之前调用并清扫，事件监听解绑、反订阅可观察对象和停止计时器等操作可在此钩子进行。</li>
</ul>
<p><strong>视图封装</strong></p>
<p>组件的 CSS 样式被封装进了自己的视图中，而不会影响到应用程序的其它部分。视图封装有三种模式：</p>
<ul>
<li><code>ShadowDom</code> 模式使用浏览器原生的 Shadow DOM 实现，来为组件的宿主元素附加一个 Shadow DOM。组件的视图被附加到这个 Shadow DOM 中，组件的样式也被包含在这个 Shadow DOM 中。(不进不出，没有样式能进来，组件样式出不去。)</li>
<li><code>Emulated</code> 模式（<strong>默认值</strong>）通过预处理（并改名）CSS 代码来模拟 Shadow DOM 的行为，以达到把 CSS 样式局限在组件视图中的目的。(只进不出，全局样式能进来，组件样式出不去)</li>
<li><code>None</code> 意味着不使用视图封装。 Angular 会把 CSS 添加到全局样式中。而不会应用上前面讨论过的那些作用域规则、隔离和保护等。 从本质上来说，这跟把组件的样式直接放进 HTML 是一样的。</li>
</ul>
<p><strong>组件交互</strong></p>
<ul>
<li><p>输入型绑定（父传子）：父组件通过在模板上添加属性指令 [] 将数据传入子组件，子组件需要在类中使用 @Input 装饰器引入对应的数据变量名</p>
<ul>
<li>setter 拦截输入数据变化：在上面输入型绑定的基础上，子组件中可以对传入的属性进行 get 和 set 重写，在 set 方法中可以拦截输入数据变化</li>
<li><p>ngOnChanges() 钩子监听数据变化：在上面输入型绑定的基础上，子组件的类可以实现 onChanges 接口，在 ngOnChanges 方法中监听输入数据的变化</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// parent component</span></span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Component</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-name-parent&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`&lt;app-name-child *ngFor=&quot;let name of names&quot; [name]=&quot;name&quot;&gt;&lt;/app-name-child&gt;`</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">NameParentComponent</span> &#123;</span><br><span class="line">  names = [<span class="string">&#x27;Dr IQ&#x27;</span>, <span class="string">&#x27;   &#x27;</span>, <span class="string">&#x27;  Bombasto  &#x27;</span>];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// child component</span></span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Component</span>, <span class="title class_">Input</span>, <span class="title class_">OnChanges</span>, <span class="title class_">SimpleChanges</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-name-child&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">&#x27;&lt;h3&gt;&quot;&#123;&#123;name&#125;&#125;&quot;&lt;/h3&gt;&#x27;</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">NameChildComponent</span> implements <span class="title class_">OnChanges</span> &#123;</span><br><span class="line">  @<span class="title class_">Input</span>()</span><br><span class="line">  <span class="keyword">get</span> <span class="title function_">name</span>(): string &#123; <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">_name</span>; &#125;</span><br><span class="line">  <span class="keyword">set</span> <span class="title function_">name</span>(<span class="params">name: string</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">_name</span> = (name &amp;&amp; name.<span class="title function_">trim</span>()) || <span class="string">&#x27;&lt;no name set&gt;&#x27;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  private _name = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line"></span><br><span class="line">	<span class="title function_">ngOnChanges</span>(<span class="params">changes: SimpleChanges</span>) &#123;</span><br><span class="line">		<span class="variable language_">console</span>.<span class="title function_">log</span>(changes);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>父组件监听子组件的事件：子组件暴露一个 EventEmitter 属性，当事件发生时，子组件利用该属性 emits (向上弹射)事件。父组件绑定到这个事件属性，并在事件发生时作出回应。</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// parent component</span></span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Component</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-vote-taker&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">		&lt;p&gt;&#123;&#123;clickCount&#125;&#125;&lt;/p&gt;</span></span><br><span class="line"><span class="string">    &lt;app-child (changeMsg)=&quot;onMsg($event)&quot;&gt;&lt;/app-child&gt;</span></span><br><span class="line"><span class="string">		`</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">VoteTakerComponent</span> &#123;</span><br><span class="line">  message = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">onMsg</span>(<span class="params">msg: string</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">message</span> = msg.<span class="property">length</span> &gt; <span class="number">0</span> ? msg : <span class="string">&#x27;&#x27;</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Component</span>, <span class="title class_">EventEmitter</span>, <span class="title class_">Input</span>, <span class="title class_">Output</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-voter&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;button (click)=&quot;onChangeMsg(&#x27;a&#x27;)&quot;&gt;Show A&lt;/button&gt;</span></span><br><span class="line"><span class="string">    &lt;button (click)=&quot;onChangeMsg(&#x27;b&#x27;)&quot;&gt;Show B&lt;/button&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">VoterComponent</span> &#123;</span><br><span class="line">  @<span class="title class_">Input</span>()  <span class="attr">name</span>: string;</span><br><span class="line">  @<span class="title class_">Output</span>() changeMsg = <span class="keyword">new</span> <span class="title class_">EventEmitter</span>&lt;string&gt;();</span><br><span class="line"></span><br><span class="line">  <span class="title function_">onChangeMsg</span>(<span class="params">str: string</span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">changeMsg</span>.<span class="title function_">emit</span>(str);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>父组件、子组件通过本地变量交互，父组件在使用子组件时，在子组件标签中添加 #[tagName] 可以以此来表示子组件引用，通过 tagName 来访问子组件的属性和方法等（该方法只能在父组件模板中进行子组件引用的使用）</p>
</li>
<li><p>为了能够在父组件的类中调用子组件引用，父组件可以调用 @ViewChild()：</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">ParentComponent</span> &#123;</span><br><span class="line">  @<span class="title class_">ViewChild</span>(<span class="title class_">ChildComponent</span>)</span><br><span class="line">  private <span class="attr">childComponent</span>: <span class="title class_">ChildComponent</span>;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">start</span>(<span class="params"></span>) &#123; <span class="variable language_">this</span>.<span class="property">childComponent</span>.<span class="title function_">someFun</span>(); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>父组件和它的子组件共享同一个服务，利用该服务在组件家族内部实现双向通讯。父组件中需要在装饰器的 provider 传入该服务，父子组件构造函数中都需要有该服务作为参数传入。</p>
</li>
</ul>
<p><strong>组件样式</strong></p>
<p>组件在 @Component 装饰器里可以通过 styles 字段（字符串数组）设置组件样式，也可以通过 styleUrls 字段（字符串数组）设置外部的样式文件。这两种方式都只会影响本组件，不会影响其他组件、模块以及投影进来的组件。</p>
<p>可以通过模板中内联样式来设置组件的样式：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-hero-controls&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;style&gt;</span></span><br><span class="line"><span class="string">      button &#123;</span></span><br><span class="line"><span class="string">        background-color: white;</span></span><br><span class="line"><span class="string">        border: 1px solid #777;</span></span><br><span class="line"><span class="string">      &#125;</span></span><br><span class="line"><span class="string">    &lt;/style&gt;</span></span><br><span class="line"><span class="string">    &lt;button (click)=&quot;activate()&quot;&gt;Activate&lt;/button&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>还可以通过模板中添加 link 标签设置组件样式</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-hero-team&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    &lt;link rel=&quot;stylesheet&quot; href=&quot;../assets/hero-team.component.css&quot;&gt;</span></span><br><span class="line"><span class="string">    &lt;ul&gt;</span></span><br><span class="line"><span class="string">      &lt;li *ngFor=&quot;let member of hero.team&quot;&gt;</span></span><br><span class="line"><span class="string">        &#123;&#123;member&#125;&#125;</span></span><br><span class="line"><span class="string">      &lt;/li&gt;</span></span><br><span class="line"><span class="string">    &lt;/ul&gt;`</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>组件目录下的样式文件中可以使用 <code>@import</code> 语法引用其他样式文件。</p>
<p><strong>动态组件</strong></p>
<p><strong>Angular 元素和自定义元素</strong></p>
<h3 id="模板"><a href="#模板" class="headerlink" title="模板"></a>模板</h3><p><strong>插值</strong></p>
<p>在模板中，可以使用双花括号在标记文本中嵌入表达式，Angular 对所有双花括号中的表达式求值，把求值的结果转换成字符串，并把它们跟相邻的字符串字面量连接起来。</p>
<p>模板表达式不能使用具有或可能引发副作用的 JavaScript 表达式：</p>
<ul>
<li>赋值 (<code>=</code>, <code>+=</code>, <code>=</code>, <code>...</code>)</li>
<li><code>new</code>、<code>typeof</code>、<code>instanceof</code> 等运算符</li>
<li>使用 <code>;</code> 或 <code>,</code> 串联起来的表达式</li>
<li>自增和自减运算符：<code>++</code> 和 <code>-</code></li>
<li>一些 ES2015+ 版本的运算符</li>
<li>不支持位运算，比如 | 和 &amp;</li>
<li>只能使用表达式上下文的成员，不能使用 window、document 等全局命名空间的任何东西</li>
</ul>
<p>模板表达式上下文，指的是在这个组件实例中各种绑定值的来源。由模板变量、指令的上下文变量（如果有）和组件的成员叠加而成的，其优先级为模板变量 &gt; 指令的上下文变量 &gt; 组件的成员。</p>
<p><strong>模板语句</strong></p>
<p>是可在 HTML 中用于响应用户事件的方法或属性。 <code>(click)=&quot;deleteHero()&quot;</code> 等号右边引号中的就是模板语句。模板语句不允许使用以下 JavaScript 和模板表达式语法：</p>
<ul>
<li><code>new</code></li>
<li>递增和递减运算符 <code>++</code> 和 <code>-</code></li>
<li>赋值运算符，例如 <code>+=</code> 和 <code>=</code></li>
<li>按位运算符，例如 <code>|</code> 和 <code>&amp;</code></li>
<li><a href="https://angular.cn/guide/pipes">管道操作符</a></li>
</ul>
<p>使用插值表达式还是属性绑定？<strong>如果数据类型不是字符串，必须使用属性绑定</strong>。</p>
<p><strong>管道</strong></p>
<p>管道是一些简单的函数，用来对字符串、货币金额、日期和其他显示数据进行转换和格式化。可以在<strong>模板表达式</strong>中用来接受输入值并返回一个转换后的值。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span>&gt;</span>The hero&#x27;s birthday is &#123;&#123; birthday | date | uppercase&#125;&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>为自定义数据转换创建管道，可以使用 @Pipe() 装饰器（ng generate pipe）：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Pipe</span>, <span class="title class_">PipeTransform</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Pipe</span>(&#123;<span class="attr">name</span>: <span class="string">&#x27;exponentialStrength&#x27;</span>&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">ExponentialStrengthPipe</span> implements <span class="title class_">PipeTransform</span> &#123;</span><br><span class="line">  <span class="title function_">transform</span>(<span class="attr">value</span>: number, exponent?: number): number &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Math</span>.<span class="title function_">pow</span>(value, <span class="built_in">isNaN</span>(exponent) ? <span class="number">1</span> : exponent);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="Angular-表单"><a href="#Angular-表单" class="headerlink" title="Angular 表单"></a>Angular 表单</h3><p><strong>表单底层构造块</strong></p>
<ul>
<li><code>[FormControl](https://v7.angular.cn/api/forms/FormControl)</code> 实例用于追踪单个表单控件的值和验证状态。</li>
<li><code>[FormGroup](https://v7.angular.cn/api/forms/FormGroup)</code> 用于追踪一个表单控件组的值和状态。</li>
<li><code>[FormArray](https://v7.angular.cn/api/forms/FormArray)</code> 用于追踪表单控件数组的值和状态。</li>
<li><code>[ControlValueAccessor](https://v7.angular.cn/api/forms/ControlValueAccessor)</code> 用于在 Angular 的 <code>[FormControl](https://v7.angular.cn/api/forms/FormControl)</code> 实例和原生 DOM 元素之间创建一个桥梁。</li>
</ul>
<p><strong>表单模型</strong></p>
<p>响应式表单建立表单模型（表单模型是数据源），使用 FormControl 来建立单个表单控件的模型：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Component</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">FormControl</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/forms&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-reactive-favorite-color&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    Favorite Color: &lt;input type=&quot;text&quot; [formControl]=&quot;favoriteColorControl&quot;&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">FavoriteColorComponent</span> &#123;</span><br><span class="line">  favoriteColorControl = <span class="keyword">new</span> <span class="title class_">FormControl</span>(<span class="string">&#x27;&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>模板驱动表单（模板是数据源），使用 ngModel 与示例的数据进行联动：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Component</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"></span><br><span class="line">@<span class="title class_">Component</span>(&#123;</span><br><span class="line">  <span class="attr">selector</span>: <span class="string">&#x27;app-template-favorite-color&#x27;</span>,</span><br><span class="line">  <span class="attr">template</span>: <span class="string">`</span></span><br><span class="line"><span class="string">    Favorite Color: &lt;input type=&quot;text&quot; [(ngModel)]=&quot;favoriteColor&quot;&gt;</span></span><br><span class="line"><span class="string">  `</span></span><br><span class="line">&#125;)</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">FavoriteColorComponent</span> &#123;</span><br><span class="line">  favoriteColor = <span class="string">&#x27;&#x27;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><strong>更新表单的值：</strong></p>
<ul>
<li>setValue()：单个控件设置新值</li>
<li><p>patchValue()：用传入对象中的属性进行对应值的替换</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title function_">updateProfile</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="variable language_">this</span>.<span class="property">profile</span>.<span class="title function_">patchValue</span>(&#123;</span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;Keefe&#x27;</span></span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 只有 name 替换了，age 还是原来的值</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
<p><strong>FormBuilder</strong></p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">FormBuilder</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/forms&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">ProfileEditorComponent</span> &#123;</span><br><span class="line">  profileForm = <span class="variable language_">this</span>.<span class="property">fb</span>.<span class="title function_">group</span>(&#123;</span><br><span class="line">    <span class="attr">firstName</span>: [<span class="string">&#x27;&#x27;</span>],</span><br><span class="line">    <span class="attr">lastName</span>: [<span class="string">&#x27;&#x27;</span>],</span><br><span class="line">    <span class="attr">address</span>: <span class="variable language_">this</span>.<span class="property">fb</span>.<span class="title function_">group</span>(&#123;</span><br><span class="line">      <span class="attr">street</span>: [<span class="string">&#x27;&#x27;</span>],</span><br><span class="line">      <span class="attr">city</span>: [<span class="string">&#x27;&#x27;</span>],</span><br><span class="line">      <span class="attr">state</span>: [<span class="string">&#x27;&#x27;</span>],</span><br><span class="line">      <span class="attr">zip</span>: [<span class="string">&#x27;&#x27;</span>]</span><br><span class="line">    &#125;),</span><br><span class="line">  &#125;);</span><br><span class="line"> </span><br><span class="line">  <span class="title function_">constructor</span>(<span class="params">private fb: FormBuilder</span>) &#123; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>响应式表单：使用 FormArray 可以动态添加表单控件</p>
<p><strong>表单验证</strong></p>
<p>模板驱动表单的验证，Angular 会对 HTML 自带的一些属性进行同名函数验证，这里的appForbiddenName 是一个验证器的指令</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">&lt;input </span><br><span class="line">	id=<span class="string">&quot;name&quot;</span> </span><br><span class="line">	name=<span class="string">&quot;name&quot;</span> </span><br><span class="line">	<span class="keyword">class</span>=<span class="string">&quot;form-control&quot;</span></span><br><span class="line">  required minlength=<span class="string">&quot;4&quot;</span> </span><br><span class="line">	appForbiddenName=<span class="string">&quot;bob&quot;</span></span><br><span class="line">  [(ngModel)]=<span class="string">&quot;hero.name&quot;</span> </span><br><span class="line">	#name=<span class="string">&quot;ngModel&quot;</span> &gt;</span><br><span class="line"><span class="comment">// 这里的 #name=&quot;ngModel&quot; 是将 ngModel 导出成 name 变量，方便下面检测其 dirty 和touched 属性（判断是否填写过）</span></span><br><span class="line"></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;<span class="name">div</span> </span></span></span><br><span class="line"><span class="tag"><span class="language-xml">	*<span class="attr">ngIf</span>=<span class="string">&quot;name.invalid &amp;&amp; (name.dirty || name.touched)&quot;</span></span></span></span><br><span class="line"><span class="tag"><span class="language-xml">  <span class="attr">class</span>=<span class="string">&quot;alert alert-danger&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;<span class="name">div</span> *<span class="attr">ngIf</span>=<span class="string">&quot;name.errors.required&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    Name is required.</span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;<span class="name">div</span> *<span class="attr">ngIf</span>=<span class="string">&quot;name.errors.minlength&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    Name must be at least 4 characters long.</span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;<span class="name">div</span> *<span class="attr">ngIf</span>=<span class="string">&quot;name.errors.forbiddenName&quot;</span>&gt;</span></span></span><br><span class="line"><span class="language-xml">    Name cannot be Bob.</span></span><br><span class="line"><span class="language-xml">  <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br><span class="line"><span class="language-xml"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span></span><br></pre></td></tr></table></figure>
<p>响应式表单验证，FormControl的第一个参数是表单绑定的数据变量，第二个是验证条件（可以是数组，也可以是单个验证函数）</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="variable language_">this</span>.<span class="property">heroForm</span> = <span class="keyword">new</span> <span class="title class_">FormGroup</span>(&#123;</span><br><span class="line">    <span class="string">&#x27;name&#x27;</span>: <span class="keyword">new</span> <span class="title class_">FormControl</span>(<span class="variable language_">this</span>.<span class="property">hero</span>.<span class="property">name</span>, [</span><br><span class="line">       <span class="title class_">Validators</span>.<span class="property">required</span>,</span><br><span class="line">       <span class="title class_">Validators</span>.<span class="title function_">minLength</span>(<span class="number">4</span>),</span><br><span class="line">       <span class="title function_">forbiddenNameValidator</span>(<span class="regexp">/bob/i</span>) </span><br><span class="line">    ]),</span><br><span class="line">    <span class="string">&#x27;alterEgo&#x27;</span>: <span class="keyword">new</span> <span class="title class_">FormControl</span>(<span class="variable language_">this</span>.<span class="property">hero</span>.<span class="property">alterEgo</span>),</span><br><span class="line">    <span class="string">&#x27;power&#x27;</span>: <span class="keyword">new</span> <span class="title class_">FormControl</span>(<span class="variable language_">this</span>.<span class="property">hero</span>.<span class="property">power</span>, <span class="title class_">Validators</span>.<span class="property">required</span>)</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p><strong>自定义验证器</strong></p>
<p>验证器是一个工厂，返回一个验证器函数</p>
<p><strong>控件状态 CSS 类</strong></p>
<ul>
<li><code>.ng-valid</code></li>
<li><code>.ng-invalid</code></li>
<li><code>.ng-pending</code></li>
<li><code>.ng-pristine</code></li>
<li><code>.ng-dirty</code></li>
<li><code>.ng-untouched</code></li>
<li><code>.ng-touched</code></li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>Angular</tag>
      </tags>
  </entry>
  <entry>
    <title>了解浏览器标签页的休眠策略</title>
    <url>/2021/09/01/2021-09-01-understand-freezing-strategy-of-browser.html</url>
    <content><![CDATA[<p>标签页的休眠目前越来越多地被应用在了各大浏览器中，这个特性设计主要通过 Unload 标签页使其进入休眠模式，来达到节约电脑资源、内存的目的。与关闭标签页的效果不同，休眠的标签页是将标签页保持在浏览器中，但是状态为未加载的状态，点击标签页即可重新加载页面内容。这个过程可能会有白屏的情况出现，这是浏览器在重绘页面内容。</p>
<p>主流的浏览器中，Google Chrome 浏览器支持 Tab Freezing，Microsoft Edge 浏览器支持 Tab Sleeping，Opera 浏览器支持 Tab Snoozing，Firefox 浏览自支持 Automatic Tab Unloading。另外，大部分基于 Chrome 内核的浏览器支持了实验特性 Tab Groups，也支持了标签页以组维度休眠的特性功能。</p>
<span id="more"></span>
<h3 id="Google-and-Firefox"><a href="#Google-and-Firefox" class="headerlink" title="Google and Firefox"></a>Google and Firefox</h3><p>谷歌浏览器在2019年将 Tab Freeze 作为实验功能引入了 Chrome 浏览器。然而早在2015年谷歌就曾将一个类似的叫做 Tab Discard 的特性从浏览器中移除。目前 Tab Freeze 已经作为原生特性集成在 Chrome 浏览器中。</p>
<p>Tab Discard 是2015年谷歌新增的意向特性，称该特性能够节省内存。当电脑空闲内存较低时，浏览器会自动地将“uninteresting”的 tab 页丢弃。丢弃的页面会以<strong>隐藏起来的 tab 页</strong>、<strong>没有交互过的页面</strong>为准，越早的页面会先被丢弃。页面被丢弃时，页面会<strong>从系统内存中抹去</strong>（Chrome 的任务管理器中看不到该页面），但是其页面状态会保存在硬盘中，浏览器界面上的表现是没有任何改变的。当切换至被丢弃的页面时，浏览器会重载页面。</p>
<p>Tab Freezing 与上面的 Tab Discard 不一样。当一个 tab 页被冻结，其<strong>页面内容仍然会保留在系统内存中</strong>，但是 tab 页面将<strong>不会消耗 CPU</strong> 、<strong>也不会运行任何后台任务</strong>。举例来说，当我们打开一个数据量很大、性能消耗大的页面，过一阵子不与该页面交互，浏览器会自动冻结该页面，并停止页面上的任何脚本运行，直到再次与该页面交互。</p>
<p><strong>当浏览器实在需要放开一些内存时，会将冻结的页面丢弃。</strong></p>
<p>Tab Freezing 是在 Chrome 77 版本中引入的新特性，在 79 版本中，Chrome 就将该特性自动集成在浏览器中，浏览器会<strong>自动地</strong>对某些页面进行冻结和丢弃。</p>
<p>通过在浏览器中打开 chrome://discards 页面，能够对所有页面进行重新加载和从内存中丢弃：</p>
<p><img src="https://intranetproxy.alipay.com/skylark/lark/0/2022/png/12056524/1670204871308-ea866489-4bb6-4ec6-b21b-c351657eed68.png?x-oss-process=image%2Fresize%2Cw_750%2Climit_0" alt="https://intranetproxy.alipay.com/skylark/lark/0/2022/png/12056524/1670204871308-ea866489-4bb6-4ec6-b21b-c351657eed68.png?x-oss-process=image%2Fresize%2Cw_750%2Climit_0"></p>
<p>这两种方式能够有效地解决当浏览器消耗过高的浏览器内存。不过尽管当你的系统内存还很充裕，Chrome浏览器仍然会冻结一些长期没有交互的 tab 页来达到 CPU 消耗减少和节电的目的。未来 Microsoft Edge 浏览器也会逐渐引入这些特性。</p>
<p>目前网上无法查询到具体 Chrome 是采取什么策略去实施这两种休眠策略。</p>
<h3 id="参考文章"><a href="#参考文章" class="headerlink" title="参考文章"></a>参考文章</h3><p><a href="https://www.howtogeek.com/444481/how-chromes-tab-freezing-will-save-cpu-and-battery/">[1]How Chrome’s “Tab Freezing” Will Save CPU and Battery</a></p>
<p><a href="https://support.google.com/chrome/a/thread/19713332/chrome-tabs-intermittent-hang-or-crash-with-white-screen?hl=en">[2]Chrome tabs intermittent hang or crash with white screen</a></p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Browser</tag>
        <tag>Chrome</tag>
        <tag>浏览器</tag>
      </tags>
  </entry>
  <entry>
    <title>RxJS 学习</title>
    <url>/2021/07/30/2021-07-30-rxjs-study-note.html</url>
    <content><![CDATA[<p>RxJS 是异步和事件驱动编程的一个库，主要使用 Observable sequence（可观察序列）。RxJS 提供一个核心类 Observable，和一些卫星类（Observer, Schedulers, Subjects）、操作符（Operators）。</p>
<p>RxJS 是适用于“事件”的 Lodash 工具库。</p>
<span id="more"></span>
<p>RxJS 中异步事件管理的核心概念有如下：</p>
<ul>
<li><strong>Observable:</strong> 定义的在未来可能发生能被观察到的被观察事件</li>
<li><strong>Observer:</strong> 当被观察事件发生时的观察者，一般都是回调函数</li>
<li><strong>Subscription:</strong> 将观察者订阅在被观察事件上的订阅动作，可以用来取消订阅</li>
<li><strong>Operators:</strong> 是一系列纯函数，将被观察事件的结果进行函数式编程处理传送到观察者中，例如 map、filter、concat 和 reduce等</li>
<li><strong>Subject:</strong> 相当于一个事件发射器，收到一个信息后将值/事件广播到多个观察者。</li>
<li><strong>Schedulers:</strong> 并发控制器，能够协调并发事件的返回顺序？</li>
</ul>
<p>RxJS 的核心链路：</p>
<ol>
<li>建立可观察的 Observable： <code>var clicks$ = rxjs.fromEvent(document, &#39;click&#39;)</code></li>
<li>建立观察者 Observer： <code>var observer = &#123; next: (x) =&gt; &#123; console.log(x) &#125; &#125;</code></li>
<li>建立订阅关系 Subscription： <code>var subs$ = clicks$.subscribe(observer);</code></li>
<li>取消订阅关系： <code>subs$.unsubscribe()</code></li>
</ol>
<p>该链路中可以对 Observable 产生的结果进行处理再传入 Observer，这个过程需要用到 Operators，再最新版本中，应该将这些函数式编程通过 pipe() 方法进行流式处理：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; fromEvent &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; throttleTime, map, scan &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs/operators&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>).<span class="title function_">pipe</span>(</span><br><span class="line">  <span class="title function_">throttleTime</span>(<span class="number">1000</span>),</span><br><span class="line">  <span class="title function_">map</span>(<span class="function"><span class="params">event</span> =&gt;</span> event.<span class="property">clientX</span>),</span><br><span class="line">  <span class="title function_">scan</span>(<span class="function">(<span class="params">count, clientX</span>) =&gt;</span> count + clientX, <span class="number">0</span>)</span><br><span class="line">).<span class="title function_">subscribe</span>(<span class="function"><span class="params">count</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(count));</span><br></pre></td></tr></table></figure>
<p>RxJS 的中心思想：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>).<span class="title function_">subscribe</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Clicked!&#x27;</span>));</span><br><span class="line"><span class="comment">// Observable.subscribe(Observer);</span></span><br></pre></td></tr></table></figure>
<h2 id="Observer"><a href="#Observer" class="headerlink" title="Observer"></a>Observer</h2><p>是 Observable 生产的数据的消费者，由一系列回调函数组成 next, error, and complete。使用时，需要将 Observer 作为参数传入 subscribe() 方法中：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> observer = &#123;</span><br><span class="line">  <span class="attr">next</span>: <span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Observer got a next value: &#x27;</span> + x),</span><br><span class="line">  <span class="attr">error</span>: <span class="function"><span class="params">err</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">error</span>(<span class="string">&#x27;Observer got an error: &#x27;</span> + err),</span><br><span class="line">  <span class="attr">complete</span>: <span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Observer got a complete notification&#x27;</span>),</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">observable.<span class="title function_">subscribe</span>(observer);</span><br></pre></td></tr></table></figure>
<p>简单来说，Observer 就是一个拥有三个回调函数的对象，每个回调函数对应 Observable 的三种生产结果。</p>
<h2 id="Observable"><a href="#Observable" class="headerlink" title="Observable"></a>Observable</h2><p>可以将 Observable 理解为数据生产者，将 Observer 理解为数据消费者。</p>
<h3 id="Push-vs-Pull"><a href="#Push-vs-Pull" class="headerlink" title="Push vs Pull"></a>Push vs Pull</h3><p>数据生产者和消费者之间的沟通有两种模式：push 和 pull。pull 模式是指消费者来决定什么时候生产数据，类似于 ajax 请求服务器返回数据（每个 JavaScript 方法都是这种模式，还有新推出的 Generator 函数）。push 模式是指生产者来决定什么时候发送数据到消费者，Promise 是最常见的 push 模式。</p>
<p>Observable 就是一个 push 模式的数据生产者，将生产的数据送到观察者手中。</p>
<h3 id="创建-Observable"><a href="#创建-Observable" class="headerlink" title="创建 Observable"></a>创建 Observable</h3><p><strong>第一种方式是通过构造函数来创建：</strong></p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Observable 构造函数需要传入一个方法，该方法可以有内部逻辑，也可以实现给 Observer 发送数据（调用 .next() 推入数据）</span></span><br><span class="line"><span class="keyword">const</span> foo = <span class="keyword">new</span> <span class="title class_">Observable</span>(<span class="function"><span class="params">subscriber</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Hello&#x27;</span>);</span><br><span class="line">  subscriber.<span class="title function_">next</span>(<span class="number">42</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 订阅时，需要将 Observer 传入</span></span><br><span class="line">foo.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(x);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>Observable 和 Promise、EventEmitter 都不一样。函数和 Observable 都是惰性的，即不调用/不订阅时，函数和 Observable 都不会发生。为 Observable 订阅观察者就像调用一个函数。Observable 可以<strong>同步或异步</strong>地将数据生产给 Observer。</p>
<p><strong>第二种方式是通过 creation operator：</strong></p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title function_">first</span>()(<span class="title function_">of</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)).<span class="title function_">subscribe</span>(<span class="function">(<span class="params">v</span>) =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`value: <span class="subst">$&#123;v&#125;</span>`</span>));</span><br></pre></td></tr></table></figure>
<h3 id="订阅-Observable"><a href="#订阅-Observable" class="headerlink" title="订阅 Observable"></a>订阅 Observable</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">observable.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br></pre></td></tr></table></figure>
<p>上面说到订阅 Observable 就像函数调用，在 subscribe() 的参数中提供回调函数，Observable 会将生产的数据发送到这些回调函数中。</p>
<h3 id="执行-Observable"><a href="#执行-Observable" class="headerlink" title="执行 Observable"></a>执行 Observable</h3><p>Observable 执行指的就是将生产数据经过函数式编程处理传送到 Observer 回调函数中。执行会有三种情况，分别由三种回调进行传递：</p>
<ul>
<li>Next 回调：正常生产数据到观察者</li>
<li>Error 回调：try……catch 或 throw 出 JavaScript Error or exception 到观察者</li>
<li>Complete 回调：不传递任何数据，是一个空参数的方法</li>
</ul>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Observable</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs&#x27;</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">const</span> observable = <span class="keyword">new</span> <span class="title class_">Observable</span>(<span class="keyword">function</span> <span class="title function_">subscribe</span>(<span class="params">subscriber</span>) &#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    subscriber.<span class="title function_">next</span>(<span class="number">1</span>);</span><br><span class="line">    subscriber.<span class="title function_">next</span>(<span class="number">2</span>);</span><br><span class="line">    subscriber.<span class="title function_">complete</span>();</span><br><span class="line">  &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">    subscriber.<span class="title function_">error</span>(err); <span class="comment">// delivers an error if it caught one</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h3 id="取消订阅-Observable"><a href="#取消订阅-Observable" class="headerlink" title="取消订阅 Observable"></a>取消订阅 Observable</h3><figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="keyword">from</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> observable = <span class="title function_">from</span>([<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>]);</span><br><span class="line"><span class="keyword">const</span> subscription = observable.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br><span class="line">subscription.<span class="title function_">unsubscribe</span>();</span><br></pre></td></tr></table></figure>
<p>unsubscribe() 方法可以将已经触发的 Observable 停止触发。可以在创建 Observable 时，在其中返回一个 unsubscribe 函数来指明如何取消该 Observable 的订阅。</p>
<h2 id="Operators"><a href="#Operators" class="headerlink" title="Operators"></a>Operators</h2><p>RxJS 中的操作符即函数，其中有两种操作符：</p>
<ul>
<li><p>管道类操作符：可以使用 <code>observableInstance.pipe(operator())</code> 管道方法的操作符函数，它接受一个 Observable 并生成另一个 Observable</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">obs.<span class="title function_">pipe</span>(</span><br><span class="line">  <span class="title function_">op1</span>(),</span><br><span class="line">  <span class="title function_">op2</span>(),</span><br><span class="line">  <span class="title function_">op3</span>(),</span><br><span class="line">  <span class="title function_">op3</span>(),</span><br><span class="line">)</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建类操作符：可以单独调用来创建一个新的 Observable</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> observable = <span class="title function_">interval</span>(<span class="number">1000</span> <span class="comment">/* number of milliseconds */</span>);</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="宝石图"><a href="#宝石图" class="headerlink" title="宝石图"></a>宝石图</h3><p><img src="RxJS%20%E5%AD%A6%E4%B9%A0%2077e4aba53ab04c9594330a0dc988ebc3/marble-diagram-anatomy.svg" alt="RxJS%20%E5%AD%A6%E4%B9%A0%2077e4aba53ab04c9594330a0dc988ebc3/marble-diagram-anatomy.svg"></p>
<p>宝石图中，时间从左到右流动，上图描述了每一块“宝石”是如何生产的。上面箭头右边的竖线表示 complete。中间的框指的是上面的输入如何进行处理的方法，即 Operator。下面箭头表示经过 Operator 操作后输出的 Observable。× 表示异常终止。</p>
<h3 id="创建操作符"><a href="#创建操作符" class="headerlink" title="创建操作符"></a>创建操作符</h3><p>使用 pipe() 函数创建新的操作符：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Observable</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">delay</span>(<span class="params">delayInMillis</span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function">(<span class="params">observable</span>) =&gt;</span> <span class="keyword">new</span> <span class="title class_">Observable</span>(<span class="function"><span class="params">observer</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// this function will called each time this</span></span><br><span class="line">    <span class="comment">// Observable is subscribed to.</span></span><br><span class="line">    <span class="keyword">const</span> allTimerIDs = <span class="keyword">new</span> <span class="title class_">Set</span>();</span><br><span class="line">    <span class="keyword">const</span> subscription = observable.<span class="title function_">subscribe</span>(&#123;</span><br><span class="line">      <span class="title function_">next</span>(<span class="params">value</span>) &#123;</span><br><span class="line">        <span class="keyword">const</span> timerID = <span class="built_in">setTimeout</span>(<span class="function">() =&gt;</span> &#123;</span><br><span class="line">          observer.<span class="title function_">next</span>(value);</span><br><span class="line">          allTimerIDs.<span class="title function_">delete</span>(timerID);</span><br><span class="line">        &#125;, delayInMillis);</span><br><span class="line">        allTimerIDs.<span class="title function_">add</span>(timerID);</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="title function_">error</span>(<span class="params">err</span>) &#123;</span><br><span class="line">        observer.<span class="title function_">error</span>(err);</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="title function_">complete</span>(<span class="params"></span>) &#123;</span><br><span class="line">        observer.<span class="title function_">complete</span>();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="comment">// the return value is the teardown function,</span></span><br><span class="line">    <span class="comment">// which will be invoked when the new</span></span><br><span class="line">    <span class="comment">// Observable is unsubscribed from.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="function">() =&gt;</span> &#123;</span><br><span class="line">      subscription.<span class="title function_">unsubscribe</span>();</span><br><span class="line">      allTimerIDs.<span class="title function_">forEach</span>(<span class="function"><span class="params">timerID</span> =&gt;</span> &#123;</span><br><span class="line">        <span class="built_in">clearTimeout</span>(timerID);</span><br><span class="line">      &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="Subject"><a href="#Subject" class="headerlink" title="Subject"></a>Subject</h2><p>RxJS Subject 是一种特殊的 Observable，它可以让生产的数据广播到多个 Observers。Subjects 类似于 EventEmitter，他们维护着多个注册的观察者。</p>
<p>Subject 既是 Observable 也是 Observer。也就是说 Subject 可以被 subscribe，也拥有 next、error 和 complete 回调函数。</p>
<p>由于 Subject 是 Observer，所以它可以作为 Observer 去订阅一个 Observable：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">Subject</span>, <span class="keyword">from</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> subject = <span class="keyword">new</span> <span class="title class_">Subject</span>&lt;number&gt;();</span><br><span class="line"></span><br><span class="line">subject.<span class="title function_">subscribe</span>(&#123;</span><br><span class="line">  <span class="attr">next</span>: <span class="function">(<span class="params">v</span>) =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`observerA: <span class="subst">$&#123;v&#125;</span>`</span>)</span><br><span class="line">&#125;);</span><br><span class="line">subject.<span class="title function_">subscribe</span>(&#123;</span><br><span class="line">  <span class="attr">next</span>: <span class="function">(<span class="params">v</span>) =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`observerB: <span class="subst">$&#123;v&#125;</span>`</span>)</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> observable = <span class="title function_">from</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line"></span><br><span class="line">observable.<span class="title function_">subscribe</span>(subject); <span class="comment">// You can subscribe providing a Subject</span></span><br></pre></td></tr></table></figure>
<p>所以，Subjects 可以订阅 Observable，也可以被多个 Observer 订阅：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="keyword">from</span>, <span class="title class_">Subject</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs&#x27;</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; multicast &#125; <span class="keyword">from</span> <span class="string">&#x27;rxjs/operators&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> source = <span class="title function_">from</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line"><span class="keyword">const</span> subject = <span class="keyword">new</span> <span class="title class_">Subject</span>();</span><br><span class="line"><span class="comment">// multicast 返回一个 ConnectableObservable，这是有着 connect() 方法的 Observable</span></span><br><span class="line"><span class="keyword">const</span> multicasted = source.<span class="title function_">pipe</span>(<span class="title function_">multicast</span>(subject));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 底层是 `subject.subscribe(&#123;...&#125;)`:</span></span><br><span class="line">multicasted.<span class="title function_">subscribe</span>(&#123;</span><br><span class="line">  <span class="attr">next</span>: <span class="function">(<span class="params">v</span>) =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`observerA: <span class="subst">$&#123;v&#125;</span>`</span>)</span><br><span class="line">&#125;);</span><br><span class="line">multicasted.<span class="title function_">subscribe</span>(&#123;</span><br><span class="line">  <span class="attr">next</span>: <span class="function">(<span class="params">v</span>) =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`observerB: <span class="subst">$&#123;v&#125;</span>`</span>)</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 底层是 `source.subscribe(subject)`，这才是真正决定共享的 Observable 开始执行</span></span><br><span class="line">multicasted.<span class="title function_">connect</span>();</span><br></pre></td></tr></table></figure>
<h3 id="引用计数-ref-counting"><a href="#引用计数-ref-counting" class="headerlink" title="引用计数 ref counting"></a>引用计数 ref counting</h3><p>refCount() 方法可以让一个被共享的 Observable自动从第一个订阅关系开始执行，到最后一个订阅解除时停止。</p>
<h3 id="特殊的-Subject"><a href="#特殊的-Subject" class="headerlink" title="特殊的 Subject"></a>特殊的 Subject</h3><ul>
<li><p>BehaviorSubject</p>
<p>  主要用于时间上连续的值，比如一个人的年龄 Stream 可以用 BehaviorSubject 表示。例如一个 Observer 中途订阅了一个 BehaviorSubject，那么他会紧跟最近一次生产的数据而非下一此生产的数据（普通的 Subject 是从下一次生产数据开始）</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 传入的参数是初始值</span></span><br><span class="line"><span class="keyword">const</span> subject = <span class="keyword">new</span> <span class="title class_">BehaviorSubject</span>(<span class="number">0</span>); </span><br></pre></td></tr></table></figure>
</li>
<li><p>ReplaySubject</p>
<p>  这种 Subject 会记录所有生产的旧值，在新的观察者订阅时，会将所有旧值重复发送一遍</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 传入的参数是缓存的旧值个数，例如新的订阅从5开始，那么新的订阅会收到3、4、5三个旧值</span></span><br><span class="line"><span class="keyword">const</span> subject = <span class="keyword">new</span> <span class="title class_">ReplaySubject</span>(<span class="number">3</span>); </span><br></pre></td></tr></table></figure>
</li>
<li><p>AsyncSubject</p>
<p>  这种特殊的 Subject 只会将最后一次生产的数据发送给 Observer，无论新旧订阅都是这样</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> subject = <span class="keyword">new</span> <span class="title class_">AsyncSubject</span>();</span><br></pre></td></tr></table></figure>
</li>
</ul>
<h2 id="Scheduler"><a href="#Scheduler" class="headerlink" title="Scheduler"></a>Scheduler</h2><p>Scheduler 能够控制一个订阅何时开始执行、数据何时生产。它有三个组件：</p>
<ul>
<li>是一种数据结构：它知道如何存储、基于优先级来对任务进行排列</li>
<li>是一个执行上下文：表示任务在哪里、何时执行，比如立即执行、在 setTimeout 的回调中等</li>
<li>有一个虚拟时钟：由 now() 方法提供获取时间的方法</li>
</ul>
<h2 id="Testing"><a href="#Testing" class="headerlink" title="Testing"></a>Testing</h2><h2 id="常见-Observable"><a href="#常见-Observable" class="headerlink" title="常见 Observable"></a>常见 Observable</h2><ul>
<li><p>of</p>
<p>  Each argument becomes a next notification.</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="title function_">of</span>(<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>)</span><br><span class="line">.<span class="title function_">subscribe</span>(</span><br><span class="line">  <span class="function"><span class="params">next</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;next:&#x27;</span>, next),</span><br><span class="line">  <span class="function"><span class="params">err</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;error:&#x27;</span>, err),</span><br><span class="line">  <span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;the end&#x27;</span>),</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
</li>
<li><p>forkJoin</p>
<p>  Wait for Observables to complete and then combine last values they emitted.</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> observable = <span class="title function_">forkJoin</span>(&#123;</span><br><span class="line">  <span class="attr">foo</span>: <span class="title function_">of</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>),</span><br><span class="line">  <span class="attr">bar</span>: <span class="title class_">Promise</span>.<span class="title function_">resolve</span>(<span class="number">8</span>),</span><br><span class="line">  <span class="attr">baz</span>: <span class="title function_">timer</span>(<span class="number">4000</span>),</span><br><span class="line">&#125;);</span><br><span class="line">observable.<span class="title function_">subscribe</span>(&#123;</span><br><span class="line"> <span class="attr">next</span>: <span class="function"><span class="params">value</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(value),</span><br><span class="line"> <span class="attr">complete</span>: <span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;This is how it ends!&#x27;</span>),</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">// 可以传对象或数组，输出也会是对应的类型</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>timer</p>
<p>  Its like interval, but you can specify when should the emissions start.</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 每一秒触发一次，开始有 3s 的 delay</span></span><br><span class="line"><span class="keyword">const</span> numbers = <span class="title function_">timer</span>(<span class="number">3000</span>, <span class="number">1000</span>);</span><br><span class="line">numbers.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br></pre></td></tr></table></figure>
</li>
<li><p>iif</p>
<p>  If statement for Observables</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> subscribeToFirst;</span><br><span class="line"><span class="keyword">const</span> firstOrSecond = <span class="title function_">iif</span>(</span><br><span class="line">  <span class="function">() =&gt;</span> subscribeToFirst,</span><br><span class="line">  <span class="title function_">of</span>(<span class="string">&#x27;first&#x27;</span>),</span><br><span class="line">  <span class="title function_">of</span>(<span class="string">&#x27;second&#x27;</span>),</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 根据 subscribeToFirst 这个变量进行条件订阅</span></span><br><span class="line">subscribeToFirst = <span class="literal">true</span>;</span><br><span class="line">firstOrSecond.<span class="title function_">subscribe</span>(<span class="function"><span class="params">value</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(value));</span><br><span class="line"></span><br><span class="line">subscribeToFirst = <span class="literal">false</span>;</span><br><span class="line">firstOrSecond.<span class="title function_">subscribe</span>(<span class="function"><span class="params">value</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(value));</span><br></pre></td></tr></table></figure>
</li>
<li><p>ajax / ajax.func</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> obs$ = ajax.<span class="title function_">getJSON</span>(<span class="string">`https://api.github.com/users?per_page=5`</span>).<span class="title function_">pipe</span>(</span><br><span class="line">  <span class="title function_">map</span>(<span class="function"><span class="params">userResponse</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;users: &#x27;</span>, userResponse)),</span><br><span class="line">  <span class="title function_">catchError</span>(<span class="function"><span class="params">error</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;error: &#x27;</span>, error);</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">of</span>(error);</span><br><span class="line">  &#125;)</span><br><span class="line">);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> users = <span class="title function_">ajax</span>(&#123;</span><br><span class="line">  <span class="attr">url</span>: <span class="string">&#x27;https://httpbin.org/delay/2&#x27;</span>,</span><br><span class="line">  <span class="attr">method</span>: <span class="string">&#x27;POST&#x27;</span>,</span><br><span class="line">  <span class="attr">headers</span>: &#123;</span><br><span class="line">    <span class="string">&#x27;Content-Type&#x27;</span>: <span class="string">&#x27;application/json&#x27;</span>,</span><br><span class="line">    <span class="string">&#x27;rxjs-custom-header&#x27;</span>: <span class="string">&#x27;Rxjs&#x27;</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="attr">body</span>: &#123;</span><br><span class="line">    <span class="attr">rxjs</span>: <span class="string">&#x27;Hello World!&#x27;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;).<span class="title function_">pipe</span>(</span><br><span class="line">  <span class="title function_">map</span>(<span class="function"><span class="params">response</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;response: &#x27;</span>, response)),</span><br><span class="line">  <span class="title function_">catchError</span>(<span class="function"><span class="params">error</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;error: &#x27;</span>, error);</span><br><span class="line">    <span class="keyword">return</span> <span class="title function_">of</span>(error);</span><br><span class="line">  &#125;)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
</li>
<li><p>fromEvent</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> clicks = <span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>); <span class="comment">// target, event</span></span><br><span class="line">clicks.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br></pre></td></tr></table></figure>
</li>
<li></li>
</ul>
<h2 id="常见-Operators"><a href="#常见-Operators" class="headerlink" title="常见 Operators"></a>常见 Operators</h2><ul>
<li><p>filter</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> clicks = <span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> clicksOnDivs = clicks.<span class="title function_">pipe</span>(<span class="title function_">filter</span>(<span class="function"><span class="params">ev</span> =&gt;</span> ev.<span class="property">target</span>.<span class="property">tagName</span> === <span class="string">&#x27;DIV&#x27;</span>));</span><br><span class="line">clicksOnDivs.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br></pre></td></tr></table></figure>
</li>
<li><p>map</p>
<p>  将每次生产的数据进行修改</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> clicks = <span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> positions = clicks.<span class="title function_">pipe</span>(<span class="title function_">map</span>(<span class="function"><span class="params">ev</span> =&gt;</span> ev.<span class="property">clientX</span>));</span><br><span class="line">positions.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br></pre></td></tr></table></figure>
</li>
<li><p>takeUntil</p>
<p>  初始化两种 Observable，在第二种 Observable 触发之前一直触发第一种</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> source = <span class="title function_">interval</span>(<span class="number">1000</span>);</span><br><span class="line"><span class="keyword">const</span> clicks = <span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> result = source.<span class="title function_">pipe</span>(<span class="title function_">takeUntil</span>(clicks));</span><br><span class="line">result.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br><span class="line"><span class="comment">// 0, 1, 2... till click on the DOM</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>switchMap</p>
<p>  先将每个值生成对应的 Observable，然后将这些 Observable 展开</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> switched = <span class="title function_">of</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>).<span class="title function_">pipe</span>(<span class="title function_">switchMap</span>(<span class="function">(<span class="params">x: number</span>) =&gt;</span> <span class="title function_">of</span>(x, x ** <span class="number">2</span>, x ** <span class="number">3</span>)));</span><br><span class="line">switched.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br><span class="line"><span class="comment">// 1, 1, 1, 2, 4, 8</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>tap</p>
<p>  可以对每一个生产的值进行处理，然后再返回原值（相当于轻轻触碰）</p>
  <figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> clicks = <span class="title function_">fromEvent</span>(<span class="variable language_">document</span>, <span class="string">&#x27;click&#x27;</span>);</span><br><span class="line"><span class="keyword">const</span> positions = clicks.<span class="title function_">pipe</span>(</span><br><span class="line">  <span class="title function_">tap</span>(<span class="function"><span class="params">ev</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(ev)),</span><br><span class="line">  <span class="title function_">map</span>(<span class="function"><span class="params">ev</span> =&gt;</span> ev.<span class="property">clientX</span>),</span><br><span class="line">);</span><br><span class="line">positions.<span class="title function_">subscribe</span>(<span class="function"><span class="params">x</span> =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(x));</span><br></pre></td></tr></table></figure>
</li>
<li><p>finalize</p>
<p>  在生产结束时执行传入的指定方法</p>
</li>
<li><p>catchError</p>
</li>
<li>startWith</li>
<li>debounceTime</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Angular</tag>
        <tag>RxJS</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript 文档精粹</title>
    <url>/2021/05/21/2021-05-21-typescript-docuemnt-essence.html</url>
    <content><![CDATA[<h3 id="Introduction-to-TypeScript"><a href="#Introduction-to-TypeScript" class="headerlink" title="Introduction to TypeScript"></a>Introduction to TypeScript</h3><ul>
<li>TypeScript is a super set of JavaScript. 一个没有任何预发错误的 js 文件也可以被 ts 正常编译。</li>
<li>TS 一般会支持到 JS 未来版本 stage3</li>
</ul>
<span id="more"></span>
<h3 id="TypeScript-Setup"><a href="#TypeScript-Setup" class="headerlink" title="TypeScript Setup"></a>TypeScript Setup</h3><ul>
<li>install NodeJS</li>
<li>install TypeScript compiler (npm install -g typescript)</li>
</ul>
<h3 id="TypeScript-Type"><a href="#TypeScript-Type" class="headerlink" title="TypeScript Type"></a>TypeScript Type</h3><p>Type 描述一个值拥有什么样的属性和方法，每一个值都有一个类型。Type的主要目的，一是为了 TypeScript 编译器分析代码错误，二是让开发者明确所处理的变量的特征和属性。</p>
<ul>
<li>Primitive Type: string, number, boolean, null, undefined, symbol<ul>
<li>null和undefined: 一般不会用作类型标记，只能赋值给void类型和其自身类型的变量</li>
</ul>
</li>
<li>Object Types: functions, arrays, classes etc.<ul>
<li>object vs. Object: object 代表所有非原始类型值，Object 描述 objects 所拥有的能力</li>
<li>{} 是一种特殊类型，空对象</li>
<li>tuple（元组）: 长度固定、元素确定且无重复的数组，可以有可选项带 ? 表示，通过[type1, type2…]进行标记</li>
<li>enum（枚举）: 一组常量，enum Month { Jan = 1, Feb, Mar … }，定义枚举，默认有各自序号，也可以为每个自行定义</li>
<li>array（数组）: 可以通过 type[] 或 Array<type> 进行标记</li>
<li>any: 不进行类型检查直接通过编译阶段</li>
<li>void: 没有任何类型，一般用于没有返回值的函数</li>
<li>never: 用不存在的值的类型，总抛出异常或永无返回值的函数表达式。只能赋值给自身类型的变量</li>
</ul>
</li>
<li>类型标记（type annotation: variable: [type]）</li>
<li>类型推断（type inference）<br>变量赋值、函数参数设置默认值、函数返回值类型确定时会自动推断其类型。TS 能够推断出当下变量最合适的一个类型。</li>
<li>类型断言：通过“尖括号 &lt;&gt; ”和 as 语法<br>当变量只声明未赋值时、变量类型无法推断、函数返回值类型不明确等情况是需要类型标记的。</li>
</ul>
<h3 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h3><ul>
<li>通过 ? 表示可选属性</li>
<li>通过添加 readonly 表示只读属性，只能在对象刚刚创建时赋值</li>
<li>如果有未知的需要扩展的属性，可以添加 [propName: string]: any 在接口中</li>
<li>方法的接口：（param1: type1, param2: type2): returnType</li>
<li>索引（类数组）类型 [index: number]: string，可以通过下标返回对应的值</li>
<li>类接口：强制一个类符合某种契约，可以通过 implements 对接口进行实现和拓展</li>
<li>接口继承：通过 extends 对接口进行集成，在原有接口上添加更多属性定义</li>
<li>混合类型：对象可以同时作为函数和对象使用</li>
</ul>
<h3 id="类"><a href="#类" class="headerlink" title="类"></a>类</h3><ul>
<li>类可以通过 extends 进行继承。派生类可以没有构造函数，若写构造函数必须调用 super()。在构造函数里访问 this 的属性之前，我们 一定要调用 super()</li>
<li>成员默认为 public；设置为 private 时就不能再声明它的类（包括派生类）外部使用；设置为 protected 时类似 private，但是派生类中可以访问</li>
<li>ts 使用鸭子类型（结构性类型系统），成员类型兼容（private、protected修饰符也要兼容）即类兼容，类实例可以相互赋值</li>
<li>构造函数也可以被标记成 protected。 这意味着这个类不能在包含它的类外被实例化，但是能被继承</li>
<li>readonly 修饰的成员必须在声明、构造函数中被初始化</li>
<li>静态成员，存在在类本身，使用时需要去访问类本身</li>
<li>abstract 修饰的抽象类作为其他派生类的基类使用，不可直接实例化，但是可以作为一种类型。抽象类中的抽象（abstract 修饰的）方法必须在派生类中实现</li>
<li>定义类即定义了实例的类型，类可以当做接口使用</li>
</ul>
<h3 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h3><ul>
<li>函数类型由参数类型（参数名并不重要）与返回值类型组成，例如 let myAdd: (x: number, y: number) =&gt; number = function(x: number, y: number): number { return x + y; };</li>
<li>可选参数必须在参数之后，用 ? 表示，不传时值为 undefined。参数默认值用 = 表示。</li>
<li>剩余参数必须在参数之后，用 …restOfParams 表示，restOfParams 作为参数数组使用。</li>
<li>如果需要对函数进行重载，需要给函数设置多个类型</li>
</ul>
<h3 id="泛型"><a href="#泛型" class="headerlink" title="泛型"></a>泛型</h3><p>当我们在设计函数或接口类型时，考虑到函数和接口的可复用性，可能会需要对参数和返回值类型进行动态传入，这时就需要类型的变量。反省函数指的就是可以传入类型变量作为参数，从而确定函数参数和返回值的类型。</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> loggingIdentity&lt;T&gt;(<span class="attr">arg</span>: T[]): T[] &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(arg.<span class="property">length</span>);</span><br><span class="line">    <span class="keyword">return</span> arg;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>泛型只用于函数，泛型使用 &lt;T, U&gt; 来表示（T 为类型参数，可以用任何参数名）</li>
<li>泛型接口：<code>interface GenericIdentityFn&lt;T&gt; &#123; (arg: T): T; &#125;</code></li>
<li>泛型类：<code>class GenericNumber&lt;T&gt; &#123; zeroValue: T; &#125;</code></li>
<li>泛型约束：给泛型增加条件，让泛型不再是任意类型：<ul>
<li>使用 extends 关键词实现约束：<code>&lt;T extends Lengthwise&gt;</code></li>
<li>传入参数为实例时：<code>function create&lt;T&gt;(c: &#123;new(): T; &#125;): T &#123; eturn new c(); &#125;</code>，<code>c:&#123;new():T&#125;</code> 和 <code>c:new()=&gt;T</code> 是一样的。new() 在此处时构造函数类型字面量，而 new func() 中 new 是操作符。</li>
</ul>
</li>
</ul>
<h3 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h3><ul>
<li>枚举用来定义一组常量，并且可以给每个枚举定义其数字值，默认从 0 开始；如果定义其值为字符串则不会有自增的特性。</li>
<li>枚举是在运行时真正存在的对象，意思是枚举在运行时可以当做对象使用</li>
<li>数字值的枚举成员可以类似下标进行反向映射</li>
<li>const 枚举：在编译阶段会被删除，<code>const enum Enum &#123; A = 1 &#125;</code></li>
<li>外部枚举：<code>declare enum Enum &#123; A = 1 &#125;</code></li>
</ul>
<h3 id="类型推论"><a href="#类型推论" class="headerlink" title="类型推论"></a>类型推论</h3><p>TS 中，没有明确指出类型，类型推论会帮助提供类型。这种推论只发生在<strong>初始化变量和成员</strong>，<strong>设置默认参数值</strong>和<strong>决定函数返回值</strong>时。</p>
<h3 id="类型兼容性"><a href="#类型兼容性" class="headerlink" title="类型兼容性"></a>类型兼容性</h3><p>TypeScript里的类型兼容性是基于结构子类型的，结构类型是一种只使用其成员来描述类型的方式，也就是鸭子模型。如果 A 属于 B，那么 B 类型可以赋值给 A 类型（也就是说多属性可以赋值给少属性的，反之则不可以）。那么这两种类型（接口）就是兼容的（兼容指的是这两种类型的变量可以相互赋值）。</p>
<ul>
<li>对象类型兼容：A、B 成员变量有包含关系时，多的可以赋值给少的。</li>
<li>函数类型兼容：参数有包含关系时，少参数的可以赋值给多参数的。返回值是简单值可以互相复制，如果是对象类型，遵循对象类型兼容。</li>
</ul>
<h3 id="高级类型"><a href="#高级类型" class="headerlink" title="高级类型"></a>高级类型</h3><ul>
<li>交叉类型：<A & B & C>，需要有 A、B、C 中的所有成员。</li>
<li>联合类型：<code>func(a: number | string)</code>，指一个变量可以是几种类型之一。需要使用类型断言（或 instanceof 操作符）判断到底是哪种类型。</li>
<li>类型别名：type NumOrStr = number | string，这不是一种新类型，而是一个名字来引用这些类型。类型别名不能被 extends/implements。</li>
<li>可辨识联合：联合的类型中有一个通用字段可以用来辨识各个类型</li>
<li>keyof 可以直接将对象或对象类型接口的 key 变成联合类型</li>
</ul>
<h3 id="Symbol"><a href="#Symbol" class="headerlink" title="Symbol"></a>Symbol</h3><p>Symbol 是一种原生类型，通过 Symbol() 构造函数创建。</p>
<ul>
<li>Symbol 创建后均为不可变且唯一的（尽管构造时传参相同）。</li>
<li>Symbol 构造的值可以被用作对象属性的 key，可以作为对象的成员属性名（包括方法）。</li>
<li>Symbol 有内置的，包括 Symbol.hasInstance、Symbol.isConcatSpreadable、Symbol.iterator、Symbol.match…</li>
</ul>
<h3 id="迭代器和生成器"><a href="#迭代器和生成器" class="headerlink" title="迭代器和生成器"></a>迭代器和生成器</h3><p>当一个对象实现了 Symbol.iterator 属性时，就认为这个对象是可迭代的。</p>
<ul>
<li>for…of：遍历可迭代的对象，调用对象上的Symbol.iterator方法，返回的是<strong>属性值</strong>。</li>
<li>for…in：与 for…of 相同，但是返回的是<strong>属性键</strong>。</li>
</ul>
<h3 id="模块"><a href="#模块" class="headerlink" title="模块"></a>模块</h3><p>模块在其自身的作用域里执行，而不是在全局作用域里；这意味着定义在一个模块里的变量，函数，类等等在模块外部是不可见的。如果想在不同作用域使用模块，必须使用 import 和 export 进行导入和导出。<strong>若没有标注 import 或 export</strong> 则视为全局作用域。</p>
<ul>
<li>导出<ul>
<li>导出声明：任何声明（比如变量，函数，类，类型别名或接口）都能够通过添加export关键字来导出。</li>
<li>B 重新导出 A 中的内容，是不会在 B 中添加变量，而是直接导出。</li>
<li>默认导出：每个模块有且仅有一个默认导出 default，类和函数声明可以直接被标记为默认导出。</li>
<li>若使用 <code>export = someClass/someFunc</code> 导出一个模块，则必须使用TypeScript的特定语法 <code>import module = require(&quot;someModule&quot;)</code> 来导入此模块。</li>
</ul>
</li>
<li>导入<ul>
<li>导入模块中某个内容 <code>import &#123; content &#125; from &#39;someModule&#39;</code>；导入整个模块<code>import * as someModule</code>。</li>
</ul>
</li>
</ul>
<h3 id="命名空间"><a href="#命名空间" class="headerlink" title="命名空间"></a>命名空间</h3><p>一个 ts 文件内部如果想使用模块，可以使用 namespace 关键字来进行声明。</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> <span class="title class_">Validation</span> &#123;</span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">interface</span> <span class="title class_">StringValidator</span> &#123;</span><br><span class="line">        <span class="title function_">isAcceptable</span>(<span class="attr">s</span>: <span class="built_in">string</span>): <span class="built_in">boolean</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="attr">validators</span>: &#123; [<span class="attr">s</span>: <span class="built_in">string</span>]: <span class="title class_">Validation</span>.<span class="property">StringValidator</span>; &#125; = &#123;&#125;;</span><br></pre></td></tr></table></figure>
<ul>
<li>就算相同的 namespace 分割在不同文件中，使用时也是同一个 namespace，编译成一个文件时需要添加 –outFile 参数</li>
<li>namespace 也可以嵌套：<code>namespace Shapes &#123; export namespace Polygons &#123; export class Triangle &#123; &#125; &#125; &#125;</code></li>
</ul>
<h3 id="模块解析"><a href="#模块解析" class="headerlink" title="模块解析"></a>模块解析</h3><p>编译器在查找导入的模块内容时，会遵循一定流程。</p>
<ul>
<li>相对导入是以 /，./，../ 开头的文件，相对于导入这些模块的文件的文件地址，不能解析为一个外部模块声明</li>
<li>所有其他类型的导入是非相对导入，通过路径映射来解析，可以被解析为外部模块声明</li>
<li><p>模块解析策略有 Classic 和 Node 两种。</p>
<ul>
<li>Classic：TS 以前的默认策略：在 <code>/root/src/folder/A.ts</code> 中 <code>import &#123; b &#125; from &#39;./moduleB&#39;;</code>从包含导入文件的目录依次向上级目录遍历<br>/root/src/folder/moduleB.ts<br>/root/src/folder/moduleB.d.ts<br>/root/src/moduleB.ts<br>/root/src/moduleB.d.ts<br>/root/moduleB.ts<br>/root/moduleB.d.ts<br>/moduleB.ts<br>/moduleB.d.ts</li>
<li>Node：通常在 NodeJS 中是通过 require 函数来导入模块的。而根据相对/非相对路径会有不同查找行为<ul>
<li>相对路径：在文件 <code>/root/src/folder/A.ts</code> 中 <code>import x = require(&#39;./moduleB&#39;);</code><ul>
<li>检查 <code>/root/src/moduleB.js</code> 文件是否存在</li>
<li>检查 <code>/root/src/moduleB</code>目录是否包含一个 package.json 文件，且 package.json 文件指定了一个 “main” 模块</li>
<li><code>/root/src/moduleB</code> 目录是否包含一个 index.js 文件</li>
</ul>
</li>
<li>非相对路径：在文件 <code>/root/src/folder/A.ts</code> 中 <code>import x = require(&#39;moduleB&#39;);</code><ul>
<li>首先在同一级目录下的 node_modules 目录中进行寻找，其次会在父级目录下的 node_modules 目录中进行寻找，以此向根目录方向查找：<br><code>/root/src/node_modules/moduleB.js</code> =&gt; <code>/root/src/node_modules/moduleB/package.json</code> =&gt; <code>/root/src/node_modules/moduleB/index.js/root/node_modules/moduleB.js</code> =&gt; <code>/root/node_modules/moduleB/package.json</code> =&gt; <code>/root/node_modules/moduleB/index.js</code></li>
</ul>
</li>
</ul>
</li>
<li><p>TypeScript（模仿 Node.js 运行时的解析策略来编译定位模块定义文件）：</p>
<ul>
<li>引入相对路径的文件时：<ul>
<li>从本级目录向根目录方向查找</li>
<li>在同级目录下，会依据 moduleB.ts =&gt; moduleB.tsx =&gt; moduleB.d.ts =&gt; moduleB/package.json =&gt; moduleB/index.ts =&gt; moduleB/index.tsx =&gt; moduleB/index.tsx =&gt; moduleB/index.d.ts</li>
</ul>
</li>
<li>引入绝对路径的模块时：<ul>
<li>从本级目录下的 node_modules 向根目录下的 node_modules 方向查找</li>
<li>在同级目录下，同“引入相对路径”相同</li>
</ul>
</li>
</ul>
<p>在引入的文件是相对路径时，路径计算都是相对于当前路径计算；如果引入文件是绝对路径时，会根据 “tsconfig.json” 里的 baseUrl 属性值来计算；如果想指定某些 npm 包的解析路径，可以在 ”tsconfig.json” 中的 compilerOptions.paths 中进行设置，而 paths 的值是基于 baseUrl 进行解析。</p>
<h3 id="声明合并"><a href="#声明合并" class="headerlink" title="声明合并"></a>声明合并</h3><p>指编译器将针对同一个名字的两个独立声明合并为单一声明。任何数量的声明都可被合并；不局限于两个声明。</p>
<p>TypeScript 中的声明可以产生<strong>命名空间</strong>、<strong>类型</strong>和<strong>值</strong>三种实体。</p>
</li>
<li><p>接口合并</p>
<ul>
<li>接口的非函数的<strong>成员应该是唯一</strong>的。如果<strong>不是唯一的，那么必须是相同的类型</strong>。如果同名但类型不同，则编译器会报错。</li>
<li>如果成员为函数，每个同名函数声明都会被当成这个函数的一个重载。</li>
<li>接口内部成员声明顺序不变，接口间则是后声明的接口成员重载出现会排在前面</li>
</ul>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> <span class="title class_">Box</span> &#123;</span><br><span class="line">    <span class="attr">height</span>: <span class="built_in">number</span>;</span><br><span class="line">    <span class="attr">width</span>: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">interface</span> <span class="title class_">Box</span> &#123;</span><br><span class="line">    <span class="attr">scale</span>: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="attr">box</span>: <span class="title class_">Box</span> = &#123;<span class="attr">height</span>: <span class="number">5</span>, <span class="attr">width</span>: <span class="number">6</span>, <span class="attr">scale</span>: <span class="number">10</span>&#125;;</span><br></pre></td></tr></table></figure>
</li>
<li><p>合并命名空间</p>
<ul>
<li>namespace 中的 export 成员相互是可访问</li>
<li>namespace 中的非 export 成员，其他命名空间合并来的是无法访问的</li>
</ul>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> haveMuscles = <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">animalsHaveMuscles</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> haveMuscles;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title class_">Animal</span> &#123;</span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">doAnimalsHaveMuscles</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> haveMuscles;  <span class="comment">// Error, because haveMuscles is not accessible here</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>合并命名空间和类：可以实现内部类</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Album</span> &#123;</span><br><span class="line">    <span class="attr">label</span>: <span class="title class_">Album</span>.<span class="property">AlbumLabel</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">namespace</span> <span class="title class_">Album</span> &#123;</span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">AlbumLabel</span> &#123; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>合并命名空间和函数</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">buildLabel</span>(<span class="params">name: <span class="built_in">string</span></span>): <span class="built_in">string</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> buildLabel.<span class="property">prefix</span> + name + buildLabel.<span class="property">suffix</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> buildLabel &#123;</span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">let</span> suffix = <span class="string">&quot;&quot;</span>;</span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">let</span> prefix = <span class="string">&quot;Hello, &quot;</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>合并命名空间和枚举：扩展枚举型</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Color</span> &#123;</span><br><span class="line">    red = <span class="number">1</span>,</span><br><span class="line">    green = <span class="number">2</span>,</span><br><span class="line">    blue = <span class="number">4</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title class_">Color</span> &#123;</span><br><span class="line">    <span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">mixColor</span>(<span class="params">colorName: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (colorName == <span class="string">&quot;yellow&quot;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="title class_">Color</span>.<span class="property">red</span> + <span class="title class_">Color</span>.<span class="property">green</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (colorName == <span class="string">&quot;white&quot;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="title class_">Color</span>.<span class="property">red</span> + <span class="title class_">Color</span>.<span class="property">green</span> + <span class="title class_">Color</span>.<span class="property">blue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (colorName == <span class="string">&quot;magenta&quot;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="title class_">Color</span>.<span class="property">red</span> + <span class="title class_">Color</span>.<span class="property">blue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (colorName == <span class="string">&quot;cyan&quot;</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="title class_">Color</span>.<span class="property">green</span> + <span class="title class_">Color</span>.<span class="property">blue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="JSX"><a href="#JSX" class="headerlink" title="JSX"></a>JSX</h3></li>
<li><p>使用 JSX 需要：文件改为 .tsx 扩展名，启用 jsx 选项（preserve、react 和 react-native）</p>
</li>
<li>JSX 结果类型默认为 any，可以指定 JSX.Element 接口进行自定义</li>
<li>JSX 允许使用 {} 内嵌表达式</li>
<li>as 操作符：.tsx 文件不支持 <foo>bar 的类型断言，必须使用 bar as foo</li>
<li><p>元素类型检查：</p>
<ul>
<li>固有元素会生成字符串，以小写字母开头，在 JSX.IntrinsicElements 接口中查找</li>
<li>值元素（无状态函数组件、类组件），以大写字母开头，在其作用域里按标识符查找<ul>
<li>无状态函数组件：组件被定义成  Javascript 函数，第一个参数为 props 对象</li>
<li>类组件：<Expr />，元素类的类型即 Expr 的类型；如果组件是 ES6 的类，类类型就是类的<strong>构造函数+静态部分</strong>，如果组件是工厂函数，类类型就是这个<strong>函数</strong>。元素的实例类型必须赋值给 <code>JSX.ElementClass</code> 或抛出一个错误。</li>
</ul>
</li>
</ul>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyComponent</span> &#123;</span><br><span class="line">    <span class="title function_">render</span>(<span class="params"></span>) &#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> myComponent = <span class="keyword">new</span> <span class="title class_">MyComponent</span>();</span><br><span class="line"><span class="comment">// 元素类的类型 =&gt; MyComponent</span></span><br><span class="line"><span class="comment">// 元素实例的类型 =&gt; &#123; render: () =&gt; void &#125;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">MyFactoryFunction</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> &#123;</span><br><span class="line">		    <span class="attr">render</span>: <span class="function">() =&gt;</span> &#123;</span><br><span class="line">		    &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> myComponent = <span class="title class_">MyFactoryFunction</span>();</span><br><span class="line"><span class="comment">// 元素类的类型 =&gt; FactoryFunction</span></span><br><span class="line"><span class="comment">// 元素实例的类型 =&gt; &#123; render: () =&gt; void &#125;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>元素的属性类型检查</p>
<ul>
<li>固有元素：还是在 JSX.IntrinsicElements 接口中查找</li>
<li>值元素：在元素实例的 props 中指定属性</li>
</ul>
<h3 id="装饰器"><a href="#装饰器" class="headerlink" title="装饰器"></a>装饰器</h3><p>装饰器是一种特殊类型的声明，它能够被附加到<a href="https://www.tslang.cn/docs/handbook/decorators.html#class-decorators">类声明</a>，<a href="https://www.tslang.cn/docs/handbook/decorators.html#method-decorators">方法</a>， <a href="https://www.tslang.cn/docs/handbook/decorators.html#accessor-decorators">访问符</a>，<a href="https://www.tslang.cn/docs/handbook/decorators.html#property-decorators">属性</a>或<a href="https://www.tslang.cn/docs/handbook/decorators.html#parameter-decorators">参数</a>上。 装饰器使用 <code>@expression</code> 这种形式，<code>expression</code> 求值后必须为一个函数，它会在运行时被调用，被装饰的声明信息做为参数传入。</p>
</li>
<li><p>装饰器工厂函数</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">color</span>(<span class="params">value: <span class="built_in">string</span></span>) &#123; <span class="comment">// 这是一个装饰器工厂，传入的是与 target 无关的参数</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">target</span>) &#123; <span class="comment">//  这是装饰器，target 就是类、方法等</span></span><br><span class="line">        <span class="comment">// do something with &quot;target&quot; and &quot;value&quot;...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>装饰器组合：类似复合函数 <strong>f(g(x))</strong>，由上至下对装饰器表达式求值，由下至上将求值结果进行传参调用</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="meta">@f</span></span><br><span class="line"><span class="meta">@g</span></span><br><span class="line">x</span><br></pre></td></tr></table></figure>
</li>
<li><p>装饰器分为：类装饰器、参数装饰器、方法装饰器、访问符装饰器和属性装饰器。</p>
<p>  类装饰器应用于<strong>构造函数</strong>，用于监视、修改和替换类定义，不能用于声明文件（.d.ts）和外部上下文（declare 的类）。传参仅构造函数一个。</p>
  <figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// @sealed 类装饰器：类将不可增减属性、属性也不可配置</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">sealed</span>(<span class="params">constructor: <span class="built_in">Function</span></span>) &#123;</span><br><span class="line">    <span class="title class_">Object</span>.<span class="title function_">seal</span>(constructor);</span><br><span class="line">    <span class="title class_">Object</span>.<span class="title function_">seal</span>(constructor.<span class="property"><span class="keyword">prototype</span></span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>方法装饰器</p>
<p>  方法声明之前声明，会被应用到方法的属性描述符上，用来监视、修改和替换方法定义。传参有三个：类的构造函数（对于静态成员）/类实例的原型对象（对于实例成员）、成员名字、成员的属性描述符。返回值会被用作方法的<em>属性描述符</em>（如果输出版本大于ES5）</p>
  <figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">enumerable</span>(<span class="params">value: <span class="built_in">boolean</span></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">target: <span class="built_in">any</span>, propertyKey: <span class="built_in">string</span>, descriptor: PropertyDescriptor</span>) &#123;</span><br><span class="line">        descriptor.<span class="property">enumerable</span> = value;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>访问器装饰器：和成员方法装饰器类似，返回值会被用作方法的<em>属性描述符</em>（如果输出版本大于ES5）</p>
  <figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">configurable</span>(<span class="params">value: <span class="built_in">boolean</span></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">function</span> (<span class="params">target: <span class="built_in">any</span>, propertyKey: <span class="built_in">string</span>, descriptor: PropertyDescriptor</span>) &#123;</span><br><span class="line">        descriptor.<span class="property">configurable</span> = value;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Point</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">    <span class="meta">@configurable</span>(<span class="literal">false</span>)</span><br><span class="line">    <span class="keyword">get</span> <span class="title function_">x</span>() &#123; <span class="keyword">return</span> <span class="variable language_">this</span>.<span class="property">_x</span>; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>属性装饰器</p>
<p>  属性装饰器作用是用来监视类中是否有某个名字的属性。传参有两个：类的构造函数（对于静态成员）/类实例的原型对象（对于实例成员）、成员名字。返回值会被忽略。</p>
  <figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">format</span>(<span class="params">formatString: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="title class_">Reflect</span>.<span class="title function_">metadata</span>(formatMetadataKey, formatString);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Greeter</span> &#123;</span><br><span class="line">    <span class="meta">@format</span>(<span class="string">&quot;Hello, %s&quot;</span>)</span><br><span class="line">    <span class="attr">greeting</span>: <span class="built_in">string</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>参数装饰器</p>
<p>  运行时当做函数被调用，返回值会被忽略。</p>
  <figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">required</span>(<span class="params">target: <span class="built_in">Object</span>, propertyKey: <span class="built_in">string</span> | <span class="built_in">symbol</span>, parameterIndex: <span class="built_in">number</span></span>) &#123;&#125;</span><br><span class="line"><span class="keyword">function</span> <span class="title function_">validate</span>(<span class="params">target: <span class="built_in">any</span>, propertyName: <span class="built_in">string</span>, descriptor: TypedPropertyDescriptor&lt;<span class="built_in">Function</span>&gt;</span>) &#123;&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Greeter</span> &#123;</span><br><span class="line">    <span class="meta">@validate</span></span><br><span class="line">    <span class="title function_">greet</span>(<span class="params"><span class="meta">@required</span> name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;Hello &quot;</span> + name + <span class="string">&quot;, &quot;</span> + <span class="variable language_">this</span>.<span class="property">greeting</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
</ul>
<h3 id="Mixins"><a href="#Mixins" class="headerlink" title="Mixins"></a>Mixins</h3><p>设计一个类可以同时具有多个类的属性和方法。</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Disposable</span> &#123;</span><br><span class="line">  <span class="attr">isDisposed</span>: <span class="built_in">boolean</span>;</span><br><span class="line">  <span class="title function_">dispose</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">isDisposed</span> = <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Activatable</span> &#123;</span><br><span class="line">  <span class="attr">isActive</span>: <span class="built_in">boolean</span>;</span><br><span class="line">  <span class="title function_">activate</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">isActive</span> = <span class="literal">true</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="title function_">deactivate</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="property">isActive</span> = <span class="literal">false</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">SmartObject</span> <span class="keyword">implements</span> <span class="title class_">Disposable</span>, <span class="title class_">Activatable</span> &#123;</span><br><span class="line">	<span class="comment">// 因为要混合多个类，所以只能用 implements 而非 extends，但是原有类的方法实现没法直接使用</span></span><br><span class="line">	<span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="built_in">setInterval</span>(<span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="variable language_">this</span>.<span class="property">isActive</span> + <span class="string">&quot; : &quot;</span> + <span class="variable language_">this</span>.<span class="property">isDisposed</span>), <span class="number">500</span>);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="title function_">interact</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">this</span>.<span class="title function_">activate</span>();</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Disposable</span></span><br><span class="line">  <span class="attr">isDisposed</span>: <span class="built_in">boolean</span> = <span class="literal">false</span>;</span><br><span class="line">  <span class="attr">dispose</span>: <span class="function">() =&gt;</span> <span class="built_in">void</span>;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Activatable</span></span><br><span class="line">  <span class="attr">isActive</span>: <span class="built_in">boolean</span> = <span class="literal">false</span>;</span><br><span class="line">  <span class="attr">activate</span>: <span class="function">() =&gt;</span> <span class="built_in">void</span>;</span><br><span class="line">  <span class="attr">deactivate</span>: <span class="function">() =&gt;</span> <span class="built_in">void</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="title function_">applyMixins</span>(<span class="title class_">SmartObject</span>, [<span class="title class_">Disposable</span>, <span class="title class_">Activatable</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 这里就是将具体方法实现也混合到结果类中</span></span><br><span class="line"><span class="keyword">function</span> <span class="title function_">applyMixins</span>(<span class="params">derivedCtor: <span class="built_in">any</span>, baseCtors: <span class="built_in">any</span>[]</span>) &#123;</span><br><span class="line">  baseCtors.<span class="title function_">forEach</span>(<span class="function"><span class="params">baseCtor</span> =&gt;</span> &#123;</span><br><span class="line">    <span class="title class_">Object</span>.<span class="title function_">getOwnPropertyNames</span>(baseCtor.<span class="property"><span class="keyword">prototype</span></span>).<span class="title function_">forEach</span>(<span class="function"><span class="params">name</span> =&gt;</span> &#123;</span><br><span class="line">      derivedCtor.<span class="property"><span class="keyword">prototype</span></span>[name] = baseCtor.<span class="property"><span class="keyword">prototype</span></span>[name];</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="三斜线指令"><a href="#三斜线指令" class="headerlink" title="三斜线指令"></a>三斜线指令</h3><p>三斜线指令是包含单个XML标签的单行注释。 注释的内容会做为编译器指令使用。三斜线指令只能放在文件的最顶端，它的前面只能出现单行/多行注释或其他三斜线指令，否则会被当成普通注释。</p>
<figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">/// &lt;reference path=&quot;...&quot; /&gt;</span></span><br><span class="line"><span class="comment">// 最常用的指令，用于声明文件间的依赖，告诉编译器在编译过程中要引入的额外的文件</span></span><br></pre></td></tr></table></figure>
<ul>
<li><p>预处理输入文件</p>
<p>  编译器会对输入文件进行预处理来解析所有三斜线引用指令。 在这个过程中，额外的文件会加到编译过程中。</p>
</li>
<li><p>错误：引用不存在的文件会报错。 一个文件用三斜线指令引用自己会报错。</p>
</li>
<li>使用 <code>--noResolve</code> ：指定该编译选项，三斜线指令会被忽略。</li>
<li><code>/// &lt;reference types=&quot;...&quot; /&gt;</code> 和 <code>/// &lt;reference path=&quot;...&quot; /&gt;</code> 类似，用来声明依赖，与 import 类似。</li>
<li><code>/// &lt;reference no-default-lib=&quot;true&quot;/&gt;</code> 将一个文件标记成默认库，在编译过程中不能编译这个默认库，类似 —noLib 命令</li>
<li><code>/// &lt;amd-module /&gt;</code></li>
</ul>
<h3 id="Javascript-文件类型检查"><a href="#Javascript-文件类型检查" class="headerlink" title="Javascript 文件类型检查"></a>Javascript 文件类型检查</h3><p>TypeScript 2.3以后的版本支持使用<code>--checkJs</code>对<code>.js</code>文件进行类型检查和错误提示。</p>
<ul>
<li>设置是否进行检查的方式：<ul>
<li>如果设置了 —checkJs 配置，可以通过添加<code>// @ts-nocheck</code>注释来忽略类型检查。</li>
<li>如果没有 —checkJs 配置，可以通过去掉<code>--checkJs</code>设置并添加一个<code>// @ts-check</code>注释来选则检查某些<code>.js</code>文件。</li>
<li>可以使用<code>// @ts-ignore</code>来忽略本行的错误。</li>
</ul>
</li>
<li><p>使用 JSDoc 表示类型信息，表示类属性的类型信息</p>
  <figure class="highlight tsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">number</span>&#125; */</span></span><br><span class="line"><span class="keyword">var</span> x;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span> &#123;</span><br><span class="line">    <span class="title function_">constructor</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">number | undefined</span>&#125; */</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">prop</span> = <span class="literal">undefined</span>;</span><br><span class="line">        <span class="comment">/** <span class="doctag">@type</span> &#123;<span class="type">number | undefined</span>&#125; */</span></span><br><span class="line">        <span class="variable language_">this</span>.<span class="property">count</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>ES2015中，构造函数等同于类</p>
</li>
<li>.js 文件中，ts能够识别出 CommonJS 模块</li>
<li>类、函数和对象字面量都是命名空间</li>
<li>对象属性字面量是开放的，对象中的属性可以被赋值任意值，也可以添加任意属性</li>
<li><strong><strong>null，undefined，和空数组的类型是any或any[]</strong></strong></li>
<li>函数参数是默认可选的，但是不能传过多的参数；未指定类型的参数默认为 any</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>基础知识</tag>
        <tag>TypeScript</tag>
      </tags>
  </entry>
  <entry>
    <title>Angular 的脏检到底如何工作</title>
    <url>/2022/06/12/2022-06-12-how-detect-change-work-in-angular.html</url>
    <content><![CDATA[<h3 id="脏检如何实现？"><a href="#脏检如何实现？" class="headerlink" title="脏检如何实现？"></a>脏检如何实现？</h3><p>Angular 的变化检测机制是建立在 Javascript 的可覆写机制。Angular 在启动阶段会对浏览器的一些底层 API 进行补丁，比如在 addEventListener 方法中添加一些功能：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">function</span> <span class="title function_">addEventListener</span>(<span class="params">eventName, callback</span>) &#123;</span><br><span class="line">     <span class="comment">// call the real addEventListener</span></span><br><span class="line">     <span class="title function_">callRealAddEventListener</span>(eventName, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">        <span class="comment">// first call the original callback</span></span><br><span class="line">        <span class="title function_">callback</span>(...);     </span><br><span class="line">        <span class="comment">// and then run Angular-specific functionality</span></span><br><span class="line">        <span class="keyword">var</span> changed = angular.<span class="title function_">runChangeDetection</span>();</span><br><span class="line">         <span class="keyword">if</span> (changed) &#123;</span><br><span class="line">             angular.<span class="title function_">reRenderUIPart</span>();</span><br><span class="line">         &#125;</span><br><span class="line">     &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个为底层浏览器 API 打补丁的过程是由 zone.js 工具库提供的。补丁和脏检机制一班发生在以下几种异步情况：</p>
<ul>
<li>所有浏览器事件（鼠标事件、键盘事件…）</li>
<li>setTimeout() 和 setInterval()</li>
<li>Ajax HTTP 请求</li>
</ul>
<p>为了了解 zone 的概念，我们需要先了解浏览器任务和时间循环的机制。</p>
<span id="more"></span>
<p>Angular 官方文档中指出，一个 zone 指的是跨多个异步任务存在的执行上下文。</p>
<blockquote>
<p>执行上下文可理解为作用域，JavaScript 有全局上下文（相对于函数之外的任何代码）、本地上下文（函数执行时自己创建）和由 eval()上下文（由 eval 函数创建的上下文）。程序开始运行，全局上下文即创建，执行到某个函数时则会为该函数创建其上下文，并推入执行上下文栈中；退出该函数或该程序时，其上下文也会从执行栈中弹出并销毁。</p>
</blockquote>
<p>zone.js 就是通过创建一个持久的跨多个异步操作的执行上下文，并提供了异步操作的各个生命周期（onScheduleTask、onInvokeTask、onHasTask、onInvoke），在这些生命周期中可以监测数据变化，从而达到脏检的目的。</p>
<h3 id="JavaScript-运行时和事件循环（来源）"><a href="#JavaScript-运行时和事件循环（来源）" class="headerlink" title="JavaScript 运行时和事件循环（来源）"></a>JavaScript 运行时和事件循环（<a href="https://developer.mozilla.org/zh-CN/docs/Web/API/HTML_DOM_API/Microtask_guide/In_depth">来源</a>）</h3><p>为了了解 zone 的机制，我们还需要复习下 JavaScript 运行时。 执行 JS 代码时，JavaScript 运行时维护了一组用于执行 JS 代码的代理，每个代理包含一组执行上下文的集合、执行上下文栈、主线程、一组可能创建用于执行 worker 的额外的线程集合、一个任务队列以及一个微任务队列构成。</p>
<h4 id="事件循环"><a href="#事件循环" class="headerlink" title="事件循环"></a>事件循环</h4><p>每个线程都拥有自己的事件循环。每个循环负责收集事件（用户、非用户事件），对任务进行排队（安排回调的执行），执行排队中的 Task（即宏任务），然后执行微任务，最后在下一次循环之前执行一些必要的渲染和绘制操作。事件循环有三种：</p>
<ul>
<li>Window 事件循环：驱动所有同源（意为起源，即一个窗口中打开另一个窗口或一个窗口中有多个 iframe，可能会共享一个事件循环）窗口。</li>
<li>Worker 事件循环：驱动 workder 的事件循环</li>
<li>Worklet 事件循环：用于驱动运行 worklet 的代理</li>
</ul>
<h4 id="任务和微任务"><a href="#任务和微任务" class="headerlink" title="任务和微任务"></a>任务和微任务</h4><p>任务是指按标准机制执行的任何 JavaScript，包括程序初始化、事件触发的回调、setTimeout 添加的任务。微任务指特定的一些任务，例如 Promise、Object.observe、MutationObserver、 NodeJS 环境下的 process.nextTick 和 async/await</p>
<ul>
<li>每一次事件循环的迭代会执行队列中的每一个任务，每次迭代开始后加入的任务在下一次迭代才被执行</li>
<li>每次当一个任务退出且执行上下文为空时，事件循环会执行微任务队列中的每一个微任务，直到微任务队列为空才停止，期间新添加的微任务也会在这个队列被执行。可以通过 queueMicrotask() 方法添加新的微任务。</li>
</ul>
<p>这也就是为什么 promise 的执行顺序要在 setTimeout 回调之前的原因。</p>
<h3 id="变化检测树"><a href="#变化检测树" class="headerlink" title="变化检测树"></a>变化检测树</h3><p>每个 Angular 组件都有对应的一个变化检测器，在应用启动时被创建。Angular 的数据流自顶向下单向流动，变化检测树与之呼应（检测完父组件后，子组件也可能改变父组件的数据使得父组件再次被检查，所以在开发环境会出现 ExpressionChangedAfterItHasBeenCheckedError 报错）。</p>
<p>需要注意的是，Angular 使用 view 作为底层抽象，一个 view 与一个 component 相互关联，所有的属性值的变化检测和 DOM 更新都是在 view 上操作。自顶向下，每一个 view 都与其 child views 链接，通过 nodes 属性对 child views 进行操作。每一个 view 都有一个状态（state，分别有 FirstCheck、ChecksEnabled、Errored 和 Destroyed 四种状态，默认策略下，状态可以相互组合）。</p>
<p>针对 view 变化检测的主要逻辑是在 checkAndUpdateView 方法中。</p>
<blockquote>
<p>默认的检测机制，只在模板表达式的值发生改变时触发；并且不会针对对象进行深比较（只对模板中使用到的属性进行检测）。</p>
</blockquote>
<p>上面提到的单向流动同时也表示数据更新后视图会随之更新，但是视图的更新不会触发更多的视图更新。在 ngAfterViewChecked 生命周期里调用 this.callbacks 会触发 “EXCEPTION: Expression ‘ in App@3:20’ has changed after it was checked” 的报错（该报错仅在开发环境中出现）。</p>
<p>有时候变化检测不到怎么办？</p>
<p>在开发过程中，经常会遇到某一些数据变化但是视图上没有展示出来，甚至将数据打印出来已经发生了变化，但是视图上却不会变化的情况。</p>
<ul>
<li>第一种情况可能是由于修改了对象或数组类型的某个属性，这时导致的脏检不成功，整个情况尝试整个变量重新赋值。</li>
<li>第二种情况可能是触发试图改变，而视图又触发视图改变，这个时候可以尝试手动使用 setTimeout() 方法进行包裹。</li>
<li>还有一种比较特殊的情况，上面我们提到了 Angular 对三种类型的一部操作进行变化检测，而当我们使用第三方的库导致的异步操作，但这些操作并没有触发到以上三种底层的异步操作时，变化就没办法被检测到。这个时候可以尝试引入 NgZone，使用 NgZone.run 包裹这个异步操作即可。</li>
</ul>
<h3 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h3><p><a href="https://blog.angular-university.io/how-does-angular-2-change-detection-really-work/">官方文档</a></p>
<p><a href="https://jakearchibald.com/2015/tasks-microtasks-queues-and-schedules/">Tasks, microtasks, queues and schedules</a></p>
<p><a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/EventLoop">The event loop</a></p>
<p><a href="https://stackoverflow.com/questions/51455545/when-to-use-ngzone-run">When to use Ngzone.run()?</a></p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Framework</tag>
        <tag>Angular</tag>
        <tag>Change Detection</tag>
      </tags>
  </entry>
  <entry>
    <title>浏览器怎样渲染一个页面？—— DOM，CSSOM及其渲染</title>
    <url>/2022/02/11/2022-02-11-how-browser-render-an-HTML-page.html</url>
    <content><![CDATA[<p>在网页开发的过程中，影响用户体验的因素有很多，大部分归咎于资源加载速度慢、初始页面渲染时加载非必须的文件（甚至会出现没有样式的文档）等等。为了避免这些问题的产生，我们需要理解浏览器渲染一个特定的网页的整个周期和流程。</p>
<p>首先，我们需要搞清楚什么是 DOM。当一个浏览器向服务端请求一个 HTML 文档，服务器会将这个 HTML 文档以二进制形式的数据（一个带着 <code>Content-Type = &#39;text/html; charset=UTF-8&#39;</code> 的text 文件）进行响应。这其中， <code>text/html</code> 是一种 MIME 类型（MIME 能够告诉浏览器这是一个 HTML 文档，并且字符编码是 UTF-8）。有了这个信息，浏览器就能将二进制文件转换成一个可理解的文本文件，如下图所示</p>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN01IYGRnh1NfTM2NnrLz_!!6000000001597-2-tps-1000-259.png" alt="6000000001597-2-tps-1000-259"></p>
<span id="more"></span>
<p>如果这个响应头丢失，那么浏览器将无法对文件进行解析和渲染。当一切就绪，浏览器就可以对 HTML 文档进行解析。HTML 文档的样子通常如下面代码所示</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="keyword">HTML</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">title</span>&gt;</span>Rendering Test<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">      </span><br><span class="line">        <span class="comment">&lt;!-- stylesheet --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;./style.css&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;container&quot;</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">h1</span>&gt;</span>Hello World!<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">          <span class="tag">&lt;<span class="name">p</span>&gt;</span>This is a sample paragraph.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">      </span><br><span class="line">        <span class="comment">&lt;!-- script --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;./main.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>在上面的 HTML 文档中，网页主要依赖 <code>style.css</code> 文件来给 HTML 元素提供样式，依赖 <code>main.js</code> 来进行一些 JavaScript 脚本代码的执行。有了 CSS 样式，我们的页面看起来会非常棒</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN01zDFoLg28gBuztbuXv_!!6000000007961-2-tps-700-388.png" alt="6000000007961-2-tps-700-388"></p>
<p>但我们还是没有聊到重点——浏览器是如何将一个漂亮的页面从一个简单的只充满了文本 HTML 文件渲染出来的呢？接下来我们就聊聊 DOM、CSSOM 和渲染树（Render Tree）。</p>
<h3 id="DOM（Document-Object-Model）"><a href="#DOM（Document-Object-Model）" class="headerlink" title="DOM（Document Object Model）"></a>DOM（Document Object Model）</h3><p>当浏览器解析 HTML 代码时，遇到像 html、body 和 div 等等的标签，会创建一个称作<strong>节点</strong>（Node） 的JavaScript 对象。最终，所有的 HTML 元素都会被转换成这样的节点。</p>
<p>HTML 元素都会有自己不同的属性（properties），相应的，节点对象会从不同的类被构造出来。例如 div 元素会被创建为一个 HTMLDivElement 类的节点实例（继承于 Node 类），如下图</p>
<p><img src="https://img.alicdn.com/imgextra/i3/O1CN01WabkAj21pclWOTfpz_!!6000000007034-2-tps-1000-259.png" alt="6000000007034-2-tps-1000-259"></p>
<p>浏览器有很多这样内置的类，例如 HTMLDivElement、HTMLScriptElement 和 Node 类等等。</p>
<p>当浏览器解析完 HTML 文档并相应的创建出许多的节点后，还会将这些节点构建成为一个树状结构（如下图）。就想 HTML 文档中元素都是相互嵌套的，浏览器也需要相应的把这些节点构建成类似的嵌套的结构。这种结构会让浏览器更加高效的渲染和操作。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN01xRFCZ41ZagbbDtNHl_!!6000000003211-0-tps-520-500.jpg" alt="6000000003211-0-tps-520-500"></p>
<p>DOM 树的结构就如上图所示。一棵 DOM 树由 html 元素节点开始，其中的其他元素像树枝和叶子一样被层层嵌套（在 HTML 文档解析过程中，每当遇到一个 HTML 元素，就会从相应的节点类中构造出一个 DOM 节点并添加在 DOM 树上）。</p>
<blockquote>
<p>DOM 节点并不总是 HTML 元素。当浏览器构建 DOM 树时，也会将注释、属性（attributes）和文本等保存成树上的节点。为了整个流程的简单描述，文中的节点我们都认为是由 HTML 元素创建的节点。<a href="https://www.w3schools.com/jsref/prop_node_nodetype.asp">点击这里</a>查看所有的节点类型。</p>
</blockquote>
<p>你可以在谷歌浏览器的 Devtools 控制台中可视化地查看 DOM 树的层级（如下图）。</p>
<p><img src="https://img.alicdn.com/imgextra/i4/O1CN01SS9zOD22DRMMsu1kM_!!6000000007086-2-tps-700-476.png" alt="6000000007086-2-tps-700-476"></p>
<p>JavaScript 是无法理解 DOM 是什么东西的。DOM 是浏览器为了搞笑渲染网页而向开发者暴露的 Web API，让开发者可以动态的去操作这些 DOM 元素。</p>
<blockquote>
<p>开发者能够使用 DOM API 来进行 HTML 元素的添加和删除，改变其样式，为其绑定一些事件监听器。使用 DOM API 还能够在内存中人工创建和复制一些 HTML 元素，而不受已经渲染好的 DOM 树的影响。这些特性都让开发者为了提升用户体验而更好地开发动态网页。</p>
</blockquote>
<h3 id="CSSOM（CSS-Object-Model）"><a href="#CSSOM（CSS-Object-Model）" class="headerlink" title="CSSOM（CSS Object Model）"></a>CSSOM（CSS Object Model）</h3><p>在设计一个网页时，我们会尽可能地让网页看起来更加美观。我们会在 HTML 元素中添加 CSS 样式来达到此目的。CSS 选择器让我们能够对选定的 DOM 元素设置类似 color 或 font-size 等的样式属性。</p>
<p>为元素添加样式有很多方法，有直接向 HTML 元素中添加样式（内联样式），或在 HTML 文档中添加 <code>&lt;style&gt;</code> 标签并在其中添加一些样式，也可以引入一个外部的 CSS 文件。最终，将 CSS 样式添加到 DOM 元素这个重担还是要落在浏览器身上。</p>
<p>针对上面提供的 HTML 文档，我们采用下面的 CSS 样式来进行美化。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">html</span> &#123;</span><br><span class="line">    <span class="attribute">padding</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">margin</span>: <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-tag">body</span> &#123;</span><br><span class="line">    <span class="attribute">font-size</span>: <span class="number">14px</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.container</span> &#123;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">300px</span>;</span><br><span class="line">    <span class="attribute">height</span>: <span class="number">200px</span>;</span><br><span class="line">    <span class="attribute">color</span>: black;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.container</span> &gt; <span class="selector-tag">h1</span> &#123;</span><br><span class="line">    <span class="attribute">color</span>: gray;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="selector-class">.container</span> &gt; <span class="selector-tag">p</span> &#123;</span><br><span class="line">    <span class="attribute">font-size</span>: <span class="number">12px</span>;</span><br><span class="line">    <span class="attribute">display</span>: none;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>DOM 树构建完成后，浏览器将读取资源（外部的、内联的、HTML文档中的和用户代理）中的 CSS 样式文件，并构建出一个 CSSOM 树。CSSOM 意为 CSS Object Model，就和 DOM 树一样都是树结构。</p>
<p>在 CSSOM 树上，每一个节点都是针对一个 DOM 元素所包含的样式。而且，CSSOM 树不像 DOM 树（<code>&lt;meta&gt;</code>、<code>&lt;script&gt;</code>等）一样能够被打印在屏幕上。</p>
<p>用户代理样式，是大部分浏览器自带的一些样式表。浏览器在计算 CSS 样式属性时，会首先加载用户代理样式，然后会将开发人员提供的样式表（根据一定的 CSS 优先级规律）进行覆盖，来构造出每一个节点。</p>
<p>即使有的 HTML 元素没有用户代理和开发者提供特定样式，样式的默认值也会根据 W3C CSS 标准进行默认赋值。而一些元素的属性也会继承逐步继承这些默认的属性值。</p>
<p>例如，color 和 font-size 属性如果没有进行任何值的指定，他们会继承父元素的属性值。所以我们能够想象这些样式都是一层层进行继承和覆盖。这就是为什么样式表被称之为 CSS（cascading style sheets），这种规则也是浏览器能构建出 CSSOM 树的原因。</p>
<blockquote>
<p>你能使用谷歌浏览器的 DevTools 控制台的 Elements 面板，在左侧选中一个 HTML 元素，点击后能够在右侧的 computed 面板中看到相应的样式计算结果。</p>
</blockquote>
<p>我们将之前例子中的 CSSOM 树可视化为下面的图表。</p>
<p><img src="https://img.alicdn.com/imgextra/i2/O1CN010i1cLv1Eh08OfwFUZ_!!6000000000382-0-tps-357-500.jpg" alt="6000000000382-0-tps-357-500"></p>
<p>如上图所示，CSSOM 树是不包含能够打印出来的元素和标签的。上面红色的 CSS 属性和值就是从 body 元素上面的样式继承而来的，而 h1 标签的 color 属性则覆盖了其父元素 div 中的 color 属性。</p>
<h3 id="Render-Tree"><a href="#Render-Tree" class="headerlink" title="Render Tree"></a>Render Tree</h3><p>渲染树也是一个树形结构，它结合了 DOM 树和 CSSOM 树。浏览器接下来需要计算每一个可见元素的布局并将其绘制在屏幕上，为了达到这一目的，浏览器需要用到 Render Tree。而 Render Tree 又是由 Dom 和 CSSOM 树得到的。</p>
<p>由于渲染树是能最终在屏幕上打印出的底层表示，<strong>它不会包含任何不包含像素矩阵面积的节点</strong>——例如使用了 <code>display: none</code> 样式的元素，这样的元素是不会出现在渲染树上的。</p>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN01Cm3lf91tsob708zM5_!!6000000005958-2-tps-1000-402.png" alt="6000000005958-2-tps-1000-402"></p>
<p>如上图所示，渲染树结合了 DOM 树和 CSSOM 树来构建出一个树形结构，这个树结构只包含能够最终绘制在屏幕上的元素。</p>
<p>上面代码中的 p 元素由于被设置了 <code>display: none</code> 样式，所以这个元素以及该元素的子元素都不会呈现在渲染树中，因为它不占任何屏幕空间。但是有一些例外，比如被设置了 <code>visibility:hidden</code> or <code>opacity:0</code> 样式的元素，他们虽然没有占据任何屏幕空间，但是仍然会被添加到渲染树上。</p>
<p>和 DOM API 能够让开发者去操控 DOM 元素不同，CSSOM 是被隐藏起来的，但由于渲染树结合了两者，所以浏览器通过提供高级的 DOM 上的 API 来暴露出 CSSOM 节点，这让开发者能够去操作和修改 CSSOM 节点的 CSS 属性。</p>
<blockquote>
<p>本文不讨论怎样使用 JavaScript 脚本去操作元素的样式，这里仅提供描述了 CSSOM API 的链接 <a href="https://css-tricks.com/an-introduction-and-guide-to-the-css-object-model-cssom/">CSS Tricks Article</a>。<a href="https://developers.google.com/web/updates/2018/03/cssom">CSS Typed Object</a> 一文提供了在 JavaScript 中更精确地操作元素样式属性的 API。</p>
</blockquote>
<h3 id="渲染流程"><a href="#渲染流程" class="headerlink" title="渲染流程"></a>渲染流程</h3><p>现在我们对 DOM，CSSOM 以及渲染树都有了一定的了解，接下来我们将揭露浏览器如何对一个网页进行渲染。对这个流程有一个简单的了解会让网站开发者提升网站的用户体验有极大的作用。</p>
<p>当一个页面加载时，浏览器首先会扫描 HTML 文档并构建出 DOM 树，然后会处理 CSS 文件（无论是内联还是外部的文件）并构建出 CSSOM 树。</p>
<p>当 DOM 树和 CSSOM 树构建出来之后，浏览器会结合两者来构建出渲染树。一旦渲染树构建完成，浏览器就能把每一个元素绘制在屏幕上。</p>
<h4 id="布局（layout）"><a href="#布局（layout）" class="headerlink" title="布局（layout）"></a>布局（layout）</h4><p>首先，浏览器会计算出每一个渲染树节点的布局。这里所说的布局包括这个节点的像素大小、以及绘制位置。这个过程被称作“布局（layout）“，有些时候也会被称作是回流（reflow）或浏览器回流（browser reflow）。回流是在用户进行滚动、缩放浏览器或操作 DOM 元素时会触发的。<a href="https://stackoverflow.com/a/27637245/2790983">这里</a>列举了一系列能够触发回流的事件和操作。</p>
<blockquote>
<p>我们应当避免网页出现过多的布局和回流，因为这是一个极其消耗性能的操作。Paul Lewis 的<a href="https://developers.google.com/web/fundamentals/performance/rendering/avoid-large-complex-layouts-and-layout-thrashing">文章</a>讲述了一些如何避免复杂和消耗性能的操作。<a href="https://kellegous.com/j/2013/01/26/layout-performance/">Layout thrashing</a> 一文也可供参考。</p>
</blockquote>
<h4 id="绘制（paint）"><a href="#绘制（paint）" class="headerlink" title="绘制（paint）"></a>绘制（paint）</h4><p>目前我们已经有了一系列需要打印在屏幕上的文字和图案。由于渲染树上的元素有可能会彼此覆盖，甚至会有一些 CSS 属性让这些元素频繁的更改样式、位置等（例如动画），浏览器为此创建了图层（layer）。</p>
<p>图层让浏览器能够高效地进行绘制，无论是在网页上滑动还是缩放浏览器窗口。图层也能帮助浏览器正确地把元素以层叠的方式（开发者所需要的）进行绘制。</p>
<p>现在浏览器能够结合图层，将元素绘制在屏幕上了。但是，浏览器是不会一次性把所有图层绘制出来的，每一个图层都是有先后的绘制顺序。</p>
<p>在每一层中，浏览器会填充每一个可见属性的像素点（例如边框、背景颜色、阴影和文字等）。这个过程也被称作栅格化。为了提升性能，浏览器会使用不同的线程来进行栅格化操作。</p>
<p>这里的图层就像是 Photoshop 软件中的图层。你可以在 Chrome DevTools 中查看每一个不同的图层。打开 Chrome DevTools，在 more tools 选项中选择 Layers 标签。能够在 Rendering 面板上看到每一个层的边界。</p>
<blockquote>
<p>栅格化一般都是由 CPU 来执行的，所以速度慢且成本高，但是现在出现的 GPU 技术能够很大程度提升这一步骤的性能。这篇 <a href="https://software.intel.com/en-us/articles/software-vs-gpu-rasterization-in-chromium">intel article</a> 文章详细概括了绘制流程（尽量去阅读它！）。为了能够更加深入地理解图层，你需要查看<a href="https://www.html5rocks.com/en/tutorials/speed/layers/">这篇文章</a>。</p>
</blockquote>
<h4 id="渲染层合并（compositing-operation）"><a href="#渲染层合并（compositing-operation）" class="headerlink" title="渲染层合并（compositing operation）"></a>渲染层合并（compositing operation）</h4><p>到现在，我们还没有在屏幕上绘制出任何一个元素。但是我们现在有了能够按照一定顺序绘制在屏幕上的一系列不同的图层。在渲染层合并这个流程里，这些图层将被送往 GPU 进行最终的绘制。</p>
<p>将所有的图层一起进行传输显然是低效的，因为每一次回流和重绘都需要全部发送一遍。所以每一个图层都被拆解为不同的块（tiles），这些块才是能够最终被绘制在屏幕上的单元。在 Chrome DevTools 的 Rendering 面板中也能够看到这些块。</p>
<p><img src="https://img.alicdn.com/imgextra/i1/O1CN01pUzddD1RkHz6ttIEm_!!6000000002149-0-tps-2400-179.jpg" alt="6000000002149-0-tps-2400-179"></p>
<p>上面画出了目前为止的所有渲染流程，也被称作 <a href="https://developer.mozilla.org/en-US/docs/Web/Performance/Critical_rendering_path">critical rendering path</a> 。</p>
<blockquote>
<p>Mariko Kosaka 写过<a href="https://developers.google.com/web/updates/2018/09/inside-browser-part3">一篇文章</a>来详细讲述这一流程中的每一个概念（极力推荐）</p>
</blockquote>
<h4 id="浏览器引擎"><a href="#浏览器引擎" class="headerlink" title="浏览器引擎"></a>浏览器引擎</h4><p>DOM 树、CSSOM 树以及控制整个渲染的逻辑都是在一个被叫做浏览器内置的浏览器引擎（<a href="https://en.wikipedia.org/wiki/Browser_engine">Browser Engine</a>)（也被称作是渲染引擎或布局引擎）。这个浏览器引擎包含了所有将 HTML 文档渲染成为屏幕上的像素点所必须的元素和逻辑。</p>
<p>你听说过 WebKit 吗，这就是一个浏览器引擎。WebKit 是在苹果的 Safari 浏览器中，也是之前谷歌 Chrome 浏览器默认的渲染引擎。现在 Chromium 项目已经开始用 Blink 作为 Chrome 浏览器默认的引擎。<a href="https://stackoverflow.com/questions/3468154/what-is-webkit-and-how-is-it-related-to-css/3468311#3468311">这是</a>一系列不同的浏览器对应的引擎。</p>
<h3 id="浏览器的渲染过程"><a href="#浏览器的渲染过程" class="headerlink" title="浏览器的渲染过程"></a>浏览器的渲染过程</h3><p>众所周知 JavaScript 是一种符合 ECMAScript 标准的语言，事实上由于 JavaScript 已经被注册为商标，我们现在都称其为 ECMAScript。所以，任何一种 JavaScript 引擎（包括 V8，Chakra，SpiderMonkey）都需要遵守这一标准。</p>
<p>这一标准让所有的 JavaScript 运行时环境（浏览器、Node、Deno等）一致的体验。这让 JavaScript 能够在多平台进行统一的应用开发。</p>
<p>然而浏览器渲染页面却很难提现这样的一致性，因为 HTML、CSS 和 JavaScript，这些语言都有自己独有的标准。所以 Chrome 和 Safari 浏览器在渲染流程上就有所不同。因此我们很难去预测不同浏览器背后所支持的渲染流程。但是 HTML5 规范对理论上如何渲染做出了一些努力，但是是否遵循这一规范不同的浏览器厂家也各有不同。</p>
<p>不管上面所提到的不同点，所有的浏览器在渲染过程其实遵循的规则大体上通常是一致的。我们这里就来了解一下浏览器渲染流程周期中共通的一些事件。为了更好地理解这个过程，下面准备了一个小的工程来测试不同的渲染场景。</p>
<p><a href="https://github.com/course-one/browser-rendering-test/tree/master">course-one/browser-rendering-test</a></p>
<h4 id="解析"><a href="#解析" class="headerlink" title="解析"></a>解析</h4><p>文档解析是扫描 HTML 文档内容并构建 DOM 树的一个过程。所以这个解析也被称作是 DOM 解析，执行解析的程序被称作 DOM 解析器。</p>
<p>大多数浏览器都会提供 DOMParser Web API 来将 HTML 文档构建成 DOM 树。一个 DOMParser 类的实例代表一个 DOM 解析器，这个解析器使用 parseFromString 原型方法来将 HTML 文本解析成 DOM 树结构。</p>
<p><img src="https://img.alicdn.com/imgextra/i3/O1CN01MmDmPo1ggpDnBi8D1_!!6000000004172-2-tps-700-302.png" alt="6000000004172-2-tps-700-302"></p>
<p>当浏览器向服务器请求并返回了一个 HTML 文档时，浏览器就可以开始解析这个文档。因此浏览器能够逐步地构建 DOM 树，一次构建一个节点。浏览器是从上到下地扫描 HTML 文档。</p>
<p><img src="https://miro.medium.com/max/1242/1*1bcaVVjG_077zHVzGfHUyw.gif" alt="https://miro.medium.com/max/1242/1*1bcaVVjG_077zHVzGfHUyw.gif"></p>
<p>上图展示了在 Node 服务器上以 10kbps 的速度请求 incremental.html 文件。因为这个请求获取文件的过程需要很长一段时间，浏览器加载这个文件从一开始的一小部分代码就能够不断地增量渲染出节点。</p>
<p>如果你在 Chrome DevTools 中的 Performance 面板中查看上述请求，能够看到每一步流程的时序。这些事件都被称作是性能指标，当这些事件能靠的很近，那么说明用户体验会很好。</p>
<blockquote>
<p>使用⟳按钮可以将某一个性能档案进行捕捉。</p>
</blockquote>
<p>FP 代表着 First Paint，意思是浏览器开始绘制。FCP 代表着 First Contentful Paint，意思是浏览器开始绘制第一个像素点。LCP 代表着 Largest Contentful Paint，意思是浏览器渲染一大块文本或图像的时间。</p>
<blockquote>
<p>你可能听过 FMP，代表着 First Meaningful Paint，与 LCP 指标类似，但是已经被 Chrome 弃用了。</p>
</blockquote>
<p>L 代表着 onload 事件（浏览器在 window 对象上所触发的事件）。DCL 代表着 DOMContentLoaded 事件（在 document 对象上触发但是会冒泡到 window 对象）。因此可以监听 window 对象。这些事件有一些复杂，我们会简单介绍一下。</p>
<p>每当浏览器扫描到了外部的资源，比如一些 <code>&lt;script&gt;</code> 标签标示的脚本文件（JavaScript文件）或 <code>&lt;link&gt;</code> 引入的样式文件，亦或是图片元素，浏览器会开始在后台下载这些资源（而不是 JavaScript 执行的主线程）。</p>
<p>最重要的一点是要记住——DOM解析通常是在主线程上进行的。所以当 JavaScript 主线程忙时，DOM 解析会被挂起直到线程空闲。为什么 JavaScript 会阻塞 DOM 的解析？那是因为脚本元素是解析阻塞的（脚本会影响 DOM 树的生成），而样式表、图片、pdf和视频等静态资源文件则不会阻塞。</p>
<h4 id="阻塞解析脚本"><a href="#阻塞解析脚本" class="headerlink" title="阻塞解析脚本"></a>阻塞解析脚本</h4><p>阻塞解析脚本指的是会让 HTML 文档解析阻塞的脚本文件。当浏览器遇到 script 标签时，如果是嵌入式脚本，脚本会被立即执行然后再继续解析 HTML 文档来构建 DOM 树。</p>
<p>如果遇到的 script 标签是一个引入的外部脚本文件，浏览器会在后台非主线程上开始下载这个脚本文件，在下载过程中，主线程的任务也是被暂时挂起的。在外部脚本没有下载完成之前，DOM 解析不会开始。</p>
<p>一旦脚本文件下载完成，浏览器会在首先主线程上执行这个脚本文件，然后开始 DOM 解析。浏览器再遇到相同的脚本标签，也会重复这一过程。为啥要这么做呢？这是因为浏览器将 DOM API 暴露给 JavaScript 运行时，这意味着我们可以使用 JavaScript 脚本来操控 DOM 元素。这也是 React 和 Angular 这些框架能够动态改变网页的原因。但是如果浏览器并行地解析 DOM 和执行脚本，可能会存在条件竞争漏洞（不知道到底哪个最终会影响 DOM 树的生成）。</p>
<p>但是很显然，在脚本下载的过程中暂停 DOM 的解析也是非常没有必要的，所以 HTML5 为 script 标签新增了 async 属性——当浏览器遇到了带有 async 属性的 script 标签，不会暂停解析过程并在后台下载脚本文件，但是当文件下载完成，解析过程会被挂起直到脚本被执行完。</p>
<p>另外，HTML5 还提供了 defer 属性，有点类似 async 属性，不同的是这种脚本在下载完成之后也不会立即执行。所有带 defer 属性的 script 标签，都会在后台并行下载，等到 HTML 解析完成，DOM 树构建完成，这些脚本才会被执行。</p>
<p>所有其他正常的 script 标签代表的脚本，都是阻塞解析的脚本。所有 async 脚本都是下载期间不阻塞解析，下载完成后阻塞解析。而 defer 脚本是无论如何都不会阻塞 DOM 解析的。</p>
<p><img src="https://miro.medium.com/max/1230/1*5xdQ1j6Ai2PZYCRQuXM5wg.gif" alt="https://miro.medium.com/max/1230/1*5xdQ1j6Ai2PZYCRQuXM5wg.gif"></p>
<p>上图中的例子，是 parser-blocking.html 文件，其中包含了30个元素之后的解析阻塞脚本。如果我们看一眼 Performance 面板，其中 FP 和 FCP 都是在 HTML 准备完成尽可能早的开始构建 DOM 树。LCP 在5秒之后开始，这是因为脚本阻塞了 DOM 解析（下载时间），只有30个文本元素被绘制出来。一旦脚本下载和执行完成，DOM 解析便会重新开始。</p>
<blockquote>
<p>解析阻塞也被称为渲染阻塞，因为此时渲染也不会进行（渲染需要依赖渲染树，而渲染树依赖 DOM 树）。但是两者还是有一些不同的。</p>
</blockquote>
<p>Some browsers may incorporate a <strong><a href="https://developer.mozilla.org/en-US/docs/Glossary/speculative_parsing">speculative parsing</a></strong> strategy where the HTML parsing (<em>but not the DOM tree construction</em>) is offloaded to a separate thread so that browser can read elements such as <code>link</code>(<em>CSS</em>), <code>script</code>, <code>img</code>, etc. and download these resources <strong>eagerly</strong>.</p>
<p>This is quite helpful if you have three <code>script</code> elements one after the other, but the browser won’t be able to start the download of the second script until the first script is downloaded as the DOM parser couldn’t read the second <code>script</code> element. We can fix this easily by using <code>async</code> tag but asynchronous scripts are not guaranteed to execute in order.</p>
<p>The reason it is called speculative parsing because the browser is making a <strong>speculation</strong> that a particular resource is expected to load in the future, so better load it now in the background. However, if some JavaScript manipulates DOM and removes/hides the element with an external resource, then speculation fails and these files were loaded for nothing. Tough.</p>
<blockquote>
<p>💡 Every browser has a mind of its own, so when or if speculative parsing will happen is not guaranteed. However, you can ask the browser to load some resources ahead of time using the <link rel="preload"> element.</p>
</blockquote>
<h4 id="渲染阻塞-CSS"><a href="#渲染阻塞-CSS" class="headerlink" title="渲染阻塞 CSS"></a>渲染阻塞 CSS</h4><p>我们了解了，任何除了解析阻塞脚本之外的外部资源请求都是不会阻塞 DOM 解析和树构建的。所以 CSS 并不会阻塞 DOM 解析，等一下，<strong>其实是会阻塞的</strong>。为了理解这一点，我们需要了解渲染的过程。</p>
<p>DOM 和 CSSOM 树的构建都是在主线程上的，而且这些树时并行进行构建的。它们一起构建出了渲染树，用来绘制元素。这一过程其实也是<strong>跟随着 DOM 树的增量构建进行</strong>的。</p>
<p>上面谈到 DOM 树的生成时增量生成的（并非一下子生成），也就是浏览器一边解析 HTML 文件，一遍在 DOM 树上增加节点。<strong>但是 CSSOM 可不是这样</strong>——CSSOM 树的构建并不是增量构建的。</p>
<p>当浏览器遇到 style 嵌入代码块时，它会扫描所有的 CSS 代码，并依据相应的规则更新 CSSOM 树。然后，它会继续解析 HTML 文档，内联的样式也是一样的。</p>
<p>但是外部的样式表就有所不同了。我们知道外部样式表不是解析阻塞资源，所以它能够在后台与 DOM 解析并行地进行下载。但是与脚本不同，浏览器不会在外部样式表下载完后第一时间进行解析，这是因为<strong>浏览器无法一边扫描 CSS 文件一边构建 CSSOM 树</strong>。原因很简单：CSS 的规则表明，越后面的样式属性，越有可能覆盖前面的属性。一但浏览器增量地生成 CSSOM 树，那么会产生多个渲染树，这会造成很差的体验。</p>
<p>所以浏览器不会增量地生成 CSSOM 树，而是 CSS 文件全部扫描完后才生成完整的 CSSOM。一旦 CSSOM 树生成后，渲染树才会更新并绘制。</p>
<p>CSS 是渲染阻塞资源。一旦浏览器去请求一个外部的样式表文件，渲染树的构建就会暂停（因为渲染树依赖 CSSOM树），因此 CRP（Critical Rendering Path）也会阻塞。但是，DOM 树还是会进行解析。</p>
<p><img src="https://miro.medium.com/max/1232/1*y3QmSfyergjmVV32nH7tPA.gif" alt="https://miro.medium.com/max/1232/1*y3QmSfyergjmVV32nH7tPA.gif"></p>
<p>A browser <strong>could have</strong> used an older state of the CSSOM tree to generate <strong>Render Tree</strong> as HTML is getting parsed to render things on the screen incrementally. But this has a huge downside. In this case, once the stylesheet is downloaded and parsed, and CSSOM is updated, Render Tree will be updated and rendered on the screen. Now the Render Tree nodes generated with older CSSOM will be <strong>repainted with new styles</strong> and it could also lead to <strong><a href="https://webkit.org/blog/66/the-fouc-problem/">Flash of Unstyled Content</a></strong> (<em>FOUC</em>) which is is very bad for UX.</p>
<p>Hence browsers will wait until the stylesheet is loaded and parsed. Once the stylesheet is parsed and CSSOM is updated, the Render Tree is updated, and <strong>CRP is unblocked</strong> which leads to the paint of Render Tree on the screen. Due to this reason, it is recommended to load all external stylesheets as early as possible, possibly in the <code>head</code> section.</p>
<p>Let’s imagine a scenario where the browser has started parsing HTML and it encounters an external stylesheet file. It will start the download of the file in the background, block the CRP, and continue with the DOM parsing. But then it encounters a <code>script</code> tag. So it will start the download of the external script file and block the DOM parsing. Now the browser is sitting idle waiting for the stylesheet and script file to download completely.</p>
<p>But this time, the external script file has been <strong>downloaded completely</strong> while the stylesheet is still being downloaded in the background. Should the browser execute the script file? Is there any harm doing that?</p>
<p>As we know, CSSOM provides a high-level JavaScript API to interact with the <strong>styles</strong> of the DOM elements. For example, you can read or update the background color of a DOM element using <code>elem.style.backgroundColor</code> property. The <code>style</code> object associated the <code>elem</code> element exposes the CSSOM API and there are many other APIs to do the same (<em>read this <a href="https://css-tricks.com/an-introduction-and-guide-to-the-css-object-model-cssom/">css-tricks article</a></em>).</p>
<p>As a stylesheet is being downloaded background, JavaScript can still execute as the main thread is not being blocked by the loading stylesheet. If our JavaScript program accesses CSS properties of a DOM element (<em>through CSSOM API</em>), we will get a proper value (<em>as per the current state of CSSOM</em>).</p>
<p>But once the stylesheet is downloaded and parsed, which leads to CSSOM update, our JavaScript now has a bad CSS value of the element since the new CSSOM update could have changed the CSS properties of that DOM element. Due to this reason, it’s not safe to execute JavaScript while the stylesheet is being downloaded.</p>
<p>As per the <strong><a href="https://html.spec.whatwg.org/multipage/semantics.html#interactions-of-styling-and-scripting">HTML5 specification</a></strong>, the browser may download a script file but it will not execute it unless all previous stylesheets are parsed. When a stylesheet blocks the execution of a script, it is called a <strong>script-blocking</strong> stylesheet or a <strong>script-blocking</strong> CSS.</p>
<p><img src="https://miro.medium.com/max/1232/1*atsh0R6Do25SriYvvskkgA.gif" alt="https://miro.medium.com/max/1232/1*atsh0R6Do25SriYvvskkgA.gif"></p>
<p>In the above example, the <code>[script-blocking.html](https://github.com/course-one/browser-rendering-test/blob/master/html/script-blocking.html)</code> contains a <code>link</code> tag (<em>for an external stylesheet</em>) followed by a <code>script</code> tag (<em>for an external JavaScript</em>). Here the script gets downloaded really fast without any delay but the stylesheet takes 6 seconds to download. Hence, even though the script is downloaded completely as we can see from the <strong>Network</strong> panel, it wasn’t executed by the browser immediately. Only after the stylesheet is loaded, we see the <code>Hello World</code> messaged logged by the script.</p>
<blockquote>
<p>💡 Like async or defer attribute makes script element non-parser-blocking, an external stylesheet can also be marked as non-render-blocking using the media attribute. Using the media attribute value, the browser can make a smart decision when to load the stylesheet.</p>
</blockquote>
<h3 id="Document’s-DOMContentLoaded-Event"><a href="#Document’s-DOMContentLoaded-Event" class="headerlink" title="Document’s DOMContentLoaded Event"></a><strong>Document’s <code>DOMContentLoaded</code> Event</strong></h3><p>The <code>[DOMContentLoaded](https://developer.mozilla.org/en-US/docs/Web/API/Document/DOMContentLoaded_event)</code> (<strong><em>DCL</em></strong>) event marks a point in time when the browser has constructed a complete DOM tree from all the available HTML. But there are a lot of factors involved that can change when the <code>DCL</code> event is fired.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">document.addEventListener( &#x27;DOMContentLoaded&#x27;, function(e) &#123;</span><br><span class="line">    console.log( &#x27;DOM is fully parsed!&#x27; );</span><br><span class="line">&#125; );</span><br></pre></td></tr></table></figure>
<p>If our HTML doesn’t contain any scripts, DOM parsing won’t get blocked and <code>DCL</code> will fire as quickly as the browser can parse the entire HTML. If we have parser-blocking scripts, then <code>DCL</code> has to wait until all parser-blocking scripts are downloaded and executed.</p>
<p>Things get a little complicated when stylesheets are thrown into the picture. Even though you have no external scripts, <code>DCL</code> will wait until all stylesheets are loaded. Since <code>DCL</code> marks a point in time when the entire DOM tree is ready, but DOM won’t be safe to access (<em>for the style information</em>) unless CSSOM is also fully constructed. Hence most browsers wait until all external stylesheets are loaded and parsed.</p>
<p>Script-blocking stylesheet will obviously delay the <code>DCL</code>. In this case, since the script is waiting for the stylesheet to load, the DOM tree is not getting constructed.</p>
<p><code>DCL</code> is one of the website performance metrics. We should optimize the <code>DCL</code> to be as small as possible (<em>the time at which it occurs</em>). One of the best practices is to use <code>defer</code> and <code>async</code> tag for <code>script</code> element whenever possible so that browser can perform other things while scripts are being downloaded in the background. Second, we should optimize the <strong>script-blocking</strong> and <strong>render-blocking</strong> stylesheets.</p>
<h3 id="Window’s-load-event"><a href="#Window’s-load-event" class="headerlink" title="Window’s load event"></a><strong>Window’s <code>load</code> event</strong></h3><p>As we know JavaScript can block DOM tree generation but that’s not the case with external stylesheets and files such as images, videos, etc.</p>
<p>The <code>DOMContentLoaded</code> event marks a point in time when the DOM tree is fully constructed and it is safe to access, the <code>window.onload</code> event marks a point in time when external stylesheets and files are downloaded and our web application (<em>complete</em>) has finished downloading.</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">window.addEventListener( &#x27;load&#x27;, function(e) &#123;</span><br><span class="line">  console.log( &#x27;Page is fully loaded!&#x27; );</span><br><span class="line">&#125; )</span><br></pre></td></tr></table></figure>
<p><img src="https://miro.medium.com/max/1368/1*DuLBecXpJjFh1qnakXjWWg.png" alt="https://miro.medium.com/max/1368/1*DuLBecXpJjFh1qnakXjWWg.png"></p>
<p>In the above example, the <code>[rendering.html](https://github.com/course-one/browser-rendering-test/blob/master/html/rendering.html)</code> file has an external stylesheet in the <code>head</code> that takes around 5 seconds to download. Since it’s in the <code>head</code> section, the <code>FP</code> and <code>FCP</code> occurs after 5 seconds since the stylesheet will block the rendering of any content below it (<em>as it blocks CRP</em>).</p>
<p>After that, we have an <code>img</code> element that loads an image that takes around 10 seconds to download. So the browser will keep downloading this file in the background and move on with the DOM parsing and rendering (<em>as an external image resource is neither parser-blocking nor render-blocking</em>).</p>
<p>Next, we have three external JavaScript files and they take 3s, 6s, and 9s to download respectively and most importantly, they are not <code>async</code>. This means the total load time should be close to <strong>18 seconds</strong> as the subsequent script won’t start downloading before the previous one is executed. However, looking at the <code>DCL</code> event, our browser seemed to have used the <strong>speculative strategy</strong> to eagerly download the script files so the total time to load is close to <strong>9 seconds</strong>.</p>
<p>Since the last file to download that can affect the <code>DCL</code> is the last script file with the load time of <strong>9 seconds</strong> (<em>since stylesheet has already been downloaded in 5 seconds</em>), the <code>DCL</code> event occurs around <strong>9.1 seconds</strong>.</p>
<p>We also had another external resource which was the image file and it kept loading in the background. Once it was fully downloaded (<em>which takes 10 seconds</em>), the window’s <code>load</code> event was fired after <strong>10.2 seconds</strong> which marks that the webpage (<em>application</em>) is fully loaded.</p>
]]></content>
      <categories>
        <category>Translation</category>
      </categories>
      <tags>
        <tag>Browser</tag>
        <tag>Chrome</tag>
        <tag>浏览器</tag>
      </tags>
  </entry>
  <entry>
    <title>Angular 脏检的细节理解</title>
    <url>/2022/07/03/2022-07-03-angular-detect-change-detail.html</url>
    <content><![CDATA[<p>前端框架最主要解决的就是当数据状态改变时去更新 DOM，Angular 使用的是 change detection。change detection 一般可分为两部分，更新应用数据模型 model，Angular 根据更新后的 model 重新渲染 DOM。<br>大概的一个过程：</p>
<ol>
<li>开发者代码更新组件的数据模型 model（例如请求接口返回数据更新）</li>
<li>angular 检测变化</li>
<li>脏检机制对整个应用从根到叶检测对应的数据模型是否变化</li>
<li>如果有数据更新，则更新 DOM</li>
</ol>
<span id="more"></span>
<p>change detector 是整个应用启动时创建的（每个组件都会有），它会检测值变化前后的值，如果改变了会设置 isChanged 属性为 true。</p>
<p>官方文档<a href="[https://blog.angular-university.io/how-does-angular-2-change-detection-really-work/">Angular Change Detection - How Does It Really Work?</a>](<a href="https://blog.angular-university.io/how-does-angular-2-change-detection-really-work/)">https://blog.angular-university.io/how-does-angular-2-change-detection-really-work/)</a>)</p>
<p>Angular 的 cd 是基于 JS runtime 重载机制。Angular 启动时会重载一些底层浏览器 API，将 cd、DOM更新代码塞入。这个重载主要是在 Zone.js 实现的。</p>
<p>zone：是能够存活多个 JS 事件循环的执行上下文？<br>zone 重载了以下浏览器异步 API ：所有浏览器事件（鼠标、键盘等事件）、setTimeout 和 setInterval、ajax http 请求。<br>cd tree：每一个 Angular 组件都有对应的 cdor，在应用启动时生成。<br>zone.js：可以跟踪并拦截任意的异步任务。Angular 使用 zone 并将其成为 NgZone，全局只存在一个 NgZone，cd 只在这个 NgZone 中的异步操作时触发。<br>对于嵌套的对象：<br>cd 默认对模板表达式所用的值改变进行检测（针对所有组件），对象类型不进行深度比较和检测除非模板表达式中使用了其中的一些属性值</p>
<p>angular 有两种 cd 策略：</p>
<ul>
<li>Default：默认策略，该策略在每次异步操作时从根到叶对所有组件进行检测（性能较差）</li>
<li>OnPush：在 @Component 装饰器添加 <code>changeDetection: ChangeDetectionStrategy.OnPush</code> 可以启用该策略。该策略只针对以下几种情况进行 cd</li>
<li>@Input 引用变化（修改对象、数组属性不触发 cd，可以引入 Immutable.js）</li>
<li>该组件和其子组件触发 event handler（setTimeout/setInterval/Promise.then/this.http.get/subscribe…不会触发 cd）</li>
<li>手动触发 cd（三种方式，ChangeDetectorRef.detectChanges()/ApplicationRef.tick()/cd（三种方式，ChangeDetectorRef.markForCheck()）</li>
<li>模板中使用 async pipe 抛出新值</li>
</ul>
<p>深入理解<a href="[https://indepth.dev/posts/1053/everything-you-need-to-know-about-change-detection-in-angular">Everything you need to know about change detection in Angular</a>](<a href="https://indepth.dev/posts/1053/everything-you-need-to-know-about-change-detection-in-angular)">https://indepth.dev/posts/1053/everything-you-need-to-know-about-change-detection-in-angular)</a>)</p>
<h3 id="Core-concept-View"><a href="#Core-concept-View" class="headerlink" title="Core concept - View"></a>Core concept - View</h3><p>Angular app 就是一棵组件树，更底层的抽象被叫做视图（View），每一个组件都和一个视图绑定。一个视图绑定着对应组件实例上的属性的引用。cd 和 DOM 更新都是在视图上进行操作的。</p>
<p>视图是应用 UI 的构成基础，是页面元素最小组成单位（创建和销毁）。视图中元素的属性可以被修改，但是元素个数、顺序无法被修改，只能通过 ViewContainerRef 插入、移动和删除。每个视图包含多个 View Container。每个视图与其子视图通过 nodes 属性关联。</p>
<p>每个 View 都有一个状态 state，angular 会根据 state 来判断是否对其和其子视图进行 cd 与否。与 cd 相关的状态有 FirstCheck、ChecksEnabled、Errored 和 Destroyed。</p>
<p>在状态 ChecksEnabled 为 false、或为 Errored、Destroyed 时，cd 会跳过该视图及其子视图。默认在视图初始化时状态为 ChecksEnabled（除非启动 OnPush 模式）。状态的值可以是组合的。</p>
<h3 id="cd-operations"><a href="#cd-operations" class="headerlink" title="cd operations"></a>cd operations</h3><p>cd 主要逻辑存在于 checkAndUpdateView 方法，其功能主要是被用来递归地对组件树进行检测。对某个组件的 view 进行检测主要有以下过程：</p>
<ul>
<li>为 ViewState.firstCheck 赋值，第一次 cd 设为 true，第一次 cd 完后设为 false</li>
<li>将子组件/指令实例的 @Input 属性 cd 和更新</li>
<li>更新子视图的 cd state</li>
<li>对视图进行 cd（进入递归）</li>
<li>如果子组件属性有变化，执行 nChanges 钩子</li>
<li>执行子组件的 OnInit 和 ngDoCheck</li>
<li>更新 ContentChildren</li>
<li>执行 AfterContentInit（只在第一次 cd 执行） 和 AfterContentChecked 钩子</li>
<li>如果当前组件实例属性有变化，更新 DOM 中的插入值</li>
<li>对子视图进行 cd （继续递归）</li>
</ul>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Framework</tag>
        <tag>Angular</tag>
        <tag>Change Detection</tag>
      </tags>
  </entry>
  <entry>
    <title>认识 Tapable</title>
    <url>/2022/09/22/2022-09-22-to-know-tapable.html</url>
    <content><![CDATA[<p>实现了一种发布订阅者模式的插件机制。通过 Tapable 我们可以注册时间，从而在不同时机去触发注册的事件进行执行。Tapable 怎么使用呢？</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化 hook，此处的字符串数组比较重要</span></span><br><span class="line"><span class="keyword">const</span> hook = <span class="keyword">new</span> <span class="title class_">SyncHook</span>([<span class="string">&#x27;arg1&#x27;</span>, <span class="string">&#x27;arg2&#x27;</span>, <span class="string">&#x27;arg3&#x27;</span>]);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为 hook 注册事件，同步 hook 只有 tap 方法，</span></span><br><span class="line"><span class="comment">// 接受两个参数，一个是字符串的标识，第二个是注册的函数，在调用时执行</span></span><br><span class="line">hook.<span class="title function_">tap</span>(<span class="string">&#x27;flag1&#x27;</span>, <span class="function">(<span class="params">arg1, arg2, arg3</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;flag1:&#x27;</span>, arg1, arg2, arg3)</span><br><span class="line">&#125;)</span><br><span class="line">hook.<span class="title function_">tap</span>(<span class="string">&#x27;flag2&#x27;</span>, <span class="function">(<span class="params">arg1, arg2, arg3</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;flag2:&#x27;</span>,arg1, arg2, arg3)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用事件并传递执行参数</span></span><br><span class="line">hook.<span class="title function_">call</span>(<span class="string">&#x27;19Qingfeng&#x27;</span>, <span class="string">&#x27;wang&#x27;</span>, <span class="string">&#x27;haoyu&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment">// 打印结果</span></span><br><span class="line"><span class="comment">// flag1: 19Qingfeng wang haoyu</span></span><br><span class="line"><span class="comment">// flag2: 19Qingfeng wang haoyu</span></span><br></pre></td></tr></table></figure>
<span id="more"></span>
<h3 id="hook-的分类"><a href="#hook-的分类" class="headerlink" title="hook 的分类"></a>hook 的分类</h3><h4 id="根据同步和异步分类"><a href="#根据同步和异步分类" class="headerlink" title="根据同步和异步分类"></a>根据同步和异步分类</h4><p><img src="Tapable%20b61b89bee0e14e4096b199f1b9e6f3b8/%25E6%2588%25AA%25E5%25B1%258F2023-03-16_%25E4%25B8%258B%25E5%258D%25884.16.48.png" alt="截屏2023-03-16 下午4.16.48.png"></p>
<p>同步的 hook，只有 tap 是唯一的注册事件的方法，通过 call 方法触发 hook 的执行。</p>
<p>异步的 hook，可以通过 tap、tapAsync 和 tapPromise 三种方式来注册，可以通过 call、callAsync 和 promise 三种方式来触发注册函数。tapAsync 注册函数额外接受一个 callback 参数，调用 callback 表示函数执行完毕。tapAsync 和 tapPromise 如果有错误对象或 reject，则中断执行。</p>
<p>异步的 hook，另外还可以分为可串行和可并行执行两种类型。</p>
<h4 id="根据执行机制分类"><a href="#根据执行机制分类" class="headerlink" title="根据执行机制分类"></a>根据执行机制分类</h4><ul>
<li>Basic Hook：仅执行钩子注册的函数，不关心任何的函数返回值。例如 SyncHook；</li>
<li>Waterfall：类似与基础类型钩子，但是函数执行时会将非 undefined 的返回值传递给后续的函数作为参数。例如 SyncWaterfallhook;</li>
<li>Bail：如果注册函数执行后返回非 undefined 的值，则会中断整个 hook 的函数执行。例如 SyncBailHook；</li>
<li>Loop：如果注册函数执行后返回非 undefined 的值，会立刻葱头开始重新执行函数，直到所有注册函数都返回 undefined。例如 SyncLookHoop；</li>
</ul>
<h3 id="拦截器"><a href="#拦截器" class="headerlink" title="拦截器"></a>拦截器</h3><p>Tapable 提供的所有 Hook 都支持注入 Interception，通过拦截器对整个 Tapable 发布/订阅流程进行监听，从而触发对应的逻辑。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> hook = <span class="keyword">new</span> <span class="title class_">SyncHook</span>([<span class="string">&#x27;arg1&#x27;</span>, <span class="string">&#x27;arg2&#x27;</span>, <span class="string">&#x27;arg3&#x27;</span>]);</span><br><span class="line"></span><br><span class="line">hook.<span class="title function_">intercept</span>(&#123;</span><br><span class="line">  <span class="comment">// 每次调用 hook 实例的 tap() 方法注册回调函数时, 都会调用该方法,</span></span><br><span class="line">  <span class="comment">// 并且接受 tap 作为参数, 还可以对 tap 进行修改;</span></span><br><span class="line">  <span class="attr">register</span>: <span class="function">(<span class="params">tapInfo</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`<span class="subst">$&#123;tapInfo.name&#125;</span> is doing its job`</span>);</span><br><span class="line">    <span class="keyword">return</span> tapInfo; <span class="comment">// may return a new tapInfo object</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 通过hook实例对象上的call方法时候触发拦截器</span></span><br><span class="line">  <span class="attr">call</span>: <span class="function">(<span class="params">arg1, arg2, arg3</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Starting to calculate routes&#x27;</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// 在调用被注册的每一个事件函数之前执行</span></span><br><span class="line">  <span class="attr">tap</span>: <span class="function">(<span class="params">tap</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(tap, <span class="string">&#x27;tap&#x27;</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="comment">// loop类型钩子中 每个事件函数被调用前触发该拦截器方法</span></span><br><span class="line">  <span class="attr">loop</span>: <span class="function">(<span class="params">...args</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(args, <span class="string">&#x27;loop&#x27;</span>);</span><br><span class="line">  &#125;,</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<ul>
<li>register：注册 tap、tapAsync、tapPromise 函数时，会触发该拦截器；接受注册的 tap 作为参数，同时可以对于注册的事件进行修改。</li>
<li>call：调用 hook 实例的 call 方法时执行，接受的参数就是函数传入的参数。</li>
<li>tap：每个被注册的函数调用之前执行，接受的参数是对应的 Tap 对象。</li>
<li>loop：每次重新开始循环时执行，接受的参数就是函数传入的参数</li>
</ul>
<h3 id="Tapable-其他-API"><a href="#Tapable-其他-API" class="headerlink" title="Tapable 其他 API"></a>Tapable 其他 API</h3><p>注册函数时，之前我们传入的第一个参数为标识符字符串，其实它还可以是一个对象，这个对象表示注册函数时的一些其他配置。</p>
<h4 id="before"><a href="#before" class="headerlink" title="before"></a>before</h4><p>before 属性的值可以传入一个数组或者字符串,值为注册事件对象时的名称，它可以修改当前事件函数在传入的事件名称对应的函数之前进行执行。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">hooks.<span class="title function_">tap</span>(&#123;</span><br><span class="line">	  <span class="attr">name</span>: <span class="string">&#x27;flag1&#x27;</span>,</span><br><span class="line">	&#125;,</span><br><span class="line">  <span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;This is flag1 function.&#x27;</span>)</span><br><span class="line">);</span><br><span class="line">hooks.<span class="title function_">tap</span>(&#123;  <span class="comment">// flag2 事件函数会在flag1之前进行执行</span></span><br><span class="line">    <span class="attr">name</span>: <span class="string">&#x27;flag2&#x27;</span>,</span><br><span class="line">    <span class="attr">before</span>: <span class="string">&#x27;flag1&#x27;</span>,</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="function">() =&gt;</span> <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;This is flag2 function.&#x27;</span>)</span><br><span class="line">);</span><br></pre></td></tr></table></figure>
<h4 id="stage"><a href="#stage" class="headerlink" title="stage"></a>stage</h4><p>该属性能够标识注册函数执行的前后权重，类型是数字，默认为0，也支持传入负数，数字越大其回调函数执行的越晚。</p>
<p>结合上面的 before 属性，如果同时使用，优先会处理 before 属性值，满足该条件后再判断 stage 属性值的判断。所以使用 stage 属性时最好不要使用 before 属性。</p>
<h4 id="Hookmap"><a href="#Hookmap" class="headerlink" title="Hookmap"></a>Hookmap</h4><p>本质上是一个辅助类，可以更好地管理 hook。另外还有 MultiHook，主要作用也就是通过 MultiHook 批量注册事件函数在多个钩子中。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 创建 HookMap 实例</span></span><br><span class="line"><span class="keyword">const</span> keyedHook = <span class="keyword">new</span> <span class="title class_">HookMap</span>(<span class="function">(<span class="params">key</span>) =&gt;</span> <span class="keyword">new</span> <span class="title class_">SyncHook</span>([<span class="string">&#x27;arg&#x27;</span>]));</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在 keyedHook 中创建一个 name 为 key1 的 hook，同时为该 hook 通过 tap 注册事件 </span></span><br><span class="line">keyedHook.<span class="title function_">for</span>(<span class="string">&#x27;key1&#x27;</span>).<span class="title function_">tap</span>(<span class="string">&#x27;Plugin 1&#x27;</span>, <span class="function">(<span class="params">arg</span>) =&gt;</span> &#123;</span><br><span class="line">  <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Plugin 1&#x27;</span>, arg);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从 HookMap 中拿到 name 为 key1 的 hook</span></span><br><span class="line"><span class="keyword">const</span> hook = keyedHook.<span class="title function_">get</span>(<span class="string">&#x27;key1&#x27;</span>);</span><br><span class="line"><span class="keyword">if</span> (hook) &#123;</span><br><span class="line">  <span class="comment">// 通过call方法触发Hook</span></span><br><span class="line">  hook.<span class="title function_">call</span>(<span class="string">&#x27;hello&#x27;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="深入源码"><a href="#深入源码" class="headerlink" title="深入源码"></a>深入源码</h3><p><a href="https://github.com/sanfran1068/tapable">https://github.com/sanfran1068/tapable</a></p>
]]></content>
      <categories>
        <category>SourceCodeReading</category>
      </categories>
      <tags>
        <tag>Webpack</tag>
        <tag>Tapable</tag>
        <tag>Compile Tool</tag>
      </tags>
  </entry>
  <entry>
    <title>理解 Angular 的依赖注入</title>
    <url>/2022/04/19/2022-04-19-understanding-DI-in-angular.html</url>
    <content><![CDATA[<h3 id="概览"><a href="#概览" class="headerlink" title="概览"></a>概览</h3><p>依赖注入是一种设计模式，它的主要作用是让类与其依赖相互独立。它能够解决的现实问题是，当一个类 classA 需要依赖另一些类 classB、classC 时，不再需要 classA 中去实例化 classB、classC，即一个类不需要知道它所依赖的其他服务的具体实现，而是直接去使用其依赖。另外还能够便于编写单测、减少冗余代码、使应用便于扩展。</p>
<p>注入的依赖可以是任何类或值。<br>Angular 框架中有三种依赖注入的类型：</p>
<ul>
<li>Constructor Injection：injector 将依赖（服务）在类构建时注入其中</li>
<li>Setter Injection：injector 将依赖注入到 setter 属性方法中</li>
<li>Interface Injection： 依赖本身自带 injector，可以将依赖注入任何传入其中的使用者。接受该依赖的使用者必须通过 interface 暴露一个 setter 方法。</li>
</ul>
<span id="more"></span>
<h3 id="Injector"><a href="#Injector" class="headerlink" title="Injector"></a>Injector</h3><p>Injector 是能够存储指令来实现 service 的数据结构，能够将 service 实例化，是 DI 系统里的一个中介角色。<br>Module、directive 和 component 类都拥有自己的 metadata 装饰器，其中 providers: [] 可以声明使用哪些将哪些 service 注册到类中。每一个 injector 都会陪伴某一个类，也就是说整棵应用树会对应一棵 injector tree。</p>
<p>在 service 里，通过 Injectable({providedIn: string}) 表示 injector 在哪里创建 service。providedIn 属性指明该 service 应该用哪个 injector 去注册。这样可以很容易实现 tree-shaking 来减少不必要的 service 实例化。该属性默认是 ‘root’，也就是说该服务在应用内任何地方均可实例化使用。<br>注册好的 service 是单例的——只有一个 injector 可以去初始化该 service。</p>
<p>在 @Directive、@Component  和 @Module  元数据中则是通过 providers 来表示。<br>Module 和 Component都拥有自己的 injector，注册在其中的 service 是跟随其生命周期存在。</p>
<h3 id="实例化-References"><a href="#实例化-References" class="headerlink" title="实例化 References"></a>实例化 References</h3><p>在 Angular 中，reference 也是 service 的一种。但是我们从来不会手动将 ElementRef、Renderer2 这些服务写在任何 providers 中，这是因为 angular 会自动将很多 service 注册在 root provider 中。</p>
<p>Injection Token</p>
<p>Angular 的 DI 系统使用 tokens 来标识一个 Provider。有三种 tokens：Type Token、String Token、Injection Token。</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line">providers :[&#123; <span class="attr">provide</span>: <span class="title class_">ProductService</span>, <span class="attr">useClass</span>: <span class="title class_">ProductService</span> &#125;]</span><br></pre></td></tr></table></figure>
<p>其中 provide 属性值就是一个 DI Token 标识，userClass 属性值就是一个 Provider，服务的具体实现。</p>
<h4 id="Type-Token"><a href="#Type-Token" class="headerlink" title="Type Token"></a>Type Token</h4><p>最常见的是当依赖是一个 service 时，provide 属性值就是这个服务类的 type。</p>
<h4 id="String-Token"><a href="#String-Token" class="headerlink" title="String Token"></a>String Token</h4><p>当注入的依赖没有具体的类型时，可以直接使用 string token 进行标识：</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="attr">providers</span>: [&#123; <span class="attr">provide</span>: <span class="string">&#x27;USE_FAKE&#x27;</span>, <span class="attr">useValue</span>: <span class="literal">true</span> &#125;]</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">AppComponent</span> &#123;</span><br><span class="line">	<span class="attr">products</span>: <span class="title class_">Product</span>[];</span><br><span class="line">	<span class="title function_">constructor</span>(<span class="params">@Inject(<span class="string">&#x27;USE_FAKE&#x27;</span>) private fake: boolean</span>) &#123;&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但是 string token 有一个很严重的问题，就是在一个应用中，可能不同的开发者会使用相同的 string token 去注入不同的依赖，甚至可能会和第三方包中的 token 发生重复和冲突。</p>
<p>所以需要另外一种： Injection Token</p>
<h4 id="Injection-Token"><a href="#Injection-Token" class="headerlink" title="Injection Token"></a>Injection Token</h4><p>Injection Token 能够创建一个 token，它可以是任意类型</p>
<figure class="highlight jsx"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">InjectionToken</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@angular/core&#x27;</span>;</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">const</span> <span class="variable constant_">APIURL</span> = <span class="keyword">new</span> <span class="title class_">InjectionToken</span>&lt;string&gt;(<span class="string">&#x27;&#x27;</span>);</span><br><span class="line"><span class="attr">providers</span>: [&#123; <span class="attr">provide</span>: <span class="variable constant_">APIURL</span>, <span class="attr">useValue</span>: <span class="string">&#x27;http://SomeEndPoint.com/api&#x27;</span> &#125;]</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">class</span> <span class="title class_">AppComponent</span> &#123;</span><br><span class="line">	<span class="title function_">constructor</span>(<span class="params">@Inject(APIURL) public ApiUrl: <span class="built_in">String</span></span>) &#123; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>可以看到，Injection Token 类似与 String Token，但是能够保证全局唯一。</p>
<p>参考文章：</p>
<p><a href="https://angular.io/guide/dependency-injection">https://angular.io/guide/dependency-injection</a></p>
<p><a href="https://angular.io/guide/dependency-injection-overview">https://angular.io/guide/dependency-injection-overview</a></p>
<p><a href="https://angular.io/guide/architecture-services">https://angular.io/guide/architecture-services</a></p>
<p><a href="https://www.simplilearn.com/tutorials/angular-tutorial/angular-dependency-injection">https://www.simplilearn.com/tutorials/angular-tutorial/angular-dependency-injection</a></p>
<p><a href="https://www.codesolutionstuff.com/dependency-injection-in-angular-with-example/">https://www.codesolutionstuff.com/dependency-injection-in-angular-with-example</a></p>
<p><a href="https://www.tektutorialshub.com/angular/injection-token-in-angular/">https://www.tektutorialshub.com/angular/injection-token-in-angular</a></p>
]]></content>
      <categories>
        <category>Document</category>
      </categories>
      <tags>
        <tag>Framework</tag>
        <tag>Angular</tag>
        <tag>Dependency Injection</tag>
      </tags>
  </entry>
</search>
